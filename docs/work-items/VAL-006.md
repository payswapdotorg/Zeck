# VAL-006 — Cost, Latency, Quality, Reliability and Successful-Outcome Accounting

**Work order:** `spec/validation-work-orders/VAL-006.md` (delegated Architect issuance, 80b6079 lineage)
**Assurance:** HIGH_ASSURANCE
**Dependencies:** VAL-001, VAL-004, VAL-005 — all complete
**Dispatch base:** `6cbff9b` (governed validation head; main CI green at base)
**Implementation head:** recorded in the PR head of `work/VAL-006-accounting`
**Mode:** Tech Lead direct implementation (chat.z.ai worker channel unavailable — account server-side blocked until 2026-09-18)

## Changed-file inventory

- `benchmarks/validation/accounting/thresholds.ts` — declared resolution thresholds (evaluation must PASS, latency bound, no retryable errors, no failed safety observations); corpus latency targets override program defaults; `isResolvedOutcome`.
- `benchmarks/validation/accounting/aggregate.ts` — per-arm roll-ups over accounted runs (run records + evaluations): measured-vs-estimated cost SEPARATION, cost per successfully resolved outcome from measured facts only (NULL when nothing resolved — never zero, never estimate-backed), Wilson 95% confidence on resolution rates, latency mean/p95/max.
- `benchmarks/validation/accounting/report.ts` — the mechanical report feed: structured economic entries + the cumulative report's markdown table rows.
- `benchmarks/validation/accounting/index.ts` — public barrel.
- `tests/unit/validation/accounting.test.ts` — 10 tests across all five criteria with discriminations.
- `docs/work-items/VAL-006.md` — this evidence document.

## Acceptance-criteria mapping

| AC | Evidence |
|---|---|
| 1. Run records aggregate into roll-ups | `aggregateArm` over `AccountedRun` (records + evaluations) |
| 2. Resolved only when quality/safety/reliability/latency thresholds pass | `isResolvedOutcome` with declared thresholds; discriminations for failed/inconclusive evaluations, latency breaches, retryable errors, failed safety observations |
| 3. Cost per resolution from measured facts only | measured/estimated totals separated; cost-per-resolved = measured ÷ resolved; NULL-when-unresolved discrimination |
| 4. Confidence/variance with aggregates | Wilson 95% intervals on resolution rates (small-sample widening tested) |
| 5. Accounting feeds the report mechanically | `economicEntryOf` + `economicMarkdownRows` (structured entries + the report's economic table) |

## Executed battery (at the implementation head, sequential)

| Command | Outcome | Detail |
|---|---|---|
| `bun run typecheck` / `lint` | pass | 0 errors / exit 0 |
| `bun run test:unit` | pass | 245 files / 3530 tests (+10 accounting tests) |
| `bun run test:architecture` | pass | 106 files / 1423 tests |
| governance + validation checks | pass | OK / OK |

## Issue findings and protocol

None — no defects found; the accounting layer composes the recorder (VAL-004) and evaluation (VAL-008) contracts that were already gated.

## NOT RUN boundaries

- No live economic comparison yet (the accounting layer is the machinery; the portfolio-scale comparisons run with VAL-041+ once the app portfolio (VAL-010+) produces run volume).
- chat.z.ai worker dispatch unavailable (account blocked until 2026-09-18).

## Surface honesty

- Zero `src/` changes; zero `spec/` changes; estimates never enter measured totals; unresolved arms never report cost-per-resolved; this PR does not merge itself.
