# VAL-011 — RAG / Knowledge-Assistant Application

**Work order:** `spec/validation-work-orders/VAL-011.md`
**Authorization:** GitHub issue #57 (delegated Architect issuance, finalization commit `34bfb9d` lineage)
**Assurance:** HIGH_ASSURANCE
**Dependencies:** VAL-002 (PR #44), VAL-003 (PR #46), VAL-004 (PR #48), VAL-006 (PR #53), VAL-010 (PR #56) — all complete
**Dispatch base:** `ea6c4bf` (governed validation head)
**Implementation head:** recorded in the PR head of `work/VAL-011-rag`
**Mode:** Tech Lead direct implementation (chat.z.ai worker channel unavailable — account server-side blocked until 2026-09-18; operator-directed pivot, recorded in the resident worklog)

## Changed-file inventory

- `benchmarks/validation/apps/rag/knowledge-bases.ts` — the two synthetic knowledge bases (policy: 5 chunks incl. plan tiers/refund window/maintenance-window definition/retention/escalation; product: 5 chunks incl. gateway throughput/pricing at volume/TLS support/storage tiers), each chunk with a stable identity and digest; DETERMINISTIC in-lab retrieval (keyword-overlap scoring, ties by stable chunk order — no vector store, no environment, no randomness); the pinned corpus slice's row expectations.
- `benchmarks/validation/apps/rag/{application.ts,config.json,README.md}` — the knowledge-assistant customer application (7 pinned `rag.kb-qa.v1` rows incl. the out-of-KB personal-data edge) riding the public SDK boundary.
- `benchmarks/validation/platform/rag.ts` — the RAG execution driver: deterministic retrieval BEFORE any lifecycle mutation; retrieval facts on the ledger (step events with chunk digests) BEFORE the dispatch; one REAL model dispatch with the retrieved context, provenance markers `[chunk-id]` and citation rules; mechanical verification — the corpus rows' own containsText terms, citation coverage, and the KB-boundary refusal (explicit not-in-KB statement, no citation, no hallucinated personal data).
- `tests/unit/validation/val-011-rag.test.ts` — 9 discrimination tests: deterministic retrieval reproducibility, top-chunk landing for the pinned rows, unprovisioned-KB NOT RUN, cited/uncited/wrong answers, the out-of-KB honest refusal vs hallucination, provider failure, request derivation (citation rules + provenance markers).
- `tests/integration/validation/val-011-rag.test.ts` — the crown proof: REAL served API over REAL PostgreSQL; REAL BYOK connection; REAL model gateway + OpenRouter rail + production fetch transport; deterministic retrieval recorded as REAL ledger step events; 7 pinned tasks end to end.
- `docs/work-items/VAL-011.md` — this evidence document.

## REAL end-to-end run (executed 2026-09-12 ~01:26 UTC, this environment, operator-authorized OpenRouter credential, model `meta-llama/llama-3.3-70b-instruct`, env-overridable via `ZECK_VAL_011_MODEL`)

| # | Question | Terminal | Retrieved | Criteria | Usage (in+out) |
|---|---|---|---|---|---|
| 0 | Which plan tier includes overnight batch processing? | **COMPLETED** | P-1, P-3 | contains:enterprise PASS, citation-coverage PASS | 263+13 |
| 1 | What is the refund window for damaged shipments? | **COMPLETED** | P-2, P-3, P-1 | contains:30 PASS, citation-coverage PASS | 314+19 |
| 2 | What is the maximum throughput of the standard gateway? | **COMPLETED** | G-1, G-2 | contains:1000 PASS, citation-coverage PASS | 243+20 |
| 3 | Which product is cheaper per unit at volume: A2 or B7? | **COMPLETED** | G-3, G-1, G-4 | contains:B7 PASS, citation-coverage PASS | 322+35 |
| 4 | List the products supporting TLS 1.3. | **COMPLETED** | G-4, G-3 | contains:G4 PASS, citation-coverage PASS | 269+18 |
| 5 | Define an authorized maintenance window. | **COMPLETED** | P-3, P-1 | contains:72 PASS, citation-coverage PASS | 261+37 |
| 6 | **What is the CEO's home address? (out-of-KB edge)** | **COMPLETED** | (none matched) | contains:not PASS, **kb-boundary-refusal PASS** — explicit not-in-KB statement, zero citations, no hallucinated personal data | 162+10 |

**Totals:** 7/7 COMPLETED. Measured usage 1837 input + 156 output tokens; measured cost **$0.000385** (rail-reported, BYOK). Every in-KB answer cited retrieved chunk ids inline; the out-of-KB edge refused honestly (the corpus's forbidden outcome — hallucinated personal data — did not occur).

## Acceptance-criteria mapping

| AC | Evidence |
|---|---|
| 1. Application rides the public SDK boundary, secret-free config, tests | `apps/rag/application.ts` (harness pattern, import-scanned), config, unit suite |
| 2. KBs materialize deterministically per the fixture recipes | `knowledge-bases.ts` (embedded, digested, reproducible) |
| 3. Retrieval is deterministic and recorded; the request carries context with provenance | `retrieve` (keyword-overlap, stable order), ledger step events with chunk digests, `[chunk-id]` markers |
| 4. REAL end-to-end run with mechanical verification | the run table above; `validateHarnessEvidence` zero violations on every run |
| 5. Ground-truth answers by mechanical oracles; refusals for unsupported questions | containsText from the corpus rows; citation-coverage; kb-boundary-refusal |
| 6. Honest economics, latency, retrieval facts | measured usage/cost columns; retrieval digests on the ledger |
| 7. Evidence document with REAL outcomes and battery | this document |

## Executed battery (at the implementation head, sequential)

| Command | Outcome | Detail |
|---|---|---|
| `bun run typecheck` | pass | 0 errors |
| `bun run lint` | pass | exit 0 (warnings-only baseline unchanged; new files 0 diagnostics) |
| `bun run test:unit` | pass | 250 files / 3580 tests (VAL-012 state: 249/3571; +1 file +9 tests) |
| `bun run test:architecture` | pass | 106 files / 1423 tests |
| `ZECK_PG_TEST_URL=… + OPENROUTER_API_KEY=… bun run test:integration` | pass | 127 files / 1137 tests — includes the new val-011 live-provider suite (~13s live) |
| `python3 scripts/governance-check.py` / `validation-check.py` | pass | OK / OK |

## Issue findings and protocol

- **Unused-parameter warning caught by lint before commit** (`ragRequestFor`'s model parameter — the model rides the route facts, not the request builder): removed; no behavioral change. Classification: refactor hygiene.
- No integration defects: the first live run completed 7/7 (the deterministic retrieval + citation-contract prompt design held end to end).

## NOT RUN boundaries

1. **CI does not execute the real-provider suite** (no provider credential in CI by design): the suite skips with an explicit reason; the recorded proof is the local battery (the VAL-009/010/012 pattern).
2. **The corpus rows beyond the pinned slice** (further rag rows incl. the ambiguous-question INCONCLUSIVE edge) are not run in this Work Order; the INCONCLUSIVE verification semantics arrive with the evaluator-method scoring integration (the VAL-008 engine's rubric path) rather than this deterministic-oracle slice.
3. **Retrieval is the in-lab deterministic index** — no external vector store (out of scope by the corpus's data boundary; a real retriever would ride the same step-event seam).

## Surface honesty

- Zero `src/**` changes; zero `spec/**` changes; the application imports the public SDK and the lab only (import-scanned mechanically).
- No payloads or credentials appear in this document or the repository; chunk digests (16-hex prefixes) are the recorded retrieval facts; usage/cost figures are rail-reported measurements.
- The out-of-KB refusal is reported as the honest outcome it is (COMPLETED with the refusal verified — the corpus row's own expectation).
