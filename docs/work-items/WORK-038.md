# WORK-038 Evidence — Trust, evidence, artifacts and competence experience

Work Order: `WORK-038` (spec/work-orders/WORK-038.md) · Assurance: **HIGH_ASSURANCE** · Requirement IDs: N/A (presentation realization of the existing verification, artifact, learning and competence authorities; frozen technical requirement ownership untouched)

Frozen base: `a29b81775866d487e49641bddd728f61d07df3d0` (main — the WORK-038 dispatch head: WORK-037 complete and merged as PR #66 at `dffc9eb…`, the governance state at the base OK with 41 Work Orders / 102 requirements / inFlight=['WORK-038'] / frontier=[], exactly as dispatched via issue #67). Branch: `work/WORK-038-trust-evidence-artifacts-competence` · **Final head: this doc's commit** (the house two-phase binding — the exact SHA is recorded by the orchestrator in the PR body; the last code commit is `198bfe2`) · Zero merge commits; the merge-base is `a29b817…` exactly.

**Dispatch-record note (report-only, never worker-fixed — the W037 lesson)**: the work-order FILE's Dispatch Record line says "Exact base: `9426b0e…`" while issue #67, the worker branch's actual position and the main head are `a29b817…`. The difference is exactly the Architect's own final pin commit `a29b817` (which changed only that line of that file). The worker implemented on the branch exactly as dispatched (`a29b817`, 0 commits ahead / 0 behind at start) and did not touch the line.

**Incident disclosure (honest)**: a full sandbox environment reconstruction preceded this session — the userspace (including the prior worklog, the prior harnesses and the PG installation) was rebuilt before work began. Nothing repository-side was lost: the clone re-verified against origin, PostgreSQL 16.4 was re-provisioned at the same coordinates (127.0.0.1:55432, postgres superuser, trust), the PAT was re-persisted, and the complete baseline gate was re-proven GREEN on the reconstructed environment at the exact base BEFORE any change (below). The driven-browser harness was rebuilt for W038 at /home/z/w038-harness (outside the repository, the house convention).

## Implementation history (the ratchet — every commit compiles and is suite-green)

1. `198bfe2` — the complete W038 realization (one implementation commit; the ratchet ran the full gate at the committed tree — see the gate section):
   - **`apps/dashboard/trust.ts` (NEW, 360 lines)** — the ONE trust-state presentation module (Implementation Requirement 2): `TRUST_AXIS_LABELS` (the four user-language dimensions) + `trustAxisLabel` (pages.ts's local copy REMOVED — one vocabulary everywhere), `TRUST_NOTE` (the never-a-score sentence, the same string the W036 evidence tab carried — centralized, not reworded), `TRUST_SUMMARY_LEAD`, `axisEvidenceLocation` (AC4: each axis's contextual drill-down location — provider → `#route-facts`, quality → `#verification-results`, execution/policy → the activity timeline; all same-execution links, never through an index), `trustSummarySection` (AC1: the Result-view four-axis summary, each fact carrying its label + platform fact + evidence link), `trustAxesTable` (AC2/AC3: the Evidence-view axes table with the "See the evidence" column), `evidenceRefLink`/`evidenceRefLinks` (AC2: a recorded evidence reference becomes a LINK only when it matches an output artifact of this execution — the platform's own membership; otherwise the reference renders verbatim with the explicit no-public-object note — never a fabricated target), `artifactMetadataTable` (AC5: exactly id/digest/createdAt), `artifactVerificationReferences` (AC5: the producing execution's checks whose recorded evidenceRefs point at this artifact), `artifactParentLineage` (AC5: the recorded input references as parent links, with the honest absence), `artifactUsageReferences` (AC5: the consuming executions from the browser's public per-execution records, with the explicit no-cross-work-route scope), `contextTraversal` (AC4: the Result/Evidence/Activity(/artifact) contextual strip).
   - **`apps/dashboard/projection.ts`** — the pure derivations: `inputArtifactRefsOf` (the parents: ONLY the `execution.created` event's recorded `inputArtifactRefs` payload — non-array/non-string/absent ⇒ the honest empty list, never a guessed parent), `consumesArtifact` (usage), `checksReferencing` (the artifact→evidence linkage from the checks' own evidenceRefs), `competenceDiscoveryFacts` (AC6: the five discovery families — task outcome, relevance, success rate, typical cost/time, verification status — every cell the explicit absence anchored to the competence authority), `competenceDetailFacts` (AC7: the six detail families — provenance, procedures, validation population, uncertainty, compatibility, promotion state — "only when available from the API"; the promotion cell states the boundary: nothing implies a promotion), `evaluationStatusRows` (AC8: observation / recommendation / validation / authoritative production — four DISTINCT statuses; the recommendation row states "advisory only … until the existing validation and promotion rules are satisfied"; the production row states it is "never implied by an observation or a recommendation").
   - **`apps/dashboard/components.ts`** — `verificationSummary` gains the optional `renderEvidenceRef` callback (the full table's evidence refs render through the trust module's linker when supplied; the default rendering is byte-identical without it — pinned); the full table gains `id="verification-results"` (the quality axis's anchor target); `resultSurface` accepts the optional `trustSummaryHtml` (the Result tab renders the trust module's summary as the "Can you trust it?" lead; without it the W036 rendering is unchanged); the generic `distinctionList` (label/fact/backed rows — the shared presentation behind the training-state and evaluation-status lists; `trainingStateList` now delegates to it, byte-identical markup, the W037 pins unchanged).
   - **`apps/dashboard/pages.ts`** — the route surfaces: the Result tab composes `trustSummarySection`; the Evidence tab renders `trustAxesTable` + the verification table with LINKED refs + the artifacts-referenced block + the contextual traversal strip + the (id'd) provenance disclosure; the artifact view is the full AC5 realization (the producing execution resolved from the URL context or — when absent — from the public output references of executions opened in this browser; metadata, the honest preview absence, provenance + route disclosure, source (the task), parent lineage, verification references, usage references, traversal; the honest no-artifact-by-id-route state when unresolvable; the honest 404 state for an invisible executionId); `/trust/evidence` and `/trust/lineage` are LIVE per-execution projections from the recents scope (each anchor carrying the platform verification chip; the lineage page rendering the per-run inputs → execution → outputs chains) with the honest cross-work absence notes; `/improve/evaluations` renders the four-status distinction through `distinctionList` with the honest authority absence and the live-evidence pointer; the competence discovery/detail surfaces render the AC6/AC7 families as explicit absences with "Using a competence" as a governed work action (link to the governed create; the frozen create contract carries no competence-selection field; no picker, no POST form, no local run).
   - **`apps/dashboard/shell.ts`** — the Trust nav descriptions updated honestly (Evidence: "live per run; cross-work evidence search is not public yet"; Lineage: "live; the cross-work lineage graph is not public yet"). Labels/paths/mode-visibility untouched (the navigation pins are unchanged).
   - **`apps/dashboard/tokens.ts`** — the WORK-038 CSS block: `.trust-summary` (+ axes/lead/chip/note), `.axis-evidence`, `.evidence-ref`/`.evidence-ref-plain`, `.lineage-list`/`.lineage-chains`/`.lineage-chain`/`.lineage-step`, `.context-traversal`, with a ≤640px mobile rule — every rule consuming the existing spacing/surface/border/text tokens; no one-off hierarchy.
   - The proof suites (the exact inventory below): the trust-presentation unit suite (30), the four required trust-state fixtures (4), the linked-ref component pins (2), the (y)–(ac) journeys (17), the D15–D18 discriminations (12).
2. `2be1c4a` — the evidence doc as originally committed (the first final head; superseded by the correction below).
3. The evidence-count correction commit (THIS doc — the final head). The Architect's re-review of PR #68 (the CHANGES-REQUIRED record, comment `5545542659`, 2026-09-04) found the surface-diff count statement wrong: this doc's header said "Exactly 11 files" while the filename list immediately below it enumerates 12, and the PR body's breakdown read "5 `apps/dashboard` incl. the NEW `trust.ts`" where the actual dashboard count is 6 (5 modified + `trust.ts` new). Corrected here to exactly 12, the complete filename list preserved unchanged (the list was always correct — the count arithmetic was not; the count is now stated with its explicit breakdown and cross-checked against both `git diff --name-only a29b817…HEAD` and the GitHub PR file list). NO code, test or surface change: the implementation commit `198bfe2` and its tree are untouched; the only delta between `2be1c4a` and this head is this document. The complete gate was re-run TWICE consecutively at this correction head per the same instruction (the gate section below).

## Baseline gate (readiness checkpoint — the exact frozen base, BEFORE implementation)

governance OK (41 WOs, inFlight=['WORK-038'], frontier=[]) · typecheck 0 · biome clean (965 files) · **full suite with real PG = 279 files / 3946 tests, exit 0** at exactly `a29b817` — the WORK-037-era baseline reproduced on the reconstructed environment before any change.

## What changed (the surface diff, every touched file vs `a29b817…`)

Exactly 12 files — 6 under `apps/dashboard/` (5 modified + the NEW `trust.ts`) + 5 dashboard-local test files + this evidence document; the count cross-checked against `git diff --name-only a29b817…HEAD` AND the GitHub PR #68 file list (both list exactly these 12; rg-verified — zero files under `src/`, `sdk/`, `cli/`, `scripts/`, `spec/` (incl. development-state), `migrations/`, `.github/`, or root configs; the count of files outside `apps/dashboard/` + `tests/` + `docs/work-items/` is ZERO):

- `apps/dashboard/trust.ts` (NEW — the centralized trust-state presentation)
- `apps/dashboard/projection.ts` (the W038 derivations; every existing derivation untouched)
- `apps/dashboard/components.ts` (the three additive optional extensions above; the default renderings byte-identical — pinned)
- `apps/dashboard/pages.ts` (the route surfaces above; the local axisLabel removed in favor of the one vocabulary; NO new routes and NO new POST routes — D11's "exactly the governed POST routes" pin unchanged)
- `apps/dashboard/shell.ts` (the two honest description strings; nothing structural)
- `apps/dashboard/tokens.ts` (the W038 CSS block)
- `tests/unit/dashboard/trust-presentation.test.ts` (NEW, 30)
- `tests/unit/dashboard/trust-state.test.ts` (+4 — the four required fixtures)
- `tests/unit/dashboard/components.test.ts` (+2 — the linked-ref/default-unchanged pins)
- `tests/integration/dashboard/journeys.test.ts` (76 → 93 — the (y)–(ac) journeys + the W038 seeds)
- `tests/discrimination/dashboard-journey.discrimination.test.ts` (15 → 27 — D15–D18)
- `docs/work-items/WORK-038.md` (this doc)

No migration claimed (the migration inventory is untouched). No new dependency (the driven-browser harness lives OUTSIDE the repository at /home/z/w038-harness; the repository stays zero-dependency).

## Acceptance-criteria mapping (the Work Order's AC1–AC10)

- **AC1** (a clear trust summary, never an unsupported magic score) — the Result view's `trustSummarySection`: four separate labeled facts (each a platform derivation), each linked to its evidence location, the verification chip, and the centralized never-a-score note; pinned by the (y) journey, trust-presentation (the merge-mutant pin: no "Overall" axis, the count is exactly four), D16 (zero-verification renders the honest chip, no confidence verdict, no chip-derived usage) and the harness (four `<li>` facts, the note, no merged vocabulary).
- **AC2** (evidence explains which checks passed/failed and links each claim to available evidence) — the Evidence view: the axes table (each axis's platform facts + source + drill-down link), the verification table with statuses, and evidence refs LINKED to the artifact view exactly when the platform exposes an artifact with that id (the opaque-ref case renders verbatim with the explicit note); pinned by the (z) journey, D15 (the link-everything mutant differs on the same input; the membership guard is the pinned source), trust-presentation (mixed refs, hostile refs, empty refs) and the harness (the linked ref + the plain opaque ref measured).
- **AC3** (provider/execution/quality/policy success remain distinct) — the centralized four-axis vocabulary renders on the Result summary, the Evidence table and the header strip (all through trust.ts/projection.ts); pinned by the four required fixtures (provider-success/task-failure NEVER claims task success; execution-success/quality-failure NEVER claims quality success; policy-blocked is its own dimension; verified-success still renders four separate facts), the (y) journey and D9 (the W036 pins, unchanged and green).
- **AC4** (contextual traversal between result, evidence, artifact, producing execution and source) — `axisEvidenceLocation` (same-execution anchors), the Evidence view's traversal strip + artifacts-referenced block, the artifact view's traversal strip + actions (result/evidence/activity of the producing execution) + the source section (the task); pinned by trust-presentation (every location contextual — no index routes), the (z)/(aa) journeys and the harness (the strip's links, the artifact actions, the anchors measured).
- **AC5** (artifact pages: preview/metadata, provenance, parent lineage, verification and usage references where available) — the artifact view's full section set, each a public-wire fact or an explicit honest absence: metadata (exactly id/digest/createdAt — the invented-fact mutant pinned out), the preview absence (no content route exists), provenance (the producing execution + route disclosure), source (the task), parent lineage (ONLY the `execution.created` payload's recorded input refs — the free-text/later-event mutant pinned out by D18), verification references (the checks whose evidenceRefs point here), usage references (other executions' recorded inputs, browser-scoped, the no-cross-work-route scope stated); pinned by the (aa) journey (the full set over the lineage fixture: parents, the hostile-ref escape, the referencing check, the consumer usage, the recents-based producer resolution, the honest unresolvable/404 states), D18 and the harness.
- **AC6** (competence discovery emphasizes task outcome, relevance, success rate, typical cost/time and verification status) — the discovery grid: exactly those five families, each the explicit absence anchored to the competence authority (the facts render only when the authority ships); no fabricated rows (no success-rate numbers — pinned); pinned by trust-presentation, the (ac) journey and the harness (five cells, no POST form).
- **AC7** (competence detail exposes the six families only when available from the API) — the detail grid: exactly provenance, procedures, validation population, uncertainty, compatibility, promotion state — every cell the explicit absence; the promotion cell states the authority's own rules and that nothing on the page implies a promotion (the D17 mutant pin); pinned by trust-presentation, the (ac) journey and D17.
- **AC8** (evaluation views distinguish observation, recommendation, validation and authoritative production) — the four-status distinction list (each row its own fact, "Explicit absence" markers, the advisory boundary stated on the recommendation row, production "never implied"); pinned by trust-presentation (the boundary sentences), the (ab) journey, D17 (the backed-flip mutant fails; four Explicit-absence markers rendered; no claim vocabulary) and the harness (four states, four markers).
- **AC9** (trust-state tests fail if a UI-only value is mistaken for platform verification truth) — the mutant pins: D16 (the chip is EXACTLY `deriveVerificationChip`'s output — a UI-computed "High confidence (UI)" differs; no extra axis; zero-verification renders no verdict), D15 (the link-everything mutant differs on the same input), D17 (the backed/production-claim mutants fail), D18 (the fabricating-parent mutant differs), and the trust-state fixture pins (each dimension's success never claims another's).
- **AC10** (keyboard, screen-reader, responsive and mobile behavior preserve trust hierarchy and evidence navigation) — the harness record: the skip link the first tab stop, the brand second, the trust-summary evidence links keyboard-reachable with a visible focus outline, keyboard navigation proven end-to-end through the form-Enter path, one h1 + landmarks on every W038 surface, the axes table carrying `aria-label`, the trust strip's existing `aria-label` unchanged, mobile 390 (the trust summary, the tabs, the artifact sections, the 44px primary touch target) — 47/47 checks, ZERO console errors and ZERO page errors across the whole session.

## The required discriminations (D15–D18, in tests/discrimination/dashboard-journey.discrimination.test.ts)

- **D15 evidence-link fabrication**: MODULE — a ref with no public artifact renders verbatim (the link-everything mutant's output differs on the same input; the known ref IS linked — the guard is a discriminator); STATIC — the membership check is the pinned source (the mutant deleting it is flagged by the same scanner); RUNTIME — a zero-verification run renders no evidence-ref links and no rendered chip.
- **D16 client-truth fabrication**: RUNTIME — the zero-verification Result view renders the honest chip and NO confidence verdict (the stylesheet may name the chip class; the RENDERED usage is pinned out); MODULE — the chip is exactly the platform derivation, and the summary renders EXACTLY the four derived axes (the extra "Overall" verdict mutant differs).
- **D17 learning/competence promotion conflation**: MODULE — every evaluation status row is the explicit absence (the backed-flip mutant fails) with the advisory boundary and the never-implied sentences pinned; the competence promotion cell never claims a promotion; RUNTIME — the competence and evaluation surfaces carry no claim vocabulary, no fabricated rows and no mutation surface (no POST form).
- **D18 lineage fabrication**: MODULE — input refs read ONLY the execution.created payload (a later event claiming different inputs is ignored; no-created-event ⇒ the honest empty list); the no-parents rendering states the absence (the fabricating mutant's output differs); RUNTIME — an artifact with no resolvable public record renders the honest state with NO provenance/lineage/verification blocks.

## The driven-browser verification record (real Chromium over the real createDashboard server + the scope-enforcing fake world)

The harness (OUTSIDE the repository — /home/z/w038-harness, playwright 1.57 / Chromium, rebuilt after the environment reconstruction) boots the real `createDashboard` against a fake API world implementing the REAL application-scope wire rule, then drives the canonical W038 journeys. **47/47 checks passed, ZERO console errors and ZERO page errors across the whole session** (screenshots at /home/z/w038-browser-*.png, the machine-readable record at /home/z/w038-harness/report.json): the Result trust summary (four axis facts, each linked to its evidence; the chip; the never-a-score note; no merged vocabulary); the Evidence view (the axes table with drill-down links; the check's ref LINKED to the public artifact; the opaque ref plain; the traversal strip); the artifact view (metadata, the honest preview absence, provenance, source, parent lineage, the hostile ref never injecting, verification references, usage references from the consumer run, the three traversal actions); the Trust surfaces (/trust/evidence, /trust/lineage, the four evaluation statuses each an explicit absence); the competence surfaces (five discovery cells, six detail cells, no POST forms); the keyboard discipline (the skip link first, the brand second, the trust-summary evidence link reachable with a visible focus outline, its target the contextual evidence view, Enter-in-the-lookup-form navigating end-to-end to the run page); the frame (one h1 + landmarks on all seven W038 surfaces); and mobile 390 (the trust summary, the tabs, the artifact sections, the 44px primary touch target).

**Disclosed harness limitation (honest, reproduced on a minimal page)**: synthesized Enter in this headless Chromium does not activate plain anchors (a minimal `<a href>` + focus + Enter does not navigate — reproduced outside the app). Keyboard navigation is therefore proven through the form-Enter path (which the same shell honors end-to-end: type an execution id in the lookup form, press Enter, land on the run page) plus link-reachability, visible-focus and href-target checks; the anchor navigation paths themselves are exercised by the driven gotos (every linked surface is loaded and verified in the same session). The repository's own automated proof is unaffected (the journeys drive HTTP directly).

## The complete gate (run at the final head, TWICE consecutively green)

`python3 scripts/governance-check.py` (OK: 41 WOs, inFlight=['WORK-038'], frontier=[]) · `bun run typecheck` (0 errors) · `bun run lint` (biome clean, 967 files, zero diagnostics/warnings) · `ZECK_PG_TEST_URL=postgres://postgres@127.0.0.1:55432/postgres bun run test` = **280 files / 4011 tests, exit 0** — run TWICE consecutively green at the exact final head (THIS doc's commit — the evidence-count correction head, re-run per the Architect's PR #68 re-review instruction; the run-pair record with timestamps is carried in the PR body and the reply to the finding). The identical gate had already run twice consecutively green at the pre-correction head `2be1c4a` (2026-09-04 18:55/18:58 UTC, verification comment `5545205505`); the only delta between the two heads is this document, so the counts are necessarily identical (the re-run proves it). The 279/3946 baseline + 1 file / +65 tests vs the base: trust-presentation 30, the four trust-state fixtures 4, the component linked-ref pins 2, the (y)–(ac) journeys 17, the D15–D18 discriminations 12.

## Test inventory (the exact delta vs the base)

- NEW unit: `trust-presentation.test.ts` (30 — the vocabulary 3, the axis locations 4, the evidence-ref links 5, the trust summary 3, the axes table 2, the artifact presentations 5, the traversal 1, the lineage derivations 4, the competence families 2, the evaluation statuses 2)
- EXTENDED unit: `trust-state.test.ts` (+4 — the four required fixtures: provider-success/task-failure, execution-success/quality-failure, policy-blocked, verified-success), `components.test.ts` (+2 — renderEvidenceRef supplied/absent)
- EXTENDED integration: journeys 76 → 93 ((y) the trust summary + the four fixtures ×4, (z) the linked evidence ×2, (aa) the artifact lineage ×4, (ab) the Trust surfaces ×4, (ac) the competence journey ×3) + the W038 seeds (PROVFAIL/POLICY/VERIFIED/CONSUMER with recorded input refs, evidenceRef-linked checks and the hostile ref)
- EXTENDED discrimination: dashboard-journey 15 → 27 (D15 ×3, D16 ×3, D17 ×3, D18 ×3)
- Total dashboard-local: 14 unit files / 266 unit tests + 93 journeys + 27 journey discriminations

## Required checkpoint contracts

- **SELF-HOSTING-BOUNDARY**: the dashboard still imports ONLY `../../sdk` + node builtins (trust.ts adds no import outside apps/dashboard + the sdk re-exports); the client script performs ZERO network calls (unchanged, D5 of the foundation suite); no new dependency; every fact remains a live SDK read (the trust summary, the axes table, the evidence links, the artifact sections, the Trust surfaces and the competence/evaluation absences are all derived at render time from the scoped reads); the transport scope remains the SDK's bound application scope (the (l) journeys still prove the wire; the new pages' reads all go through the same bound client — the harness world enforces the same header rule).
- **EXECUTION-PROVENANCE**: no new platform facts — the trust presentation renders `deriveTrustAxes`' public derivations (the same one derivation, now three presentations: strip, summary, table); every trust claim's evidence link goes to a PUBLIC object (an output artifact of the same execution, the run's own views) or renders the platform's recorded reference verbatim with the explicit no-public-object note; the artifact parents/usage derive ONLY from the `execution.created` event's recorded inputArtifactRefs (never free text, never later events); the artifact verification references derive ONLY from the checks' own evidenceRefs; competence/evaluation facts render ONLY as explicit absences (no public authority — no promotion, no validated state, no success-rate statistics, ever implied); learning stays advisory (the recommendation row's own boundary statement); the artifact metadata is exactly the public reference fields (id/digest/createdAt — nothing invented); money renders from integer micro-USD strings (BigInt only, unchanged); no visual primitive manufactures trust, policy or lineage facts.

## Required verification mapping (the Work Order's list)

| Required | Evidence |
|---|---|
| governance checker | OK: 41 WOs, inFlight=['WORK-038'], frontier=[] at the base and at the head |
| typecheck | 0 errors at the base and at the head |
| lint (biome) | clean (967 files) at the head |
| dashboard/unit/integration tests | 266 unit (14 files) + 93 journeys + 27 journey discriminations |
| trust-state discrimination suite | D9 (unchanged) + D16 + the four-fixture trust-state suite + the (y) journey + the harness trust-summary checks |
| artifact/provenance traversal tests | the (aa) journey + D18 + trust-presentation's artifact/traversal pins + the harness artifact-view checks |
| competence discovery/use tests | the (ac) journey + D17 + trust-presentation's family pins + the harness competence checks |
| responsive browser verification | the harness record (desktop 1280 + mobile 390: the trust summary, the tabs, the artifact sections, the 44px touch target) |
| keyboard/accessibility verification | the harness record (the skip link first, the brand second, the trust links reachable with visible focus, the form-Enter navigation, one h1 + landmarks + aria-labels on every W038 surface) |
| secret-exposure discrimination | the hostile-ref pins ((aa) journey escape + trust-presentation's evidenceRefLink escape + D3/D5 of the foundation suite, unchanged) |
| full suite twice consecutively at exact final head | the complete gate section above |

## Limitations (honest)

1. **No public competence, evaluation, artifact-content or cross-work lineage authority exists** — the competence/evaluation surfaces render their fact families as explicit absences (the honest structure ahead of the facts), the artifact preview states the no-content-route absence, and the Trust surfaces render the per-execution facts that ARE public with the cross-work absence notes. When each authority ships, its facts feed the same presentations (the grids, the distinction lists and the trust vocabulary are the structure it feeds).
2. Usage references are browser-scoped by construction: the public wire carries no cross-work usage route (no execution listing), so the artifact page states the scope explicitly — usage renders only from executions opened in this browser (the same scope rule as every recents-based surface since the foundation).
3. The artifact's producing execution resolves from the URL context or the browser's public output references; with neither, the honest no-artifact-by-id-route state renders (there is no artifact-by-id route on the public wire — stated, never worked around).
4. The driven-browser harness is a session artifact OUTSIDE the repository (the repository stays zero-dependency); its 47-check record is the evidence; the repository's own automated proof is the 386-test dashboard-local suite (266 unit + 93 journeys + 27 discriminations). The synthesized-Enter-anchor limitation is disclosed above (reproduced on a minimal page; the form-Enter path proves keyboard navigation end-to-end).
5. The trust strip's per-axis facts remain the same derivations the Evidence view renders in full (deriveTrustAxes) — compact labels, no new fact source (by design: one derivation, now three presentations through the one module).
6. The substrate changes are three additive optional extensions (`renderEvidenceRef`, `trustSummaryHtml`, the generic `distinctionList` with `trainingStateList` delegating byte-identically); the W033–W037 renderings are unchanged without them — pinned (the components suite's default-unchanged pins, and the full W036/W037 suites green through this change).
