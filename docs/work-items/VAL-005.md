# VAL-005 — Baseline/Comparator Harness for Direct, Optimized and Competing Stacks

**Work order:** `spec/validation-work-orders/VAL-005.md` (delegated Architect issuance, 80b6079 lineage)
**Assurance:** HIGH_ASSURANCE
**Dependencies:** VAL-001 complete (PR #42)
**Dispatch base:** `9c9c0b6` (governed validation head; main CI green at base)
**Implementation head:** recorded in the PR head of `work/VAL-005-comparator-harness`
**Mode:** Tech Lead direct implementation (chat.z.ai worker channel unavailable — account server-side blocked until 2026-09-18)

## Changed-file inventory

- `benchmarks/validation/comparators/guards.ts` — mechanical fair-comparison guards: `pinComparisonPlan` (thresholds declared BEFORE runs, content-digested and frozen) + `verifyFairExecution` (identical slices per arm, unplanned/substituted/skipped task detection, threshold-drift detection, undeclared-arm detection).
- `benchmarks/validation/comparators/baseline.ts` — the baseline application contract (transport-injected, network-free in the benchmark tree): identical inputs, the shared `ObservedOutcome` shape the VAL-008 engine consumes, documented integration notes + optimizations per run.
- `benchmarks/validation/comparators/direct-provider.ts` — the direct single-provider baseline template (OpenAI-compatible request shape, no routing/retries/cache, direct cost accounting) + `runDirectTask`.
- `benchmarks/validation/comparators/optimized-baseline.ts` — the optimized non-Zeck baseline (documented optimization set: prompt compression, content-identity response cache, bounded retry, batching) with the `ResponseCache`.
- `benchmarks/validation/comparators/competing-stack.ts` — the competing-stack integration contract + `declareCompetingStackNotRun` (honest NOT RUN with the exact access requirement).
- `benchmarks/validation/comparators/index.ts` — public barrel.
- `tests/unit/validation/comparator-harness.test.ts` — 13 tests: plan pinning + validations, fair-execution verification (substitution/skips/drift/undeclared-arm discriminations), template contracts, the fake-transport direct run evaluated through the SHARED engine.
- `tests/integration/validation/direct-baseline-openrouter.test.ts` — the REAL direct-provider baseline (env-gated): 3-task corpus slice through OpenRouter's own chat endpoint with per-run identities and shared evaluation.
- `docs/work-items/VAL-005.md` — this evidence document.

## REAL comparison evidence (executed 2026-09-11 ~23:20 UTC)

Pinned plan: arms [zeck, direct-openrouter], slice `text.summarize-doc.v1#000..002`, thresholds declared before runs (rubric 0.7, terminal-failure ≤ 0.34, latency ≤ 60s).

| Task | Direct baseline transport | Shared evaluation verdict |
|---|---|---|
| text.summarize-doc.v1#000 | HTTP 200, 2072ms | inconclusive (3/3 oracles pass — the rubric task awaits a judge, honest per the engine) |
| text.summarize-doc.v1#001 | HTTP 200, 963ms | inconclusive (2/2 oracles pass) |
| text.summarize-doc.v1#002 | HTTP 200, 597ms | fail (2/3 oracles — the response did not contain the expected "root cause" phrase; recorded honestly) |

Fairness verification: zero violations (identical slice, pinned thresholds, declared arms). Every run carries a `val-run-<sha256>` identity.

## Acceptance-criteria mapping

| AC | Evidence |
|---|---|
| 1. Direct-provider baseline executes corpus tasks with identical inputs/evaluation | `direct-provider.ts` + the REAL OpenRouter run above |
| 2. Optimized non-Zeck baseline template with documented choices | `optimized-baseline.ts` (4 documented optimizations, per-run recording, response cache) |
| 3. Competing-stack baseline with documented integration | `competing-stack.ts` + NOT RUN declaration pattern |
| 4. Comparator runs each arm over the SAME slice with shared evaluation + run identities | the pinned plan + the fake-transport unit test (shared engine) + the real run (per-run identities) |
| 5. Mechanical fair-comparison guards | `pinComparisonPlan` + `verifyFairExecution` discriminations |
| 6. Missing baseline access recorded NOT RUN with exact requirements | env-gated integration test + `declareCompetingStackNotRun` |

## Executed battery (at the implementation head, sequential)

| Command | Outcome | Detail |
|---|---|---|
| `bun run typecheck` / `lint` | pass | 0 errors / exit 0 |
| `bun run test:unit` | pass | 244 files / 3520 tests (+13 comparator tests) |
| `bun run test:architecture` | pass | 106 files / 1423 tests (comparator tree network-free by contract) |
| real OpenRouter baseline (env-gated) | pass | 2 tests; 3 real provider calls recorded above |
| governance + validation checks | pass | OK / OK |

## NOT RUN boundaries

- The Zeck arm of the real comparison: Zeck's real model routing rides the app portfolio (VAL-010+) where the platform's provider dispatch is exercised end to end; the Zeck-side machinery (harness, evaluation, guards) is proven by the unit comparisons and the VAL-002 crown proof.
- The optimized and competing-stack arms in live execution: the optimized baseline is transport-injected (executable wherever a provider credential exists); the competing stack is declared NOT RUN until the operator provides stack access (exact requirement recorded).
- chat.z.ai worker dispatch unavailable (account blocked until 2026-09-18).

## Surface honesty

- Zero `src/` changes; zero `spec/` changes; no flattering comparisons (the failed oracle verdict is recorded, not hidden); credentials only as env-var references; this PR does not merge itself.
