# Work Item Evidence

Each Work Order has a corresponding evidence record in this directory once implementation begins.

Recommended filename: `WORK-NNN.md`.

Evidence is not authoritative by itself. The authority chain is:

```text
Work Order -> implementation PR -> checkpoints/verification -> architect review -> merge -> program-state finalization
```

PR comments and chat are coordination only.
