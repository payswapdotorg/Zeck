# WORK-033 Evidence — Zeck UX experience shell and dashboard realization (reconciled pass)

Work Order: `WORK-033` (spec/work-orders/WORK-033.md) · Assurance: **HIGH_ASSURANCE** · Requirement IDs: N/A (presentation realization; frozen requirement ownership untouched)

**This is the second implementation pass, on the reconciled contract.** The first pass (PR #58, final head `35d7003`, frozen base `9bc408d`) was rejected by the Architect on 2026-09-03: its dashboard constructed the SDK client WITHOUT an application scope, while the real API requires the canonical `X-Zeck-Application` header on every scoped read and governed command — the dashboard passed its fake-API integration world only because that world did not enforce the requirement. WORK-034 (PR #59, merged as `5579c6516a2aaf983b4a0615f85644096c53a49f`) reconciled the contract: the canonical `ZECK_APPLICATION_HEADER` in `src/shared/wire.ts`, the single-sourced server rule, the scoped SDK client (`ZeckClientOptions.applicationId`, fail-fast when a scoped method is called without a scope) and the real-server × real-SDK cross-tier proof. Per the Architect's activation direction (issue #56): a **fresh branch from the reconciled base**, the dashboard's SDK client **constructed with `applicationId`**, the integration/fake-API proof updated **so scoped reads and governed commands require the application header**, and the complete WORK-033 evidence re-run at the final exact head. PR #58 was closed as obsolete and was NOT resurrected: nothing from its branch was pushed or reused except the implementation content re-verified and re-committed onto the fresh base below.

Frozen base: `5579c6516a2aaf983b4a0615f85644096c53a49f` (the WORK-034 merge — the reconciled transport contract; the governance state at that exact revision is self-consistent: 34 Work Orders, frontier=['WORK-034'], WORK-033 pending with its WORK-034 dependency complete on main). Branch: `work/WORK-033-ux-dashboard-reconciled` · **Final head: this doc's commit** (the house two-phase binding — the exact SHA is recorded by the orchestrator in the PR body; the last code commit is `26c437d`) · Zero merge commits; the merge-base is `5579c651…` exactly.

## Implementation history (the ratchet — every commit compiles and is full-suite green at the reconciled base)

1. `308f9f3` — the view system **with the application-scoped SDK client binding**: the ported dashboard realization (the design tokens, the HTML-first typed component system, the pure projection model, the app shell, the HTTP kernel, the entry server, the full route map, the client script and the four unit suites — the first pass's `8025292` content, re-verified file-by-file on the fresh base) PLUS `DashboardOptions.applicationId` (required, validated non-empty at construction: a scopeless dashboard could create but never render, so it fails fast at the deployment boundary), the binding into `createZeckClient`, and the direct-execution entry's required `ZECK_APPLICATION_ID`.
2. `26c437d` — the proof suites **on the enforced contract**: request-mapping, the (a)–(l) integration journeys and the D1–D8 surface discriminations. The fake-API world now implements the REAL application-scope wire rule: every scoped read and governed command requires the canonical header (missing ⇒ 422 `CAPABILITY_UNAVAILABLE` with the server's exact surface-prefixed message, mirrored from WORK-034's single-sourced `applicationScopeOf`), and visibility is keyed to the request's application scope (a cross-scope miss is an indistinguishable 404 — the real M1 rule; the 404 shape is the real `CAPABILITY_UNAVAILABLE` "execution not found"). The (l) journeys prove both sides (the bound client's header on every scoped wire call; the headerless 422; the cross-scope 404 + empty agent inventory; the cross-scope cancel that cannot touch the execution; the scopeless-construction rejection). D8 pins the binding structurally (the mutant dropping `applicationId` from `createZeckClient` is flagged — the PR #58 rejection defect, made impossible to silently reintroduce).
3. This doc (the final head).

The ported content was critically re-reviewed against the reconciled base before committing (the wave-7/8 ratchet convention): the view system is unchanged except the binding (the UX realization was accepted in review — only the transport contract was rejected); all test-world semantics were re-derived from the REAL server sources at `5579c651…` (`src/api/request-identity.ts`, `src/api/routes/executions.ts`, `src/api/routes/agents.ts`, `sdk/index.ts`), not from the first pass's fake world.

## Gate runs per commit (the ratchet record; the suite at the exact committed tree)

- `308f9f3`: governance OK (34 orders, frontier=['WORK-034'] — the branch's own self-consistent state at the base) · typecheck 0 errors · biome clean (949 files) · full suite with real PG = **3687 tests passed** (3604 baseline + 83 dashboard unit tests; verified at the committed tree with the later files stashed).
- `26c437d`: governance OK · typecheck 0 errors · biome clean (952 files) · full suite with real PG = **3760 tests passed** (3604 + 156 dashboard tests; the only unstaged file was this doc, which the gate is inert to).
- The doc commit (final head): the complete gate below, run TWICE consecutively.

## The application-scope reconciliation (the core of this pass)

- **The binding**: `createDashboard` requires `DashboardOptions.applicationId` (non-empty, validated) and constructs the SDK client with it — every scoped read (execution detail/results/events/verification, agent inventory + status) and every governed command (cancel) sends the canonical `X-Zeck-Application` header the real API requires; the effective tenant/application scope is still derived server-side (the selector never authorizes by itself). Creation keeps its per-request `applicationId` in the request body — the wire contract's split selector (body for creation, header for scoped access), unchanged from the first pass's form flow.
- **The direct-execution entry** (`bun run apps/dashboard/index.ts`) requires `ZECK_APPLICATION_ID` alongside `ZECK_TOKEN` and exits with a clear message when missing.
- **The integration world enforces the real rule**: the fake `fetchImpl` mirrors WORK-034's single-sourced `applicationScopeOf` exactly — surfaces "execution reads" / "execution commands" / "agent inventory reads", 422 `CAPABILITY_UNAVAILABLE` with the canonical message on a missing/empty header, and per-scope visibility (executions are matched by id AND application scope; agents are inventoried per application; a miss is an indistinguishable 404). The first pass's world accepted headerless scoped reads — that omission is exactly what let the defective client pass; it is now impossible.
- **The proof**: the (l) journeys — (1) every scoped wire call the dashboard makes carries the bound application (recorded per call by the world); (2) a raw headerless scoped read is 422 with the exact real message; (3) a second dashboard bound to a different application, sharing the SAME world, gets the honest 404 for this world's executions and an honestly empty agent inventory (never a leak, never a fabricated error); (4) a cross-scope cancel is a 404 and does NOT transition the execution; (5) a scopeless construction throws. D8 additionally pins the binding statically. The real-server × real-SDK behavior itself is pinned by WORK-034's own cross-tier suite (`tests/unit/api/sdk-reconciliation.test.ts`, merged at the base — untouched here).
- **Legacy behavior preserved (AC10)**: the legacy routes (POST /executions/:id/cancel, GET /executions/:id, GET /executions?id=) flow through the SAME bound client — API-backed and tenant-safe under the reconciled contract.

## The integration world (an honest disclosure of the test double)

The journeys' fake `fetchImpl` implements the public API wire surface over an in-memory world: the create path enforces the REAL contract semantics (mandatory Idempotency-Key header, the closed create body vocabulary — unknown keys rejected 400, same key + same fingerprint → the same receipt with `replayed: true`, same key + different fingerprint → 409 `IDEMPOTENCY_KEY_REUSED`); the cancel path enforces the governed transition (scope-checked visibility → idempotent replay → terminal check 409 → the transition lands `CANCELLED` + the `execution.cancel` event); and (this pass) the scoped routes enforce the REAL application-scope rule described above. The dashboard never knows the difference — it only ever speaks to the SDK client — and the REAL API behavior is pinned by the existing (untouched) API suites and WORK-034's cross-tier proof. Test-local state (the world Maps) is allowed by the briefing exactly as the frozen R-M24 scan scopes to `apps/`.

## The architecture decision (DISCLOSED deviation from the UX plan's recommendation — carried from the accepted first-pass review)

`docs/UX-IMPLEMENTATION-PLAN.md` recommends a React implementation shape. This realization is a **zero-dependency, server-rendered, HTML-first typed component system with native progressive enhancement** (TypeScript functions returning escaped HTML strings; `<details>/<summary>` for all progressive disclosure; real links for tabs; GET forms for search and the create review; POST forms for the two governed mutations; one small vanilla-JS enhancement script served as a static asset). Reason: the frozen gate `tests/architecture/dependency-direction.test.ts` pins root `package.json` dependencies to exactly `["fastify"]`; React would require either an out-of-surface semantic edit of that frozen gate, a dishonest devDependencies placement of a runtime dependency, an out-of-surface root `tsconfig.json` "jsx" edit, or an `apps/dashboard`-local install that CI's root `bun install --frozen-lockfile` would not cover (red CI). The accepted `docs/UX-ARCHITECTURE.md` §32 non-goals explicitly include "require a particular frontend framework", so the binding UX contract does not require one. **One disclosed route addition** (carried from the first pass): `GET /appearance` (a GET form target that sets the presentation cookie and redirects back) — the no-JavaScript fallback for the appearance control; it changes no platform fact, mutates nothing, and is pinned in navigation.test.ts.

## The honest projection map (live vs honestly unavailable — NEVER fabricated)

The SDK client (`createZeckClient`, exactly 8 methods, now application-scoped) is the ONLY transport. **Live surfaces**: Home's attention/active/recent (live re-reads of the recents ids), the execution detail (getExecution/getResult/listEvents/listVerification), create (`createExecution` — body scope), cancel (`cancelExecution` — header scope), agents inventory + detail (`listAgents`/`getAgentStatus`), command-search agent matches (`listAgents`), artifact anchors from executions opened in this browser (per-execution `getResult` facts). **Honestly unavailable** (the public API exposes no route — each renders the unavailable-state primitive with a one-line user-language concept explanation and the pointer to where its facts WILL come from): execution LISTING (no listing route — hence lookup-by-id + browser-local recents), scheduled runs, artifact inventory/content/lineage (artifacts cross only as per-execution references), competences, connections (with the secret-safety note), evaluations, insights, learning, policies, budgets, team, environments, audit, deployment views, workload/training authoring, agent creation (read-only agents surface, M14/M15). No economic-action or codebase-analysis surface is rendered at all (never hand-rolled fetch — SDK only).

## The mutation inventory (EXACTLY two, both SDK-mediated)

1. `createExecution` — from the POST /build/execution review form. The **idempotency key is generated at step 1** (`dash-<uuid>`), carried as a hidden field through the review, the edit link, the invalid-review re-render and the platform-rejection re-render, so retries converge on one durable execution (pinned: request-mapping + journeys (b); the fake API enforces the real semantics). The request body carries the per-request `applicationId` (the creation scope selector — the wire contract's split rule).
2. `cancelExecution` — only from the confirmation-gated flow (`?action=cancel` view → POST /runs/:id/cancel, or the preserved legacy POST /executions/:id/cancel). The confirmation states the consequence and the authorization; its own idempotency key makes a double submit converge; a terminal-execution cancel surfaces the platform 409 as a redirect to the unchanged live view. The scoped command carries the bound application header (journeys (l)).

Everything else is a GET. The command surface proposes mutations ONLY as links into the confirmation flow (D7). The form→request builder can never emit a forbidden key (closed vocabulary; pinned in request-mapping + journeys).

## The recents cookie (DISCLOSED navigation-only presentation state — carried from the first pass)

`zeck_recent_executions` (comma-separated ids, max 8, most-recent-first, HttpOnly, SameSite=Lax, Path=/), set/refreshed on every /runs/:executionId GET, dropped ids pruned when the live read 404s. It is **navigation state, never a fact cache**: every Home/Runs/Active/History/Artifacts view re-reads each id LIVE through the SDK on every request; no fact is ever served from the cookie; it is never sent to the API; the sections are labeled "recently opened in this browser — navigation only; every view reads live through the governed API". The appearance cookie (`zeck_appearance`, not HttpOnly so the enhancement script can keep it in sync) is presentation state only, allowed by the plan's data rules.

## The trust-axes model (the trust checkpoint — four SEPARATE axes, never one score; unchanged from the accepted first pass)

| Axis | Honest derivation | Fact source |
|---|---|---|
| Provider success | route ≠ null && modelCalls > 0 → "Provider calls completed (N)"; route null → "No route recorded yet"; calls 0 → honest zero-call label | ExecutionResult.route |
| Execution success | COMPLETED → "Execution completed"; FAILED/CANCELLED/EXPIRED → the honest terminal label; else "In progress (<status>)" | Execution.status |
| Quality success | "M of N checks passed"; zero results → "No verification results recorded" — NEVER a confidence verdict; the derived "High confidence" chip appears ONLY when all checks PASS and every confidence is present, and always carries its derivation ("N/N checks passed") | ExecutionResult.verification / listVerification |
| Policy success | `execution.policy-denied` event → "Policy denied admission" (also surfaced as an attention card); progression past CREATED (status ≥ AUTHORIZED or any non-created event) → "Admitted by policy"; else "Not yet admitted" | Execution.status + events |

Pinned in trust-state.test.ts (19 tests incl. the fabricated-confidence mutant pin), components.test.ts, journeys (a).

## Surface diff inventory (every touched file, vs the frozen base `5579c651…`)

| File | Purpose (one line) |
|---|---|
| apps/dashboard/tokens.ts (new, 492) | The ONE stylesheet: semantic design tokens, light/dark/system, 1024/640px responsive contract, ≥44px mobile targets, focus-visible, reduced-motion |
| apps/dashboard/components.ts (new, 586) | The typed HTML component system (header/verification/timeline/why-panel/attention/result/state primitives/badge/money) |
| apps/dashboard/projection.ts (new, 608) | Pure view-model derivation: trust axes, titles/stages, secret-shape redaction, recents helpers, form→request mapping |
| apps/dashboard/shell.ts (new, 299) | The app shell: skip link, header + command search + appearance, the UX §3 nav tree, main/footer landmarks |
| apps/dashboard/http.ts (new, 229) | The pure routing kernel: route table, params, size-capped form reading, cookies, response constructors |
| apps/dashboard/pages.ts (new, 1609) | Page composition for the whole route map incl. the honest unavailable states and the legacy routes |
| apps/dashboard/client.ts (new, 66) | The ONE enhancement script (Cmd/Ctrl+K, appearance, roving focus) served as /assets/client.js |
| apps/dashboard/index.ts (modified) | createDashboard entry: **the required application scope validated and bound into the SDK client** (WORK-034 reconciliation) + http server + dispatch + the public-error-shape error pages + the ZECK_APPLICATION_ID direct entry |
| apps/dashboard/package.json (modified, 1 line) | Description only (no dependency changes) |
| tests/unit/dashboard/html-escape.test.ts (new) | The esc boundary over every component surface |
| tests/unit/dashboard/components.test.ts (new) | Every component contract incl. trust honesty pins |
| tests/unit/dashboard/trust-state.test.ts (new) | The four-axis derivation table + mutant pins |
| tests/unit/dashboard/navigation.test.ts (new) | Route map, IA tree, active marking, legacy 303s, 413, a11y frame, responsive/appearance evidence — on the bound client |
| tests/unit/dashboard/request-mapping.test.ts (new) | Form→ExecutionRequest mapping + forbidden keys + idempotency key carriage — on the bound client |
| tests/integration/dashboard/journeys.test.ts (new) | Journeys (a)–(l) over the real server + the scope-ENFORCING fake-API world with real idempotency semantics |
| tests/discrimination/dashboard-surface.discrimination.test.ts (new) | D1–D8 static + runtime discriminations over the real apps tree (D8 = the application-scope binding pin) |
| docs/work-items/WORK-033.md (this doc) | The evidence record |

No file outside `apps/dashboard/**`, the new dashboard test files and this doc was touched. No migration claimed (inventory 0001–0025 + WORK-034's none; 0015 burned). No spec/development-state/ file was touched (the Work Order forbids it; the branch carries the base's own self-consistent governance state).

## Test inventory (exact counts — 7 files / 156 tests; +7 files / +156 over the 263/3604 reconciled baseline)

| File | Tests | Proves |
|---|---|---|
| tests/unit/dashboard/html-escape.test.ts | 11 | Hostile values through every component surface render escaped (incl. attribute/head contexts, secret-shaped task fields) |
| tests/unit/dashboard/components.test.ts | 33 | ExecutionHeader facts; VerificationSummary honesty (0 results ⇒ no invented confidence; all-PASS+confidence ⇒ derived chip with derivation); timeline chronology + verbatim unknown types + payload-carried percentages only; WhyPanel platform-facts-only (provider/model ONLY inside the advanced disclosure); attention variants; ResultSurface next-actions per status family; AdvancedDisclosure collapsed-by-default; badge symbol+text; money/duration formatting (BigInt exactness) |
| tests/unit/dashboard/trust-state.test.ts | 19 | The four-axis derivation table + the fabricated-confidence mutant pin + axis separation (denial ≠ failure) |
| tests/unit/dashboard/navigation.test.ts | 20 | Every route renders (incl. tabs/views/action=cancel); the IA tree exactly; aria-current marking; legacy 303 preservation; the 413 page; the client.js asset; the a11y frame on every page; viewport/meta-query/44px/reduced-motion/light-dark-system evidence; the appearance route |
| tests/unit/dashboard/request-mapping.test.ts | 16 | dollarsToMicroUsd exactness/rejections; per-field validation; closed vocabulary (never a forbidden key); the SDK's client-side forbidden-key rejection; the honest wire body + idempotency header; the key's carriage through review/edit/re-render; never a keyless create |
| tests/integration/dashboard/journeys.test.ts | 36 | Journeys (a)–(k) as in the first pass (first execution end-to-end; idempotent create convergence + the 409 re-render; failed/waiting/cancel flows; agents; command surface links only; legacy routes; 404; 502 + unreachable transport; recents cookie; a11y frame + hostile echo escaping) PLUS the (l) reconciliation journeys: the bound header on every scoped wire call; the headerless 422 with the canonical message; the cross-scope indistinguishable 404 + empty inventory; the cross-scope cancel that cannot transition; the scopeless-construction rejection |
| tests/discrimination/dashboard-surface.discrimination.test.ts | 21 | D1–D8 (below) |

**D1–D8**: D1 every `client.<method>(` call site in the real apps tree ∈ the exactly-8 SDK methods (13 call sites today; mutant flagged); D2 no direct `fetch(` in apps code (mutant flagged); D3 no module-level Map cache pattern in ANY apps file — R-M24 extended beyond index.ts (mutants flagged in pages.ts AND components.ts); D4 `publicSurfaceViolations` empty over the real apps file list + no SQL-shaped text (provider-literal and SQL-comment mutants flagged); D5 hostile secret-shaped task/metadata/payload values never echo into any rendered surface (result tab, raw activity JSON, failure-message path); D6 zero-verification honesty with the fabricated-confidence mutant pin; D7 the /command page carries links only (no POST form — mutant flagged) and a proposed cancel links into the confirmation flow; **D8 (new this pass)** the `createZeckClient` construction in apps/dashboard/index.ts binds `applicationId: options.applicationId` and the direct-execution entry requires `ZECK_APPLICATION_ID` (the mutant dropping the binding is flagged — the exact PR #58 rejection defect).

## Acceptance-criteria mapping (the Work Order's AC1–AC12)

| AC | Where realized | Pinned by |
|---|---|---|
| 1 Home outcome-first + attention/active/recent, no analytics aesthetics | Home's textarea-first form + suggested actions, the attention/active/recent sections from live re-reads, no charts (flat tokens, no gradients) | journeys (a)/(j); navigation (CSS no-gradient pin) |
| 2 Execution detail Result/Evidence/Activity + header facts + How Zeck did it | executionDetailPage + executionHeader/whyPanel/resultSurface/tabs | components; trust-state; journeys (a) |
| 3 Runs active/history/scheduled + recoverable failure/waiting | runsOverview/active/history/scheduled + failedSurface/waitingSurface | navigation (route map); journeys (c)/(d) |
| 4 Build outcome-first entries with proposed-plan review | buildOverview + the two-step execution flow + the agent/workload proposal entries with honest terminal states | request-mapping; journeys (a)/(b) |
| 5 Agents/artifacts/competences/connections/improve/admin map to API objects without parallel state machines | Live agents reads (application-scoped); honest unavailable states for every unexposed surface; D1/D3 prove no second authority | navigation; journeys (e); discrimination D1/D3 |
| 6 Global command/search + mutations only through governed paths | The shell command form + /command; D7 (links only) | journeys (f); discrimination D7 |
| 7 Responsive desktop/tablet/mobile preserving hierarchy | tokens.ts media queries + the same nav DOM + the driven browser below | navigation (CSS evidence); the browser record |
| 8 Keyboard/screen-reader usable, non-color status | The a11y contract section + the driven keyboard record below | navigation; journeys (k); components (symbol+text); the browser record |
| 9 No raw credential/secret/authoritative backend state in the frontend | Secret-shape redaction + esc boundary + D5; zero module state (D3) | html-escape; discrimination D3/D5 |
| 10 Existing developer dashboard behavior stays API-backed and tenant-safe | Every legacy route preserved (POST /executions/:id/cancel, GET /executions/:id, GET /executions?id=) with SDK-only reads — now through the application-scoped client: API-backed AND scope-safe under the reconciled contract | navigation; journeys (g) + (l); discrimination D8 |
| 11 Expert views without polluting the default experience | AdvancedDisclosure everywhere: route/compute/warnings, versions/selection, raw events/payloads, activity views | components; journeys (a)/(e) |
| 12 The public API mental model preserved (no second execution semantics) | The projection renders only wire shapes; the two mutations use the platform idempotency discipline; D1 pins the 8-method client surface; the scope selector follows the wire contract's split rule (body for creation, header for scoped access) | request-mapping; journeys (b)/(l); discrimination D1/D8 |

## Required checkpoint contracts (doc-only evidence per the house convention)

- **SELF-HOSTING-BOUNDARY**: the dashboard imports ONLY `../../sdk` + node builtins (D4's scanner run + the frozen architecture gate over the real tree); no provider literal, no SQL-shaped text, no module state (D1–D4); every fact is a live SDK read (D3, journeys); the transport scope is the SDK's own application scope — no dashboard-side scope authority exists (the client binding D8; the server derives the effective scope, WORK-034).
- **EXECUTION-PROVENANCE**: the trust axes + the verification table render only platform-recorded facts with their sources; the WhyPanel discloses what is NOT carried (capability detail, plan graph, route rationale); money renders from the platform's integer micro-USD strings; unknown events render verbatim (trust-state; components; journeys (a)).

## Required verification mapping (the Work Order's list)

governance checker / typecheck / lint / full suite → the complete gate below (twice at the final head). Existing dashboard tests → the legacy behaviors preserved and re-proven (navigation + journeys (g)). Frontend/unit component tests → the four unit suites. API projection/integration tests for primary journeys → journeys (a)–(l) over the real server against the scope-ENFORCING world. Authority-boundary discrimination → D1/D2/D3/D8. Secret-exposure discrimination → D5 (+ html-escape). Responsive browser verification + keyboard/accessibility verification → the driven-browser record below (real Chromium, this pass). Result/Evidence/Activity trust-state tests → trust-state + components. Command/action authorization-path tests → journeys (f) + D7.

## The route map (as shipped; query-param driven, zero server session state — unchanged from the first pass)

GET / · /home(→/) · /build · /build/execution (GET review + POST create) · /build/agent · /build/workload · /runs · /runs/active · /runs/history · /runs/scheduled · /runs/:id (?tab=result|evidence|activity, ?view=events|raw, ?action=cancel) · POST /runs/:id/cancel · /agents · /agents/:id · /assets/artifacts · /assets/artifacts/:id (?executionId=) · /assets/competences(+/:id) · /assets/connections · /improve/{evaluations,insights,learning} · /admin/{policies,budgets,team,environments,audit} · /command?q= · /assets/client.js · /appearance (the disclosed no-JS appearance fallback) · legacy: POST /executions/:id/cancel · GET /executions/:id (303) · GET /executions?id= (303).

## Accessibility evidence (the mechanical checklist, its pins, and THIS PASS's driven-browser record)

Every page: `<html lang="en">` (navigation/journeys (k)); unique `<title>` per route (navigation); exactly one `<h1>` (navigation + journeys (k) + the 404/413 pages); header/nav/main/footer landmarks + `role="search"` on the command form (navigation/journeys); skip link as the first focusable element (navigation/journeys; **driven: Tab lands on "Skip to main content" first, then the brand**); `aria-current="page"` on the active nav item AND the active tab (navigation); visible `:focus-visible` outlines (tokens.ts + navigation CSS evidence; **driven: the focused command input shows its outline**); labels on ALL inputs (shell + form fields); status by text+symbol, never color alone (statusBadge; components); tables with `th scope="col"`/`scope="row"` (components); inline form errors via `aria-describedby` + an `aria-live="polite"` region (request-mapping); confirmation before the destructive action (journeys (d); **driven: the cancel confirmation page**); ≥44px touch targets in the mobile breakpoint (tokens.ts + navigation; **driven: measured 44px on the mobile nav targets**); rem-based scalable typography (tokens.ts); `prefers-reduced-motion` honored (tokens.ts + navigation); native `<details>` disclosures (components; **driven: the nav group and the Why panel open**); no modals, no hidden destructive actions. **Keyboard driven**: Ctrl/Cmd+K focuses `#command-input` (driven this pass).

## Responsive evidence (mechanical + driven this pass)

Media queries at 1025px (persistent sidebar grid `nav header/nav main/nav footer`), ≤1024px (collapsed details-based nav bar, single-column detail grid) and ≤640px (single column, Home order preserved outcome → attention → active → recent, ≥44px targets) — pinned in navigation.test.ts (the CSS evidence tests) over `DASHBOARD_CSS`; `<meta name="viewport" content="width=device-width, initial-scale=1">` asserted on every rendered page. **Driven at 768×1024** (the nav renders as collapsed groups, content single-column) **and 390×844** (single column, nav group opens on interaction, 44px targets measured) — screenshots captured below.

## The driven-browser verification record (this pass — real Chromium over the real server with the BOUND client and the ENFORCING world)

A fresh orchestrator-side harness (outside the repo) boots the real `createDashboard` with `applicationId` bound and a fake-API world implementing the reconciled wire rule. Its boot self-proof: a raw headerless scoped read ⇒ **HTTP 422** from the world; the dashboard's scoped read through the bound client ⇒ **HTTP 200**; every recorded scoped wire call carries the bound application header. A real browser was then driven through: Home → the outcome form (filled) → the review step (scope/targets table + the idempotency key) → **Execute** → the created execution's run view (the scoped reads succeeded through the bound header); the Result tab (status/duration/cost/artifacts table), the **Evidence tab** (the four trust axes + the "never merged into a single score" statement), the **Activity tab** (the 7-event chronological timeline) and the **How Zeck did it** panel (understood task / plan / capabilities / route / compute — provider and model only inside the advanced disclosure); the keyboard contract (skip link first Tab stop; Ctrl+K → `#command-input` with a visible focus outline); the **WAITING_USER decision surface** → the cancel confirmation (consequence + authorization + idempotency disclosure) → **the governed POST** → the run view now **CANCELLED**; the command surface (`?q=agents` — results are links only, **0 POST forms** in the results, verified in-DOM); tablet 768×1024 (collapsed nav) and mobile 390×844 (single column, 44px nav targets measured, group opens); emulated **dark mode** (the dark token background); the agents inventory. **ZERO browser console errors and ZERO page errors across the entire driven session** (checked after the full run). 14 screenshots captured and machine-inspected (VLM) against the expected structures: home, review, created run, result, evidence, activity, waiting, cancel-confirmation, cancelled, command-agents, tablet, mobile, mobile-nav-open, dark. The screenshots live outside the repo (orchestrator-side evidence, `/home/z/w033b-browser-*.png`); the in-repo executable evidence is the (a)–(l) journeys + the harness self-proof semantics above.

## The complete gate (run at the final head, TWICE consecutively green)

- RUN 6: governance `Governance OK: 34 Work Orders, 102 requirements, inFlight=[], frontier=['WORK-034']` (the branch's own self-consistent state at the frozen base — the Architect's post-merge main-side repin is the authoritative record; this Work Order does not touch spec/development-state/) · typecheck 0 errors · biome clean (952 files) · full suite with real PG (`ZECK_PG_TEST_URL=postgres://postgres@127.0.0.1:55432/zeck_w033b_gate`) = **270 files / 3760 tests passed** (rc=0).
- RUN 7: governance OK · typecheck 0 errors · biome clean (952 files) · full suite with real PG = **270 files / 3760 tests passed** (rc=0) — consecutive with RUN 6.

**Flake disclosure (honest, the WORK-034 record's own convention)**: across the recorded runs at this head, TWO runs each observed ONE intermittent failure — always the inherited `tests/integration/postgres/computer-use-lifecycle.test.ts > CUI concurrency (physical convergence in PostgreSQL) > N=8 same-key dispatchAction calls converge to ONE action row` (WORK-027's computer-use surface, the documented wave-5 flake family: a `sql-computer-use-store` gapless-sequence guard under 8-way physical concurrency; the same family WORK-034's record disclosed in its byoa variant). Zero relationship to this work item: the failing surface (`src/modules/tools/adapters/`) and its imports are untouched by WORK-033 (the changed surface is apps/dashboard + dashboard-local tests + this doc); the test passes **17/17 in isolation, twice re-verified**; the baseline at `5579c651…` ran green (263/3604) and the twice-consecutive-green standard was met at the exact final head (RUN 6 + RUN 7, rc=0 both).

(The gate is inert to this doc — governance reads spec/work-orders, typecheck/vitest discover only TS/tests, biome scans the code files with or without it — so the recorded runs execute identically at the last code commit and at this doc's commit; the orchestrator executed the full gate at the exact final head, per the house two-phase binding.)

## Limitations (honest)

- The driven browser is Chromium via automation; no multi-browser matrix (no Firefox/Safari rendering pass), no visual regression harness beyond the captured screenshots.
- The client script's Cmd/Ctrl+K, appearance toggle and roving focus are verified by driven interaction for the K-shortcut and focus outline, and at the source/asset level for the rest (the appearance toggle and roving focus are not click-driven in this record; the /appearance no-JS fallback route is test-pinned).
- No real multi-user/session matrix: the journeys drive one cookie jar; the 401/403 permission-denied page is exercised through the public error shape but not through a real authenticated multi-tenant API.
- The fake-API world is a faithful mirror of the reconciled wire rule (message-exact, surface-exact, semantics derived from the real route sources at the base), but it is still a mirror — the real server's own behavior is pinned by WORK-034's cross-tier suite and the API route suites, both merged and untouched here.
- English-only copy (the house language rule); no i18n.
- The recents cookie scopes Home's attention/active/recent to ONE browser (disclosed design, not a platform listing).
- A deployment that binds the dashboard to application X while a user creates an execution under application Y in the form will get an honest 404 view of that execution (the governed transport scope is X) — correct-by-construction, disclosed here as a UX limitation of the split selector (the create body scope vs the transport header scope), not a defect.

## No-merge statement

The implementer does not merge. The Architect (repository owner) is the merge authority; the orchestrator performs the independent verification and opens exactly one PR. Zero spec/, src/, sdk/, cli/, scripts/ or root-config files were touched by this work item.

**Final head: this doc's commit (the branch head; the exact SHA is recorded by the orchestrator in the PR body per the house two-phase binding). The last code commit is `26c437d`; the complete gate ran green twice at the final head (both runs recorded above).**
