# WORK-046 Evidence — Execution worker deployment fabric

Work Order: `WORK-046` (spec/work-orders/WORK-046.md) · Canonical remote: **`payswapdotorg/Zeck`** · Canonical issue: **#9** · Assurance: **HIGH_ASSURANCE** · Governing architecture: Deployment & Runtime Architecture **D1.0** (subordinate to frozen v1.0), roadmap phase **D-05**.

Exact dispatch base: `e6b417fd5c9dfaf6fb00135a62d529cc9ccc6db9` (verified present; the worker branch was created at exactly that SHA and carries the D-05 implementation commit + this evidence document; zero merge commits). Branch: `work/WORK-046-execution-worker-deployment-fabric` · **Final head: this doc's commit** (the D-05 implementation commit `0bb4c63...` carries the code; this doc's commit follows it on the same branch). One PR, opened by the worker, **not merged by the worker**.

## Baseline gate at the exact dispatch base (readiness checkpoint — BEFORE implementation)

- **`python3 scripts/governance-check.py` PASSED at the dispatch base** — `Governance OK: 46 Work Orders, 102 requirements, inFlight=['WORK-046'], frontier=[]` (the first dispatch of the program whose registration commits precede the binding base; the base carries the green gate and the branch carries **zero** `spec/` changes, mechanically verified).
- `bun run typecheck` — 0 errors at `e6b417f`.
- `bun run lint` — biome clean (1072 files) at `e6b417f`.
- Full suite with real PostgreSQL 16.2 (`ZECK_PG_TEST_URL`, 127.0.0.1:55432) at exactly `e6b417f`: **330 files / 4562 tests, 4562 passed, 11 skipped** (5 real-R2 + 2 real-queue + 4 real-workflow NOT RUN with exact reasons), **0 failed**. Executed as the three sequential script invocations `test:unit` / `test:integration` / `test:architecture` (163 / 87 / 80 files).

## Provider account/resource access (readiness checkpoint)

**NOT RUN — no Cloudflare (or other provider) credential exists in this worker environment.** The only credential held is the operator-provided GitHub PAT for `payswapdotorg/Zeck` (environment-only; used solely for the Git/PR lifecycle of this Work Order). No container-runner host exists in this sandbox.

Consequences, per the evidence contract (never convert unavailable provider access into a PASS):

- **Live container-runner round-trip: NOT RUN** — no runner host exists. The runner adapter is instead verified against the **documented** Zeck container-runner REST protocol (deploy/README.md §D-05: `POST /v1/runs` with `{runId, config, timeoutMs}` → 202/409; `GET /v1/runs/{runId}` → running/terminal shapes/404; Bearer auth; 400/401/403/404 → permanent, 429/5xx/network/timeout → transient; bounded output) over **real HTTP against an in-process protocol-verifying server** (`tests/integration/compute/container-runner-protocol.test.ts`) — protocol correctness, explicitly NOT a deployed-runner claim. The real-runner suite (`tests/integration/compute/runner-live.test.ts`) is env-gated on `ZECK_CONTAINER_RUNNER_URL` / `ZECK_CONTAINER_RUNNER_API_TOKEN` and **skips with the exact missing-variable reason**.
- **Real HTTP reachability + classification evidence (no runner deployed):** `deploy:smoke`'s execution-compute concern probe, executed in BOTH directions: unconfigured → the exact `ZECK_SECRET_CONTAINER_RUNNER_TOKEN_REF` unavailability; materialized with an invalid token against a REAL local HTTP endpoint answering 401 → the honest `runner rejected the credential (http 401); the token binding must be repaired before dispatching compute` — fail closed, never a silent success.
- The worker service composition (`deploy:worker`) is **executed against the real local environment** (the converged `zeck_local` database, 27/27 migrations): `inspect` (the read-only worker-plane snapshot + the bounded policy + the exact Git revision), `compact` (honest zero-activity outcome with the executions-authority terminality seam), `quota` (the per-compute-environment quota set + read-back: `{quota: 4, liveClaims: 0}`), and the **full governed customer-runner lifecycle** (`register` → pending, `activate` → active, `suspend` → suspended, `activate` → active again, `revoke` → revoked with the exact reason — the attributable, revocable, NON-AUTHORITATIVE contract, executed against real PostgreSQL).
- `run-once`/`run`/`recover` require the queue transport configuration (Cloudflare Queues credential — no material exists in this sandbox); executed to their exact honest fail-closed configuration errors. The worker fabric itself (the full claim→lease→settle→work→completion pipeline, the recovery scan, the drain) is proven by the real-PostgreSQL integration battery below — the transport double implements the D-03 at-least-once pull-consumer port contract.
- The durable PostgreSQL worker-plane machinery (claims, quotas, registrations, runners, fencing convergence) is proven over **real PostgreSQL 16.2 through the production `DatabasePort` adapter** (the established local-evidence convention).
- No provider resource in any non-GitHub account was mutated by this worker.

## What this order IS

D-05: the execution-plane worker fabric — a provider-neutral, independently deployable worker service that consumes asynchronous dispatch deliveries and carries executions to durable completion, while PostgreSQL and the existing execution/policy/capability/budget/secret/tenant/verification authorities remain the only authority. Durable execution leases/heartbeats with stale-worker fencing at the authoritative persistence boundary, cancellation and cooperative interruption through the governed paths, graceful drain/shutdown with bounded in-flight convergence, bounded worker concurrency and per-environment quotas enforced before resource admission, idempotent dispatch consumption with correlation to existing execution identity, crash/restart recovery driven from the executions authority alone, the concrete container `ComputeEnvironment` runtime behind the documented container-runner REST protocol, the governed (attributable, revocable, non-authoritative) customer-runner registration contract, and the operator surface for all of it. No D-06 observability, no D-07 multi-region/DR, no second execution state machine, no frozen-architecture change.

## The worker model (what the machinery actually does)

- **The compute-plane port** (`src/platform/compute/port.ts`): the provider-neutral contract family — the FOUR module seams the owning modules implement (`ExecutionDispatchStartEffect` — the governed lifecycle re-entry; `WorkerLeaseAuthority` — the single lease system composed, never duplicated; `ExecutionWorkExecutor` — the owning module's admission+execution; `WorkerCompletionEffect` — the lease-guarded commit), the `ComputeWorkerStore` (the durable worker/claim/quota/runner state, schema `compute_plane`), the bounded `WorkerFabricPolicy` (every number validated fail-closed), the claim-gate refusal vocabulary, and the disjointness anchor (`WORKER_PLANE_STATE_VOCABULARIES`). No runner-protocol vocabulary ever appears on the port (architecture B2); the worker-plane state vocabularies are mechanically disjoint from the 14 frozen execution states (unit suite + B4 over the sources).
- **The fabric engine** (`src/platform/compute/fabric.ts`): the claim-carries-the-work model — pull (bounded batch/visibility) → envelope resolved from PostgreSQL AUTHORITY FIRST (binding + digest integrity fail-closed) → the START effect with the D-03 deterministic key family (the request-plane consumer and this worker are ONE governed start operation per correlation key) → work resolution (the honest not-executable dead-letter for non-sandbox tasks) → the ATOMIC claim gate (per-worker concurrency, per-environment quota, bounded re-selection attempts, one live claim per execution — physically enforced) → the durable execution lease from the WORK-028 single lease system (the correlation recorded on the claim, set once) → **the delivery settles here** (the message is the wake-up; the durable claim + lease carry the work) → work execution with the heartbeat sidecar (claim heartbeats + lease renewals with per-logical-renewal idempotency + cancellation observation; the sidecar join is signal-bounded — bounded shutdown) → the completion effect under the lease fence (success rides `verify`+`pass` with the MANDATORY verification binding; failure rides the governed `fail`) → the claim finishes with its bounded, inspectable outcome.
- **The dispositions are the guarantee**: post-settlement fenced/interrupted/refused outcomes ACK (the recovery scan re-drives from durable state — never re-fails a consumed envelope); pre-settlement refusals re-deliver within the bounded transport budget; governed refusals dead-letter explicitly; the bounded re-selection exhaustion records the honest governed failure through `failAbandoned` (never a silent drop).
- **The stale-worker fence**: every side-effecting completion passes the lease authority's guard first (the WORK-028 domain — epoch/owner/expiry); a stale worker (elapsed/superseded/foreign/released) is fenced — its late completion NEVER becomes authoritative; the claim records the typed `stale-write` abandon; the recovery re-drives and the deterministic sandbox identity replays the prior terminal outcome (C4/C5: exactly ONE provider dispatch ever).
- **Recovery** (`recover()`): stale workers offline (heartbeat age), stale claims abandoned (typed causes: `heartbeat-lost`), then re-drive of recoverable executions (RUNNING, lease absent/expired/released, no live claim) from the executions authority ALONE — no queue involvement (C10 proves zero transport pulls). Per-candidate error tolerance: one candidate's failure never aborts the scan.
- **Drain/shutdown** (`stop()`): draining stops acquisition (the claim gate refuses draining workers physically), the in-flight wait is bounded by `maxDrainMs`, stragglers are abandoned `worker-drained` (recoverable by fresh workers — never lost, never duplicated), the identity retires `offline` (terminal — a restarted process registers a NEW identity; offline rows are never resurrected).
- **The executions-module adapters** (`src/modules/executions/adapters/worker-fabric.ts`): the governed start (the D-03 transport-effect command shape and key family), the lease authority (acquire/renew/release/guard/inspect over the long-running service with deterministic per-logical-operation keys), the completion effect (verify+pass/fail with the terminal-classification convergence), the read-only recovery source (RUNNING + no-live-lease scan, application-scoped) and the status reader. The adapters import platform types only (B5).
- **The sandbox-module work executor** (`src/modules/sandbox/adapters/worker-executor.ts`): resolves the execution task's `sandbox` block (everything else is the honest not-executable refusal) and executes through the sandbox service's FULL admission chain (policy → capability → budget — the worker fabric has NO admission powers); the deterministic sandbox key `worker-exec:<executionId>` makes re-selection CONVERGE (terminal replays; crashed mid-dispatch fails closed `NON_CONVERGENT` — the honest unknown-effect barrier). The pre-dispatch lease-fence re-check and signal check mean a stale or cancelled execution never dispatches paid compute.
- **The container runtime client** (`src/platform/compute/container-runtime.ts`): the concrete `ContainerRuntimeClient` over the documented container-runner REST protocol — plain `fetch`, ZERO new SDKs (the sanctioned runtime import set of `src/` is unchanged: fastify, pg — B7); the deterministic run id (idempotent submission — 409 replays); bounded output truncation with digests computed over the bounded payload; 404 as the honest unknown-outcome class (permanent, fail closed — never re-executed); the token never enters any error; plus the authenticated synthetic probe (`probeContainerRunner` — the expected 404 proves wire + credential without executing anything).
- **The physical schema** (migration 0028, schema `compute_plane`): CHECK-bound disjoint vocabularies; registration transitions trigger-enforced with `offline` terminal-immutable; the runner lifecycle governed (pending→active|revoked, active→suspended|revoked, suspended→active|revoked; registration identity immutable; rows never deleted); **one live claim per execution physically** (partial unique index); the claim epoch unique per execution (the bounded re-selection budget countable in one query); terminal claims physically immutable; the identity core immutable; the heartbeat ledgers monotonic; the lease correlation set exactly once; terminal-shape exclusivity (finished/abandoned carry exactly their facts); the claim admission gate (terminal executions admit no claim; customer-runner workers may only claim their own application's executions); deletes rejected for live rows and allowed only for terminal claims past the retention bound; quotas bounded [1,512]; rows-never-deleted everywhere else.
- **The operator surface** (`deploy/worker.ts`): `run` (the worker service process; SIGTERM/SIGINT → the bounded drain), `run-once`, `inspect`, `sweep`, `recover`, `compact` (with the executions-authority terminality seam — the compute plane never reads the executions tables), `quota`, and the governed `runner register|activate|suspend|revoke|list` lifecycle — all fail-closed, all exact-revision-stamped.

## Acceptance-criteria mapping (Work Order §Acceptance Criteria)

| AC | Claim | Evidence |
|---|---|---|
| 1. A submitted execution can be consumed by a worker and executed without the originating HTTP request, lifecycle in PostgreSQL | PASS (real PG) | `worker-fabric-lifecycle.test.ts`: the full transformation (dispatch → claim → lease → work → verify+pass) with the COMPLETED status and the verification binding, envelope consumed at claim time, transport settled; zero live requests anywhere in the path |
| 2. Leases/heartbeats durable, tenant-scoped, idempotent; stale workers fenced | PASS (real PG) | `worker-claims.test.ts` (lease correlation set once, monotonic ledgers, physical immutability) + the crash matrix C5 (the fenced completion NEVER lands; the fresh claim converges — one dispatch) + C6 (heartbeat loss → sweep → re-drive at a new lease epoch) |
| 3. Worker crash/restart/re-selection converge to exactly one authoritative outcome without duplicate governed effects | PASS (real PG) | `worker-crash-recovery.test.ts` C1–C6, C8, C10: the deterministic crash matrix; C4 is the killer proof — provider success then crash before the completion write replays the terminal sandbox outcome with **exactly ONE provider dispatch** |
| 4. Cancellation interrupts/terminates through the governed path; converges durable cancellation; no silent widening | PASS (real PG) | C7: the governed `cancel` wins the race — the worker never drives verify/pass (ledger-proven); the claim converges (converged-elsewhere or governed work-refused — both worker-non-override classes); pre-dispatch cancellation never dispatches compute (the C3-adjacent dispatch-gate proof + the fabric's pre-work status check) |
| 5. Drain stops new acquisition, bounds in-flight, leaves recoverable executions | PASS (real PG) | C9: draining refuses acquisition (physically), the straggler claim abandons `worker-drained` (typed), the identity retires offline, the fresh worker converges the abandoned work |
| 6. Concurrency limits and per-environment quotas enforced before resource admission, bounded, not bypassable | PASS (real PG) | `worker-claims.test.ts` (typed refusals: quota-saturated, worker-concurrency-saturated, attempts-exhausted, worker-not-active, worker-unknown, duplicate-live-claim; the physical one-live-claim index rejecting a racing second live row) + the lifecycle quota test (bounded re-delivery within the transport budget, then convergence) |
| 7. Provider calls receive only scoped mediated secrets; untrusted code has no ambient access | PASS | The sandbox admission chain + the container escape validator (the protocol suite's defense-in-depth check); the runner registration/sandbox task secret-value rejection (discrimination); B6 secret-free sources; the runner token is environment-only materialization (B9) |
| 8. Worker-produced observations retain provenance; worker-local status cannot declare success | PASS (real PG) | The ledger carries `sandbox-admitted`/`sandbox-completed`/`verify`/`pass` (lifecycle test); the claim outcomes are worker-plane bookkeeping only (the discrimination proof: no completion vocabulary in the claim vocabulary); the completion requires the verification binding physically |
| 9. The worker boundary is provider-neutral; supports the existing container ComputeEnvironment without a competing substrate | PASS | Architecture B1–B7 (platform isolation, neutral port, protocol confinement, no second state machine, the port-only authority boundary, secret-free, zero new SDKs); the container provider + the runner client compose the v1.0 `ContainerConfiguration` contract unchanged |
| 10. Customer-runner registration attributable, revocable, non-authoritative; same controls as first-party | PASS (real PG) | `worker-claims.test.ts` (the governed binding — only active runners register workers; the application scope gate physical; revocation terminal; identity immutable) + the deploy:worker lifecycle executed against the real local environment |

## Worker-fabric configuration (repository-defined)

- Variables (all declared in `deploy/manifests/variables.json`, **64 total** — 47 at D-04 + 17 new): the 11 bounded policy variables (`ZECK_WORKER_LEASE_TTL_MS` 60000 [1s,1h], `ZECK_WORKER_HEARTBEAT_INTERVAL_MS` 5000 [100ms,60s], `ZECK_WORKER_DEFAULT_ENV_QUOTA` 8 [1,512], `ZECK_WORKER_MAX_CLAIM_ATTEMPTS` 3 [1,10], `ZECK_WORKER_MAX_IN_FLIGHT` 4 [1,128], `ZECK_WORKER_MAX_DRAIN_MS` 120000 [1s,10min], `ZECK_WORKER_CLAIM_VISIBILITY_MS` 30000, `ZECK_WORKER_BATCH_SIZE` 8 [1,32], `ZECK_WORKER_STALE_AFTER_MS` 90000, `ZECK_WORKER_MAX_OUTCOME_BYTES` 2048 [128,8192], `ZECK_WORKER_CLAIM_RETENTION_MS` 604800000 [1h,90d]) + the runner surface (`ZECK_CONTAINER_RUNNER_URL`, `ZECK_CONTAINER_RUNNER_API_TOKEN` credential-shaped with reference binding `ZECK_SECRET_CONTAINER_RUNNER_TOKEN_REF` → `zeck-secret://<environment>/container-runner-token`, `ZECK_CONTAINER_RUNNER_REQUEST_TIMEOUT_MS` 10000 [100,120000], `ZECK_CONTAINER_RUNNER_POLL_INTERVAL_MS` 250 [50,5000], `ZECK_CONTAINER_RUNNER_MAX_OUTPUT_BYTES` 4096 [256,65536]) + the reference-binding variable.
- Providers manifest: `zeck-container-runner` concern `execution-compute` is now `established` with port contract `src/platform/sandbox/runtime-client.ts` (the v1.0 platform seam); degradation mode `execution-compute-unavailable` (container sandbox dispatch fails closed; the execution stays durably recoverable; never an ambient fallback).
- Resources: the `container-runner` resource (preview per-branch `zeck-preview-<slug>-runner`, staging/production `zeck-<env>-runner`) + the optional `local-container-runner`.
- `bun run deploy:validate` — **valid** (4 environments / 7 providers / 12 resource kinds / **64 variables** / 4 secret-reference inventories, 0 problems).
- `deploy/smoke.ts` gained the `execution-compute` concern probe (both directions executed for evidence — see above). `deploy:worker` script added; `deploy/README.md` documents the D-05 configuration, the container-runner protocol contract, the worker model (the authority boundary), the policy-seeding composition note, and the operator surface.

## The test battery (81 new tests)

- **Unit** (15): the port vocabulary disjointness (case-insensitive, both directions, the anchor covers all five vocabularies, terminal-complete transition tables, the exact fence/abandon/outcome vocabularies, the runner registration validation fail-closed on every field) + the config loader (defaults, every-field coverage, garbage with exact variable names, out-of-bound ranges, empty fallbacks, the object-seam mirror).
- **Real-PG integration — the claim gate + store** (14): atomic admission with typed refusals, the physical one-live-claim index, per-worker concurrency, per-environment quota with override, the bounded re-selection budget exact, draining/offline admission refusal, the terminal-execution FK gate, the lease-correlation set-once + monotonic heartbeats, terminal immutability + live-row delete rejection, stale sweeps typed, compaction (terminal-only, retention-honored, fail-closed without the authority seam), registration identity immutability + offline terminality, the full governed customer-runner contract (binding, scope, revocation, identity immutability).
- **Real-PG integration — the lifecycle** (8): the full transformation, duplicate delivery convergence, the ONE-governed-start proof (consumer + worker share the key family), the governed failure path, not-executable honest dead-letter, unbacked noise refusal, unsettled-crash convergence, quota saturation bounded re-delivery + convergence.
- **Real-PG integration — the crash matrix** (10): C1–C10, the Work Order's required deterministic matrix (crash before lease; after lease before dispatch; during provider call — the honest unknown-effect barrier + bounded exhaustion + governed failure; provider success then crash — ONE dispatch ever; completion racing lease expiry — the fence; heartbeat loss + replacement; cancellation racing completion; two workers racing; drain racing dispatch; restart re-drive from the authority alone).
- **Protocol** (12 over real HTTP): the documented REST shapes (submission, observation, 409 idempotent re-submission), Bearer verification, 401 permanent / 503 transient / 404 unknown-outcome classifications, malformed-envelope fail-closed, bounded deterministic output truncation, config validation with exact variables, the escape-validator defense-in-depth, and the authenticated probe (404-expected, POST-free) in both directions.
- **Architecture** (10): B1–B10 (platform isolation over the real scanner, the neutral port, protocol confinement, no second execution state machine, the port-only authority boundary, secret-free sources, the unchanged runtime import set, the established provider contract, the declared secret + variables, the operator surface).
- **Discrimination** (6): the stale-claim guard rejections (superseded/foreign/elapsed — the authority-side fence), the no-completion-vocabulary proof (a provider success signal cannot complete — the verification binding is physical), secret-value rejection at both boundaries, the unbounded-weakening detection at the store config, runner authority-inflation rejection, the disjointness check DETECTING an injected overlap.
- **Gated live** (2): the real-runner round trip — skips with the exact missing-variable reasons + the mirror test naming them.

## Verification at the exact final head (this doc's commit)

- `python3 scripts/governance-check.py` — **PASSED** (46 Work Orders, 102 requirements; zero `spec/` changes on the branch).
- `bun run typecheck` — 0 errors.
- `bun run lint` — biome clean (1091 files).
- `bun run deploy:validate` — valid (64 variables, 7 providers, 12 resource kinds, 0 problems).
- `bun run deploy:migrate` — **27/27 converged** (0028 applied; executed against the real local environment).
- `bun run deploy:worker` — inspect / compact / quota / runner register|activate|suspend|revoke executed against the real local environment (read-only snapshot + bounded policy + exact revision; honest zero-activity compaction with the authority seam; the quota round trip; the full governed runner lifecycle); `run`/`run-once`/`recover`/`sweep` executed to their exact queue-transport fail-closed configuration errors (no Cloudflare credential — the honest boundary).
- `deploy:smoke` (preview, both directions) — the execution-compute concern reports the exact unavailability without configuration and the honest 401 credential rejection against a real local HTTP endpoint with materialization.
- Full suite with real PostgreSQL 16.2, **TWICE CONSECUTIVELY at the exact final head** — both runs identical: **339 files / 4638 tests, 4638 passed, 13 skipped** (5 real-R2 + 2 real-queue + 4 real-workflow + 2 real-runner NOT RUN with exact reasons), **0 failed**.

## Honest boundaries

- Live container-runner round-trip: **NOT RUN** (no runner host — the documented wire protocol proven over real HTTP; gated live suite committed with exact skip reasons; the 401 fail-closed reachability evidence retained from the smoke execution).
- Live Cloudflare Queues transport for the worker loop: **NOT RUN** (no credential — the fabric proven over the D-03 at-least-once transport double implementing the port contract; the real-queue gated suites carry over unchanged).
- The worker's policy composition seeds the repository baseline policy set at boot through the REAL policy authority (documented in deploy/README.md §D-05 — the policies module's durable store does not exist yet; admission is REAL and fail-closed, never bypassed; the seed is replaced by the durable read when that store lands).
- A crash mid-sandbox-dispatch (the `dispatching` row) fails closed on re-selection (`NON_CONVERGENT_EXTERNAL_EFFECT` — untrusted code execution cannot be proven idempotent); the bounded claim budget converts it into the honest governed failure (C3). This is the pre-existing WORK-012 sandbox discipline, composed honestly.
- Bun 1.3.14 was observed to fail promise resolution for completed async methods only when a sibling chain awaits a NEVER-resolving promise (reproduced and worked around in the test doubles — bounded deferrals only; production code has timeout wrappers everywhere, so the hazard is test-double-only). Disclosed for reproducibility.
- The `tests/integration/postgres/pg-database-port.test.ts` connection-pool bounds test is load-sensitive under full-suite parallel execution (observed once mid-development; passed in 4 consecutive isolated runs and in both final-head full runs — the same class as the disclosed WORK-044 timing flakes).
- The dispatch automation's registration commits (WORK-046 authorization, program/dependency/frontier state) sit on `main` BEFORE the binding base per this dispatch's convention; the branch base is exactly `e6b417f` and the branch carries zero `spec/` changes.
