# VAL-009 — Model/Provider Capability Matrix and Access/Readiness Probe

**Work order:** `spec/validation-work-orders/VAL-009.md`
**Authorization:** GitHub issue (delegated Architect issuance, commit 80b6079 lineage)
**Assurance:** HIGH_ASSURANCE
**Dependencies:** VAL-001 (PR #42), VAL-002 (PR #44) — both complete
**Dispatch base:** `80b6079` (governed validation head; main CI green at base)
**Implementation head:** recorded in the PR head of `work/VAL-009-capability-matrix`
**Mode:** Tech Lead direct implementation (chat.z.ai worker channel unavailable — account server-side blocked until 2026-09-18)

## Changed-file inventory

- `benchmarks/validation/capabilities/matrix.ts` — the capability matrix: 11 corpus capabilities (enumerated from the corpus's `requiresCapabilities` — exact bidirectional coverage checked), candidate provider access (credential by env-var NAME only), `resolveMatrix` (READY only from successful probes), `corpusCapabilityCoverage` integrity.
- `benchmarks/validation/capabilities/probe.ts` — the probe result contract (outcome, failure taxonomy: auth-rejected / region-blocked / quota-exhausted / network-error / provider-error / not-probed, latency, neutral detail, optional measured cost) + `validateProbeResult` (rejects secret-shaped material anywhere in a record) + the probe plans.
- `benchmarks/validation/capabilities/index.ts` — public barrel.
- `tests/unit/validation/capability-matrix.test.ts` — 11 tests: matrix/corpus coverage exactness, env-var-name-only registry, resolution semantics (ready-from-probe, gap-with-requirement, failed-candidate-keeps-gap), probe-record discriminations (unclassified failure, secret leakage, non-micro-USD cost, negative latency).
- `tests/integration/validation/capability-probes.test.ts` — the REAL probe executor: minimal authenticated invocations per provider (env-gated: absent credential = NOT RUN with reason; live-network classification from response bodies); the VLM probe generates a valid 1×1 white PNG in-code and asserts the vision round trip.
- `docs/work-items/VAL-009.md` — this evidence document.

## REAL probe results (executed 2026-09-11 ~21:51 UTC, this environment, operator-authorized credentials)

| Provider | Outcome | Classification | Evidence |
|---|---|---|---|
| openrouter (text probe: open-weights chat model) | **ready** | — | HTTP 200, ~1.2s (chat completion returned) |
| openrouter (VLM probe: vision model + generated 1×1 white PNG) | **ready** | — | HTTP 200, ~0.7s (vision round trip: answered "white") |
| openai | failed | **region-blocked** | HTTP 403 `unsupported_country_region_territory` (~30ms) |
| qwen | failed | auth-rejected | HTTP 401 at dashscope compatible-mode — the home.qwencloud.com-issued key is not a dashscope credential; the correct endpoint for this key type is unresolved (api.qwen.ai paths return 404) |
| byteplus-ark | failed | network-error | `ark.ap-southeast-1.bytepluses.com` does not resolve from this environment (DNS failure) |
| seedance | failed | network-error | same DNS failure on the ARK host family |

## Capability resolution (matrix × probe outcomes)

| Capability | Status | Ready via | Gap detail / operator requirement |
|---|---|---|---|
| model:text | **READY** | openrouter | open-weights routes from this egress; flagship geo-gated routes documented |
| model:vlm | **READY** | openrouter | vision round trip proven ("white" on a generated 1×1 PNG) |
| model:vision | **READY** | openrouter | same vision path (classification/OCR ride it) |
| model:asr | GAP | — | OpenAI region-blocked; needs a region-accessible ASR provider (or region-acceptable egress) |
| model:audio-understanding | GAP | — | same OpenAI region block |
| model:realtime-voice | GAP | — | same OpenAI region block |
| model:image-generation | GAP | — | OpenAI region-blocked; BytePlus ARK host unreachable — confirm a reachable image-generation endpoint |
| model:video-generation | GAP | — | BytePlus/Seedance ARK host does not resolve from this environment — operator should confirm the exact endpoint host for the ap-southeast-1 keys |
| model:three-d | GAP | — | no 3D-generation provider in the authorized set — an explicit provider credential is required |
| agent:browser | GAP | — | browser-agent execution infrastructure (planned with VAL-019) |
| agent:computer-use | GAP | — | computer-use execution infrastructure (planned with VAL-019) |

## NOT RUN boundaries (surfaced to the operator)

1. **OpenAI region block** — every OpenAI-dependent capability (ASR, audio, realtime voice, image generation) is NOT RUN from this environment. Minimal remedy: region-acceptable egress for api.openai.com (operator-side proxy/VPN), or an alternative authorized provider for those modalities.
2. **Qwen key endpoint** — the home.qwencloud.com-issued key (`sk-ws-…` shape) is not accepted by dashscope and no working endpoint was found on api.qwen.ai. Minimal remedy: the operator confirms the API base URL for this key type.
3. **BytePlus/Seedance ARK host** — the documented international ARK host does not resolve from this environment. Minimal remedy: the operator confirms the exact endpoint hostname for the ap-southeast-1 keys.
4. **3D generation** — no provider in the authorized set. Minimal remedy: an explicit 3D-generation provider credential.
5. **Browser/computer-use agents** — infrastructure, not credentials; arrives with the app portfolio (VAL-019).

## Executed battery (at the implementation head, sequential)

| Command | Outcome | Detail |
|---|---|---|
| `bun run typecheck` | pass | 0 errors |
| `bun run lint` | pass | exit 0 |
| `bun run test:unit` | pass | 240 files / 3465 tests → includes the 11 new capability-matrix tests |
| `bun run test:architecture` | pass | 106 files / 1423 tests (capabilities tree is network-free by contract; the executor lives in tests/) |
| `ZECK_PG_TEST_URL=… + provider keys → capability-probes.test.ts` | pass | 7 tests: 2 ready (openrouter text + VLM), 4 classified failures, 1 plan-registry check; one transient live-network flake passed on rerun (recorded honestly) |
| `python3 scripts/governance-check.py` / `validation-check.py` | pass | OK / OK |

## Issue findings and protocol

- **Flagship OpenRouter models are geo-gated from this egress; open-weights models route fine.** Reproduction: chat completion with a flagship model → 403 "This model is not available in your region" (routing funnel's geo gate); the same call with open-weights models → 200. Impact: model choice for text/vision validation must prefer open-weights routes. Root cause: provider-side geo routing. Solutions: open-weights routes (chosen; proven), operator egress change, different provider. Verification: probes above.
- **First probe-suite run had one transient openrouter failure** (live-network provider error) — rerun green. Classification: provider-observability limit; recorded, not hidden.

## Surface honesty

- Zero `src/` changes; zero `spec/` changes; credentials referenced ONLY by environment-variable names (validated mechanically — secret-shaped material in any probe record is rejected); no payloads recorded; this PR does not merge itself.
