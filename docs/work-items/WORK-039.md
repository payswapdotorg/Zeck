# WORK-039 Evidence — Control, spend, connections and improvement experience

Work Order: `WORK-039` (spec/work-orders/WORK-039.md) · Assurance: **HIGH_ASSURANCE** · Requirement IDs: N/A (dashboard projection realization; the policy, budgets, integrations, membership, audit and learning authorities' ownership unchanged — frozen technical requirement ownership untouched)

Frozen base: `b332b13434eef294d3c9890d4bd1c9694d8845ab` (main — the WORK-039 dispatch head: WORK-038 complete and merged as PR #68 at `020d880…`, the governance state at the base OK with 41 Work Orders / 102 requirements / inFlight=['WORK-039'] / frontier=[], exactly as dispatched via issue #69). Branch: `work/WORK-039-control-spend-connections-improvement` · **Final head: this doc's commit** (the house two-phase binding — the exact SHA is recorded by the orchestrator in the PR body; the last code commit precedes it) · Zero merge commits; the merge-base is `b332b13…` exactly.

**Dispatch-record note (report-only, never worker-fixed — the W037/W038 lesson)**: none this time — the Work Order's dispatch record and issue #69 agree exactly (base `b332b13`, branch name, IN-FLIGHT). The worker branch was verified 0 commits ahead / 0 behind the dispatch base at start.

## Implementation history (the ratchet — every commit compiles and is suite-green)

1. The implementation commit — the complete W039 realization:
   - **`apps/dashboard/controls.ts` (NEW, ~505 lines)** — the ONE control/improvement presentation module (Implementation Requirement 1): `CONTROL_FAMILY_LABELS`/`controlFamiliesTable` (AC1: the seven user-level families — quality/spend/latency/approvals live, data/tools/autonomy the explicit absences), `blockedExplanation`/`createBlockedExplanation` (AC2: the platform-recorded denial reason rendered VERBATIM as the controlling rule, for the run page and the create-refusal page), `policyCompositionDisclosure` (AC2/IR2: the effective-policy composition as ADVANCED detail — never resolved here), `spendSummarySection`/`spendRunsTable`/`accountingDetailDisclosure` (AC3/IR3: the simple Spend view — current usage, limits, major categories; the per-run cost+limit table; reservations/settlement/ledger as the accounting detail with their honest public absence), `connectionsSection` (AC4: the live routing facts, the BYOK/secret-mediated setup story, the honest health absence), `environmentsSection`/`teamSection` (AC5: recorded isolation boundaries and the live approval queue, organized around safe operational intent), `auditLedgerSection` (the per-run governed-action ledgers — the closest live audit record), `recommendationFamiliesSection`/`recommendationDispositionList` (AC6: the five families and the three dispositions — advisory/review/applicable), `learningDistinctionSection` (AC7: evidence/recommendation/authoritative-production with the live agent-selection record beneath), `runLink` (IR4: the contextual same-execution links).
   - **`apps/dashboard/projection.ts`** — the pure derivations: `policyDenialOf` (AC2: reads ONLY the `execution.policy-denied` event's payload `{ denied, reason }` — the real wire's own shape; no other event type, no other key, a non-string/blank reason ⇒ no denial fact), `runSpendFacts` (AC3: the recorded cost, the declared limit, the routed provider — each null when absent, never zero/guessed), `sumMicroUsd` (AC3: BigInt-only over integer micro-USD strings; malformed values contribute nothing), `providerCategoryFacts` (AC3: the major categories = the recorded providers, null grouping as the honest no-provider row), `environmentFacts` (AC5: groups by the RECORDED environmentId, null rendering as the platform's default), `approvalQueueFacts` (AC5/AC8: the platform's WAITING_USER/WAITING_HUMAN states), `agentSelectionFacts` (AC7: the agent inventory's own promotion/rollback selection record), `recommendationDispositionRows`/`learningAuthorityRows` (AC6/AC7/IR6: the static distinction vocabularies — the recommendation row carries "Learning produces recommendations and evidence, never authorization"; the production row anchors to the platform's own selection rules).
   - **`apps/dashboard/pages.ts`** — the route surfaces: `/admin/policies` (the Rules surface: controls-first, the live blocked-runs list with each denial's recorded controlling rule, the composition advanced detail), `/admin/budgets` (the Spend surface: the simple summary, the per-run table, the accounting advanced detail, the workspace-budgets absence), `/assets/connections` (the live routing facts + the BYOK story + the inventory absence), `/admin/environments`, `/admin/team`, `/admin/audit` (each live from the recents scope with its authority absence), `/improve/insights` (the five families + three dispositions + the live evidence pointers), `/improve/learning` (the three-stage distinction + the LIVE production record from listAgents/getAgentStatus); the run page composes `blockedExplanation` when (and only when) a recorded denial exists; the create-refusal path renders `createBlockedExplanation` for POLICY_DENIED/BUDGET_EXCEEDED (AC8: the consequence/authorization framing before any retry). NO new routes and NO new POST routes — D11's exactly-the-governed-POST-routes pin unchanged.
   - **`apps/dashboard/components.ts`** — the whyPanel's "Why was that approach permitted?" answer gains the recorded controlling rule when a denial exists (additive; without a denial the rendering is unchanged).
   - **`apps/dashboard/shell.ts`** — the eight honest nav-description updates (Policies/Spend/Team/Environments/Audit/Connections/Insights/Learning — live phrasing now that the surfaces are live). Labels/paths/mode-visibility untouched (the navigation pins are unchanged).
   - **`apps/dashboard/tokens.ts`** — the WORK-039 CSS block: `.state.state-blocked` (the warn-variant state), `.spend-summary`, `table.spend-runs` (the mono run-id column), `.connection-facts`, with a ≤640px mobile rule — every rule consuming the existing spacing/surface/border/text/status tokens; no one-off hierarchy.
   - The proof suites (the exact inventory below): the control-presentation unit suite (24), the control-state fixtures (14), the whyPanel denial pins (+2), the (ad)–(am) journeys (+25), the D19–D22 discriminations (+13).
2. This doc (the final head).

## Baseline gate (readiness checkpoint — the exact frozen base, BEFORE implementation)

governance OK (41 WOs, inFlight=['WORK-039'], frontier=[]) · typecheck 0 · biome clean (967 files) · **full suite with real PG = 280 files / 4011 tests, exit 0** (160.07s) at exactly `b332b13` — the WORK-038-era baseline reproduced before any change.

## What changed (the surface diff, every touched file)

Exactly 12 files — 6 under `apps/dashboard/` (5 modified + the NEW `controls.ts`) + 5 dashboard-local test files + this evidence document; the count cross-checked against `git diff --name-only b332b13…HEAD` AND the GitHub PR file list (the W038 correction lesson: the count stated with its explicit breakdown, both sources agreeing); rg-verified — zero files under `src/`, `sdk/`, `cli/`, `scripts/`, `spec/` (incl. development-state — untouched), `migrations/`, `.github/`, or root configs; the count of files outside `apps/dashboard/` + `tests/` + `docs/work-items/` is ZERO:

- `apps/dashboard/controls.ts` (NEW — the ONE control/improvement presentation module)
- `apps/dashboard/projection.ts` (the W039 derivations; every existing derivation untouched)
- `apps/dashboard/pages.ts` (the live Control/Improve/Connections surfaces; the run page's blocked explanation; the create-refusal explanation; NO new routes/POSTs)
- `apps/dashboard/components.ts` (the whyPanel denial reason — additive, default unchanged)
- `apps/dashboard/shell.ts` (the eight honest description strings; nothing structural)
- `apps/dashboard/tokens.ts` (the WORK-039 CSS block)
- `tests/unit/dashboard/control-presentation.test.ts` (NEW, 24)
- `tests/unit/dashboard/control-state.test.ts` (NEW, 14)
- `tests/unit/dashboard/components.test.ts` (+2 — the whyPanel denial pins)
- `tests/integration/dashboard/journeys.test.ts` (93 → 118 — the (ad)–(am) journeys + the W039 fixtures incl. the policy-denial reason, the spend/environment/approval runs and the fake-wire admission ceiling)
- `tests/discrimination/dashboard-journey.discrimination.test.ts` (27 → 40 — D19–D22)
- `docs/work-items/WORK-039.md` (this doc)

No migration claimed (the migration inventory is untouched). No new dependency (the driven-browser harness lives OUTSIDE the repository at /home/z/w039-harness; the repository stays zero-dependency).

## Acceptance-criteria mapping (the Work Order's AC1–AC10)

- **AC1** (policy pages present user-level controls first: quality, spend, latency, data, tools, approvals and autonomy where available) — the Rules surface's `controlFamiliesTable`: the seven families in the Work Order's exact order; quality/spend/latency/approvals render as live families (the create contract's constraints vocabulary and the platform's waiting states — each stating where its live facts live); data/tools/autonomy render as their own explicit absences (never a fabricated default); pinned by control-presentation (the family list and the 4+3 marker counts) and the (ad) journey + the harness (seven `<li>` rows, four Platform-fact markers, three Explicit-absence markers).
- **AC2** (users understand why an action is blocked and which effective rule controls it, without policy-engine internals) — `policyDenialOf` reads ONLY the `execution.policy-denied` event payload (the real wire's `{ denied, reason }`); the reason renders VERBATIM in the run page's blocked explanation, in the whyPanel's permission answer, in the Rules surface's blocked list, and in the create-refusal explanation (POLICY_DENIED/BUDGET_EXCEEDED); the effective-policy composition renders only as the advanced disclosure with its honest absence; pinned by control-presentation, control-state (the fabricated-reason mutants), the (ad)/(al)/(am) journeys, D19 and the harness (the blocked explanation, the verbatim reason, the composition disclosure).
- **AC3** (spend: current usage, limits, major categories in a simple view; reservations/settlement/ledger as advanced detail) — the Spend surface: the simple summary (usage = the BigInt sum of the recents runs' recorded costs, the scope stated; limits = the declared per-run constraints; categories = the recorded providers), the per-run table (cost and limit side by side, each row linking to its run), the accounting detail as the advanced disclosure with the honest public absence, the workspace-budgets absence; pinned by control-presentation (BigInt-only sums, the null-facts discipline), control-state, the (ae) journey, D20 and the harness ($7.50 usage, the categories, the accounting disclosure, mobile 390).
- **AC4** (connection pages communicate health and setup state without rendering secrets) — the Connections surface: the live routing facts (the platform's opaque provider strings with run counts and settled totals, browser-scoped), the BYOK/secret-mediated setup story (the create contract carries no connection field at all — the forbidden-key rule), the honest health absence (decided platform-side at dispatch; each run's outcome is the closest live record); NO credential-shaped value anywhere (no public wire shape even carries a field where a secret could appear); pinned by control-presentation, the (af) journey, D21 and the harness (the routing rows, the BYOK story, the no-secrets regex).
- **AC5** (team/environment controls organized around safe operational intent, not backend module topology) — the Environments surface (the RECORDED environment ids as isolation boundaries with run links; the authority's inventory/configuration honestly absent) and the Team surface ("who decides what" — the live approval queue from the platform's waiting states; the membership absence); pinned by control-presentation, the (ag)/(ah) journeys and the harness.
- **AC6** (improve surfaces present recommendations with observed evidence, expected impact, confidence, affected work and disposition from platform data) — the Insights surface: the five families as the honest structure ahead of the facts (each anchored to where its facts will come from — the learning authority), the three dispositions as distinct rows (advisory/review/applicable — none ever claiming effect), and the live evidence pointers (IR4: links to the evidence surface, the evaluations distinction and the learning surface); pinned by control-presentation (the family list, the disposition rows), the (aj) journey and the harness.
- **AC7** (evaluation and learning views distinguish evidence from recommendation and recommendation from authoritative production) — the Learning surface's `learningDistinctionSection`: three DISTINCT stages — the evidence row (live: per-execution verification), the recommendation row (the never-authorizes boundary sentence, never backed), the production row (live: the agent inventory's selection facts) — with the LIVE production record beneath (the promotion/rollback selections with who/when, read through the governed API); pinned by control-presentation, control-state (the authority-flip mutant), the (ak) journey, D22 and the harness.
- **AC8** (consequential control changes and recommendation application show consequence and authorization before commitment) — the existing two-step create flow already carries the consequence preview (the runCommitmentCard: consequence, affected, cost, authorization, reversibility, idempotency — through the WORK-035 confirmation primitive); W039 adds the create-REFUSAL explanation (the typed code, the platform's message as the controlling rule, the adjust-and-resubmit framing — never a silent retry) and states on every Improve surface that recommendation application is a governed platform operation that does not exist as a public action (no apply mutation anywhere — D22's structural proof); pinned by the (am) journey (the refused create renders the explanation; the adjusted resubmit succeeds) and the harness.
- **AC9** (API-backed tests prove scope isolation, secret safety and no frontend-owned authority) — the (l) scope journeys (unchanged, green through this change), D10/D11/D12 (unchanged), and the new D19 (policy-reason fabrication), D20 (frontend-accounting fabrication — BigInt-only, no float parsing, no zero-guessing), D21 (connection secret leakage — no credential-shaped rendering, hostile strings escaped), D22 (learning-authority mutation — no POST form, no apply action, the authority-flip mutant, zero POST wire calls across every W039 journey).
- **AC10** (responsive and accessible behavior preserves the control/improvement hierarchy) — the harness record: one h1 + landmarks on every W039 surface (the frame checks ran on all nine), the skip link the first tab stop, the form-Enter keyboard path proven end-to-end (click-focus, type, Enter → the run page), mobile 390 (the spend summary, the per-run table, the controls table, the ≥44px touch target) — 102/102 checks, ZERO console errors and ZERO page errors across the whole session.

## The required discriminations (D19–D22, in tests/discrimination/dashboard-journey.discrimination.test.ts)

- **D19 policy-reason fabrication**: MODULE — the denial fact is the recorded reason ONLY (status-derived, fail-message and canned-default mutants all differ on the same input); STATIC — the guard is the typed event + payload-reason read (the mutant widening the event type is flagged); RUNTIME — a run with NO recorded denial renders no blocked explanation and no controlling-rule line.
- **D20 frontend-accounting fabrication**: MODULE — sumMicroUsd is integer-BigInt-only (float-shaped and garbage values contribute nothing; precision beyond Number.MAX_SAFE_INTEGER survives — the float mutant differs) and runSpendFacts leaves every missing fact null (the zero-guessing mutant differs); STATIC — the total flows only through the BigInt guard (the Number mutant is flagged); RUNTIME — the spend page renders the recorded cost exactly ($4.18) and NO reservation/settlement figures.
- **D21 connection secret leakage**: MODULE — a hostile provider string renders as an opaque escaped label, never as markup or a credential; STATIC — the connections presentation states the no-connection-field boundary (the credential-render mutant is flagged); RUNTIME — the connections page renders the routing facts with no secret-shaped value and no POST form.
- **D22 learning-authority mutation** (IR6): MODULE — the recommendation row carries the never-authorizes boundary and is never backed (the authority-flip mutant differs); STATIC — the controls module renders no POST form and no apply action (the apply-form mutant is flagged); RUNTIME — driving every W039 GET journey issues ZERO POST wire calls (every wire call a scoped GET read through the bound scope), the learning page renders the boundary sentence and NO apply form.

## The driven-browser verification record (real Chromium over the real createDashboard server + the scope-enforcing fake world)

The harness (OUTSIDE the repository — /home/z/w039-harness, playwright/Chromium, rebuilt for W039) boots the real `createDashboard` against a fake API world implementing the REAL application-scope wire rule (scoped reads without the header ⇒ 422; the create excepted — body-scoped, exactly the real routes' split) plus the W039 control-plane fixtures (a policy-denied run carrying the platform's recorded reason, a constrained+costed spend run, an environment-tagged run, a waiting-human approval run, an agent with a recorded promotion selection, and the admission ceiling on the create). **102/102 checks passed, ZERO console errors and ZERO page errors across the whole session** (screenshots at /home/z/w039-browser-*.png, the machine-readable record at /home/z/w039-harness/report.json): the Rules surface (seven families, four live + three absent markers, the blocked list with the recorded reason and the run link, the composition disclosure); the Spend surface (the $7.50 usage total, the limits, the categories, the per-run table with cost+limit+provider, the accounting disclosure, the workspace absence); the Connections surface (the routing rows, the BYOK story, the health honesty, the secret-mediated boundary, the no-secrets regex); the Environments surface (env-staging + the default, the run links, the authority absence); the Team surface (who decides what, the live approval queue, the human-review wording, the membership absence); the Audit surface (the ledger rows, the activity links, the cross-work absence); the Insights surface (the five families, the three dispositions, the live evidence pointers, no POST form); the Learning surface (the three stages, the never-authorizes boundary, the live selection record with the agent link, no POST form); the blocked-run explanation (the state block, the verbatim reason, the boundary sentence; a non-denied run renders none); the create-refusal explanation (the honest 422, the typed code, the state block, the controlling rule); the keyboard discipline (the skip link first, the form-Enter navigation end-to-end to the run page); the frame (one h1 + landmarks on all nine W039 surfaces); and mobile 390 (the spend summary, the per-run table, the controls table, the ≥44px touch target).

**Disclosed harness limitation (honest, carried over from W038 — reproduced on a minimal page)**: synthesized Enter in this headless Chromium does not activate plain anchors; keyboard navigation is proven through the form-Enter path (click-focus, type, Enter → the governed redirect lands on the run page — proven on both /runs and /admin/policies) plus link-reachability and focus checks; every linked surface is driven and verified in the same session. The repository's own automated proof is unaffected (the journeys drive HTTP directly).

## The complete gate (run at the final head, TWICE consecutively green)

`python3 scripts/governance-check.py` (OK: 41 WOs, inFlight=['WORK-039'], frontier=[]) · `bun run typecheck` (0 errors) · `bun run lint` (biome clean, 970 files, zero diagnostics/warnings) · `ZECK_PG_TEST_URL=postgres://postgres@127.0.0.1:55432/postgres bun run test` = **282 files / 4087 tests, exit 0** — run TWICE consecutively green at the exact final head (THIS doc's commit; the run-pair record with timestamps is carried in the PR body and the verification comment). The 280/4011 baseline + 2 files / +76 tests vs the base: control-presentation 22, control-state 14, the whyPanel denial pins 2, the (ad)–(am) journeys 25, the D19–D22 discriminations 13.

## Test inventory (the exact delta vs the base)

- NEW unit: `control-presentation.test.ts` (22 — the control families 3, the blocked explanation 4, the spend derivations 5, the connections presentation 2, the environments/team derivations 4, the learning-authority distinction 4), `control-state.test.ts` (14 — the policy-explanation fixtures 3, the spend/limit fixtures 3, the secret-safety fixtures 3, the learning-authority fixtures 3, the environments/approvals fixtures 2)
- EXTENDED unit: `components.test.ts` (+2 — the whyPanel denial pins: the recorded controlling rule when a denial exists; no controlling-rule line without one)
- EXTENDED integration: journeys 93 → 118 ((ad) the rules journey ×3, (ae) the spend journey ×3, (af) the connections journey ×2, (ag) the environments journey ×2, (ah) the team journey ×2, (ai) the audit journey ×2, (aj) the insights journey ×3, (ak) the learning journey ×3, (al) the blocked-run journey ×3, (am) the create-refusal journey ×2) + the W039 fixtures (the POLICY denial reason payload, SPEND with constraints+cost+route, ENV with environmentId, APPROVAL WAITING_HUMAN, the fake-wire admission ceiling) + the SeedInput constraint/environment extension
- EXTENDED discrimination: dashboard-journey 27 → 40 (D19 ×3, D20 ×4, D21 ×3, D22 ×3)
- Total dashboard-local: 16 unit files / 344 unit tests + 118 journeys + 40 journey discriminations

## Required checkpoint contracts

- **SELF-HOSTING-BOUNDARY**: the dashboard still imports ONLY `../../sdk` + node builtins (controls.ts adds no import outside apps/dashboard + the module's own siblings); the client script performs ZERO network calls (unchanged, D5 of the foundation suite); no new dependency; every fact remains a live SDK read (the control, spend, connection, environment, approval, audit and selection facts are all derived at render time from the scoped reads — the recents scope is the disclosed navigation-only cookie, unchanged since the foundation); the transport scope remains the SDK's bound application scope (the (l) journeys still prove the wire; the D22 runtime proves every W039 wire call is a scoped GET through the bound scope).
- **EXECUTION-PROVENANCE**: no new platform facts — the controlling rule is the platform's own recorded denial reason (verbatim, never reworded or re-derived); policy remains the authorization boundary (the dashboard explains, never resolves — the composition is advanced detail with its honest absence); accounting remains canonical (every money figure is a platform recording; the sum is BigInt presentation arithmetic over recorded per-run figures, labeled with its scope; reservations/settlement/ledger are the authority's, absent from the public wire and stated); credentials remain secret-mediated (the connection presentation renders only the platform's opaque neutral routing strings — no public wire shape carries a field where a secret could appear, and the create contract rejects connection selection fail-closed); learning produces recommendations and evidence, never authorization (the three stages stay distinct; the production row anchors to the platform's own selection records; NO apply mutation exists on any Improve surface); the only mutations remain createExecution and cancelExecution through the governed SDK client (D11/D12 unchanged and green); money renders from integer micro-USD strings (BigInt only); no visual primitive manufactures control, spend, connection or lineage facts.

## Required verification mapping (the Work Order's list)

| Required | Evidence |
|---|---|
| governance checker | OK: 41 WOs, inFlight=['WORK-039'], frontier=[] at the base and at the head |
| typecheck | 0 errors at the base and at the head |
| lint (biome) | clean (970 files) at the head |
| dashboard/unit/integration tests | 344 unit (16 files) + 118 journeys + 40 journey discriminations |
| policy explanation tests | control-state's policy-explanation fixtures + control-presentation's blocked-explanation pins + the (ad)/(al)/(am) journeys + D19 + the harness blocked-run/create-refusal checks |
| spend/limit tests | control-presentation's spend derivations + control-state's spend/limit fixtures + the (ae) journey + D20 + the harness spend checks |
| secret-exposure discrimination | D21 + the hostile-payload pins (control-state) + D3/D5 of the foundation suite, unchanged |
| learning recommendation authority tests | control-state's learning-authority fixtures + control-presentation's distinction pins + the (ak) journey + D22 + the harness learning checks |
| responsive browser verification | the harness record (desktop 1280 + mobile 390: the spend summary, the per-run table, the controls table, the ≥44px touch target) |
| keyboard/accessibility verification | the harness record (the skip link first, the form-Enter navigation end-to-end, one h1 + landmarks + the distinction/aria vocabulary on every W039 surface) |
| full suite twice consecutively at exact final head | the complete gate section above |

## Limitations (honest)

1. **No public policy-rule, budget, connection-inventory, environment-inventory, membership, audit or learning authority surface exists** — each Control/Improve surface renders its live closest public facts (the recorded constraints, waiting states, routing strings, environment ids, event ledgers, verification evidence and agent selections — all recents-scoped or inventory-scoped through the governed API) with the authority's own records as the honest absence. When each authority ships, its facts feed the same presentations.
2. The spend aggregate is browser-scoped by construction: the public wire exposes no cross-work spend aggregate (no execution listing), so the simple view's "current usage" is the sum over the runs opened in this browser, with the scope stated on the surface itself; the budgets authority's own accounting (reservations, settlement, ledger) never crosses this surface.
3. The policy-denied reason is the platform's recorded reason string (the controlling rule in the platform's own words); the effective-policy composition (policy set identity, version, precedence) is not on the public wire — the advanced disclosure states the absence rather than approximating it.
4. The codebase-analysis public routes (findings with advisory/candidate/verified states, governed transitions) exist on the platform but have no SDK client methods, so they are NOT reachable through the dashboard's SDK-only read path; the Insights surface therefore renders the recommendation families as the honest structure ahead of the facts (never a fabricated recommendation, impact figure or confidence level).
5. The driven-browser harness is a session artifact OUTSIDE the repository (the repository stays zero-dependency); its 102-check record is the evidence; the repository's own automated proof is the 502-test dashboard-local suite (344 unit + 118 journeys + 40 discriminations). The synthesized-Enter-anchor limitation is disclosed above (carried over from W038; the form-Enter path proves keyboard navigation end-to-end).
6. The substrate changes are additive (the whyPanel's denial reason renders only when a denial exists; the nav descriptions changed honestly with the surfaces going live — labels/paths/modes untouched); the W033–W038 renderings are unchanged — pinned by the full W036/W037/W038 suites green through this change.
