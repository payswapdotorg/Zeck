# WORK-036 Evidence — Home, Work creation and execution experience

Work Order: `WORK-036` (spec/work-orders/WORK-036.md) · Assurance: **HIGH_ASSURANCE** · Requirement IDs: N/A (dashboard projection realization; frozen technical requirement ownership untouched)

Frozen base: `5131e70c9ad694fb49ad14afb4035af8b6caed4f` (main — the post-WORK-035-finalization head: WORK-035 merged as PR #62 (`55ae99bf…`) and finalized; governance at the base: OK, 41 Work Orders / 102 requirements / inFlight=[] / frontier=['WORK-036'] — the sole eligible frontier, exactly as dispatched via issue #63). Branch: `work/WORK-036-home-work-execution` · **Final head: this doc's commit** (the house two-phase binding — the exact SHA is recorded by the orchestrator in the PR body; the last code commit is `be222d0`) · Zero merge commits; the merge-base is `5131e70…` exactly.

## Implementation history (the ratchet — every commit compiles and is suite-green at the base)

1. `889bb48` — the journey implementation: the outcome composer (Home + /build/execution) with the optional `attachments` field (parsed/validated into `inputArtifactRefs` on the closed create request) and the honest secondary affordances (competences/templates as disclosed not-yet-exposed states — never fabricated pickers); the proposed-approach review envelope (Purpose / Estimated cost and time — the declared envelope, with the explicit statement that the platform exposes no pre-run estimate / the permission-and-risk envelope in user language / the proposed verification approach, honest that the platform chooses it) with **Run** as the primary action (UX-SCREEN-SPEC-V2 §3's commitment vocabulary); the execution-header **trust strip** (the four axes rendered as four separate facts, AC5); the §11 seven-question **How Zeck did it** panel; the **wait decision surface** (the recorded question, what deciding means, what cancelling means, return-to-work); the honest **failure distinction** — `classifyFailure` derives ONLY from public facts (FAILED ⇒ execution failure; COMPLETED with FAIL checks ⇒ quality failure); the timeline-default **Activity** with Graph/Events/Raw moved inside one advanced disclosure.
2. `de75d5f` — the proof suites: the work-journey unit suite, the journey discriminations D9–D12, the (p)–(r) journeys.
3. `98140b6` — the first evidence doc (the PR #64 submission head; CI green, three jobs).
4. `be222d0` — **the Architect re-review amendment (PR #64 comment 5528889999)**, closing the two contract gaps:
   - **AC9**: the create commitment now states ALL five pre-commit consequence facts **immediately before Run** through the WORK-035 `confirmationCard` primitive (never a parallel confirmation pattern): what will happen (one governed execution; events/verification/artifacts/cost recorded platform-side), the affected resource (the application-scoped execution record, environment, end-user attribution, the attached input artifacts), the authorization requirement (no user pre-approval in the public create contract — policy admission at dispatch is the boundary; approval-gated side effects surface as waiting states), the cost estimate status (the honest no-pre-run-estimate fact + the declared spend limit as the enforced constraint), and reversibility (not exposed by the public contract — the governed stop is Cancel; performed work and evidence stay recorded). The substrate gains exactly ONE additive optional field (`ConfirmationView.reversibleDetail` — the default Yes/No vocabulary renders byte-identically when absent, pinned); the create-error path renders the same block with "Try again"; "Not now" returns to editing the same details (key preserved).
   - **AC10**: the heuristic prose over the recorded reason ("When the recorded reason describes…") is REMOVED. `deriveRecoverability()` reads ONLY platform-typed fields on the public event stream (a literal `retryable` boolean; the platform's own `failureClass`/`outcomeClass` vocabulary): a recorded `retryable=true` fact is surfaced verbatim and makes the governed new attempt primary; `retryable=false` states the honest not-retryable consequence (no blind-retry push); a class without the bit surfaces the class verbatim plus the explicit missing-bit limitation; with no typed fact at all the surface states explicitly that the public contract exposes no authoritative recoverability or provider/infrastructure classification — never a guessed "recoverable" badge, always distinguished from the completed-quality-failure dimension whose failed checks ARE the authoritative facts (sharpened in the quality-failure notice).
5. This doc (the final head).

## Baseline gate (readiness checkpoint — the exact frozen base, BEFORE implementation)

governance OK (41 WOs, frontier=['WORK-036']) · typecheck 0 · biome clean (962 files) · **full suite with real PG = 276 files / 3833 tests, exit 0** at exactly `5131e70` — the WORK-035 merged baseline reproduced on this environment before any change. (The PR #64 submission pass reproduced it again: 276/3833 at the base, 278/3875 at the head `98140b6`. The amendment pass re-reproduced the environment after an infrastructure destruction — incident-#3 — by rebuilding the toolchain and real PostgreSQL 16.4 from source at the same coordinates: 278/3875 at `98140b6` before the amendment, then 278/3889 through the amendment pass at its final head.)

## What changed (the surface diff, every touched file vs `5131e70…`)

Exactly 13 files (all inside the declared surfaces — 5 `apps/dashboard`, 7 dashboard-local test files, the evidence doc; rg-verified — zero files under `src/`, `sdk/`, `cli/`, `scripts/`, `spec/` (incl. development-state), `migrations/`, `.github/`, or root configs). The submission head touched 11; the amendment adds exactly two, both inside the same declared surfaces: `apps/dashboard/states.ts` (the substrate's additive optional field) and `tests/unit/dashboard/state-primitives.test.ts` (its pins):

- `apps/dashboard/projection.ts` — `ExecutionFormValues.attachments` + `parseAttachmentRefs` (newline/comma-separated ids, hostile tokens rejected by validation — never silently dropped); `buildExecutionRequest` emits `inputArtifactRefs`; `classifyFailure` + `waitQuestion` (the honest derivations); **(amendment)** `deriveRecoverability` + `RecoverabilityFacts` (the platform-typed recoverability derivation — a literal `retryable` boolean and the `failureClass`/`outcomeClass` platform vocabulary, most-recent-fact-wins, free-text fields never consulted).
- `apps/dashboard/components.ts` — `executionHeader` renders the four-axis trust strip; the waiting surface (question + both consequences + return-to-work); **(amendment)** the failed surface (dimension + recorded reason verbatim + the recoverability block — platform-recorded fact or explicit limitation — + the quality-failure authoritative-fact distinction + safe actions constrained by the recorded fact); the `qualityFailureNotice` (the OTHER dimension, never merged — the failed checks are the platform's authoritative facts); the whyPanel restructured into the §11 answers.
- `apps/dashboard/pages.ts` — the Home composer + secondary affordances; the composer's attachments field; the proposed-approach envelope (review + the create-error path); **(amendment)** `runCommitmentCard` + `commitmentHiddenFields` (the AC9 consequence block immediately before Run / Try again, through `confirmationCard`; the old bare POST form and the duplicate idempotency note folded into the card's Idempotency row); `FORM_KEYS` carries `attachments` through the hidden fields; the header passes the trust axes; the activity default/disclosure restructure.
- `apps/dashboard/states.ts` — **(amendment)** ONE additive optional field on the substrate's `ConfirmationView` (`reversibleDetail?: string` — the caller-supplied honest reversibility wording; the default Yes/No vocabulary renders exactly as before when absent, pinned by the unchanged-default test). The substrate's semantics are otherwise untouched — no parallel confirmation pattern is introduced.
- `apps/dashboard/tokens.ts` — the trust-strip, composer-secondary, review-envelope, decision-consequences, failure-dimension, **recovery-fact + failure-distinction** and quality-failure CSS (all token-consuming; no one-off hierarchy).
- `tests/unit/dashboard/request-mapping.test.ts` — the attachments mapping/validation pins + the envelope/Run review pins + **(amendment) the AC9 commitment pins** (all five fact rows, the honest absences, the DOM order envelope → block → Run, the Not-now edit path preserving the key).
- `tests/unit/dashboard/components.test.ts` — the trust-strip pins (four separate facts, no merged verdict) + the header view extended.
- `tests/unit/dashboard/html-escape.test.ts` — the hostile trust-axis labels pass through the escape boundary.
- `tests/unit/dashboard/work-journey.test.ts` (30 — was 20 at submission; **+10 amendment tests**) — the classification/wait/attachment/§11 contract + `deriveRecoverability` (typed-fact reads, both booleans, the class-only and no-fact surfaces, free-text never consulted, last-fact-wins, chronological ordering) + the amended failure-surface pins.
- `tests/unit/dashboard/state-primitives.test.ts` — **(amendment)** the `reversibleDetail` pins (the detail replaces the default; the default unchanged without it).
- `tests/discrimination/dashboard-journey.discrimination.test.ts` (10) — D9–D12 (unchanged; all green through the amendment).
- `tests/integration/dashboard/journeys.test.ts` — the (p)–(r) journeys + the quality/waiting fixtures + the WhyPanel §11 assertions + **(amendment)** the retryable/not-retryable FAILED fixtures (platform-typed facts mirroring the tools runtime's real mapping: timeout → retryable, output-contract → not retryable) + the (p) commitment pins + the amended (q) failure assertions.
- `docs/work-items/WORK-036.md` (this doc).

No migration claimed (the migration inventory is untouched). No new dependency (the driven-browser playwright harness lives OUTSIDE the repository at /home/z/w036-harness).

## Acceptance-criteria mapping (the Work Order's AC1–AC12)

- **AC1** Home makes outcome entry dominant and prioritizes Attention, active work and recent results over analytics — measured in the driven browser: the composer form is the first form in main with the outcome textarea; the §6 heading order (outcome → suggested actions → attention → happening now → recent results) is positionally ascending; ZERO chart/canvas/svg elements in main; pinned by the (a) journey and the harness record.
- **AC2** the composer with optional attachments, saved competences or templates without provider/model selection — the attachments field maps to `inputArtifactRefs` (the (p) journey proves the wire request carries them); the competence affordance links to the honest unavailable state; the templates affordance is the honest disclosed not-exposed note; the harness measured zero provider/model selects; the request-mapping/work-journey units pin the parsing and the closed-vocabulary mapping (a forbidden key can never be emitted).
- **AC3** purpose, estimated cost/time, permission/risk envelope and proposed verification approach before execution — the envelope's four sections (each a platform fact or an honest absence: "the platform exposes no pre-run cost or time estimate, so none is shown"); pinned by request-mapping + the (p) journey + the harness.
- **AC4** execution detail opens on Result; Evidence and Activity are peer views — unchanged from the foundation and re-pinned ((a), (q), (r), the harness "run" check).
- **AC5** the header presents status, duration, cost and trust state from platform facts — the facts line + the four-axis trust strip (rendered from `deriveTrustAxes` — platform facts only); pinned by components + the (q) journey + the harness (4 `<li>`, the exact four kinds).
- **AC6** progress is a chronological timeline by default; Graph/Events/Raw are advanced views — the timeline renders FIRST; the three advanced views live inside one `advancedDisclosure`; the graph is the honest expert-surface note; the events/raw views carry "Advanced view — …" + "Return to the timeline"; pinned by the (r) journey + the harness (disclosure AFTER the timeline, DOM-ordered).
- **AC7** `How Zeck did it` explains interpretation, capabilities, plan, route, compute and rationale without infrastructure as the default mental model — the panel answers all seven §11 questions; provider/model stay inside the route advanced disclosure + the focused sheet; pinned by components + the work-journey suite + the (a) journey + the harness.
- **AC8** WAITING_USER/WAITING_HUMAN provide clear decisions, consequence and return-to-work — the recorded question (when the platform event carries one), what deciding means (resolve through the governed path; the public API exposes no resolve command — stated), what cancelling means (terminal, consequence preview), return-to-work link; pinned by work-journey + the (q) journey + the harness.
- **AC9** consequential actions expose consequence, affected resource, authorization requirement, estimated cost and reversibility before commitment — **(amended per the re-review)** the CREATE commitment now renders the full consequence/commitment block immediately before Run through the WORK-035 `confirmationCard`: "What will happen" (one governed execution; the events, verification results, output artifacts and settled cost are recorded platform-side), "Who or what is affected" (the governed execution record in application X, environment, end-user attribution, the attached input artifacts read as inputs), "What it costs" (the honest "no pre-run estimate — the platform's public contract exposes none, so none is shown" + the declared spend limit as the enforced constraint), "Why it is allowed" (no user pre-approval in the public create contract; policy admission at dispatch is the authorization boundary; approval-gated side effects surface as waiting states), "Can it be undone" (No — "a committed execution cannot be undone through the public contract. The governed stop is Cancel (its own consequence preview); work already performed and its evidence stay recorded and inspectable"), "Approval required" and "Idempotency" (the key converges duplicate submits on one execution) — every fact a public-contract fact or an honest absence, nothing fabricated; the create-error path renders the same block before "Try again"; the CANCEL commitment keeps the WORK-035 `confirmationCard` (all rows before the single governed POST, pinned by the (d) journey); pinned by request-mapping (the full five-fact + DOM-order + Not-now pins), the (p) journey, state-primitives (the `reversibleDetail` addition with the default unchanged), the (d) journey and the harness.
- **AC10** failure states distinguish recoverable provider/infrastructure failure from task/quality failure and offer safe next actions — **(amended per the re-review)** the two DIMENSIONS stay derived from public facts only (FAILED ⇒ execution failure; COMPLETED+FAIL checks ⇒ quality failure — `classifyFailure`, never merged); within the execution-failure dimension, the recoverability answer is now **authoritative-or-explicit**: `deriveRecoverability` reads only platform-typed fields (a literal `retryable` boolean; the platform's own `failureClass`/`outcomeClass` vocabulary) — a recorded `retryable=true` fact is surfaced verbatim ("the platform recorded this failure as retryable — failure class timeout, on the execution.tool-result event. A new attempt is the governed path…") with the retry as the primary action; a recorded `retryable=false` fact states the honest consequence ("repeating the identical request is expected to fail the same way. Refine the request before starting a new attempt."); a class without the bit surfaces the class verbatim plus the explicit no-retryable-bit limitation; and with NO typed fact the surface states the limitation explicitly ("the public contract exposes no authoritative recoverability or provider/infrastructure classification for this execution — only the terminal status and the recorded events are the facts, and the dashboard does not classify the recorded reason") — the recorded reason is displayed verbatim and NEVER classified by description (the rejected heuristic prose is absent, pinned); the quality-failure dimension states that its failed checks ARE the platform's authoritative facts (no recoverability question arises there); safe actions are constrained by the recorded fact; pinned by work-journey (13 tests across the amendment) + the (q) journey (four failure journeys: no-fact, retryable, not-retryable, quality) + the harness (all four surfaces).
- **AC11** Result → Evidence → Activity preserves context and stays tenant-safe — the tab links preserve the execution id (the route context); the (l) scope journeys still prove the tenant-safety (the bound scope on every scoped read; the cross-scope miss is an indistinguishable 404); D10 re-pins the composer-scope rule.
- **AC12** end-to-end API-backed tests including denial/failed/waiting paths — the (a)–(r) journeys (57) over the REAL server + REAL SDK client against the scope-ENFORCING fake world: the first-execution journey, the failed journey (c), the waiting journey (d), the denial/404 journey (h), the composer journey (p), the trust/failure journeys (q, incl. the three amended failure dimensions), the activity journey (r).

## The required discriminations (Implementation Requirement 7 — D9–D12, in tests/discrimination/dashboard-journey.discrimination.test.ts)

- **D9 trust-state confusion**: `deriveTrustAxes` returns exactly the four kinds (the merge mutant's single "overall" verdict fails the count and the kind set); `deriveConfidenceChip` refuses empty/FAIL/missing-confidence inputs (the fabrication mutant's "High confidence" differs on the same input — the pin discriminates); the rendered detail keeps four separate `<li>` facts with the honest "No verification results" and no merged-verdict vocabulary anywhere.
- **D10 cross-scope access**: STATIC — no scoped client call site in pages.ts passes any application/query/form argument (the mutant passing `ctx.form.applicationId` is flagged by the same scanner); RUNTIME — a request with a foreign application in the URL query reads through the deployment's bound scope only (every scoped wire call carried the bound application) and the page renders THIS application's execution facts.
- **D11 unsafe command paths**: STATIC — the route table registers exactly the three governed POST routes (create, the legacy cancel alias, the v2 cancel — one handler); the mutant adding `/command` as a POST route is flagged; RUNTIME — direct POSTs to non-command routes are refused (404/405) with ZERO wire mutations; the two governed commands ARE reachable and post to exactly `/executions` and `/executions/:id/cancel`.
- **D12 accidental customer-domain mutation**: RUNTIME — driving every read journey (Home, build, the review GET, runs, detail, tabs, views, agents, attention, command) issues ZERO POST wire calls; every GET wire call carries the bound scope; STATIC — the mutation vocabulary in the journey code is exactly `createExecution` + `cancelExecution` (a `dispatchExternalSideEffect`-style mutant is neither).

## The driven-browser verification record (real Chromium 1.57 over the real createDashboard server + the scope-ENFORCING fake world)

The harness (OUTSIDE the repository — /home/z/w036-harness, playwright 1.57) boots the real `createDashboard` against a fake API world implementing the REAL application-scope wire rule, then drives the canonical WORK-036 journeys. The re-review pass re-drove the session at the amendment head: **19/19 checks passed, ZERO console errors and ZERO page errors across the whole session**: the Home decision surface (the composer the first form in main with the outcome as its first visible field, zero analytics elements); the review envelope AND the amended AC9 commitment block (all five consequence facts + idempotency present, Run as the block's own confirm button, the honest absences explicit — no pre-run estimate, no public-contract undo — and the affected-resource fact carrying the application scope + the two attached artifacts); the keyboard discipline (the skip link the first tab stop, Run reachable and focused); the full journey (Run POSTs the governed create → 303 → the Result view); the three amended failure surfaces (the honest no-typed-fact limitation with the heuristic prose absent AND the dimension distinction; the platform-recorded retryable fact with the governed retry primary; the not-retryable fact with the honest consequence and no blind-retry push); the quality-failure dimension (the authoritative-checks fact, the execution-failure wording absent); mobile 390 (the commitment block renders, Run stays a 44px touch target, the failure surface and the Result|Evidence|Activity tabs survive); and the wire discipline (every scoped read carried the bound application scope).

## The complete gate (run at the final head, TWICE consecutively green)

`python3 scripts/governance-check.py` (OK) · `bun run typecheck` (0 errors) · `bun run lint` (biome clean, 964 files, zero diagnostics) · `ZECK_PG_TEST_URL=postgres://postgres@127.0.0.1:55432/postgres bun run test` = **278 files / 3889 tests, exit 0** — run TWICE consecutively green at the exact final head (the 276/3833 baseline + 2 files / +56 tests vs the base: work-journey 30, dashboard-journey discriminations 10, the (p)–(r) journeys + the amended failure journeys, the attachments/trust-strip/envelope/commitment/recoverability pins across the suites; +14 tests vs the PR #64 submission head — the amendment's own proofs).

## Test inventory (the exact delta vs the base, and vs the submission head)

- NEW unit: `work-journey.test.ts` (30 = 20 at submission + 10 amendment: the failure-dimension/classification tests 11, `deriveRecoverability` 6, the wait 5, the attachments 4, the §11 4)
- NEW discrimination: `dashboard-journey.discrimination.test.ts` (10 — D9 ×3, D10 ×2, D11 ×3, D12 ×2)
- Extended integration: journeys 46 → 57 (the (p) ×3 incl. the commitment pins, (q) ×4 → ×6 with the retryable/not-retryable failure journeys, (r) ×2) + the WhyPanel §11 assertions + the quality/waiting/typed-fact fixtures
- Updated units: components (+2 trust-strip pins), request-mapping (+the attachments pins, the Run/envelope pins, the AC9 commitment pins), html-escape (the hostile axis labels), state-primitives (+2 `reversibleDetail` pins)
- Total dashboard-local: 12 unit files / 197 unit tests + 57 journeys + 10 journey discriminations

## Required checkpoint contracts

- **SELF-HOSTING-BOUNDARY**: the dashboard still imports ONLY `../../sdk` + node builtins; the client script performs ZERO network calls (unchanged, D5 of the foundation suite); no new dependency; every fact remains a live SDK read; the transport scope remains the SDK's bound application scope (D10 re-pins it for the composer surfaces; the (l) journeys still prove the wire).
- **EXECUTION-PROVENANCE**: no new platform facts — the trust strip renders `deriveTrustAxes` (public wire shapes only); the failure dimension derives ONLY from the execution status + verification results; the recoverability facts derive ONLY from platform-typed event payload fields (a literal `retryable` boolean, the platform's own failureClass/outcomeClass vocabulary — never free-text interpretation); the wait question comes ONLY from the recorded wait event; the envelope's estimates are the declared request constraints (never a platform-estimate fabrication — the text says so); the commitment block's five facts are public-contract facts or honest absences (no pre-run estimate exists; reversibility is not exposed by the public contract — both stated, never fabricated); money renders from integer micro-USD strings (BigInt only); the attachments field maps to the wire's `inputArtifactRefs` exactly; no visual primitive manufactures trust or policy facts.

## Required verification mapping (the Work Order's list)

| Required | Evidence |
|---|---|
| governance checker | OK: 41 WOs, 102 reqs, inFlight=[], frontier=['WORK-036'] at the base and at the head |
| typecheck | 0 errors at the base and at the head |
| lint (biome) | clean (964 files) at the head |
| dashboard/unit/integration tests | 197 unit + 57 journeys + 10 journey discriminations |
| API-backed Home → Execution → Result → Evidence smoke | the (a)/(p) journeys over the REAL server + REAL scoped SDK client against the scope-ENFORCING world (the 303 → Result → Evidence chain) |
| trust-state discrimination | D9 + the trust-state suite + the (q) journey + the harness strip check |
| command authorization-path tests | D11 + the command-surface suite + the (f)/(n) journeys |
| responsive browser verification | the harness record (desktop / mobile 390, incl. the amended commitment and failure surfaces) |
| keyboard/accessibility verification | the harness record (skip link first, Run reachable and focused) + the (k) frame checks |
| secret-exposure discrimination | the html-escape suite (incl. the hostile trust-axis labels) + D5 of the foundation suite + the (k) hostile-query journey |
| full suite twice consecutively at exact final head | the complete gate section above |

## Limitations (honest)

1. The pre-run "estimated cost/time" cannot be a platform estimate — the public API exposes none — so the envelope presents the DECLARED constraints and the commitment block's cost row states the absence plainly. When a planning/estimate surface ships, its facts feed the same envelope section and the same row.
2. The public event stream today carries no `retryable` bit on execution-failure events (the platform's failure records carry it internally; `PublicError.retryable` exists on the transport error model). The dashboard therefore implements the honest both-ways contract: a typed fact is surfaced verbatim when the platform records one (proven by the retryable/not-retryable fixtures, which mirror the tools runtime's real mapping), and the explicit limitation renders otherwise (the real-platform-today case, proven by the no-fact fixture). No heuristic classification over free text exists anywhere — pinned.
3. The wait's "resolve" action remains outside the public API (the honest no-resolve note, unchanged from the foundation); the decision surface links the governed cancel and the return-to-work path.
4. The composer's competence/template affordances are honest not-yet-exposed states (the authorities are not public); the attachments affordance is the one live secondary input.
5. The driven-browser harness is a session artifact OUTSIDE the repository (the repository stays zero-dependency); its 19-check re-review record is the evidence; the repository's own automated proof is the 264-test dashboard-local suite (197 unit + 57 journeys) plus the 10 journey discriminations.
6. The trust strip's per-axis facts are the same derivations the Evidence tab renders in full (deriveTrustAxes) — compact labels, no new fact source (by design: one derivation, two presentations).
7. The substrate change is exactly one additive optional field (`ConfirmationView.reversibleDetail`); the WORK-035 default rendering is byte-identical without it (pinned) — the foundation is consumed, never redefined.
