# VAL-047 — Longitudinal Cost Curve and Cost-per-Successful-Outcome Improvement

**Work order:** `spec/validation-work-orders/VAL-047.md`
**Authorization:** Architect spec issuance at `6ace505` (session B's VAL-046 finalize — the alternation rule named the Lead session A lane); claimed at `67707c3` (the frontier-claim commit "VAL-047 in flight (Lead session A)" on main)
**Assurance:** HIGH_ASSURANCE
**Dependencies:** VAL-045, VAL-046 complete
**Dispatch base:** `67707c3` (main including the merged VAL-046)
**Implementation head:** the branch head of `work/VAL-047-longitudinal-curve` (exact SHA in the merge)
**Mode:** local engine — the slice was built by the Lead's local worker engine in two phases (core / discrimination+crown), and the battery below is the LEAD's own independent re-run over the program's embedded PostgreSQL (the live verification of record)

## Rails (per the work order's design guidance)

- **The offline curve corpus needs NO provider rail** — the curve semantics under test are pure derivations over RECORDED results: the longitudinal ledger points (the VAL-007 immutable ledger's recorded history), the VAL-045 attribution results, the VAL-046 substrate facts — all read-only inputs — over the REAL platform path over REAL PostgreSQL (`127.0.0.1:55432`, the program's embedded PostgreSQL 16.4). A re-measurement masquerading as curve derivation FAILS mechanically; the recorded-input curve never re-measures.
- **The Lead's battery RAN the full crown over the program's own PostgreSQL** — `ZECK_PG_TEST_URL` pointed at the program PG; the suite skips cleanly when absent (never a fake success).
- **NOT RUN in this sandbox: the live model rail** — no operator-authorized `OPENROUTER_API_KEY` exists in this environment (session B holds the live credential and completed the live reviews of VAL-032/040/042/043/046; the VAL-047 live row can be live-verified the same way). Never a fake success.

## Architecture note (within the work order's allowed surfaces)

The work order allowed ONLY the app surface (`benchmarks/validation/apps/economic-longitudinal-curve/**`) — no platform edit. The curve engine therefore lives app-locally in `driver.ts`, importing the existing platform/harness modules (the app-owned pattern). The slice's PURE oracles: `deriveWindowHonesty` (the declared window matches the recorded data — a cherry-picked suffix or prefix window FAILs with the omitted generations NAMED), `derivePriceRegimeMarking` (every point's pinned price revision verified against the VAL-040 model manifests and the VAL-046 substrate manifests; a price-regime change is MARKED on the curve with both revisions — a silent normalization, an unmarked change and a fabricated marking each FAIL named), `deriveNoExtrapolation` (points beyond the recorded evidence FAIL with the evidence's last generation named), `deriveImprovementRateReconciliation` (the improvement decomposes EXACTLY against the VAL-045 attribution — attributed per mechanism + substrate overhead + the honest residual EQUALS the measured delta per generation; an unexplained or hidden residual FAILs with BOTH sides named), `deriveCohortHonesty` (a regressing cohort is reported as regressing — the cohort series named), estimate/measure separation (planner quotes ride as SEPARATE estimate facts — an estimate claimed as measurement FAILs), Wilson 95% confidence + minimum-sample enforcement (no post-hoc exclusions — `post-hoc-excluded:gN` and `undeclared-point:gN` FAIL named; a below-minimum verdict claim FAILs), and the input-integrity oracle (digest-verified RECORDED inputs; a re-measurement masquerade FAILs field-by-field with the mismatch named — 316 ≠ 315).

**The curve honesty invariants this slice pins:** the declared window is the recorded window; price-regime changes are marked, never normalized away; no point exists beyond its evidence; the improvement rate reconciles EXACTLY against the recorded attribution (no unexplained residual); regressing cohorts are reported, never hidden; estimates never masquerade as measurements; verdicts carry Wilson confidence and respect minimum samples with no post-hoc exclusions; the recorded inputs are served read-only by content digest (the facts digest IDENTICAL after the whole corpus; a different-content commit under a recorded key THROWS); evidence carries payload DIGESTS, never payload bytes.

The app (`benchmarks/validation/apps/economic-longitudinal-curve/`) declares the corpus (AC1): 12 offline rows + 1 env-gated live row. Per row the corpus declares the recorded input digests (the longitudinal ledger points, the VAL-045 attribution results, the substrate facts), the curve family under test (cost-per-outcome-trajectory / improvement-rate-decomposition / plateau-maturity-verdict) and the expected verdict. The corpus includes the four honest classes (invoice-extraction trajectory-improving with the g2 model price-regime change MARKED rev-001→rev-002; code-search trajectory-plateaued over the honest flat tail; tool-routing never-materialized; rag-retrieval mixed-cohort-regressing with the det cohort's 120→40 regression reported) and the eight adversarial probes (cherry-picked-window / regime-normalizing / extrapolating / residual-hiding / cohort-hiding / post-hoc-exclusion / sample-size-violation / re-measurement-masquerade) whose populations are pinned now and whose adversarial fixture variants FAIL mechanically in the discrimination suites. The app rides the PUBLIC SDK boundary (`createZeckClient` — no `src/**` import).

## Changed-file inventory

- `benchmarks/validation/apps/economic-longitudinal-curve/driver.ts` (2044 lines) — the app-local curve engine: the PURE oracles above (window honesty, price-regime marking, no-extrapolation, improvement-rate reconciliation, cohort honesty, estimate/measure separation, Wilson + minimums, input integrity), the family synthesis (the cost-per-outcome trajectory, the improvement statistics, the derived cohorts, the Wilson interval, the mechanical verdict), the row driver and the app-boundary outcome contract.
- `benchmarks/validation/apps/economic-longitudinal-curve/corpus.ts` (944 lines) — the corpus (13 rows): the four honest classes, the eight probes, the live row (env-gated); the pinned recorded-input digest references per row.
- `benchmarks/validation/apps/economic-longitudinal-curve/fixtures.ts` (473 lines) — the deterministic recorded-input fakes: the pre-seeded longitudinal ledger points + VAL-045 attribution results + substrate facts, with the cherry-picked / regime-normalizing / extrapolating / residual-hiding / cohort-hiding / post-hoc-exclusion / sample-starved / re-measurement variants; the fake API world; the tick clock.
- `benchmarks/validation/apps/economic-longitudinal-curve/application.ts` (212 lines) — the curve application on the public SDK boundary: submission, the observation phase (outcome contract re-derived), the exported task slice.
- `benchmarks/validation/apps/economic-longitudinal-curve/{config.json,README.md}` — the secret-free configuration (consistency unit-tested) and the README.
- `tests/unit/validation/val-047-longitudinal-curve.test.ts` — 50 unit tests over the driver: every oracle matrix (each adversarial shape FAILing its NAMED criterion), the family synthesis pins, the digest discipline, the verdict derivation over the pinned thresholds.
- `tests/unit/validation/val-047-apps.test.ts` — 26 unit tests: task-slice ↔ corpus ↔ config consistency; well-formedness (every row's digest references resolving); the app over the honest fake world; each fixture variant FAILing its specific criterion.
- `tests/discrimination/longitudinal-curve-validation.discrimination.test.ts` (1041 lines, 44 tests) — the AC6 families over the PURE derivations, each as the derivation-level catch (NAMED evidence asserted) + the row-level catch (the probe row over the purpose-built leaky stack) + the honest control: window cherry-picking, regime normalization, extrapolation, residual hiding, cohort hiding, post-hoc exclusion, sample-size violation, re-measurement masquerade; the Wilson application + digest determinism/discrimination; the honest controls over the fixture stack.
- `tests/integration/validation/val-047-longitudinal-curve.test.ts` (1350 lines, 2 tests) — the crown proof (AC3/4/5/7): the recorded basis served READ-ONLY over REAL SQL and the whole offline corpus driven over the REAL platform path; the live row env-gated on `OPENROUTER_API_KEY` (absent credentials = the recorded NOT RUN boundary).
- `docs/work-items/VAL-047.md` — this evidence document.

**Shared files:** ZERO edits to `apps/shared/**` or `benchmarks/validation/platform/**` (the work order forbids both — the engine is app-local). `src/**` and `spec/**` untouched. No existing test file is modified; every suite is a NEW file. One 1-line mechanical biome organize-exports fix inside the app's own `fixtures.ts` (phase 2, in-surface, no behavior change).

## REAL end-to-end run — RAN over the program's embedded PostgreSQL 16.4 (the Lead's battery)

The crown suite ran end to end against the REAL platform path over the program's own PostgreSQL (`ZECK_PG_TEST_URL`, `127.0.0.1:55432`). The measured per-row facts (the suite's own console output, from the Lead's recorded battery run):

| Row | Curve family | Points | Trajectory (µ$/outcome) | Improvement | Wilson 95% | Verdict | Latency |
|---|---|---|---|---|---|---|---|
| invoice-extraction-cost-per-outcome-trajectory | cost-per-outcome-trajectory | 4/4 verified | 56 → 43 → 30 → 17 (the g2 price-regime change rev-001→rev-002 MARKED) | 340 µ$ | [0.790, 0.957] | **COMPLETED** trajectory-improving | 71 ms |
| code-search-plateau-maturity-verdict | plateau-maturity-verdict | 6/6 verified | 22 → 18 → 13 → 7 → 7 → 7 | 375 µ$ | [0.975, 1.000] | **COMPLETED** trajectory-plateaued | 83 ms |
| tool-routing-never-materialized | plateau-maturity-verdict | 4/4 verified | 43 → 43 → 43 → 43 | 0 µ$ | [0.652, 0.895] | **COMPLETED** never-materialized | 77 ms |
| rag-retrieval-improvement-rate-decomposition | improvement-rate-decomposition | 4/4 verified | 72 → 48 → 29 → 15 | 525 µ$ | [0.790, 0.957] | **COMPLETED** mixed-cohort-regressing (the det cohort's 120 → 40 regression reported) | 52 ms |
| probe-cherry-picked-window | plateau-maturity-verdict | 3 (suffix 7→7→7) | — | — | — | **FAILED** (the omitted generations named) | 47 ms |
| probe-regime-normalizing | cost-per-outcome-trajectory | 4 | — | — | — | **FAILED** (REGIME-NORMALIZED named per point, both revisions) | 47 ms |
| probe-extrapolating | cost-per-outcome-trajectory | 5 (verified 4; the 5th at 6 µ$ beyond the evidence) | — | — | — | **FAILED** (EXTRAPOLATED POINT named with the evidence's last generation) | 55 ms |
| probe-residual-hiding | improvement-rate-decomposition | 4 | — | — | — | **FAILED** (claimed 0 vs the recorded 30/30/35, both sides named) | 107 ms |
| probe-cohort-hiding | improvement-rate-decomposition | 4 | — | — | — | **FAILED** (COHORT-HIDING with the series [120, 40, 0] named) | 45 ms |
| probe-post-hoc-exclusion | cost-per-outcome-trajectory | 5 | — | — | — | **FAILED** (post-hoc-excluded:g4 named) | 57 ms |
| probe-sample-size-violation | cost-per-outcome-trajectory | 2 | 65 → 40 | 205 µ$ | [0.690, 0.957] | **FAILED** (BELOW-MINIMUM VERDICT CLAIM) | 42 ms |
| probe-remeasurement-masquerade | cost-per-outcome-trajectory | 4 (verified 3) | 56 → 43 → 31 → 17 | — | — | **FAILED** (RE-MEASUREMENT MASQUERADE, 316 ≠ 315 field-by-field) | 48 ms |

The eight probe rows pin their populations NOW and drive the HONEST analyses over them (each landing its durable execution, the observed-terminal-readback PASSing — the row-outcome contract is honest even when the row's verdict is FAILED); their adversarial fixture variants each FAIL mechanically in the unit/discrimination suites below.

**The rag improvement-rate decomposition (reconciled EXACTLY against the VAL-045 attribution):** the recorded deltas 190/180/155 µ$ decompose as attributed 150/140/110 µ$ (per the recorded per-mechanism attribution) + substrate overhead 10 µ$ (the VAL-046 substrate share, priced at its own pinned revision) + the honest residual 30/30/35 µ$ — the reconciliation identity holding per generation, the residual reported, never hidden, never forced.

**Battery-level curve integrity (the whole offline corpus, over the REAL SQL):** 4 COMPLETED + 8 honest FAILED of 12 driven rows (every honest verdict mechanically derived; every probe FAILing its NAMED criterion — no narrative verdicts anywhere); **35 recorded rows served READ-ONLY from REAL SQL** across two seeding operations (20 longitudinal ledger points + 15 VAL-045 attribution results — the canonical payload-free digests as fingerprints; the identical re-commit REPLAYS, asserted; a different-content commit under a recorded key THROWS, asserted on a mutated invoice g0 runs count; every served row's re-derived digest == its own fingerprint; every served point's substrate revision verifies against the VAL-046 substrate manifests); the ledger holds exactly **12 durable executions + 12 idempotency records** (zero phantoms, zero drift, zero orphan events); every execution_events sequence gapless; every verified point sealed through the REAL recorder with the measured model cost + substrate overhead at their own pinned revisions while the planner quotes ride as SEPARATE estimate facts; every curve reproduces the pinned synthesis field-for-field on the honest rows; usage/latency honestly the recorded history's own offline (none freshly measured — the live rail owns that).

**NOT RUN boundary (recorded honestly):** the live row demands three REAL curve points of three REAL workload dispatches each over the pinned OpenRouter rail (BYOK, measured usage, every priced input at its pinned manifest revision rev-001, max_tokens 32 pinned explicitly, the substrate share priced at sub-rev-001) with the verdict recorded through the REAL recorder; no operator-authorized credential exists in this environment, so the row is NOT RUN — never a fake success. The live-rail code path is fully wired and will run as soon as an operator-authorized credential is present (reserved for the operator/session-B live review, the VAL-032/040/042/043/046 pattern).

**Design notes (recorded honestly):** (1) the crown serves the RECORDED basis over REAL SQL (the VAL-045 seeding discipline at curve scale — two read-only operations) rather than verifying against in-memory imports only; (2) the live-rail gate-open path wires direct OpenRouter dispatches for its three measured points rather than riding the platform model gateway — the VAL-041 direct-provider BYOK precedent; the seam choice is flagged here for the live review when a credential is present.

## Acceptance-criteria mapping

| AC | Evidence |
|---|---|
| 1. Public SDK boundary, secret-free config, executable tests; per row the recorded input digests, the curve family, the expected verdict | the app (`createZeckClient`, no `src/**` import), `config.json` (consistency unit-tested), the corpus module (13 rows with the full oracle per row), the unit suites |
| 2. Deterministic fixtures; recorded results referenced by content digest; every point's pinned price revision verified against the manifests; price-regime changes MARKED, never normalized away | `fixtures.ts`; the digest-verified recorded inputs; `derivePriceRegimeMarking` — unit- and discrimination-tested (the unmarked change, the fabricated marking and the silent normalization each FAIL named); the invoice g2 regime change MARKED on the trajectory above |
| 3. REAL end-to-end (env-gated) over the REAL platform path: curve points over measured facts, recorded through the REAL recorder with honest economics | `val-047-longitudinal-curve.test.ts` — **RAN** over the program's embedded PostgreSQL (the measured table above; every verified point sealed through the REAL recorder); the live row is the env-gated NOT RUN boundary |
| 4. Mechanical verification per row: window honesty, price-regime marking, no extrapolation, improvement-rate reconciliation (exactly, no unexplained residual), cohort honesty, estimate/measure separation, confidence and minimum-sample enforcement — probed adversarially | the PURE oracles — unit- and discrimination-tested incl. every named adversarial shape (44 discrimination tests) |
| 5. Honest economics and latency (measured on live rows, none-reported honestly offline); payload digests in evidence | the per-row table (µ$-scale trajectories, the reconciled decomposition, measured latencies); usage honestly none-reported offline; FNV-1a digests — payload bytes never journaled |
| 6. Discrimination tests prove the curve families against controlled fakes (window cherry-picking, regime normalization, extrapolation, residual hiding, cohort hiding, post-hoc exclusion, sample-size violation, re-measurement masquerade) | 44 discrimination tests covering exactly the eight named families + the Wilson/digest disciplines + honest controls |
| 7. Evidence document records REAL outcomes, timings, costs, NOT RUN boundaries and the executed battery honestly | this document |

## Executed battery (the Lead's independent re-run at the implementation state, sequential, over the program's embedded PostgreSQL)

Baselines measured by the Lead on the dispatch base `67707c3` (main including the merged VAL-046): unit 5222 / arch 2004+4 skipped / integration 1199+20 skipped.

| Command | Outcome | Detail |
|---|---|---|
| `bunx tsc --project <scoped app+tests>` | pass | exit 0 — the Lead's fresh re-run at the phase-2 head (attempt 1, directly verified); verified independently by both workers at the committed heads (the 67-a run caught 3 real TS errors; the 67-b run caught 1). The Lead's full-repo tsc attempts were SIGKILLed by the sandbox's memory flake (the dev-server infrastructure holds ~1.4GB of the 4GB box — required services; the type graph has grown with every WO). CI runs full-repo tsc on GitHub runners and is the binding full check — the identical local situation passed CI on PR#109/#110/#111/#112 |
| `bunx biome check . --diagnostic-level=error` | pass | ZERO errors (1751 files, full-repo, CI parity) |
| `bun run test:unit` | pass | 308 files / 5298 tests (**+76** vs the 5222 baseline — exactly the VAL-047 unit suites: 50 driver + 26 apps) |
| `bun run test:architecture` | pass | 134 files passed + 1 skipped / 2048 tests passed + 4 skipped (**+44** vs the 2004 baseline — exactly the VAL-047 discrimination suite) |
| `bun run test:integration` | pass | 156 files passed + 1 skipped / 1201 tests passed + 20 skipped (**+2** vs the 1199+20 baseline — exactly the VAL-047 crown; the offline corpus test 3.5 s, exit 0, no flakes this run) |
| `python3 scripts/governance-check.py` | pass | `Governance OK: 60 Work Orders, 110 requirements, inFlight=[], frontier=[]` |

## Defects found

None in the delivered surfaces. Four development-time TS errors were caught and fixed by the workers before commit (the direct-exit-code discipline: three in phase 1 — the digest-function Omit signature, the HonestCurveInputs re-export, a phantom guard property; one in phase 2 — a null-typed estimatedQuoteMicroUsd in the SQL ledger-row parser). The eight FAILED probe verdicts and the never-materialized/plateaued honest classes are the pinned oracle working as designed — honest classification, never a narrative.
