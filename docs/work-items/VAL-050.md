# VAL-050 — End-to-End Customer Journey

**Work order:** `spec/validation-work-orders/VAL-050.md`
**Authorization:** resident tech-lead A-lane takeover (takeover base `6d784cb`; session B's claim `aa13b7f` stalled per the takeover commit's arbitration note)
**Assurance:** HIGH_ASSURANCE
**Dependencies:** VAL-002 (harness), VAL-010 (text apps), VAL-019 (broad suite), VAL-049 (audited economics) — all complete
**Mode:** A-lane local delivery engine, phase-1 (app corpus + unit suites); phase-2 (discrimination + integration over the REAL PG path) to follow

## Phase-1 changed-file inventory

- `benchmarks/validation/apps/customer-journey/corpus.ts` — the declared journey rows (9): per row the journey shape (its stages and their recorded input digests — the onboarding stage over the VAL-002 SDK harness surface, the plan stage over the VAL-049 audited economics, the daily-usage stage over the RECORDED application portfolio: VAL-010 fixture slices + the VAL-019 customer-service task slice), the integrity families under test and the expected verdict (`JOURNEY-COMPLETED` / `JOURNEY-FAILED` with the failed criteria NAMED / `NOT-RUN` with the env var named). The per-stage recorded economics are carried from VAL-049's audited rows, each anchored by a basis digest re-derived over the audit row's own recorded evidence digests.
- `benchmarks/validation/apps/customer-journey/driver.ts` — the app-local journey machinery (the work order's allowed surface ONLY): the deterministic journey observation (pure derivation over the recorded basis) and the five mechanical integrity oracles (`verifyCustomerJourneyIntegrity`: stage completeness / cross-stage idempotency / continuation exactly-once / customer boundary / accounting reconciliation — each FAILing with the stage, intent or residual NAMED).
- `benchmarks/validation/apps/customer-journey/fixtures.ts` — the deterministic fixtures: the transport-level fake public API world (create/replay semantics, honest outcome shapes, the journey observation package at the result boundary) and the discrimination knobs (the five controlled fakes). The offline fake world NEVER serves the live rail.
- `benchmarks/validation/apps/customer-journey/application.ts` — the app proper over the public SDK boundary (VAL-002 harness): submit under its OWN idempotency key → poll → result read (statuses, honest usage, journey observation) → the five oracles re-derived AT THE BOUNDARY → the app-side journey contract (honest-vocabulary pins: expected verdict, failed criteria NAMED, terminal↔statuses agreement, usage honesty, idempotent re-issue replay, live-gate honesty).
- `benchmarks/validation/apps/customer-journey/config.json` — the secret-free, repository-reproducible configuration (the pinned task-slice mirror; corpus input digest `1e992443`).
- `benchmarks/validation/apps/customer-journey/README.md` — the corpus table + the running instructions.
- `tests/unit/validation/val-050-apps.test.ts` — 17 tests: the corpus + configuration consistency (config mirror, unique row ids, full-lifecycle declarations, pinned probe criteria, well-formed payload-free digests, portfolio resolution against the recorded corpora, VAL-049 basis-anchor re-derivation, references-only task bodies, honest live gating), the app over the honest fake world, the honest NOT-RUN live boundary (+ the fake world's live-rail refusal), the adversarial probe rows over the app boundary, and the customer-boundary discriminations.
- `tests/unit/validation/val-050-core.test.ts` — 20 tests: the five oracles over the honest rows (incl. the continuation row's exactly-once resume with the failed attempt's cost never hidden), the discrimination battery (the five controlled fakes each FAILing named — the hidden residual NAMED to the microUsd), the crafted discriminations beyond the probe vocabulary (undeclared resume, out-of-order stages, undeclared extra stage, re-priced stage, empty observation), the honest verdict vocabulary, determinism and the digest-only evidence discipline.

## Honest boundaries

- The ONE live row (`live-journey-slice`) is env-gated on `OPENROUTER_API_KEY` and is honestly `NOT-RUN` without the credential (the env var NAMED in the verdict and the evidence; zero submissions). The offline fake world refuses the live rail outright — a live journey is never fabricated offline. The live lane (REAL dispatches through the REAL platform path, REAL recorder, measured usage) is the Lead's live-review lane / phase-2.
- Offline rows are deterministic and credential-free; the stages replay RECORDED workloads (pure derivation over RECORDED results — never re-measured, never re-priced); usage is honestly none-reported offline.
- Evidence references are digest-only (payload-free FNV-1a; no payload bytes, no prices in task bodies).

## Phase-2 must cover

- The discrimination battery over the REAL platform path (the controlled fakes driven through the real seams, not only the transport fake).
- The env-gated REAL live journey slice (`OPENROUTER_API_KEY`) through the REAL recorder with honest measured economics.
- Integration over the REAL PG instance (`:5433`) — the journey's durable executions, idempotency and continuation over the real execution ledger.
- The evidence document (AC7) recording REAL outcomes, timings, costs, NOT-RUN boundaries and the executed battery honestly.

## Phase-2 delivery (the local delivery engine — discrimination + integration crown)

**Changed-file inventory (phase-2, additive and disjoint):**

- `tests/discrimination/customer-journey-validation.discrimination.test.ts` — 33 tests: the five controlled fakes each DETECTED at the seam (forced onto the HONEST row through `runCustomerJourneyApp` over the SDK/harness boundary — the NAMED oracle FAILing with the mechanism named in evidence: `missing-stage:daily-usage`, `orphaned-intent:daily-usage`, `resume-violation:daily-usage` (observed executions=2 undeclared), `boundary-leak:daily-usage` under `app-other-customer`, `cost-residual:-<exact microUsd>`), each with the honest contrast pair and the row-level catch (the probe rows settle FAILED with exactly their pinned criteria); replay determinism (bit-stable re-derivation, identical verdict over fresh fake worlds, a controlled mutation flips the verdict and the re-derived journey digest); the rubber-stamp oracle (a pass-everything oracle passes every fake and is contradicted by the REAL re-derivation on each; a platform claiming ALL-PASS statuses over a denatured journey still FAILs the app contract); the favorable-subset battery (the honest-only subset fake-passes while omitting exactly the five probe rows, NAMED); the live-rail honesty (NOT RUN without `OPENROUTER_API_KEY`, zero submissions; the offline fake world refuses the live rail even with the credential set); and the seam-level re-proof of every offline AC the unit suites cover.
- `tests/integration/validation/val-050-customer-journey.test.ts` — 3 tests, env-gated on `ZECK_PG_TEST_URL` (honest skip with the env var NAMED when absent): over REAL PostgreSQL the journey's durable surface lands through the REAL platform path (the REAL Fastify public API over the REAL SQL authorities; every stage intent submitted through the REAL SDK client over REAL HTTP under its OWN idempotency key; every landed execution driven through the REAL state machine with durable planning decisions and digest-reference step events); the recorded stage economics committed READ-ONLY into REAL SQL through the platform's own idempotency-record arbitration (identical re-commit REPLAYS; a different-content commit under a recorded key is REFUSED — the frozen-input discipline); the five oracles re-derived AT THE BOUNDARY over the DURABLE LEDGER (REAL SQL reads of the executions rows, their executing application identities, the intent submissions in the idempotency ledger and the SQL-served recorded economics) — the honest journeys PASS all five and reproduce the offline pure derivation EXACTLY (digest parity); continuation exactly-once (the resumed stage's durable executions = exactly two: FAILED attempt + one COMPLETED resume); idempotent re-issue (the identical request under the same key REPLAYS over REAL HTTP — no second execution, no second idempotency record); the five adversarial durable shapes each DETECTED by the NAMED oracle over the durable ledger (the boundary leak durably under the OTHER customer's application in REAL SQL).

**Executed battery (REAL outcomes):**

- Discrimination suite: **33/33 pass** (205 assertions; run time ≈ 0.37 s, bun test, offline deterministic — no credentials).
- Integration crown over REAL PostgreSQL 16 (`postgres://val@127.0.0.1:5433/postgres`, disposable per-run database, shipped migrations applied, dropped on teardown): **3/3 pass** (99 assertions; wall times ≈ 0.67 s / 0.72 s / 0.02 s per test, ≈ 2.4 s total incl. world seeding).
- Honest skip path (no `ZECK_PG_TEST_URL`): the crown **SKIPS** (0 pass, 0 fail, 5 skip) with the env var NAMED in the skip log — never fails, never fake-passes.
- Phase-1 regression: **37/37 pass** (`tests/unit/validation/val-050-{apps,core}.test.ts`, 1259 assertions).
- Toolchain gates: `biome check` clean on the new files; `tsc --noEmit` exit 0.

**Costs (honest):** offline rows carry the RECORDED basis only — the recorded stage-economics totals (the VAL-049 carried cost basis, never re-measured, never re-priced): `full-journey-recorded-portfolio` 13,500 microUsd / 400 ms; `journey-continuation-resume` 23,100 microUsd / 640 ms (the failed attempt's cost never hidden); `journey-idempotent-reissue` 13,500 microUsd / 400 ms; the probe rows share their shapes' bases. Over REAL SQL the crown reconciled the SQL-served recorded basis against the corpus declarations digest-for-digest with **zero residual**. No live costs are reported — none were measured (see the NOT-RUN boundary below).

**NOT-RUN boundaries (honest):**

- The ONE live row (`live-journey-slice`) is **NOT RUN** in phases 1–2: it demands the operator-authorized rail (`OPENROUTER_API_KEY` absent in this battery). The live lane — REAL live dispatches through the REAL platform path, recorded through the REAL recorder, MEASURED usage and economics — is the Lead's/live-review lane (AC3); this document records no live outcome because none was driven. Live-lane fields (measured input/output tokens, live latency, live cost, the driven live verdict): **PENDING the Lead's own battery.**
- The integration crown is NOT RUN without `ZECK_PG_TEST_URL` (the honest skip above).
- No credentials appear in the repository, logs or reports; evidence references are digest-only.
