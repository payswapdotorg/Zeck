# WORK-056 Evidence — Competence-aware Optimization and Progressive Deterministicization

Work Order: `WORK-056` (`spec/work-orders/WORK-056.md` on `main` at the dispatch base — the canonical dispatch) · Canonical remote: **`payswapdotorg/Zeck`** · Assurance: **HIGH_ASSURANCE** · Governing architecture: frozen v1.0 + Execution Intelligence Architecture **E1.0** + Economic Execution Intelligence **E1.1** (ADR-0019, ADR-0020) · E1.1 charter member **WORK-056** (the competence-economics plane — the FINAL E1.1 charter member; dispatched ALONE through Issue #30 after the entire preceding program merged: WORK-051 PR #24, WORK-052 PR #22, WORK-053 PR #26, WORK-054 PR #27, WORK-055 PR #29).

## Dispatch, branch and ancestry

Exact dispatch base: `201756c91f4451312b2e72258f2121b0022bac51` (the WORK-055 merge `ef929a4` plus the Architect finalization/authorization commit `201756c` — "finalize WORK-055 (PR #29 merged) and authorize WORK-056 (final E1.1 charter member)"; verified — the required branch `work/WORK-056-competence-aware-optimization` was created at exactly that SHA before any change, and `git merge-base HEAD 201756c` equals it). Final implementation head: **`823d0b1`** (the type-conformance commit; this evidence document rides on top; the full battery was re-run green at that revision). **5 commits at the implementation head, ZERO merge commits, ZERO `spec/` changes, ZERO changes to `src/platform/execution-ir/`, `src/platform/execution-compiler/`, `src/platform/tool-surface/`, `src/platform/context-economics/`, `src/platform/model-economics/`, `src/platform/substrate-economics/`, `src/platform/failure-recovery/`** (mechanically verified: `git log --merges 201756c..HEAD` → 0; `git diff --name-only 201756c..HEAD -- spec/` → 0; `git diff --name-only 201756c..HEAD -- src/platform/execution-ir/ src/platform/execution-compiler/ src/platform/tool-surface/ src/platform/context-economics/ src/platform/model-economics/ src/platform/substrate-economics/ src/platform/failure-recovery/` → 0 — the plane BUILDS ON the WORK-049/050 foundation and the five merged dependency planes, it never edits or forks them). ZERO changes to `src/modules/`, `src/api/`, `src/integrations/` and `src/platform/db/` (no migration; the count stays 29). ZERO shared test-surface touches (the plane declares no SDK and no adapter — nothing to reconcile). One PR, opened by the worker, **not merged by the worker**.

The C9 ancestry proof in this order's architecture battery uses the **drift-immune DYNAMIC merge-base pin** (the merge-base of this branch and `main`, computed at run time), not a hardcoded dispatch-base diff: the proof can only ever name THIS branch's own changes, so main advancing past the branch point (the Architect's finalization commits) never self-trips it — the exact stale-pin pattern WORK-055's C9 test carries (disclosed below in the baseline section).

## Baseline gate at the exact dispatch base (readiness checkpoint — BEFORE implementation)

Executed at exactly `201756c` before any change:

- **`python3 scripts/governance-check.py` PASSED** — `Governance OK: 56 Work Orders, 102 requirements, inFlight=[], frontier=['WORK-056']` (the exact expected frontier — this order dispatched alone, no siblings in flight).
- `bun run typecheck` — 0 errors.
- `bun run lint` — biome clean, exit 0 (1,269 files).
- `bun run test:unit` — **212 files / 3110 tests, all passed**.
- `bun run test:integration` with real PostgreSQL 16.9 (`ZECK_PG_TEST_URL`, 127.0.0.1:55432) — **111 files / 1052 passed | 13 skipped** (the live-provider honesty skips unchanged from WORK-048/049/050/052/053/054/055).
- `bun run test:architecture` — **1324 tests passed | 4 skipped + 1 PRE-EXISTING FAILURE** (carried through to the final head unchanged — full disclosure below).

The PostgreSQL 16.9 server was built from source in this worker environment (no system PostgreSQL 16/17 package installable without elevated privileges; the same instance pattern prior workers used — WORK-054 used 16.4, WORK-055 built 16.9) and run rootless at 127.0.0.1:55432 (user `zeck`, trust auth).

**The one pre-existing architecture failure, mechanically isolated and disclosed:** `tests/architecture/e11-failure-recovery-boundaries.test.ts` (WORK-055's merged C9 test) hardcodes WORK-055's dispatch base `9b6fa2f` and diffs `spec/` against it; the Architect's own finalization commits (`ef929a4`..`201756c`) advanced `spec/` (added `spec/work-orders/WORK-056.md` + `spec/development-state/*` updates — the authorization of THIS order), so the pin self-trips on PRISTINE `main` at `201756c`. Proof of pre-existence: a detached `git worktree` at exactly `201756c` (zero of this branch's changes present, `bun install --frozen-lockfile`, single-test run) reproduces the identical failure — `C9 build-on, never-fork ... expected '...' to be ''` naming `spec/development-state/*.json` and `spec/work-orders/WORK-056.md`. The failing diff names ONLY files this branch never touches (`git diff --name-only 201756c..HEAD -- spec/` → 0). Repairing the test would mean modifying WORK-055's merged evidence artifact — outside this order's Declared Change Surfaces — so the reconciliation is the Tech Lead's (the C9 pattern this order's own battery uses — the dynamic merge-base pin — is the drift-immune replacement). The failure is deterministic, present at the base before any change, and identical at the final head.

## External-infrastructure access (readiness checkpoint)

**NOT RUN — no live provider APIs (model providers, E2B/Daytona/Modal/self-hosted runners), no live trajectory-observation feeds and no live differential/property/replay evaluation harnesses exist in this worker environment** (none are required by this Work Order: the competence plane is pure decision evidence; the observation, evaluation and execution layers that would act on it are wired by later orders). The only credential held is the operator-provided GitHub PAT for `payswapdotorg/Zeck` (environment-only; used solely for the Git/PR lifecycle of this Work Order; never committed, never logged). No provider resource in any non-GitHub account was mutated. The trajectories, environments, equivalence suites and gate evidence in the tests are representative typed fixtures (the closed vocabularies over neutral slugs); the seams that will report REAL observations and REAL evaluations are the existing execution/decision seams, wired outside this plane.

## What this order IS

E1.1 charter member WORK-056 — the FINAL charter member: the **competence-economics plane** — a new platform plane (`src/platform/competence-economics/`, 9 modules, 3,828 new source lines; 8,104 new lines including tests and this evidence) implementing competence as a SELECTABLE REPRESENTATION (typed, content-addressed competence records carrying the trajectory digest, applicability bounds, expected-outcome evidence with verification binding and the foundation's own explicit-basis `CostClaim` — selectable as decision evidence through the EXISTING seams, never self-asserting; the structural self-assertion guard: the miner must differ from every trajectory executor, enforced at construction AND read time), progressive deterministic retrieval (the pure function of (corpus, query, configuration): tenant-scope structural isolation through the merged context-economics plane's own key discipline, capability/tag applicability bounds, environment-fingerprint exact match through WORK-055's own comparison, the foundation's own quality floor as validity, the deterministic TOTAL order cost-ascending → observation-count-descending → recordId, bounded results, and the ZERO-COMPETENCE operation: an empty corpus is always representable and fail-safe), successful-trajectory mining (MINING OBSERVES, NEVER DECIDES: success-only trajectories validated through WORK-055's own fingerprint and attribution validators, the repetition minimum, conservative evidence — minimum quality, maximum cost/latency, observed reliability — the output ALWAYS a `candidate`-stage record), deterministic replacement candidates with the FULL differential/property/replay equivalence suite (admission fails closed on any missing component or any component failing WITHIN ITS OWN DECLARED BOUNDS; the execution binding rides the merged tool-surface plane's own closed representation vocabulary through its own `isToolRepresentation`), the gated shadow→canary→deterministic promotion path (the target stage is COMPUTED rank+1 from the record's current stage — never caller-supplied, so gate-skipping is structurally unrepresentable; every gate re-proves the full equivalence suite under the governing policy floor; the shared economics admissibility through the model-economics plane's OWN `governingFacts`/`evaluateAdmissibility`; NO SELF-PROMOTION — the requesting authority must be independent of every trajectory executor and the miner; the verdict is typed evidence: promoted/hold/reject with closed reason codes), bounded typed rollback (exactly one stage back, derived never supplied; the closed reason↔evidence coherence; the independent requesting authority; the IDEMPOTENT pure apply — promoted→reverted, reverted→bounded no-op, foreign→typed rejection), and the WORK-049 decision-record ride (promotion and rollback decisions as `OptimizationDecisionRecord`s through the foundation's own `buildOptimizationDecision`, the corpus the evidence-honest pair — incumbent+replacement / promoted+reverted — with the content-addressed provenance riding the transformation-basis detail, appended by the caller through the EXISTING `SqlOptimizationDecisionStore`; hold/reject verdicts record NOTHING — the typed outcome IS the evidence). **No new authority, no new state machine, no new durable surface (zero migrations — the count stays 29), no changes to the foundation or the five dependency planes, no planner/execution-lifecycle modifications, no vendor vocabulary anywhere, no authorization surface.**

## Acceptance-criteria mapping (Work Order §Acceptance Criteria)

| AC | Claim | Evidence |
|---|---|---|
| 1. Typed competence records (trajectory digest, applicability bounds, expected-outcome evidence, cost), content-addressed and explicit-basis; self-asserted or unattributed records are rejected | PASS | `record.ts` + `catalog.ts`: the typed content-addressed record (`recordId` = sha256 over the canonical form; the frozen `recordBasis`), the four evidence loads exactly as the Work Order names them, the structural self-assertion guard (miner ∈ executors → `record-self-asserted` at construction AND at read time), the unattributed rejections (no miner identity, no repetition evidence, no explicit basis, no verification binding → `record-unattributed`), the applicability bounds (capability slug, bounded unique tags, the valid environment fingerprint through WORK-055's OWN validator), the claim through the foundation's OWN `validateCostClaim`, the tenant-scoped corpus key through the merged context-economics plane's OWN `deriveTenantScopedCacheKey`. Unit `record.test.ts` (16 tests): content-addressed round-trip, byte-identical determinism, input-order independence (tags, executors), the self-assertion guard at BOTH times (single-call capture discipline), unattributed rejections, applicability-bound validation, tamper rejection (identity re-derivation), tenant-key derivation with structural tenant safety, the no-authority-vocabulary scan, HONEST CONSUMPTION (the foundation's and failure-recovery's own typed errors propagate as their own types, never re-branded). Discrimination D1 |
| 2. Progressive retrieval ranks candidates by applicability match, evidence quality and economics; deterministic including tie-breaking; bounded result sets | PASS | `retrieval.ts` `retrieveCompetence`: the PURE decision over (corpus, query, configuration); the applicability match (tenant scope structural, capability exact, tags cover EVERY query tag, environment fingerprint EXACT match through WORK-055's own `compareFingerprints` — drift is a recorded `environment-drift` verdict, never a silent match); the quality floor as validity through the foundation's own `evaluateCandidate` (below-floor records ranked out WITH their typed evaluation — the "why the cheaper did not surface" evidence); the deterministic TOTAL order (expected successful-resolution cost ascending via the foundation's own `evaluateCandidate` economics, ties by repetition evidence descending, ties by recordId ascending); `maxResults`-bounded; every verdict RECORDED (closed inadmissible codes: capability-mismatch / tag-bounds-mismatch / environment-drift / quality-below-floor / record-shape); tampered records never surface (read-time validation). Unit `retrieval.test.ts` (16 tests) incl. the comparator totality/antisymmetry, corpus-order independence, byte-identical re-runs, purity (no corpus mutation), the corpus/result bounds, duplicate-identity rejection; discrimination D5 |
| 3. Successful-trajectory mining produces candidate records from failure-recovery/decision-record evidence — observation only, never a promotion | PASS | `mining.ts` `mineCompetenceCandidate`: MINING OBSERVES, NEVER DECIDES — the input is what the existing seams report (the governed execution identity, the WORK-049 decision record that governed it, the verification authority's OWN measurement recorded never re-judged, the observed economics, the executor identity, WORK-055's typed environment fingerprint, the optional WORK-055 validated recovery attribution); success-only (anything else is a typed `trajectory-shape` rejection, never a guessed success); the repetition minimum (single lucky runs never become competence); the self-mining guard (the mining authority must differ from every executor); environment coherence (one shape = one environment); the honest reliability denominator; the CONSERVATIVE output (minimum quality, maximum cost/latency); the claim rides the foundation's `observed` basis with the trajectory-digest evidence pin; **the output stage is ALWAYS `candidate`**. Unit `mining.test.ts` (15 tests) incl. determinism/idempotence, trajectory identity re-derivation (tamper rejection), HONEST CONSUMPTION (fingerprint + attribution validators' own errors propagate); discrimination D1 (self-mining); integration: the real-chain mining over the governed execution's evidence |
| 4. Deterministic replacement candidates admitted only with full differential/property/replay equivalence evidence within declared bounds; candidates lacking evidence are inadmissible | PASS | `equivalence.ts` `admitDeterministicReplacement`: THE complete-suite requirement by type — each missing component (differential / property / replay) is a typed `equivalence-suite-incomplete` rejection (no evidence-less replacement can EVER be admitted); the within-declared-bounds self-check at admission (differential match rate ≥ its own declared required rate; property failures ZERO; replay deviations ZERO — else `equivalence-suite-failed`); the binding rides the tool-surface plane's OWN closed vocabulary through its own `isToolRepresentation` (members `competence`/`code` — anything else a typed `replacement-shape` rejection); the incumbent carries the foundation's own validated explicit-basis claim over its own ladder class; content-addressed `replacementId` with read-time re-validation; `evaluateEquivalenceSuite` — the pure suite evaluation under the governing policy floor (the effective threshold is the MAX of declared and policy). Unit `equivalence.test.ts` (13 tests); discrimination D3 incl. the mutated-suite-at-the-gate rejection |
| 5. Shadow/canary promotion follows the gated path with policy/budget bounds; promotion decisions are recorded evidence executed by existing authorities; agents cannot self-promote | PASS | `promotion.ts` `decidePromotion` + `decisions.ts`: THE gated path — the target stage is COMPUTED (rank + 1) from the record's current stage, never caller-supplied: a candidate-stage record presented with FULL canary evidence still only advances to shadow (discrimination D2); `advanceStage` rejects any non-+1 transition; the terminal stage rejects further promotion (`gate-order-violated`); every gate re-proves the full equivalence suite under the policy floor (a suite passing its own declared rate but below the governing floor → `evidence-suite-failed` reject); the shared economics admissibility through the model-economics plane's OWN `governingFacts`/`evaluateAdmissibility` (the typed reason codes ride verbatim: quality-below-hard-floor / quality-below-assurance / reliability-below-floor / budget-ceiling / latency-ceiling); NO SELF-PROMOTION (the requesting authority ∈ executors ∪ {miner} → `self-promotion` permanent reject); the shadow gate (observation bound, zero deviations — hold below the bound, reject on deviations); the canary gate (bounded exposure, required success rate, zero deviations); the verdict is typed evidence (promoted/hold/reject with closed codes); the WORK-049 ride records ONLY `promoted` verdicts (hold/reject → `null`: the typed outcome IS the evidence), the corpus the evidence-honest incumbent+replacement pair, the selected candidate the deterministic replacement, the provenance (record id, stage transition, replacement id, equivalence verdict, floor) in the bounded transformation-basis detail, appended by the caller through the EXISTING store. Unit `promotion.test.ts` (20) + `decisions.test.ts` (10); discrimination D1/D2/D3; integration: the full gated ladder over the real chain + the durable append + read-back validation + the idempotent re-append |
| 6. Rollback reverts a degraded promotion to the prior representation with typed evidence, bounded and recorded | PASS | `rollback.ts` `buildRollback`/`applyRollbackRecord` + `decisions.ts`: exactly ONE stage back (derived from the promoted record's stage, never caller-supplied); the closed reason↔evidence coherence (equivalence-degraded requires an equivalence entry, quality-degraded a quality entry, economics-degraded an economics entry, policy-revoked a policy entry — a rollback without its coherent typed evidence is a typed `rollback-shape` rejection, NEVER silent); the bounded evidence set ([1, 64]); the independent requesting authority (no self-rollback); the content-addressed `rollbackId` with read-time re-validation; the REVERTED record rides the rollback as DATA (the restore directive); the application is PURE and IDEMPOTENT (promoted → reverted; already-reverted → bounded no-op; foreign record → typed rejection — never a partial state); the WORK-049 rollback record (corpus promoted+reverted, selected the reverted one, the rollback provenance in the detail). Unit `rollback.test.ts` (12 tests); discrimination D4; integration: the durable rollback round-trip over the real chain |
| 7. Zero-competence operation is proven (the probabilistic path with an empty corpus) | PASS | `retrieval.ts`: the empty corpus is ALWAYS representable — `retrieveCompetence([], query, config)` returns the empty ranked set with `corpusSize: 0`, NO error, no phantom candidates (fail-SAFE, not fail-closed). Unit `retrieval.test.ts` (the zero-competence determinism proof — byte-identical empty re-runs); discrimination D5 (the empty corpus is a REPRODUCIBLE empty ranking); integration: the zero-competence retrieval over the real chain's scope before the corpus is populated |
| 8. The plane is fully covered by static, dynamic and discrimination/mutation evidence at exact revisions, with honest NOT RUN boundaries | PASS | This evidence document; all gates green at the final implementation head (below); **139 new tests** (102 unit across 7 suites + the shared `world.ts` fixtures; 26 discrimination; 9 architecture boundaries C1–C9; 2 real-PG integration); every weakened invariant discriminated; the one pre-existing (WORK-055 stale-pin) failure disclosed exactly; NOT RUN boundaries stated below |

## Required checkpoint contracts

- **LEARNING-NONAUTHORITY** — competence records and learning evidence never grant capabilities, authorize work, define budgets or alter verification: the plane exposes NO admission/authorization vocabulary (architecture C5's mechanical scan + discrimination D6a's scan + D6d's serialized-verdict scan); no authority surface (auth/policies/capabilities/budgets/secret-store) references the plane (C5's reverse-dependency scan + D6b); a WORK-049 decision is byte-identical with and without a competence corpus (D6c — the mechanical closed-input-set proof); agents cannot self-promote (the requesting authority must be independent of every executor and the miner — D1, the typed `self-promotion` permanent reject), cannot self-mine (the `mining-input-shape` guard) and cannot self-assert records (the structural `record-self-asserted` guard at construction AND read time — D1); authorization/policy/budget consults of competence records are impossible (the corpus never enters any authority's inputs).
- **PROMOTION-GATES** — the shadow → canary → deterministic path is the ONLY promotion route: the target stage is COMPUTED (rank + 1) never caller-supplied (D2: full canary evidence on a candidate record still computes `shadow`); `advanceStage` rejects jumps mechanically; the terminal stage rejects further promotion; each gate is bounded by explicit policy/budget inputs (the shadow observation bound, the canary exposure bound and required success rate, the equivalence policy floor) and re-proves the full equivalence suite (D3: the mutated-suite rejection at the gate); the shadow gate HOLDS below its bound (canary never begins early) and REJECTS on deviations; the canary gate enforces its exposure bound, success rate and zero deviations.
- **ROLLBACK-SAFETY** — rollback reverts to the PRIOR representation with bounded, typed, recorded evidence: exactly one stage back (derived); the reason↔evidence coherence (a rollback without its coherent typed evidence is a typed rejection — D4); no partial states (the foreign-record apply is a typed rejection; the double-apply is a bounded no-op — D4); no silent degradation (the evidence set is bounded, non-empty and typed; the requesting authority independent); the rollback is DATA + RULES through the existing seams (the WORK-049 record; the caller executes).
- **DEPENDENCY-DIRECTION** — the plane imports ONLY `../execution-ir/*`, `../tool-surface/*`, `../context-economics/*`, `../model-economics/*`, `../failure-recovery/*`, plane-local modules and node builtins (architecture C1's per-file import-prefix proof + the repository-wide dependency-rule scanner: zero platform-isolation/undeclared-package/provider-sdk violations, and the FULL-tree scan stays clean with the plane in place); no execution-compiler, substrate-economics, sandbox, recovery or db import; NO foundation or dependency-plane file references the plane (C9 — the reverse direction never exists), and the git ancestry proves the import-only surfaces are untouched (zero diffs on all seven foundation/sibling-plane trees, the drift-immune merge-base pin).
- **IDENTITY-IDEMPOTENCY** — identical (corpus, query, configuration) produce identical rankings and promotion verdicts: byte-identical retrieval results, promotion verdicts and rollback records on re-run (unit + discrimination D5 + integration's deterministic re-derivation with the identical `decisionId` and the bounded no-op re-append `replayed: true`); the record application is idempotent (the pure double-apply is byte-identical — D4); every identity (recordId, replacementId, rollbackId, trajectoryId) is content-addressed with read-time re-derivation (tamper rejection everywhere); the zero-competence empty ranking is reproducible.
- **EXECUTION-PROVENANCE** — the exact provenance from trajectory observation through mining to competence record to promotion verdict: the trajectory carries the digest of the raw event bytes and the identity of the governed execution AND the WORK-049 decision record that governed it; the mined record's `trajectoryDigest` is sha256 over the canonical trajectory-evidence forms; the promotion verdict carries the record identity, the computed stage transition and the replacement identity; the decision record's transformation-basis detail carries `record=<id>;from=<stage>;to=<stage>;advanced=<id>;replacement=<id>;equivalence=<verdict>;floor=<floor>` (and the rollback's `rollback=<id>;from;to;reason;evidence;requestedBy`) — replayable by deterministic audit (the integration chain re-derives the identical verdict and record over the real ledger); forged provenance is rejected BEFORE any record exists (the trajectory identity re-derivation, the record/replacement coherence rejections, the read-time re-validation everywhere).
- **VERIFICATION-SEPARATION** — equivalence evidence never redefines quality gates: the mined record carries the verification authority's OWN measurement (the observed quality + the verification binding — recorded, never re-judged); the quality floors the promotion gates run under come from the governing constraint set through the model-economics plane's own `governingFacts` (hard floors can raise, never lower — the foundation's rule, consumed); the equivalence suite is behavioral equivalence evidence (differential/property/replay within declared bounds) — a SEPARATE axis from the quality gates, both required (an admissible suite does NOT waive the quality floor, and a claim above the floor does NOT waive the suite); the plane exposes no quality-gate vocabulary of its own (architecture C8's closed vocabularies carry competence/promotion codes only).
- **IMPLEMENTATION-COMPLETENESS** — this evidence package maps every AC and checkpoint contract to exact-revision results; forbidden surfaces are untouched (zero `spec/` changes, zero foundation/dependency-plane changes, zero migrations, zero module/api/integration changes — mechanically verified in the ancestry section); no new store/state machine/authority (architecture C4: the sole `OptimizationDecisionStore` implementor remains the WORK-049 one; the migration count stays 29); every NOT RUN boundary is stated below.

## Test battery and implementation evidence

The battery at the final implementation head `823d0b1` (**all green, one consecutive run each**, except the single pre-existing disclosed failure):

- `bun run typecheck` — 0 errors.
- `bun run lint` — biome clean, exit 0 (1,289 files).
- `python3 scripts/governance-check.py` — `Governance OK: 56 Work Orders, 102 requirements, inFlight=[], frontier=['WORK-056']`.
- `bun run deploy:validate` — `problems: []`, migrations 29 (unchanged — the plane adds NO durable state).
- `bun run test:unit` — **219 files / 3212 tests passed** (3110 at base + 102 new: record 16, retrieval 16, mining 15, equivalence 13, promotion 20, rollback 12, decisions 10, plus the shared `world.ts` fixtures).
- `bun run test:integration` (real PostgreSQL 16.9, `ZECK_PG_TEST_URL` @ 127.0.0.1:55432) — **112 files / 1054 passed | 13 skipped** (1052 at base + 2 new; the honesty skips are the live-provider ones inherited from prior orders).
- `bun run test:architecture` — **102 files (100 passed | 1 failed | 1 skipped) / 1359 tests passed | 4 skipped + 1 PRE-EXISTING FAILURE** (1324+4skip+1fail at base + the new `e11-competence-economics-boundaries` gate (9 tests C1–C9) + the new `competence-economics.discrimination` suite (26 tests); the four skips are the live-infrastructure honesty skips unchanged from the base; the ONE failure is WORK-055's pre-existing C9 stale-pin — proven failing identically at the pristine dispatch base via a detached `git worktree` at `201756c`, mechanically isolated from this branch: the failing diff names ONLY `spec/` files this branch never touches; full disclosure in the baseline section).

New WORK-056 tests: **139** (102 unit across 7 suites + the shared `world.ts` fixtures; 26 discrimination D1–D6; 9 architecture boundaries C1–C9; 2 real-PG integration).

Retrieval ranking results over the representative corpora (the unit `retrieval.test.ts` corpus, exact expected successful-resolution costs): the world's mined record (quality 0.92, reliability 0.5, cost 400 µUSD) evaluates to **800 µUSD expected successful-resolution cost** (ceil(400/0.5)) and surfaces at rank 1 over its query; a cheaper variant (cost 100) outranks a costlier (900) regardless of corpus order; equal-cost records resolve on repetition evidence (observations 7 before 2); full ties resolve on recordId ascending (content, never input order); cross-tenant, capability-mismatched, tag-uncovered, environment-drifted and below-floor records are ranked out WITH their typed recorded verdicts and evaluations; tampered records never surface (`record-shape` verdicts); `maxResults: 1` bounds the result set while every verdict is still recorded; the empty corpus returns the empty ranked set (zero-competence, fail-safe).

Mining round-trip evidence (trajectory → candidate record): the world's two-trajectory corpus (quality 0.92/0.94, cost 400 both, latency 2000 both, 4 total executions) mines the byte-identical candidate record on every re-run — conservative evidence (expectedQuality 0.92 = the minimum; expectedCost 400 / latency 2000 = the maxima; expectedReliability 0.5 = 2/4 observed), the claim on the foundation's `observed` basis with the trajectory-digest evidence pin, the verification binding the OBSERVED one, the stage ALWAYS `candidate`, the executors riding the record (a second executor's trajectory joins the set honestly — 3/5 = 0.6); the single-trajectory corpus, the self-mining corpus, the mixed-environment corpus and the dishonest denominator are all typed rejections; the integration chain mines over the REAL governed execution's identity and plan-decision digest.

Equivalence-evidence suite results over the representative candidates: the full suite (differential 1000/1000 at declared rate 1; property 12 properties/5000 checks/0 failures; replay 2 trajectories/0 deviations) admits the replacement (content-addressed, read-time round-trip, tag-order-independent identity); each missing component is a typed `equivalence-suite-incomplete`; each failing component a typed `equivalence-suite-failed` (999/1000 below the declared rate 1; 1 property failure; 1 replay deviation); the mutated suite (declared 0.95, observed 0.96) is ADMITTED (within its own bounds) but REJECTED at every promotion gate under the 0.99 policy floor — the pure `evaluateEquivalenceSuite` computes the effective threshold as the max of declared and policy (0.99), observed 0.96, failingKinds `["differential"]`; the `code` binding representation is as valid as `competence` (both members of the tool-surface plane's own closed vocabulary — verified through its own `isToolRepresentation`).

Promotion gate traversal evidence (the gated ladder with policy/budget bounds): the full ladder over the same record — candidate → shadow (the full admitted replacement, no gate evidence required at the first gate), shadow → canary (100/100 shadow observations, 0 deviations), canary → deterministic (50/50 bounded canary exposures at the required 0.98 success rate, 0 deviations) — each verdict `promoted` with the computed target, the advanced record a NEW content-addressed value with the SAME evidence; the terminal deterministic stage rejects further promotion (`gate-order-violated`); full canary evidence presented to the candidate record still computes `shadow` (gate-skipping structurally unrepresentable); below-bound shadow evidence HOLDS (99/100) while deviations REJECT; unbegun canary exposure HOLDS while exceeded exposure/low success rate/deviations REJECT; the below-floor replacement claim (quality 0.75 < the hard floor 0.8) is rejected with the model-economics machinery's OWN code `quality-below-hard-floor` under the governing facts (qualityFloor 0.85 = max(assurance 0.85, hard 0.8), budget ceiling 1,000,000 µUSD); self-promotion by the executor AND by the miner are permanent rejects with byte-identical verdicts on re-run.

Rollback round-trip evidence: the shadow-stage promoted record rolls back to candidate (every promoted stage proven: shadow→candidate, canary→shadow, deterministic→canary — exactly one stage back, derived); the equivalence-degraded reason with its coherent evidence constructs and round-trips (deterministic identity, read-time re-validation); the incoherent pairs (each reason without its own evidence kind) are typed rejections; the empty evidence set and the >64-entry set are typed rejections; the candidate-stage record has nothing to roll back; the executor's and the miner's rollback requests are typed rejections; the pure apply: promoted → reverted, reverted → the byte-identical no-op, foreign → the typed rejection.

Zero-competence fail-safe evidence: the empty corpus retrieval returns the empty ranked set (no error, no phantom candidates, `corpusSize: 0`) — unit, discrimination (reproducible byte-identical empty ranking) and integration (the real chain's scope before population); the probabilistic path works with an empty corpus by construction.

Determinism and idempotence proof results: byte-identical retrieval results, promotion verdicts, rollback records and decision records on re-run (unit + discrimination D5 + integration's identical `decisionId` with the idempotent re-append); corpus input-order independence; content-addressed identities with read-time re-derivation everywhere (record, replacement, rollback, trajectory); the 8-way concurrent append convergence (below).

Real PostgreSQL evidence for durable semantics: the promotion and rollback decision records ride the EXISTING WORK-049 store (`SqlOptimizationDecisionStore`) — the full chain over the REAL executions ledger and the REAL planner (governed execution → durable planning decision → derived IR (identity-verified) → captured constraints → mining over the real chain's evidence → the gated ladder verdicts → the WORK-049 record → durable append → read-time total validation (`validateOptimizationDecision` clean over the real PG row) → the deterministic re-derivation (identical `decisionId`) → the idempotent re-append (`replayed: true`) → the 8-way concurrent convergence (exactly one insert + seven replays; exactly ONE durable row counted mechanically over the real database; the record survives the race intact). **NO SECOND DURABLE SURFACE**: the plane owns no table, no store and no migration — the row counts over the real database prove it (exactly the appended decision rows exist; the migration count stays 29).

## Discrimination / mutation battery summary (D1–D6, 26 tests)

- **D1 agent self-promotion is inadmissible**: the executor's and the miner's promotion requests → permanent `reject` with `self-promotion` (byte-identical on re-run); the record self-assertion guard rejects a miner-that-is-an-executor at construction AND read time; the self-mining corpus → typed rejection; the executor's rollback request → typed rejection.
- **D2 ungated promotion is impossible**: full canary evidence on a candidate record still computes `shadow`; on a shadow record still `canary`; the stage-advance helper rejects jumps mechanically; the terminal stage rejects further promotion; the shadow gate holds below its bound.
- **D3 replacement without full equivalence evidence is rejected**: each missing component → typed inadmissibility; a component failing within its declared bounds breaks admission; the gates re-evaluate under the governing policy floor (the mutated suite is detected at the gate).
- **D4 silent rollback is impossible**: the incoherent-evidence rollback → typed rejection; the foreign-record apply → typed rejection; the double-apply → bounded no-op; the verdicts and rollbacks carry no authority vocabulary (serialized scan).
- **D5 non-deterministic rankings are rejected**: equal-cost ties resolve on content (observations, then recordId); mutated input order never flips the ranking; byte-level re-runs identical; duplicate corpus identities → typed rejection; the empty corpus is a reproducible empty ranking.
- **D6 authority consults of competence records are impossible**: the plane exposes no admission/authorization vocabulary (mechanical scan over the REAL tree); no authority surface (auth/policies/capabilities/budgets/secret-store) references the plane (reverse-dependency scan); a WORK-049 decision is byte-identical with and without a competence corpus (the closed-input-set proof); retrieval results carry the foundation's candidate shape and zero permission vocabulary with every verdict code in the closed set; the trajectory corpus is never mutated (purity).

## Architecture boundary proofs summary (C1–C9, 9 tests)

- **C1** the plane imports ONLY `../execution-ir/*`, `../tool-surface/*`, `../context-economics/*`, `../model-economics/*`, `../failure-recovery/*`, `./` and `node:` builtins (per-file import-prefix proof; no compiler/substrate/sandbox/recovery/db/queue/observability import; the repository-wide rule scanner over the plane: zero platform-isolation/undeclared-package/provider-sdk violations; the FULL-tree scan stays clean).
- **C2** provider neutrality: zero vendor vocabulary (the closed VENDOR_WORDS scan, case-insensitive with boundaries).
- **C3** no module/integration/api file references the plane.
- **C4** no second state machine / durable surface: no execution-state vocabulary, no transition tables, no SQL, no `DatabasePort`/pg import; the migration count stays 29; the sole `OptimizationDecisionStore` implementor remains WORK-049's.
- **C5** evidence-only: no admission/authorization vocabulary on the plane surface; the authority surfaces (auth/policies/capabilities/budgets) never reference the plane.
- **C6** secret-free sources (no credential-shaped literals).
- **C7** purity: no ambient clock, no randomness, no module-level mutable state (`recordedAt`/`observedAt` are explicit inputs everywhere).
- **C8** the closed vocabularies (promotion stages, equivalence kinds, rollback reasons, retrieval/promotion inadmissibility codes, invariant codes — exact-list assertions) and the declared file set (exactly the nine modules).
- **C9** build-on, never-fork: the foundation and every dependency plane carry no reference to competence-economics; the git ancestry proof uses the DRIFT-IMMUNE dynamic merge-base pin (diff the import-only surfaces against `git merge-base HEAD main` computed at run time — the diff can only ever name THIS branch's own changes; the full branch diff is asserted to touch ONLY `src/platform/competence-economics/`, `tests/` and `docs/work-items/WORK-056*`).

## Honest-consumption discipline (the foreign-error contract)

The plane consumes the foundation's and the dependency planes' own validators and NEVER re-brands their rejections: a malformed environment fingerprint inside `buildCompetenceRecord`/`validateSuccessfulTrajectory`/`retrieveCompetence` propagates WORK-055's own `FailureRecoveryError` (`fingerprint-shape`); a malformed recovery attribution propagates WORK-055's own `FailureRecoveryError`; a malformed cost claim propagates the foundation's own `CostModelError` (`unattributed-cost` / `unreliable-representation`); a malformed quality-facts input propagates the model-economics plane's own `ModelEconomicsError`. Every one of these is unit-proven with `not.toBeInstanceOf(CompetenceEconomicsError)` — the plane adds no second opinion on top of the merged planes' judgments, and a consumer sees exactly which authority rejected the value.

## Changed-file inventory (21 files, +8,104 plus this document)

Declared Change Surfaces only — the competence-economics plane, the E1.1 competence tests/evidence, and this evidence document:

- **Platform plane** (`src/platform/competence-economics/`, new, 3,828 lines): `catalog.ts` (the closed vocabularies — representation classes riding the foundation's own ladder ranks, the four promotion stages, the equivalence-evidence kinds, the rollback reasons, the retrieval/promotion inadmissibility codes, the invariant codes, the bounds, the typed error), `record.ts` (typed content-addressed competence records with the structural self-assertion guard and the tenant-scoped corpus key), `retrieval.ts` (the pure deterministic ranked retrieval and the zero-competence operation), `mining.ts` (successful-trajectory mining — observation, never decision), `equivalence.ts` (the differential/property/replay suite and the deterministic replacement admission), `promotion.ts` (the gated shadow→canary→deterministic path), `rollback.ts` (the bounded typed rollback and the idempotent pure apply), `decisions.ts` (the WORK-049 decision-record ride), `index.ts` (the plane barrel).
- **Tests**: `tests/unit/platform/competence-economics/{record,retrieval,mining,equivalence,promotion,rollback,decisions}.test.ts` + `world.ts` (2,677 lines), `tests/discrimination/competence-economics.discrimination.test.ts`, `tests/architecture/e11-competence-economics-boundaries.test.ts`, `tests/integration/postgres/competence-economics-decisions.test.ts` (1,599 lines).
- **Documentation**: this evidence document.

ZERO shared-file touches. Zero changes to `src/platform/execution-ir/` and `src/platform/execution-compiler/` (import-only — build ON, never fork), zero changes to the five merged sibling planes (`tool-surface/`, `context-economics/`, `model-economics/`, `substrate-economics/`, `failure-recovery/`), zero changes to `src/platform/db/` (no migration; the count stays 29), zero `spec/development-state/*` changes, zero planner/execution-lifecycle/module/api/integration changes, no new dependency (the plane's imports are the declared foundation, the five dependency planes and node builtins only).

## CI status

The repository CI (`.github/workflows/governance.yml` on pull requests) runs exactly the locally executed battery: `governance-check.py`, `bun install --frozen-lockfile`, `typecheck`, `lint`, `test:architecture`, `test:unit`, `test:integration`; `deployment-validation.yml` runs `deploy:validate`. Every one of these was executed locally at the final implementation head with the results above (integration over the real local PostgreSQL 16.9). The Actions runs themselves execute on the PR — their status is reported on the PR, never self-claimed here. Note for the reviewer: the WORK-055 stale-pin failure (disclosed above) reproduces in CI exactly as it does locally at this base — it is pre-existing on pristine `main` at `201756c` and mechanically isolated from this branch (the failing diff names only `spec/` files this branch never touches).

## Known limitations (honest boundaries)

- **Live provider APIs, live trajectory-observation feeds and live evaluation harnesses: NOT RUN** — no provider credentials exist in this worker environment. The trajectories, environments, equivalence suites and gate evidence in the tests are representative typed fixtures over the closed vocabularies; the seams that will report real observations and run real differential/property/replay evaluations are the existing execution/decision seams, wired by the orders that own those seams. When live feeds arrive, they enter through the typed `SuccessfulTrajectory`, `EquivalenceEvidenceSuite`, `ShadowEvidence` and `CanaryEvidence` values — the promotion gates need no change.
- **No new durable semantics are claimed beyond the existing decision-record store** — promotion and rollback decision records ride the WORK-049 store through the foundation's own builder, appended by the caller at its seam (architecture invariant 8: zero new durable surface, zero migrations). The competence corpus itself is deliberately NOT durable here: a corpus store (the retrieval input) would be a NEW durable surface — out of scope for this order (a later order's decision at the execution-layer seam; the retrieval is a pure function over a caller-supplied corpus by design).
- **The corpus is caller-supplied**: `retrieveCompetence` is a pure function of (corpus, query, configuration) — the corpus assembly (which records a tenant's runtime actually holds) is the caller's seam; this plane never invents, ranks in, or persists corpus membership.
- **The mining corpus is caller-assembled from the existing seams**: the trajectories, the total-execution denominator and the mining authority identity are inputs the caller derives from the merged failure-recovery/decision-record evidence; this plane observes them, never re-derives them.
- **Shadow/canary observation harnesses are represented, not run**: the `ShadowEvidence`/`CanaryEvidence` values are typed, bounded, validated inputs; the actual shadow evaluation alongside the probabilistic path and the bounded canary exposure are executed by the existing execution layer at its seam (the gates evaluate the evidence, they never schedule exposure — purity, invariant 5).
- **The rollback revert restores the PRIOR STAGE of the competence record** (the auditable stage lineage), while the WORK-049 decision records the substitution; reverting the *execution-layer* binding (if a deterministic replacement was already dispatched) is the execution layer's own rollback at its seam — this plane's rollback is the recorded decision evidence and the restore directive as DATA.
- **The pre-existing WORK-055 C9 stale-pin failure is carried, not repaired** — repairing it means modifying WORK-055's merged evidence artifact (outside this order's Declared Change Surfaces); the reconciliation (and the drift-immune C9 pattern this order demonstrates) is the Tech Lead's decision.
- **PostgreSQL was built from source in this environment** (16.9 at 127.0.0.1:55432, rootless, trust auth) because no system package and no elevated privileges were available — the same pattern prior workers used; the integration evidence is real-PG but environment-local.
