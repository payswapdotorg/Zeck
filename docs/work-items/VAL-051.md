# VAL-051 — Production-Style Pilot

**Work order:** `spec/validation-work-orders/VAL-051.md`
**Authorization:** resident tech-lead A-lane takeover (takeover base `3d963eb`; session B's claim `a42cbf6` stalled 2h47m06s per the takeover commit's arbitration note — the e7e1ad8 first-PR arbitration governs either way)
**Assurance:** HIGH_ASSURANCE
**Dependencies:** VAL-050 (end-to-end customer journey), VAL-049 (audited recorded economics) — both complete
**Mode:** A-lane local delivery engine in phases (phase-1: app corpus + unit suites, `7dcf392`; phase-2: discrimination suite + integration crown over the REAL PG path, `0ba3091`), each verified and committed by the Lead; the battery below is the LEAD's own independent re-run at the branch head — the battery of record

## Phase-1 changed-file inventory

- `benchmarks/validation/apps/production-pilot/corpus.ts` — the declared pilot rows (13): per row the pilot shape (its observation window over the schedule of five shifts replaying RECORDED workloads — the VAL-050 journey stages' recorded input digests chained into VAL-049's audited economics, digests only, never payload bytes), the operating discipline (per-shift reservations and latency policy, the whole-window budget, the drift bands and quality floor, the declared incidents and declared observed drift) and the expected verdict (`PILOT-COMPLETED` / `PILOT-FAILED` with the failed criteria NAMED / `NOT-RUN` with the env var named). The declared incident's shift carries the RECORDED continuation economics (the failed attempt plus exactly one resume).
- `benchmarks/validation/apps/production-pilot/driver.ts` — the app-local pilot machinery (the work order's allowed surface ONLY): the deterministic sustained-observation derivation (pure derivation over the RECORDED basis — the drift report, the incident log, the append-only budget ledger and the end-of-window accounting) and the EIGHT mechanical observation oracles (`verifyProductionPilotIntegrity`: window honesty / schedule completeness / continuation exactly-once / drift-classification honesty / incident honesty / budget-policy envelope integrity / end-of-window accounting reconciliation / customer-boundary integrity — each FAILing with the violating shift, segment, residual or identity NAMED).
- `benchmarks/validation/apps/production-pilot/fixtures.ts` — the deterministic fixtures: the transport-level fake public API world (create/replay semantics, honest outcome shapes, the pilot observation package at the result boundary) and the discrimination knobs (the SEVEN controlled fakes: a cherry-picked window, a dropped shift, a double-driven resume, a normalized drift, a hidden incident, a hidden residual, a boundary leak). The offline fake world NEVER serves the live rail.
- `benchmarks/validation/apps/production-pilot/application.ts` — the app proper over the public SDK boundary (VAL-002 harness): submit under its OWN idempotency key → poll the window → result read (statuses, honest usage, pilot observation) → the eight oracles re-derived AT THE BOUNDARY → the app-side pilot contract (honest-vocabulary pins: expected verdict, failed criteria NAMED, terminal↔statuses agreement, usage honesty, live-gate honesty).
- `benchmarks/validation/apps/production-pilot/config.json` — the secret-free, repository-reproducible configuration (the pinned task-slice mirror; corpus input digest `5ec279f2`).
- `benchmarks/validation/apps/production-pilot/README.md` — the corpus table + the running instructions.
- `tests/unit/validation/val-051-apps.test.ts` — 18 tests: the corpus + configuration consistency (config mirror, unique row ids, full-schedule declarations, pinned probe criteria, the shift digests chained EXACTLY into the VAL-050 journey basis, references-only task bodies, honest live gating), the app over the honest fake world (incl. the incident row's exactly-once resume and the three honest drift classifications), the honest NOT-RUN live boundary (+ the fake world's live-rail refusal), the adversarial probe rows over the app boundary, and the customer-boundary discriminations.
- `tests/unit/validation/val-051-core.test.ts` — 29 tests: the eight oracles over the honest rows (incl. the end-of-window accounting with zero residual and the NAMED release of the headrow reservations), the discrimination battery (the SEVEN controlled fakes each FAILing named), the crafted discriminations beyond the probe vocabulary (undeclared resume, out-of-order schedule, duplicated shift record, over-classified drift, unattributed drift, undeclared and misattributed incidents, unauthorized spend, policy latency breach, non-append-only ledger, release mismatch, cherry-picked DECLARED window, empty observation), the honest verdict vocabulary, determinism and the digest-only evidence discipline.

## Honest boundaries

- The ONE live row (`live-pilot-window`) is env-gated on `OPENROUTER_API_KEY` and is honestly `NOT-RUN` without the credential (the env var NAMED in the verdict and the evidence; zero submissions). The offline fake world refuses the live rail outright — a live pilot window is never fabricated offline. The live lane (REAL live dispatches sustained across the declared live window through the REAL platform path, REAL recorder, measured usage and economics) is the Lead's live-review lane / phase-2.
- Offline rows are deterministic and credential-free; the shifts REPLAY RECORDED workloads (pure derivation over RECORDED results — never a re-measurement, never a re-priced recorded input; a re-measurement masquerading as pilot observation FAILs); usage is honestly none-reported offline.
- Evidence references are digest-only (payload-free FNV-1a; no payload bytes, no prices in task bodies).

## Per-corpus-row verdict summary (the honest per-row table — offline pure derivation)

| Row | Kind / verdict | NAMED criteria / mechanism | Recorded basis (VAL-049 carried via VAL-050, never re-measured) |
|---|---|---|---|
| `full-window-recorded-basis` | honest / **PILOT-COMPLETED** | all eight observation families PASS (zero residual; the 10% reservation headrow settles with the released remainder NAMED: 1,350 microUsd) | 13,500 microUsd / 400 ms / quality 0.98 |
| `window-incident-resume` | honest / **PILOT-COMPLETED** | continuation exactly-once holds (exactly two durable executions for the resumed shift: the failed attempt + one resume, its cost never hidden); the incident recorded and attributed | 23,100 microUsd / 640 ms |
| `window-drift-within-bounds` | honest / **PILOT-COMPLETED** | drift-classification honesty holds (+64 microUsd / +12 ms within the declared bands, honestly classified within-bounds, the delta reconciled) | 13,500 microUsd basis + 64 microUsd declared drift |
| `window-drift-declared` | honest / **PILOT-COMPLETED** | drift-classification honesty holds (+1,152 microUsd beyond the bands, honestly classified DRIFTING with the mechanism named: provider-route-inefficiency) | 13,500 microUsd basis + 1,152 microUsd declared drift |
| `window-regression-declared` | honest / **PILOT-COMPLETED** | drift-classification honesty holds (quality 0.72 below the 0.90 floor, REPORTED AS REGRESSING with the mechanism named: output-quality-degradation) | 13,500 microUsd / 400 ms |
| `probe-window-cherry-picking` | adversarial / **PILOT-FAILED** | `window-honesty` (omitted segment NAMED: `shift-5`) + `end-of-window-accounting` (window-basis residual NAMED: 400 microUsd) | shares the full-window shape's basis |
| `probe-dropped-shift` | adversarial / **PILOT-FAILED** | `schedule-completeness` (missing shift NAMED: `shift-4`) + `end-of-window-accounting` (window-basis residual NAMED: 9,600 microUsd) | shares the full-window shape's basis |
| `probe-double-driven-resume` | adversarial / **PILOT-FAILED** | `continuation-exactly-once` (duplicated resume NAMED — three durable executions for the one shift) + `end-of-window-accounting` (residual NAMED: 9,600 microUsd) + `budget-policy-envelope` (unauthorized spend NAMED: 28,800 observed over the 19,200 reservation; budget exceeded) | shares the incident shape's basis |
| `probe-drift-normalizing` | adversarial / **PILOT-FAILED** | `drift-classification-honesty` (normalized-drift NAMED: shift-4, cost delta 1,152 beyond band 480 reported within-bounds) + `end-of-window-accounting` (unexplained residual NAMED: 1,152 microUsd) | shares the declared-drift shape's basis |
| `probe-incident-hiding` | adversarial / **PILOT-FAILED** | `incident-honesty` (hidden incident NAMED: shift-4, a resumed execution with no incident record) | shares the incident shape's basis |
| `probe-residual-hiding` | adversarial / **PILOT-FAILED** | `end-of-window-accounting` (hidden residual NAMED to the exact microUsd: `cost-residual:-4800`) | shares the full-window shape's basis |
| `probe-boundary-leak` | adversarial / **PILOT-FAILED** | `customer-boundary` (the leaking shift NAMED: `shift-4` under `app-other-customer`) | shares the full-window shape's basis |
| `live-pilot-window` | live / **NOT-RUN** | env-gated on `OPENROUTER_API_KEY` (the env var NAMED in the verdict; zero submissions; the offline fake world refuses the live rail) — the live-review lane's row | live economics UNMEASURED (none reported) |

## Phase-2 must cover

- The discrimination battery over the REAL platform path (the seven controlled fakes driven through the real seams, not only the transport fake): `tests/discrimination/production-pilot-validation.discrimination.test.ts` per the 050 pattern.
- The env-gated REAL live pilot window (`OPENROUTER_API_KEY`) with live dispatches sustained across the declared live window through the REAL recorder with honest measured economics.
- Integration over the REAL PG instance (`:5433`) — the pilot's durable window, reservations and the append-only ledger over the real execution/idempotency authorities, the eight oracles re-derived AT THE BOUNDARY over the durable ledger with offline-derivation digest parity.
- The evidence document (AC7) recording REAL outcomes, timings, costs, NOT-RUN boundaries (each with its gating env var named) and the executed battery honestly — extending this document with the Lead's battery of record.

## Phase-1 executed battery (local, offline, deterministic)

- Unit: `bun test tests/unit/validation/val-051-apps.test.ts tests/unit/validation/val-051-core.test.ts` — **47/47 pass** (3,109 assertions, ≈ 0.3 s, zero credentials).
- Phase-0 regression spot-check: `bun test tests/unit/validation/val-050-apps.test.ts tests/unit/validation/val-050-core.test.ts` — **37/37 pass** (1,259 assertions; the sibling-app imports are additive and disjoint).
- Toolchain gates: `bunx biome check` clean on the seven new files; `bunx tsc --noEmit -p tsconfig.json` EXIT 0.
- No live costs are reported — none were measured (see the NOT-RUN boundary above).

## Phase-2 changed-file inventory (the local delivery engine's discrimination + integration crown)

- `tests/discrimination/production-pilot-validation.discrimination.test.ts` — the AC6 discrimination battery at the SEAM level (40 tests): the SEVEN controlled fakes (a cherry-picked window, a dropped shift, a double-driven resume, a normalized drift, a hidden incident, a hidden residual, a boundary leak) each forced onto an HONEST row and driven through the REAL application code (`runProductionPilotApp` over the public SDK/harness boundary — the injected transport seam, never the transport fake in isolation): each DETECTED at the boundary by the NAMED oracle with the mechanism NAMED in evidence (`omitted-segment:shift-5` / `missing-shift:shift-4` / `resume-violation:shift-4` + `observed executions=3` + `unauthorized-spend` + `budget-exceeded` / `normalized-drift:shift-4` (beyond band 480) / `hidden-incident:shift-4` / `cost-residual:-4800` / `boundary-leak:shift-4` under `app-other-customer`), each with its honest contrast pair (the undenatured row PASSing the same oracle) and each probe row's row-level catch (settles FAILED with exactly its pinned criteria). REPLAY DETERMINISM: bit-stable re-derivation (honest + every fake), identical verdicts over fresh fake worlds, and a controlled mutation between replays flips the verdict AND the window digest. The RUBBER-STAMP probe: a pass-everything oracle passes every fake (the confession), the REAL boundary re-derivation contradicts it on every one, and a platform claiming ALL-PASS statuses over a denatured pilot still FAILs the app's pilot contract (`app-failed-criteria-named`). The FAVORABLE-SUBSET probe: the honest-only subset fake-passes while omitting EXACTLY the seven probe rows (NAMED). The live-rail honesty pins (NOT RUN without `OPENROUTER_API_KEY` with the env var NAMED and zero submissions; the offline fake world REFUSES the live rail even with the credential set). Every offline AC the unit suites cover re-proven at the seam (all EIGHT oracles per honest row, the exactly-once continuation, the three honest drift classifications, the headroom release NAMED (1350 over 14850 reserved), the terminal/agreement/submission contract pins, the economics + digest stability, the mechanical verdict vocabulary).
- `tests/integration/validation/val-051-production-pilot.test.ts` — the integration crown over REAL PostgreSQL (3 tests, env-gated on `ZECK_PG_TEST_URL` with an honest skip that names the env var; the house harness's disposable-DB lifecycle — fresh database off the admin URL, shipped migrations applied, dropped on teardown; NO new migrations). REAL pieces on the critical path: REAL PostgreSQL 16.14 on `:5433`; the REAL Fastify public API over the REAL SQL authorities (`seedApiPgWorld`) on a real port; the REAL SDK client over REAL HTTP (`globalThis.fetch`) submitting every shift through the public create boundary under its OWN idempotency key (the resumed shift's failed attempt and resume are separate durable submissions); the REAL executions state machine driving every landed execution through the canonical transitions with the shift's recorded-basis DIGEST reference in the step event. The recorded shift economics, the declared drift, the declared incidents and the window discipline (reservations / latency policy / window budget) are committed READ-ONLY into REAL SQL through the platform's own idempotency-record arbitration (identical re-commit REPLAYS, different-content commit under a recorded key REFUSED, served back over FRESH SQL reads). The EIGHT oracles are re-derived AT THE BOUNDARY over the DURABLE LEDGER (the drive counts are REAL SQL counts of durable execution rows; the identities from `executions.executions.application_id`): (1) the honest durable windows — all EIGHT oracles ALL-PASS, digest parity with the offline pure derivation (the boundary observation matches `pilotObservationFor` digest-for-digest, execution ids aside; the boundary verdict equals the offline verdict), continuation exactly-once pinned in REAL SQL (exactly two durable executions for the resumed shift, statuses `[FAILED, COMPLETED]`), the idempotent re-issue REPLAYS under the same key (never a second durable execution), zero orphan events, the budget-policy envelope over REAL SQL (observed spend within the SQL-served window budget; the release reconciles); (2) the seven adversarial DURABLE shapes each DETECTED by the NAMED oracles with the mechanisms NAMED and the durable evidence pinned in REAL SQL (shift-5 never lands for the cherry-pick; shift-4 never lands for the drop; THREE durable executions `[FAILED, COMPLETED, COMPLETED]` for the double-driven resume; the boundary-leak execution row sits under the OTHER customer's application; the incident-hiding shape has the FAILED+COMPLETED executions durable with ZERO incident records in SQL) — each reproducing the offline controlled fake EXACTLY (digest parity); (3) the recorded cost basis is a FROZEN read-only input (tamper REFUSED per shift), the SQL-served basis equals the corpus declarations digest-for-digest, the end-of-window accounting arithmetic (recorded basis + declared drift = observed sum = reported total; the release reconciles; the append-only ledger holds; digest-only records) and the live rail's honest NOT-RUN pin (zero durable submissions for `live-pilot-window` over REAL SQL).
- `docs/work-items/VAL-051.md` — this phase-2 record (AC7).

## Phase-2 executed battery (2026-09-14 20:49 UTC, local A-lane engine)

- Discrimination (offline, deterministic, zero credentials): `bun test tests/discrimination/production-pilot-validation.discrimination.test.ts` — **40/40 pass** (296 assertions, ≈ 0.29 s).
- Integration over REAL PostgreSQL 16.14 (`:5433`, disposable per-run database): `ZECK_PG_TEST_URL='postgres://val@127.0.0.1:5433/postgres' bun test tests/integration/validation/val-051-production-pilot.test.ts` — **3/3 pass** (187 assertions; the honest durable windows ≈ 0.83 s, the seven durable discriminations ≈ 0.88 s, the frozen-basis/accounting/envelope check ≈ 0.02 s; ≈ 2.7 s total including the disposable-DB lifecycle).
- Honest skip path (no env var): `env -u ZECK_PG_TEST_URL bun test tests/integration/validation/val-051-production-pilot.test.ts` — **SKIPS honestly** (0 pass / 0 fail / 5 skip, the skip log NAMES `ZECK_PG_TEST_URL` — never a fake pass).
- Phase-1 regression: `bun test tests/unit/validation/val-051-apps.test.ts tests/unit/validation/val-051-core.test.ts` — **47/47 pass** (3,109 assertions, ≈ 0.31 s).
- Toolchain gates: `bunx biome check` clean on both new test files; `bunx tsc --noEmit -p tsconfig.json` EXIT 0.
- No live costs are reported — none were measured (see the NOT-RUN boundary below).

## Phase-2 NOT-RUN boundaries (each with its gating env var named)

- **The LIVE lane (`live-pilot-window`) is honestly NOT RUN — PENDING the Lead/live-review battery.** Gating env var: `OPENROUTER_API_KEY` (absent in this delivery environment; zero submissions landed offline AND zero durable submissions over the crown's REAL PG — pinned in REAL SQL). The offline fake world refuses the live rail outright even with the credential set; a live pilot window is never fabricated offline. The live lane — live dispatches sustained across the declared live window through the REAL platform path, the REAL recorder, MEASURED usage and economics — remains the Lead's/live-review lane (work-order AC3); this local phase-2 record deliberately does not claim it.
- The integration crown is env-gated on `ZECK_PG_TEST_URL` (driven over REAL PostgreSQL 16.14 at `postgres://val@127.0.0.1:5433/postgres` for the executed battery above; without the variable the suite SKIPS honestly with the env var named — verified).

## Takeover provenance (the honest lane record)

- Session B claimed VAL-051 at `a42cbf6` (17:22:55 UTC — 39 minutes inside the 45-minute alternation window from the `079d2ec` spec issuance; Lead session A had delivered VAL-050). The Lead's watches 74-w1..w6 then read the claim UNCHANGED with ZERO remote-visible artifacts (no work branch, no PR, no commit beyond the state-only claim) — the stall threshold breached at 20:10:01 UTC, **2h47m06s** elapsed (session B's second consecutive stall after 050's 2h31m52s).
- Takeover executed per the `e7e1ad8` first-PR arbitration: the first 051 PR proceeds either way — if session B resurfaces and its PR lands first, it wins the merge and this A-lane build closes as review intel (the 048 pattern at `f061d25`); no 051 PR from either lane had landed at takeover time.
- Takeover declaration `3d963eb` pushed to main (fast-forward accepted); this worktree (`/home/z/zeck-val051`) on branch `work/VAL-051-production-pilot`; phase-1 `7dcf392`; phase-2 `0ba3091` (all local until this document's push).

## The Lead's battery (the executed verification of record, at the branch head `0ba3091`)

First run, in order, in the worktree `/home/z/zeck-val051` (2026-09-14, ~20:52–21:01 UTC):

| Battery | Command | Exact result | Delta vs the post-050-merge baselines |
|---|---|---|---|
| Governance | `bun run governance:check` | **OK** — 60 Work Orders, 110 requirements, inFlight=[], frontier=[] — EXIT 0 | unchanged (no spec/state edits on this branch) |
| Lint | `bun run lint` (biome, full repo) | **0 errors**, 61 warnings, 8 infos, 1787 files checked — EXIT 0 | **EXACT match to the post-050 baseline (0 errors / 61 warnings / 8 infos)** — zero drift; the 9+2 new VAL-051 files contribute ZERO diagnostics |
| Typecheck | `bun run typecheck` (`tsc --noEmit`, full repo) | **EXIT 0** (zero errors) | unchanged discipline |
| Unit | `bun run test:unit` | **5498/5498 passed** (316 test files), 92.00 s — EXIT 0 | baseline 5451 **+ 47 = exact** (the two VAL-051 unit suites) |
| Architecture + discrimination | `bun run test:architecture` | **2191 passed + 4 skipped** (138 test files passed + 1 skipped), 52.13 s — EXIT 0 | baseline 2151+4 **+ 40 = exact** (the discrimination suite); the 4 skips are the pre-existing env-gated live rails |
| Integration | `ZECK_PG_TEST_URL=postgres://val@127.0.0.1:5433/postgres bun run test:integration` | **1211 passed + 20 skipped** (160 test files passed + 1 skipped), 360.02 s over the REAL PostgreSQL 16.14 — EXIT 0 | baseline 1208+20 **+ 3 = exact** (the integration crown); the 20 skips are the pre-existing honest live-rail boundaries |

**Flake handling: NONE NEEDED** — no first-run flake appeared in any battery (all six first-run green); the cold-boot-flake protocol (isolated re-run of the failing file + one clean full re-run) was not exercised this cycle.

**REAL PostgreSQL note:** the integration battery ran against the userland-provisioned PostgreSQL 16.14 on `127.0.0.1:5433` (`/tmp/pgtool/start-pg.sh` reported the instance already running — no revival needed). CI's own PostgreSQL runs the identical crown as the binding cross-check.

## The honest NOT-RUN live boundary (the Lead's battery of record)

The ONE live row (`live-pilot-window`) is **NOT RUN** in this battery of record: no operator-authorized `OPENROUTER_API_KEY` exists in this environment. The live pilot slice — REAL live dispatches sustained across the declared live window through the REAL platform path, recorded through the REAL recorder, MEASURED usage and economics — is **reserved for the live-review lane** (the VAL-032/040/042/043/046/048/049/050 pattern). Never a fake success: the live-rail driver was delivered in the review-fix commit `55a2a03` (caught by the session-B live reviews 46-d/47-a — the shipped crown never read the key, the env-gate was welded shut); the original merge shipped the gate + the honest NOT-RUN boundary only. The offline fake world refuses the live rail outright (pinned by the discrimination suite — even with the credential set, the offline world never fabricates a live pilot window), and the crown pins **zero durable submissions for the live row over REAL SQL** off-key (the review-fix live driver lands them on-key). All live-lane fields (measured input/output tokens, live latency, live cost, the driven live verdict) stay **PENDING the live-review lane's own run** — none are reported here because none were measured.

## Per-corpus-row verdict summary of record

The phase-1 table above (13 rows) IS the verdict summary of record: **5 honest rows PILOT-COMPLETED** (`full-window-recorded-basis`, `window-incident-resume`, `window-drift-within-bounds`, `window-drift-declared`, `window-regression-declared`), **7 adversarial probe rows PILOT-FAILED with their NAMED criteria** (window-cherry-picking, dropped-shift, double-driven-resume, drift-normalizing, incident-hiding, residual-hiding, boundary-leak — each pinned in `PROBE_FAILED_CRITERIA_OF`, shared by corpus/driver/tests), **1 live row NOT-RUN** (`live-pilot-window`, env-gated on `OPENROUTER_API_KEY` — the live-review lane's). Every offline verdict was re-proven at the Lead's battery through THREE independent derivations: the pure offline derivation (the unit suites), the seam-level re-derivation over the SDK/harness boundary (the discrimination suite), and the durable-ledger re-derivation over REAL SQL (the integration crown — digest parity with the offline derivation asserted).

---

## Live-review addendum — 2026-09-15 (post-close): the live pilot window driven on-key — PILOT-COMPLETED

The post-close live-review lane (R-1 actioned — the operator-authorized
`OPENROUTER_API_KEY` provisioned 2026-09-15 ~02:39 UTC) drove this work order's live
row for the first time anywhere, at the rebased head `4341726` + the lane's branch
(after the coexistence rebase onto PR #119's review-fix that had delivered the live
rail driver — the original merge's env-gate never read the key; see the VALIDATION-
REPORT post-close addendum's coexistence note). Execution 2026-09-15 04:06:15–04:06:33
UTC over the program's PostgreSQL (`127.0.0.1:5433`), suite 4/4 GREEN, first-run, no
retries:

- **The row:** `live-pilot-window` over the pinned live plan (`LIVE_PILOT_PLAN` —
  one REAL dispatch per scheduled shift of the five-shift window,
  `dispatchesPerShift: 1`) on the pinned rail `meta-llama/llama-3.3-70b-instruct`
  (OpenRouter chat-completions, max_tokens 32 pinned explicitly, temperature unset per
  the provider's documented default, every priced token at the pinned manifest
  revision rev-001; 1 s provider-side pacing between dispatches).
- **REAL dispatches: 5** (one per scheduled shift) — every dispatch returned content
  (`successful=5`); **measured usage 158 input + 10 output tokens, measured cost
  23 µ$** — rail-reported BYOK measurements priced at the pinned rev-001 list prices,
  never estimates, and the measured spend INSIDE the window's budget-policy envelope;
  the recorded shift economics committed and identically re-committed through the
  platform's own arbitration (committed=5, replayed=5, refused=0, driftRecords=0,
  incidentRecords=0).
- **The verdict — mechanically derived:** `PILOT-COMPLETED` with ZERO failed
  criteria; the EIGHT oracles re-derived at the boundary over the durable ledger
  (`schedule-completeness`, `continuation-exactly-once`, `customer-boundary`,
  `window-honesty`, `drift-classification-honesty`, `incident-honesty`,
  `budget-policy-envelope`, `end-of-window-accounting` — the accounting evidence names
  `cost-residual:0`); the live slice's own MEASURED usage riding the usage slot.
- **The durable surface:** five terminal COMPLETED shift executions under the
  pilot's own customer identity over REAL SQL (the public read boundary serves the
  same truth), the dispatches recorded through the REAL recorder — one
  digest-only `live-dispatch-usage` step event per shift (payload bytes never land).
- **Verbatim live line:** `[VAL-051]   LIVE live-pilot-window -> PILOT-COMPLETED
  live-pilot-window -> PILOT-COMPLETED dispatches=5 measuredUsage=158+10tokens
  measured=23µ$ successful=5`.

This matches exactly what the PR #119-corrected claim promised ("the live-rail driver
was delivered in the review-fix commit `55a2a03` … the original merge shipped the gate
+ the honest NOT-RUN boundary only … the review-fix live driver lands them on-key"):
with the driver in place and a credential present, the window sustained its REAL
dispatches across the declared live window through the REAL platform path — nothing
surprised. The off-key honest NOT-RUN boundary is preserved (re-verified at the same
head: the three val-050/051/052 suites green 12/12 without the key, every boundary
line env-var named, ZERO dispatches — never a fake success; the crown's zero-durable-
submissions pin holds off-key). The offline corpus of record above is untouched; the
row remains env-gated. Session B deferred this on-key run ("the session-B live-review
lane (to follow post-merge)") — this lane executed it under the operator's standing
live-review authorization.
