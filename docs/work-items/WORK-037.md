# WORK-037 Evidence — Build, agents, deployments and workloads experience

Work Order: `WORK-037` (spec/work-orders/WORK-037.md) · Assurance: **HIGH_ASSURANCE** · Requirement IDs: N/A (dashboard presentation of existing domain/API objects; frozen technical requirement ownership untouched)

Frozen base: `47ce631d181065cb29da91bee53dd01f7b457d45` (main — the Architect-corrected dispatch head: WORK-036 complete and merged as PR #64 (`2731522ec1f6a2704656380849985d712c1ee7b1`, implementation head `caf5fc897a6272e0334a095f6d0eb065ec93cd23`), finalized; the dispatch committed WORK-037 as the sole in-flight frontier item via issue #65; the dispatch-time development-state inconsistency — frontier-state moved WORK-037 to `inFlight` while program-state stayed `pending` — was reconciled ON MAIN by the Architect authority in commit `47ce631`, exactly one file, `spec/development-state/program-state.json`, WORK-037 `pending → in-flight`; no architectural or implementation change made by the Architect). Branch: `work/WORK-037-build-agents-deployments-workloads` · **Final head: this doc's commit** (the house two-phase binding — the exact SHA is recorded by the orchestrator in the PR body; the last code commit is `e86650e`) · Zero merge commits; the merge-base is `47ce631…` exactly.

## The re-review correction record (disclosed first, honestly)

The first submission of this PR (head `d0f500a813a60b60c5a8997241011cbaf51da1e9`, superseded) carried a worker-authored governance commit `4b6afa5` — one line in `spec/development-state/program-state.json` moving WORK-037 `pending → in-flight` — an attempt to reconcile the dispatch-time state inconsistency on the worker branch, per the historical WORK-015 in-flight precedent.

The Architect review (PR #66, required changes) rejected that commit on three independent grounds: (1) `spec/development-state/*` is outside WORK-037's declared change surfaces; (2) the active worker protocol reserves development-state mutation to the Architect/finalization authority; (3) the dispatch inconsistency was created in the governance state at dispatch time, and correcting that state is an Architect responsibility — a historical precedent does not supersede the current authority boundary.

The correction applied to this branch, exactly as the review prescribed:

- `4b6afa5` is **removed from the branch history entirely**. The rebase replay preserves the implementation verbatim: the replayed implementation commit `e86650e` is tree-identical to the reviewed `8b63212` (verified: `git diff 8b63212 e86650e` is empty), and the pre-amend replayed head was byte-identical to the reviewed first-submission head `d0f500a` (verified: an empty tree diff — the Architect's main reconciliation produces the identical `program-state.json` content, so the removal deletes the unauthorized history and nothing else).
- The implementation and this evidence doc are **rebased onto the Architect-corrected main** (`47ce631`), where the Architect authority performed the program-state reconciliation of the dispatch inconsistency.
- The branch now touches **exactly 10 files, all inside the declared surfaces** (the surface diff below); zero development-state files; zero files under `spec/`.
- The complete gate was re-run TWICE at the resulting exact final head (below); the full suite was additionally recorded green at the replayed implementation commit itself (the ratchet below); the driven-browser verification was re-run (below).

The dispatch inconsistency itself — the honest record of the first round — was: at the dispatched base `252b9fe`, `governance-check.py` exited 1 and TWO suite tests failed on the pristine copy (`governance-gate.discrimination`'s negative control and `fresh-clone-governance`), because frontier-state had moved WORK-037 to `inFlight` while program-state stayed `pending`. At the Architect-corrected base `47ce631` that condition is resolved: the baseline gate below is fully green at the untouched base, with the two previously failing governance tests now passing.

## Baseline gate (readiness checkpoint — the exact frozen base, BEFORE implementation)

governance: **OK at the untouched base** (41 Work Orders / 102 requirements, inFlight=['WORK-037'], frontier=[] — the Architect-reconciled state) · typecheck 0 errors · biome clean (964 files) · **full suite with real PG = 278 files / 3889 tests, exit 0** at exactly `47ce631` (the first round's two pristine-copy governance failures — the dispatch inconsistency — are resolved by the Architect's main reconciliation; every test green).

## Implementation history (the ratchet — every commit compiles and is suite-green at its own head)

1. `e86650e` — the Build-experience implementation + the full proof surface (the surface diff below; the replay of the reviewed `8b63212` onto the corrected base — the implementation tree is byte-identical). Suite-green at its own head: governance OK · typecheck 0 · full suite with real PG = 279 files / 3946 tests, exit 0 (recorded in this correction round).
2. This doc (the final head).

## What changed (the surface diff, every touched file vs `47ce631…`)

Exactly 10 files (all inside the declared surfaces — 5 `apps/dashboard`, 4 dashboard-local test files, the evidence doc; rg-verified — zero files under `src/`, `sdk/`, `cli/`, `scripts/`, `spec/` (including all development-state files), `migrations/`, `.github/`, or root configs):

- `apps/dashboard/projection.ts` — the WORK-037 pure derivations: `validateWorkloadForm` + `buildWorkloadRequest` + `WORKLOAD_FORM_KEYS` (the workload composer mapping onto the ONE closed `ExecutionRequest` vocabulary — budget → the integer micro-USD cost constraint, datasets → `inputArtifactRefs`, never a forbidden key); `deriveWorkloadFacts` (the AC8 typed event facts: `checkpoint-recorded` payloads' `checkpointSequence`/`lastEventPosition`, and the recovery vocabulary resume-recorded/resume-denied/interruption-requested/wake-up-applied — free-text never consulted, lease/heartbeat-shaped events excluded by construction); `declaredBudgetMicroUsd` (the recorded `Execution.constraints.maxCostMicroUsd` fact); `trainingStateRows` + `completionExplainerRows` (the AC7 four-state distinction: compute complete = the terminal-status fact; training complete = the honest non-distinction; evaluation passed = the verification facts; release approved = the explicit absence, never derived from anything); `agentGlanceFacts` (the nine AC3 dimensions, each a platform fact from the agent projection or an explicit absence); `deploymentGlanceFacts` (the six AC4 dimensions, all explicit absences) + `DEPLOYMENT_EXECUTION_DISTINCTION` (the key invariant, stated on every deployment surface); the friendly stage labels for the platform's own long-running event vocabulary (Checkpoint recorded / Interruption requested / Wake-up applied / Recovered / Resume denied — the lease/heartbeat mechanics stay internal).
- `apps/dashboard/components.ts` — `glanceGrid` (the fact/absence-marked cell grid), `trainingStateList` (the four-row distinction list), `longRunningWorkloadSection` (the AC8 section: progress pointer to the timeline, checkpoint recency, spend incl. the declared budget fact, recovery state with the resume-denial consequence, the AC7 distinction, and the explicit never-expose-mechanics note).
- `apps/dashboard/pages.ts` — the Build hub (four intent tiles with live/read-only/not-exposed honesty); the agent proposal flow (purpose-first form with the outcome-level descriptors; the six-section human-readable proposal — purpose, capabilities, integrations, guardrails, verification, expected cost, each the user's intent plus the honest platform fact; the honest no-public-authoring-route state with NO create action; the governed authoring vocabulary as an advanced disclosure); the agent detail rework (the at-a-glance grid, the executions cross-link section with the honest no-agent-attribution fact + the lookup, the versions/selection disclosure with the no-command note); the workload creation flow (outcome-first form → the proposal with budget/inputs/what-this-creates/the four-state completion preview → the full commitment card through the WORK-035 `confirmationCard` → `POST /build/workload` through the ONE governed create command, with the 422 validation and platform-rejection paths re-rendering the envelope + commitment); the run page's long-running workload section (rendered ONLY when the event stream carries long-running facts); the deployment surfaces (`/build/deployment` purpose-first proposal + the AC5 consequence vocabulary with no action buttons; `/deployments` inventory; `/deployments/:deploymentId` detail with the six-dimension glance grid and the identifier-namespace distinction); the deployment command match; the route table (+`GET /build/deployment`, +`GET /deployments`, +`GET /deployments/:deploymentId`, +`POST /build/workload`).
- `apps/dashboard/shell.ts` — the Deployments nav item points at the real `/deployments` route (was `/build#deployments`); the Workloads description updated to the live status.
- `apps/dashboard/tokens.ts` — the glance-grid/glance-cell/glance-kind, distinction-list, and workload-facts CSS (all token-consuming; no one-off hierarchy).
- `tests/unit/dashboard/build-journey.test.ts` (33 — NEW) — the workload form validation/mapping pins (incl. the forbidden-key impossibility), `deriveWorkloadFacts` (typed reads, both recovery kinds, lease/heartbeat exclusion, no-facts absence, chronological most-recent-wins), `declaredBudgetMicroUsd`, the AC7 rows (four distinct states, the release always-absent even for completed+passing, the training non-distinction, the evaluation pass counts, the no-results honesty), the agent/deployment glance grids (the nine/six dimensions, fact/absence markers, escaping at the boundary), the rendered section pins, and the distinction-statement pins.
- `tests/unit/dashboard/navigation.test.ts` — the new routes in the route map and the a11y-frame lists.
- `tests/integration/dashboard/journeys.test.ts` — the fake world gains the create-body recorder and the long-running fixtures (RUNNING with checkpoints+resume+cost+budget; COMPLETED with a checkpoint and PASS checks); the NEW journeys (s)–(x); the (k) a11y list extended to the new routes.
- `tests/discrimination/dashboard-journey.discrimination.test.ts` — D11 re-pinned (four governed POST routes; the runtime proof that BOTH creates converge on the ONE wire route `/executions`), D12 re-pinned (three governed call sites, the vocabulary still exactly the two commands), D12's read-journey list extended to the new GET surfaces; the NEW D13 (deployment/execution state confusion) and D14 (training/evaluation/release conflation).
- `docs/work-items/WORK-037.md` (this doc).

No migration claimed. No new dependency (the driven-browser harness lives OUTSIDE the repository at /home/z/w037-harness).

## Acceptance-criteria mapping (the Work Order's AC1–AC10)

- **AC1** Build presents purpose/outcome before technical configuration for executions, agents, workloads and deployments — the hub's four intent tiles all lead with the outcome question; every build form is purpose-first (the harness pins the purpose field first on both the agent and workload forms; the execution composer is unchanged from WORK-036); technical configuration exists only as disclosures (the agent authoring vocabulary, the route/provenance disclosures).
- **AC2** the agent proposal covering purpose, capabilities, integrations, guardrails, verification and expected cost before detailed configuration — the six-section envelope, each section the user's stated intent plus the honest platform fact (the harness pins all six sections present; the expected-cost section is the honest "no pre-creation estimate exists"); NO create action renders (no governed route exists — the harness pins zero POST forms).
- **AC3** agent detail at-a-glance — the nine-dimension grid (purpose/capabilities/tools+integrations/autonomy/approvals/quality/cost/version/current-deployment): three platform facts (description, validation state, version) and six explicit absences, each marked "Platform fact" or "Not exposed by the public API" (the harness pins 9 cells, 3 facts, 6 absences; the (w) journey pins the honest texts).
- **AC4** deployment detail communicates availability, version, health, channels/endpoints, activity and operational controls without topology — the six-dimension glance grid, every cell the explicit absence (no public deployment authority — nothing fabricated); the distinction statement and the identifier-namespace note render on every deployment surface (the (v) journey + the harness).
- **AC5** deployment actions route through governed APIs and communicate consequence before commitment — no governed deployment-command route exists on the public contract, so the dashboard renders NO action buttons for pause/rollback/version-change: the operational-controls cell states the consequence-preview-before-commitment vocabulary that will apply when the authority ships, and a direct POST to any deployment route is refused with ZERO wire mutations (the (v) journey's runtime proof + D11's runtime list + the harness's no-POST-form pins).
- **AC6** workload/training/batch flows use outcome-first creation and show budget/cost throughout lifecycle — the purpose-first form; the proposal states the declared budget as the enforced integer micro-USD constraint; the commitment's cost row carries the budget + the honest no-pre-run-estimate; the lifecycle shows cost on the run's header facts and the long-running view's spend row (settled cost + the declared budget constraint — live platform facts); the wire proof: the (s) journey + the harness record the create body carrying `constraints.maxCostMicroUsd` + `inputArtifactRefs`.
- **AC7** training state explicitly distinguishes compute complete, training complete, evaluation passed and release approved — `trainingStateRows` (live) and `completionExplainerRows` (proposal-side) render the four states as distinct rows: compute = the terminal-status fact; training = the honest "the public contract does not separately distinguish" non-distinction; evaluation = the verification pass counts; release = the explicit absence ("No release state exists on the public execution contract — this page never presents one"), never derived from completion or evaluation (D14's static + runtime pins; the (u) journeys incl. the COMPLETED-with-passing-checks fixture).
- **AC8** long-running workload views show progress, checkpoint recency, spend and recovery state without exposing lease/heartbeat mechanics — the section renders ONLY when the public event stream carries typed long-running facts; the facts are the platform's own (`checkpoint-recorded` sequences/positions, the recovery event vocabulary, the recorded cost, the declared budget); the explicit note "lease and heartbeat mechanics are platform-internal and are never shown here"; lease/worker/epoch fields are never read (the build-journey pins exclude lease/heartbeat-shaped events by construction).
- **AC9** agent/deployment/workload pages link naturally to their executions and evidence — the agent detail's Runs-and-evidence section (the honest no-agent-attribution fact + the lookup + the runs link); the deployment pages' governed-work sections (the runs link + the lookup); the workload's lifecycle IS the run page with its Result|Evidence|Activity tabs (the (w)/(v)/(t) journeys + the harness).
- **AC10** responsive and accessibility behavior preserves the hierarchy — every new surface composes the W035/W036 shell (pageHead, landmarks, the skip link, one h1 — the (k) frame checks extended to the new routes); the harness verifies mobile 390 (the commitment block with a ≥44px Start target, the long-running view, the glance grid reflow) and the keyboard discipline (the skip link first; Start reachable and focused).

## The required discriminations (Implementation Requirement 6 — D11–D14, in tests/discrimination/dashboard-journey.discrimination.test.ts)

- **D11 unsafe command paths** (re-pinned): STATIC — exactly four governed POST routes (the execution create, the workload create, the two cancel aliases to one handler); RUNTIME — direct POSTs to non-command routes (including every deployment route and the agent/build reads) are refused 404/405 with ZERO wire mutations; the governed commands ARE reachable and BOTH creates converge on the ONE wire route (`POST /executions` — the workload create is presentation over the one execution authority, never a second mutation authority).
- **D12 accidental customer-domain mutation** (re-pinned): RUNTIME — driving every read journey (all new surfaces + both review GETs) issues ZERO POST wire calls, every GET carries the bound scope; STATIC — three governed call sites, the VOCABULARY exactly `createExecution` + `cancelExecution` (a `dispatchExternalSideEffect`-style mutant fails the vocabulary pins).
- **D13 deployment/execution state confusion** (NEW): STATIC — the three deployment page functions never call `statusBadge` and never carry the execution-status string vocabulary (the mutant importing it is flagged by the same scanner); the distinction statement is present; RUNTIME — the deployment surfaces render the distinction and NO badge/status vocabulary; the detail page bridges the two identifier namespaces through the explicit execution lookup; the run page renders the badge vocabulary and NO deployment-availability vocabulary.
- **D14 training/evaluation/release conflation** (NEW): STATIC (through the pure function) — the four states are distinct and ordered; the release row is ALWAYS the explicit absence even for a COMPLETED execution with every check passing (the conflation mutant returning a fact there fails); the training row is the honest non-distinction (not a copy of the compute row); RUNTIME — the run page never claims release approval; the workload proposal renders the four distinct states before commitment.

## The driven-browser verification record (real Chromium 1.57 over the real createDashboard server + the scope-ENFORCING fake world)

The harness (OUTSIDE the repository — /home/z/w037-harness, playwright 1.57) boots the real `createDashboard` against a fake API world implementing the REAL application-scope wire rule, then drives the canonical WORK-037 journeys: **28/28 checks passed, ZERO console errors and ZERO page errors across the whole session**: the Build hub (four intent choices on one surface; the workload-live and deployment-not-exposed honesty lines); the agent proposal (purpose first, all six AC2 sections, NO create action, the honest cost absence); the agent detail (nine at-a-glance cells; 3 fact markers + 6 absence markers; the honest execution cross-link); the workload review (the budget fact, the four-state completion preview, the full commitment block with all consequence rows, Start as the block's own confirm); the keyboard discipline (the skip link the first tab stop, Start reachable and focused); the workload journey (Start POSTs the governed create → 303 → the run page; the wire request carried `maxCostMicroUsd: 50250000` and the two dataset refs); the long-running workload view (checkpoint 2 of 2 at position 9600, the settled cost $1.81 + the declared budget $50.00, the recovered state, the never-expose-mechanics note, the four-state distinction with the release absence); the ordinary execution (no workload section without long-running facts); the deployment surfaces (the distinction with zero badge vocabulary; the six-dimension grid; the namespace distinction; the AC5 consequence vocabulary; NO POST forms anywhere); mobile 390 (the commitment block with a 44px Start target, the long-running view + distinction, the glance grid reflow); and the wire discipline (every scoped read carried the bound application scope). (Re-run at the corrected final head in the correction round — the identical implementation tree, the identical result.)

## The complete gate (run at the final head, TWICE consecutively green)

`python3 scripts/governance-check.py` (OK: 41 WOs, 102 reqs, inFlight=['WORK-037'], frontier=[]) · `bun run typecheck` (0 errors) · `bun run lint` (biome clean, 965 files, zero diagnostics) · `ZECK_PG_TEST_URL=postgres://postgres@127.0.0.1:55432/postgres bun run test` = **279 files / 3946 tests, exit 0 — run TWICE consecutively green at the exact final head** (the 278/3889 baseline fully green at the Architect-corrected base + 1 file / +57 tests: the build-journey unit suite 33, the (s)–(x) journeys 57 → 76 (+19) with the long-running fixtures, the journey discriminations 10 → 15 (+5: D13 ×2, D14 ×3) with the D11/D12 re-pins and the extended lists).

## Test inventory (the exact delta vs the base)

- NEW unit: `build-journey.test.ts` (33 — the workload form/mapping 8, `deriveWorkloadFacts` 6, `declaredBudgetMicroUsd` 2, the AC7 rows 5 + the explainer 1, the agent glance 3, the deployment glance 1, the rendered-surface pins 5, the distinction statement 1, the long-running section 4... exact counts per describe block in the file)
- Extended integration: journeys 57 → 76 (the (s) ×4, (t) ×3, (u) ×3, (v) ×4, (w) ×3, (x) ×2) + the create-body recorder + the long-running fixtures + the (k) a11y list extension
- Extended discrimination: dashboard-journey 10 → 15 (D11 ×3 re-pinned, D12 ×2 re-pinned with the extended list, D13 ×2 NEW, D14 ×3 NEW)
- Updated units: navigation (+ the new routes in the route map + the a11y list)
- Total dashboard-local: 13 unit files / 230 unit tests + 76 journeys + 15 journey discriminations

## Required checkpoint contracts

- **SELF-HOSTING-BOUNDARY**: the dashboard still imports ONLY `../../sdk` + node builtins; the client script performs ZERO network calls (unchanged, D5 of the foundation suite); no new dependency; every fact remains a live SDK read; the transport scope remains the SDK's bound application scope (D10 re-pins it for the new surfaces — the workload create's application id flows ONLY into the create request body, exactly like the execution composer; the (x) journey proves the created workload's reads carry the bound scope and a foreign query scope cannot widen it).
- **EXECUTION-PROVENANCE**: no new platform facts — the workload facts derive ONLY from platform-typed event payload fields (`checkpointSequence`/`lastEventPosition` on `checkpoint-recorded`; the platform's own recovery event vocabulary; free-text never consulted; lease/worker/epoch never read); the budget facts are the declared request constraint (the wire's own `constraints.maxCostMicroUsd`) and the recorded settled cost; the training-state rows derive ONLY from the execution status and the verification results; the release state is NEVER presented (no public fact exists for it — stated as the explicit absence); the agent glance facts derive ONLY from the public agent projection; the deployment glance facts are ALL explicit absences (no public deployment authority — zero invented health/availability/version facts); the distinction statement is the Work Order's own invariant vocabulary; money renders from integer micro-USD strings (BigInt only); the workload create maps onto the ONE closed create vocabulary (the forbidden-key impossibility is pinned).

## Required verification mapping (the Work Order's list)

| Required | Evidence |
|---|---|
| governance checker | OK at the untouched Architect-corrected base `47ce631` and at the head — the dispatch inconsistency is reconciled ON MAIN by the Architect authority; the worker branch touches zero development-state files |
| typecheck | 0 errors at the base and at the head |
| lint (biome) | clean (964 files at the base / 965 at the head) |
| dashboard/unit/integration tests | 230 unit + 76 journeys + 15 journey discriminations |
| build agent journey | the (w) journey + the harness's agent-proposal/at-a-glance records (API-backed live reads) |
| deployment state discrimination tests | D13 + the (v) journeys + the harness's deployment records |
| training/workload state tests | D14 + the (u)/(t) journeys + the build-journey AC7 suite + the harness |
| authority/tenant isolation tests | D10/D11/D12 (re-pinned) + the (x) tenancy journey (the bound scope on every scoped read; the foreign-scope query cannot widen it; the workload create's application id flows only into the create body) |
| responsive browser verification | the harness record (desktop / mobile 390, incl. the workload commitment, the long-running view and the glance grid) |
| keyboard/accessibility verification | the harness record (skip link first, Start reachable and focused) + the (k) frame checks extended to the new routes |
| secret-exposure discrimination | the html-escape suite + D5 of the foundation suite + the build-journey escaping pin (the hostile description through the glance grid) — secret-shaped values never render (the integrations BYOK note on the agent proposal reinforces the no-credential-input rule) |
| full suite twice consecutively at exact final head | the complete gate section above |

## Limitations (honest)

1. The deployment surfaces are honest unavailable states: no public deployment authority exists, so no availability/health/version/channel facts can render — the Work Order's Implementation Requirement 4 (operational statistics only when backed by API facts) makes this the required behavior, not a shortfall; when the authority ships, its facts feed the same glance grid and the same distinction statement.
2. The workload/training authorities' own state machines (the training workload statuses, the release gates) are not public; the workload create therefore goes through the ONE governed execution create (the honest presentation of governed work), and the four training/evaluation/release states are presented as the public facts plus the explicit absences — never a fabricated workload status.
3. The long-running view renders only when the public event stream carries long-running facts (checkpoints or recovery events); an execution without them correctly shows no workload section. The checkpoint payloads mirror the real long-running service's public payload shape (typed fields only); if the platform later enriches those payloads, the same typed-read discipline applies.
4. The workload create adds a second dashboard POST route to the same governed wire command; the D11/D12 pins were re-pinned accordingly (four POST routes; three governed call sites; the vocabulary still exactly the two commands) — the workload surface is presentation over the one execution authority, never a second authority.
5. The agent proposal cannot be committed (no public agent-authoring route) — the honest no-create-action state, with the governed authoring vocabulary disclosed; the agent detail's version-change is likewise a recorded fact with no command surface (the no-selection-command note).
6. The per-agent execution cross-link is the honest no-attribution note (the public execution contract forbids agent selection, so no per-agent execution listing can exist on the public wire) plus the lookup and the runs link — "natural linking" without fabricated linkage.
7. The driven-browser harness is a session artifact OUTSIDE the repository (the repository stays zero-dependency); its 28-check record is the evidence; the repository's own automated proof is the 321-test dashboard-local suite (230 unit + 76 journeys) plus the 15 journey discriminations.
8. The first submission's unauthorized development-state commit is fully removed from this branch's history (the correction record above); the reconciliation it attempted now lives on main, authored by the Architect authority (`47ce631`). The first-submission head `d0f500a` remains visible in the PR's review history for the record.
