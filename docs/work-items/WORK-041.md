# WORK-041 Evidence — UX integration hardening, usability and release gate

Work Order: `WORK-041` (spec/work-orders/WORK-041.md) · Assurance: **HIGH_ASSURANCE** · Requirement IDs: N/A (the final UX v2 integration/release gate over the accepted presentation architecture; the frozen technical requirement ownership is untouched)

Frozen base: `017e44f41eab9ce7a458843d39a38c895ba79800` (main — the WORK-041 dispatch head: WORK-040 complete and merged as PR #72 at `64bc1b11…`, the governance state at the base OK with 41 Work Orders / 102 requirements / inFlight=['WORK-041'] / frontier=[], exactly as dispatched via issue #73). Branch: `work/WORK-041-ux-integration-hardening-release-gate` · **Final head: this doc's commit** (the house two-phase binding — the exact SHA is recorded by the orchestrator in the PR body; the last code commit, `e89825d`, precedes it) · Zero merge commits; the merge-base is `017e44f…` exactly.

**Dispatch-record note (report-only, never worker-fixed — the W038/W039 lesson)**: the Work Order file's Dispatch Record line says "Binding exact base: `bcc46ee…`" — the Architect's dispatch pin that the final dispatch commit (`017e44f`, one line of that same file) itself re-pinned from `76d1ea5`. Issue #73, the worker branch's actual position and the main head all establish the effective base as `017e44f` itself (the branch was verified 0 commits ahead / 0 behind `017e44f` at start; the merge-base is `017e44f` exactly). The line was not touched by the worker.

## What this order IS

The product-level integration gate: the complete Zeck UX v2 realization (WORK-035 through WORK-040) audited as ONE product against the v2 doctrine, hardened where the audit found integration-level defects, and proven as a release — every primary journey driven end-to-end in a real browser on desktop/tablet/mobile with keyboard, screen-reader-structure, reduced-motion, secret-safety and performance evidence, plus the full gate twice consecutively at the exact final head.

## The audit (the full-surface review against UX-EXPERIENCE-ARCHITECTURE-V2 and UX-SCREEN-SPEC-V2)

Read in full: the Work Order, the screen spec (448 lines), the experience architecture v2 (705 lines), the six prior evidence docs (WORK-035..040) and the complete `apps/dashboard` surface — `states.ts`, `disclosure.ts`, `modes.ts`, `attention.ts`, `shell.ts`, `http.ts`, `client.ts`, `components.ts`, `trust.ts`, `controls.ts`, `inspection.ts`, `tokens.ts` in full; `pages.ts` (3,062 lines) in full; `projection.ts`'s complete export surface plus targeted reads; the SDK contract; the journeys' fixture vocabulary and the (k) frame list. The audit's finding: the six increments compose as one coherent product — one shell, one h1 site, one states vocabulary, one trust vocabulary, one control vocabulary, one inspection vocabulary, one mode model, one consequence primitive, one command surface — with FIVE integration-level defects, every one narrow and inside the declared surfaces:

1. **Context restoration inconsistency** — the six router-level error surfaces in `index.ts` (the 401/403→403 branch, the 404, the 502 upstream, the transport failure, the route-404, the 413) hardcoded `returnTo: "/"`, and `notFoundExecutionView` hardcoded `/runs` — the only surfaces in the product that did not follow the WORK-035 foundation rule ("Current path for the no-script appearance/mode fallback redirect"). A mode or appearance change there bounced the user to Home, losing their place.
2. **States-vocabulary inconsistency** — `commandPage`'s no-match state hand-rolled `.state.state-empty` markup instead of composing the `emptyState` primitive — the only route bypassing the one states vocabulary and its single escape boundary.
3. **Responsive/a11y gap** — the ≤640px touch-target rule omitted every interactive element INSIDE the dialog family: the command dialog's input and Search submit, the sheet's Close and action buttons (modal surfaces are primary interactive surfaces on mobile too).
4. **Performance (linear read fan-in)** — every recents-driven page performed strictly SEQUENTIAL per-run SDK reads (`readRecentExecutions` plus the per-page secondary loops: `listEvents`/`getResult` per recents run, the artifact usage reads) — up to 8×N serial round trips on the primary journeys as the recents list fills.
5. **A11y frame-sweep coverage** — the (k) journey list covered 38 of the 43 user-reachable GET routes; `/attention`, `/trust/evidence`, `/trust/lineage`, the artifact detail view and the competence detail view were absent.

## The fixes (narrow, local — the Work Order's "prefer local fixes over new abstractions")

- **`apps/dashboard/index.ts`** — the six router error surfaces pass `ctx.path` as `returnTo` (the foundation rule; a preference change on an error surface returns to the SAME view, re-rendered in the new presentation — the recovery language on the error body is untouched).
- **`apps/dashboard/pages.ts`** — `notFoundExecutionView` passes `ctx.path` (the lookup form on that view remains the recovery path); `commandPage`'s no-match state composes `emptyState` (the one vocabulary, the one escape boundary — a hostile query passes through `esc` inside the primitive); and the per-run read loops fan out through `Promise.all` — `readRecentExecutions` (used by twelve surfaces), the policies/audit/deployments event reads, the evidence/spend/connections result reads, the lineage event+result reads, the artifacts result reads and the artifact usage reads. **The fan-out is semantics-preserving by construction**: the read SET per page is identical (exactly one governed read per recents id, results kept in the recents order), every 404 catch keeps its exact 404-only absence semantics, and every non-404 error still propagates fail-closed to the router's error surfaces (the (av) fail-closed regression, the (l) scope journeys and D12's zero-POST runtime proof all stayed green through the change). Only wall-clock time changes.
- **`apps/dashboard/tokens.ts`** — the ≤640px touch-target rule gains `dialog button, dialog input, dialog select` (the dialog family obeys the same `--touch-target` minimum).

## Baseline gate (readiness checkpoint — the exact frozen base, BEFORE implementation)

governance OK (41 WOs, inFlight=['WORK-041'], frontier=[]) · typecheck 0 · biome clean (973 files) · **full suite with real PG = 284 files / 4173 tests, exit 0** (159.26s) at exactly `017e44f` — the WORK-040-era baseline reproduced before any change.

## What changed (the surface diff, every touched file)

Exactly 6 files — 3 under `apps/dashboard/` + 2 dashboard-local test files + this evidence document; the count cross-checked against `git diff --name-only 017e44f…HEAD` (the GitHub PR file list is checked again at submission — the W038 count lesson); rg-verified — zero files under `src/`, `sdk/`, `cli/`, `scripts/`, `spec/` (incl. development-state — untouched), `migrations/`, `.github/`, or root configs:

- `apps/dashboard/index.ts` (the six error surfaces' `returnTo`)
- `apps/dashboard/pages.ts` (the not-found `returnTo`; the command empty-state primitive; the read fan-out — 10 loops, one shared helper)
- `apps/dashboard/tokens.ts` (the dialog touch-target rule)
- `tests/integration/dashboard/journeys.test.ts` (the (k) frame sweep extended to every route; the NEW (aw) context-restoration journeys ×4; the NEW (ax) command empty-state journeys ×2 — 148 → 154)
- `tests/unit/dashboard/navigation.test.ts` (the responsive pin covers the dialog touch-target rule)
- `docs/work-items/WORK-041.md` (this doc)

NO new routes, NO new POST routes, NO new module, NO new dependency (the driven-browser harness lives OUTSIDE the repository at /home/z/w041-harness; the repository stays zero-dependency), NO migration, NO spec/development-state file.

## Acceptance-criteria mapping (the Work Order's AC1–AC12)

- **AC1** (one coherent information architecture from Home through Work, Build, Library, Trust, Control and Improve) — the audit's cross-route review: every route composes the ONE shell (`appShell`), the ONE `pageHead` (single-h1, breadcrumb-from-the-IA), the one nav model (§5 tree, mode-visibility only); the driven-browser frame check (one h1, the four landmarks, the skip link first, lang, title) passed on EVERY user-reachable GET route including the error surfaces (the release-gate sweep below); the primary journeys traverse the product as one surface (Home → create → run → evidence → artifact → back, never through an index).
- **AC2** (all primary journeys from UX-SCREEN-SPEC-V2 executable and internally consistent) — every spec journey driven end-to-end in the real browser: first successful execution (§3: composer → envelope → seven-row commitment → Run → the run page), failure recovery (§4), human approval (§5, through the governed cancel), agent build (§6), deployment inspection (§7), batch/training view (§8), artifact/evidence investigation (§9, the contextual traversal), competence discovery (§10), improvement review (§11), computer-use envelope (§12), long-running work (§13), spend (§14), policy interpretation (§15, the blocked vocabulary) — 421/421 checks.
- **AC3** (no route introduces a second execution semantics, policy model, financial truth, verification model or tenant authority) — unchanged by construction: the diff touches presentation only; the (l) scope journeys, D10–D12 and the W035–W040 discrimination suites (D1–D26) all green through the change; the fan-out adds zero reads the sequential form did not issue.
- **AC4** (global search/command, contextual object navigation and back/forward behavior preserve intent and context) — the command surface driven end-to-end (Ctrl+K open, input focus, live filter, Escape + focus restore ×2 paths, Enter dispatch through GET /command, the live agent match); the run tabs and the artifact → execution → evidence traversal preserve the execution id in every URL; **the W041 fix: the preference fallback's returnTo is the current view on EVERY surface including the six router error surfaces and the execution-not-found view** — pinned by the (aw) journeys ×4 and the browser round-trip (a mode change on the 404 page lands back on the same view in the new mode).
- **AC5** (desktop/tablet/mobile preserve the task hierarchy; no unsafe compressed expert controls) — the three-viewport matrix: desktop 1280 (persistent sidebar, sticky nav), tablet 768 (single-column shell, non-sticky nav, the full-height right-docked sheet, Ctrl+K), mobile 390 (single column, the composer, the attention indicator, the modality tables, the bottom-docked sheet at 90dvh, and the ≥44px touch targets — **including the dialog family, the W041 fix**, measured on the command dialog's submit and the sheet's close button); the advanced disclosures stay collapsed on mobile; the expert inspection tab renders with the same hierarchy.
- **AC6** (keyboard-only and screen-reader journeys cover Home, execution, approval/review, Build and primary object detail) — the skip link is the first tab stop with a visible outline; the form-Enter lookup journey opens an execution; the sheet open/close cycle returns focus to its opener; the command dialog owns and restores focus on both open paths; the screen-reader DOM evidence: aria-named nav/breadcrumb/tabs/trust-strip/attention landmarks, `aria-current`, the `aria-live` form-status region on the refused create, visually-hidden labels, the non-color status symbols, the accessible timeline list.
- **AC7** (reduced-motion, scalable text, non-color status and focus restoration) — the reduced-motion emulation measured (the global gate zeroes transitions: `transition-duration` resolves to 0.00001s); the rem-based heading scale measured (h1 = 1.5× the root size); every status badge pairs a symbol with its text; the dialog/sheet focus restoration measured on both primitives.
- **AC8** (visual hierarchy consistently outcome → explanation → control → internals) — the audit of every route's composition (outcome-first pageHeads, the whyPanel after the result, the deep internals inside collapsed `advancedDisclosure`s — the browser verified the candidates/substrate/checkpoint disclosures collapsed by default and reachable); the calm token system (no gradients, one stylesheet) is unchanged.
- **AC9** (error, empty, loading and permission states use a consistent recovery language; never leak internals by default) — the four error surfaces driven in the browser (route-404, execution-not-found 404, the 403 permission surface, the 502 upstream surface) — all compose `pageHead` + the one states vocabulary, no stack traces, no raw provider detail; the command no-match state now composes the same `emptyState` primitive (the W041 fix); the empty states across Home/runs/evidence/lineage/attention render the value-plus-next-action language; the loading primitive remains the shared contract (all reads complete server-side before render — the disclosed architecture; see Limitations).
- **AC10** (browser verification demonstrates no material console/page errors on the primary journeys, with desktop/tablet/mobile evidence) — **421/421 checks, ZERO real console errors, ZERO page errors across the whole session** (the six Chromium resource-load lines are the browser's standard messages for the INTENTIONAL error-surface navigations — the refused-create 422, the route-404, the execution-not-found 404, the 403 and 502 fail-closed reads — plus the favicon 404; each is classified and status-accounted in the machine-readable report, and none is a page defect); nine screenshots (desktop home/commitment/created-run/dark-home, tablet home, mobile home/run/inspection/sheet).
- **AC11** (performance acceptable; no authoritative browser persistence) — the per-route navigation timings recorded (n=96, p50 520.6ms, max 582.4ms — the `networkidle` wait strategy dominates the floor; the release-relevant signal is the uniformity, max/p50 ≈ 1.12, no outlier route) and the server-side wire-call tally recorded (526 governed reads across the whole session, ~5.5 per navigation — every one a scoped GET through the bound application scope, zero POSTs beyond the two governed creates and one governed cancel); the **fan-out fix removed the linear serial-read chain** from every recents-driven surface; the only browser state remains the navigation-only recents cookie and the two presentation cookies (unchanged, disclosed since the foundation).
- **AC12** (the evidence package identifies exact heads and demonstrates the single coherent product) — this document: the exact base/head/branch, the audit, the fix matrix, the journey matrix, the gate pair, and the three-layer chain (this doc + the PR body + the PR comment).

## The driven-browser verification record (real Chromium over the REAL createDashboard server + the scope-enforcing fake world)

The harness (OUTSIDE the repository — /home/z/w041-harness; server.ts boots the real `createDashboard` on 4514 with a fetchImpl implementing the REAL application-scope wire rule (scoped reads without the header ⇒ 422; the create body-scoped, exactly the real routes' split) over the COMPLETE UX v2 world — the verified/failed/waiting/approval/policy-denied/long-running runs, the eight W040 modality runs with every payload the REAL public wire shape, the agent inventory with a recorded promotion, the governed create (idempotent, with the $100 admission ceiling) and cancel, and the two fail-closed reads; drive.py is the release matrix). **421/421 checks passed, ZERO real console errors, ZERO page errors** (machine-readable record at /home/z/w041-harness/report.json; screenshots at /home/z/w041-browser-*.png; the wire-call tally at /home/z/w041-harness/wire-calls.json):

- **Primary journeys (spec §3–§15)**: the composer-first Home; the first-execution journey (review envelope with its four honest sections, the seven-row commitment, Run → the run page); the refused create (the controlling rule, Try again, the live status region); the four run tabs; failure recovery (the retryable fact, the dimension distinction, the new-attempt primary); the approval journey (the decision surface, the consequence preview, the governed cancel POST → CANCELLED); the agent proposal (six sections, no create action); the agent inventory and at-a-glance (9 cells, 3 platform facts + 6 honest absences); the deployments distinction + live session evidence + detail; the artifact → execution → evidence contextual traversal (parent lineage, verification references, usage references); competences; insights + learning (the never-authorizes boundary, the live promotion record, no apply form); the computer-use envelope (the denial verbatim, no form in any modality section); the long-running view (checkpoints, spend vs the declared budget, recovery); spend ($4.73 browser-scoped total, the accounting disclosure); the seven control families + the blocked list's recorded controlling rule; connections (routing facts, BYOK, the no-secrets scan).
- **The command surface**: Ctrl+K, input focus ownership, the live filter, Escape + focus restore on BOTH open paths, the Enter dispatch through GET /command, the live agent match, and the primitive-composed no-match state.
- **Modes and appearance**: simple (the flat four, zero groups), expert (Lineage + Audit), professional; the dark theme (data-theme + the measured token background).
- **The W041 proofs in the browser**: the context-restoration round-trip on the route-404 surface (returnTo + the live mode round-trip back to the same view) and on the execution-not-found and 403 surfaces; the dialog touch targets on mobile (the command submit and the sheet close ≥ 44px); the empty-state primitive on the command page.
- **Keyboard/screen-reader**: the skip link first stop + visible outline; the form-Enter lookup; the sheet open/close focus cycle; the aria vocabulary sweep.
- **Responsive**: desktop sticky sidebar; tablet single-column + full-height right-docked sheet + Ctrl+K; mobile single column + the modality tables + the collapsed disclosures + the bottom-docked sheet at 90dvh + the ≥44px targets.
- **Reduced motion + scalable text**: the transition gate measured to 0.00001s under emulation; the h1/root ratio measured at 1.5.
- **Secret-safety**: the credential-shape regex scan (sk-…, AKIA…, BEGIN PRIVATE KEY, JWT prefixes, token=/secret=/password= value shapes) over EVERY visited page's content — zero matches.
- **Performance**: the per-route timing record and the server-side wire-call tally (both in the machine-readable report).
- **Error surfaces**: the route-404, the execution-not-found, the 403 permission surface and the 502 upstream surface — all with the complete frame, the recovery language, and no stack traces.

## The complete gate (run at the final head, TWICE consecutively green)

`python3 scripts/governance-check.py` (OK: 41 WOs, inFlight=['WORK-041'], frontier=[]) · `bun run typecheck` (0 errors) · `bun run lint` (biome clean, 973 files, zero diagnostics) · `ZECK_PG_TEST_URL=postgres://postgres@127.0.0.1:55432/postgres bun run test` = **284 files / 4179 tests, exit 0** — run TWICE consecutively green at the exact final head (THIS doc's commit; the run-pair record with timestamps is carried in the PR body and the verification comment). The 284/4173 baseline + 6 tests vs the base: the (aw) context-restoration journeys ×4, the (ax) command empty-state journeys ×2.

## Test inventory (the exact delta vs the base)

- EXTENDED integration: journeys 148 → 154 — the (k) a11y frame list extended to every user-reachable GET route (+5: /attention, /trust/evidence, /trust/lineage, the artifact detail with the producing execution, the competence detail); the NEW (aw) ×4 (a content page's returnTo; the route-404 surface + the live mode round-trip back to the same view; the 403 wire-error surface; the execution-not-found view); the NEW (ax) ×2 (the empty-state primitive's exact escaped markup; the hostile no-match query through the primitive's escape boundary).
- EXTENDED unit: `navigation.test.ts` — the responsive pin asserts the mobile touch-target rule covers the dialog family.
- Total dashboard-local: 347 unit (18 files) + 154 journeys + 53 journey discriminations + 21 surface discriminations = 575; driven-browser 421/421 with zero real console/page errors.

## Required checkpoint contracts

- **SELF-HOSTING-BOUNDARY**: the dashboard still imports ONLY `../../sdk` + node builtins (no new import in the diff); the client script performs ZERO network calls (unchanged, D5); no new dependency; every fact remains a live SDK read — the fan-out changes only the concurrency of the same governed reads; the transport scope remains the SDK's bound application scope (the (l) journeys green through the change; the browser wire tally shows every scoped GET carried the bound scope); the only browser state remains the disclosed navigation-only recents cookie and the two presentation cookies.
- **EXECUTION-PROVENANCE**: no new platform facts — the diff is presentation-only: the returnTo values are the user's own current path (never a fabricated authority), the command empty state is the shared primitive over the user's own query, the touch-target rule is CSS, and the fan-out preserves every derivation's input set exactly (the derivations, the 404-only absences and the fail-closed propagation are byte-identical in behavior — pinned by the (av) fail-closed regression, the (l) scope journeys, D12's zero-POST proof and the full W035–W040 suite staying green); money still renders from integer micro-USD strings (BigInt only); no visual primitive manufactures any fact.

## Required verification mapping (the Work Order's list)

| Required | Evidence |
|---|---|
| governance checker | OK: 41 WOs, inFlight=['WORK-041'], frontier=[] at the base and at the head (both gate runs) |
| typecheck | 0 errors at the base and at the head |
| lint (biome) | clean (973 files) at the head |
| dashboard/unit/integration tests | 347 unit + 154 journeys + 53 + 21 discriminations |
| complete primary-journey browser verification | the driven-browser record (§3–§15 journeys, 421/421) |
| responsive desktop/tablet/mobile verification | the three-viewport matrix (desktop 1280 / tablet 768 / mobile 390, the touch-target measurements incl. the dialog family) |
| keyboard/accessibility verification | the keyboard discipline + the aria vocabulary sweep + the reduced-motion and rem-scale measurements |
| trust-state regression verification | the (q)/(y)/(z) journeys and D9/D19–D26 unchanged and green; the browser's four-axis evidence view and trust summary checks |
| command/action authorization-path tests | the command surface journey + D11/D12 + the (f)/(n) journeys (the dialog dispatches through GET /command; mutations only through their confirmation flows) |
| secret-exposure discrimination | D3/D5/D21 + the hostile-query journeys + the browser's credential-shape scan over every visited page |
| performance sanity checks | the navigation timing record (n=96, p50 520.6ms, max 582.4ms, max/p50 ≈ 1.12) + the wire-call tally (526 governed reads, ~5.5/navigation) + the fan-out removing the linear serial chain |
| full suite twice consecutively at exact final head | the complete gate section above |

## Limitations (honest)

1. **The synthesized-Enter limitation, carried over from W038/W039/W040 and reproduced again this session**: synthesized Enter in this headless Chromium activates FORMS but not plain anchors; keyboard navigation is therefore proven through the form-Enter journey (the lookup form → the execution page), the dialog form (Enter dispatches the governed search), the Tab-order/focus-restore measurements and link reachability — every linked surface is itself driven in the same session. The repository's own automated proof is unaffected (the journeys drive HTTP directly).
2. **The resource-load message classification (disclosed, not hidden)**: the six "Failed to load resource" console lines are Chromium's standard messages for the drive's INTENTIONAL error-surface navigations (the refused-create 422, the route-404, the execution-not-found 404, the 403 and 502 fail-closed reads) plus the favicon 404 (the dashboard ships no icon link — zero-dependency). Each is classified by status and accounted against the statuses the drive actually visited in the machine-readable report; ZERO real console errors (script errors) and ZERO page errors is the gate, and both are zero.
3. **The performance timing floor is the wait strategy, not the server**: the `networkidle` wait dominates the per-navigation number (~520ms — the deferred client script plus the 500ms quiet window); the release-relevant observations are the uniformity (max/p50 ≈ 1.12 across 96 navigations — no outlier route), the wire-call tally (the structural bound: ~5.5 governed reads per navigation) and the fan-out's removal of the linear serial-read chain. No authoritative browser persistence exists (AC11).
4. **The artifact-detail recents loop stays sequential by design**: its early-exit read set (it stops at the first producing execution in recents order) differs from a full fan-out's; preserving the exact read semantics was judged worth more than the concurrency there (the detail page is not a primary-journey list surface). Disclosed as a deliberate non-change.
5. **The loading state remains foundation-level** (disclosed since W035): every dashboard read completes server-side before render, so no live region renders `loadingState` in the product today; it stays the shared, pinned contract for the surface that will need it.
6. **The harness is a session artifact OUTSIDE the repository** (the repository stays zero-dependency); its 421-check record, the screenshots and the wire tally are the evidence; the repository's own automated proof is the 575-test dashboard-local suite plus the full 284/4179 gate.
7. **The favicon 404**: the dashboard deliberately ships no favicon link (zero-dependency, no binary asset); Chromium's automatic request produces one accounted resource-load line per session. No product surface references it.
