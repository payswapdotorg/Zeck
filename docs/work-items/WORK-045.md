# WORK-045 Evidence — Durable orchestration

Work Order: `WORK-045` (spec/work-orders/WORK-045.md) · Canonical remote: **`payswapdotorg/Zeck`** · Canonical issue: **#7** · Assurance: **HIGH_ASSURANCE** · Governing architecture: Deployment & Runtime Architecture **D1.0** (subordinate to frozen v1.0), roadmap phase **D-04**.

Exact dispatch base: `6cfbd936475a457886a174adeb457faf9b974ce9` (verified present; the worker branch was created at exactly that SHA and carries **two** commits on it — the D-04 code commit and this evidence document; zero merge commits). Branch: `work/WORK-045-durable-orchestration` · **Final head: this doc's commit**. One PR, opened by the worker, **not merged by the worker**.

## Baseline gate at the exact dispatch base (readiness checkpoint — BEFORE implementation)

- **`python3 scripts/governance-check.py` PASSED at the dispatch base** — `Governance OK: 44 Work Orders, 102 requirements, inFlight=[], frontier=[]` (the inherited D-03-era governance defects were repaired by the Architect post-merge; this is the first order of this program with a green governance gate at the base, and the branch carries **zero** `spec/` changes, mechanically verified).
- `bun run typecheck` — 0 errors at `6cfbd93`.
- `bun run lint` — biome clean (1049 files) at `6cfbd93`.
- Full suite with real PostgreSQL 16.4 (`ZECK_PG_TEST_URL`, 127.0.0.1:55432) at exactly `6cfbd93`: **318 files / 4471 tests, 4464 passed, 7 skipped** (5 real-R2 + 2 real-queue NOT RUN with exact reasons), **0 failed**. Executed as the three sequential script invocations `test:unit` / `test:integration` / `test:architecture` (161 / 79 / 78 files).

## Provider account/resource access (readiness checkpoint)

**NOT RUN — no Cloudflare (or other provider) credential exists in this worker environment.** The only credential held is the operator-provided GitHub PAT for `payswapdotorg/Zeck` (environment-only; used solely for the Git/PR lifecycle of this Work Order).

Consequences, per the evidence contract (never convert unavailable provider access into a PASS):

- **Live Cloudflare Workflows endpoint verification: NOT RUN** — no `workflow-api-token` material. The orchestration adapter is instead verified against the **documented** Cloudflare Workflows REST API (verified against the official Cloudflare API reference: create instance `POST …/workflows/{name}/instances` with `{"instance_id", "params": <JSON-encoded string>}`; instance details `GET …/instances/{id}`; send event `POST …/instances/{id}/events/{event_type}` with `{"body"}`; change status `PATCH …/instances/{id}/status` with `{"status":"terminate"}`; the documented instance-status vocabulary and the documented limits) over **real HTTP against an in-process protocol-verifying server** (`tests/integration/workflow/cloudflare-workflows-protocol.test.ts`) — protocol correctness, explicitly NOT Cloudflare evidence. The real-workflow suite (`tests/integration/workflow/workflow-live.test.ts`) is env-gated on `ZECK_CLOUDFLARE_ACCOUNT_ID` / `ZECK_WORKFLOW_NAME` / `ZECK_WORKFLOW_PROBE_NAME` / `ZECK_WORKFLOW_API_TOKEN` and **skips with the exact missing-variable reason** (4 skipped tests in every run, plus a mirror suite that names exactly the missing variables).
- **Real provider API wire reachability (no credential held):** `deploy:workflow -- probe`, executed with environment-only materialization (an invalid token against the public `api.cloudflare.com`), drove the REAL production wire path — instance creation on the dedicated probe workflow's REST path — and the real API answered `401, provider code 10000: Authentication error`; the probe failed closed with the honest permanent classification and never fabricated success. This is real-API reachability and classification evidence; it is NOT a successful round-trip (no valid credential exists in this environment).
- `deploy:workflow -- inspect` was **executed against the real local environment** (the converged `zeck_local` database, 26/26 migrations): the read-only orchestration snapshot, the explicit provider-limits descriptor and the exact Git revision (the exact-revision workflow verification record). `scan` / `recover` / `compact` were likewise executed (honest zero-activity outcomes on the local environment).
- The durable PostgreSQL orchestration machinery (correlation, waits, notifications, bounded budgets, compaction counters, recovery) is proven over **real PostgreSQL 16.4 through the production `DatabasePort` adapter** (the WORK-003/043/044 local-evidence convention).
- No provider resource in any non-GitHub account was mutated by this worker.

## What this order IS

D-04: make orchestration of waits, callbacks, approvals, retries and deadline expirations durable, correlated and recoverable while workflow state never becomes authority — the provider-neutral workflow orchestration port, the Cloudflare Workflows REST adapter behind it, the PostgreSQL durable wait records (correlation-before-reliance), idempotent resolution through the existing governed execution path, bounded instance-start/signal/effect/replacement budgets, deadline elapse through the governed expiration path, provider-limit awareness, bounded notification retention with the fold counter, provider-state compaction, backlog/attention inspection, and the restart/outage proofs. No D-05 worker fabric, no D-06 observability, no second execution state machine, no frozen-architecture change.

## The orchestration/correlation model (what the machinery actually does)

- **Workflow port** (`src/platform/workflow/port.ts`): start / observe / signal / terminate / describe-limits over opaque instance handles and REFERENCE-ONLY payloads; typed `WorkflowTransportError` classified `transient | permanent`; the bounded `WorkflowRetryPolicy` (start/signal/effect/replacement budgets + deterministic linear backoff, repository-configured, validated fail-closed) and `WorkflowStateBounds` (pointer-payload byte cap + retained-notification bound); the `GovernedOrchestrationEffect` seam; the neutral wait/notification vocabularies; deterministic key derivation (`wait:<executionId>:<kind>:<ordinal>`, `workflow-effect:<waitKey>`). **No vendor vocabulary exists on the port** (pinned by architecture B2).
- **Cloudflare Workflows adapter** (`src/platform/workflow/cloudflare-workflows.ts`): the documented REST protocol over plain `fetch` (zero new SDKs; the sanctioned runtime import set stays `fastify, pg`); Bearer auth; 401/403/404 → permanent, 429/5xx/network/timeout → transient; malformed envelopes fail closed; the token never enters an error; the provider status vocabulary (queued/running/waiting/waitingForPause/rollingBack/paused/errored/terminated/complete) is mapped HERE onto the neutral observation vocabulary and never leaves the file; the documented provider limits are exposed through `describeLimits()`; plus the round-trip `probe()` — running EXCLUSIVELY on the DEDICATED operator-owned probe workflow (`ZECK_WORKFLOW_PROBE_NAME`) and terminating EXACTLY the one instance it created in that run (the PR #6 discipline applied to orchestration: instances are addressed by id, and the probe only ever addresses ids it created itself — application instances can never be signaled, mutated or discarded by a probe).
- **Durable correlation** (`correlation.ts` + migration `0027_workflow_orchestration.sql`): the PostgreSQL wait record commits **BEFORE** any provider instance is created or relied upon (the mid-flight probe proof); the deterministic wait key; the reference-only pointer payload (ids + provenance, sha256-digested — byte-bounded, secret-free); resolution notifications are deduplicated by their deterministic key with **exactly ONE accepted notification per wait physically enforced** (partial unique index — first resolution wins, races converge); the deterministic applied-operation-key records which governed mutation was applied; bounded failure is an explicit `abandoned_waits` row; every start/signal/observe/terminate/effect attempt is append-only evidence; notification payload BYTES are never stored (digest only — the table has no payload column at all).
- **The resolution sequence** (`engine.ts` — the order IS the guarantee): (1) the durable wait record commits FIRST; (2) the provider instance starts (bounded) — the wait is ARMED and survives process and provider-worker restarts; (3) a resolution notification (callback / approval / deadline) is recorded durably FIRST and the wait moves to `signaled`; (4) the GOVERNED EFFECT re-enters the existing execution write path through the executions module adapter (every admission gate intact; idempotent by the deterministic effect key) — only then does the wait become terminal (`settled` / `elapsed`); (5) the provider instance is signaled LAST (a transport fact — a crash here is healed by the recovery scan, never by an authority claim).
- **Governed effect** (`src/modules/executions/adapters/workflow-effect.ts`): callback → the frozen `resume` command; approval approve → `resume`; approval reject → `cancel` (governed termination, never a bypass); deadline → `expire`. The state machine arbitrates everything — a resolution against a non-waiting/terminal execution is rejected `INVALID_STATE_TRANSITION` and the wait supersedes or abandons with the exact reason. The moved-on classification is domain-side (the port's `rejected.movedOn` flag); the platform plane never interprets governed rejection codes.
- **Candidate source** (`src/modules/executions/adapters/orchestration-source.ts`): the read-only scan of authoritative waiting executions with the domain-owned status→wait-kind mapping (WAITING_TOOL/WAITING_USER → callback; WAITING_HUMAN → approval). The engine reconciles this list against its own durable waits — the arm scan and the supersede reconciliation.
- **Physical schema discipline** (migration 0027): the wait state vocabulary is CHECK-bound and **disjoint from the 14-state execution vocabulary** (case-insensitively — the D-03 lesson); the legal transitions are trigger-enforced; terminal rows keep their resolution history forever (only provider observation/compaction and signal-delivery facts may still update); notifications are immutable except delivery facts (write-once delivered marker, monotonic attempts); evidence tables are append-only; tenant/execution bindings are real composite FKs (a wait for a nonexistent execution is unrepresentable); the replacement lineage is FLAT (replacements reference the ROOT — chains are unrepresentable, the budget is countable in one query).
- **Recovery** (`recoverPending` + `applyDueDeadlines` + `reconcileStaleWaits`): recorded/deferred waits re-drive the bounded instance start; signaled waits re-apply the pending governed effect (idempotent convergence); resolved waits re-deliver undelivered provider signals within budget; armed/signaled waits whose execution moved on supersede; due deadlines elapse through the governed expiration path on the **PostgreSQL deadline** (the provider's own clock is NEVER the authority for expiry). All scans read PostgreSQL authority only — provider state is never consulted for what SHOULD exist.
- **Bounded state** (`config.ts` + the fold + `compact()`): instance-start/signal/effect budgets are per-invocation with monotonic durable counters; replacements are lineage-budgeted; refused/stale notifications beyond the per-wait bound fold into the durable `folded_notifications` counter (NO rows materialize — bounded by construction, never row deletion); the provider instances of terminal waits are terminated by the compaction run (bounded provider state — an instance the provider already removed counts as compacted); the snapshot (read-only, byte-identical across repeated inspections) surfaces the attention conditions (deferred/abandoned with reasons), the fold counters and the compacted-instance count.

## Acceptance-criteria mapping (Work Order §Acceptance Criteria)

| AC | Claim | Evidence |
|---|---|---|
| 1. Durable correlation to authoritative PostgreSQL execution state BEFORE provider continuation | PASS (real PG) | `workflow-arm.test.ts`: the mid-flight probe proves the wait row is committed `recorded` BEFORE the instance-start call; one wait/key/instance per logical orchestration; duplicate scans replay the same durable wait; a wait for a nonexistent execution is unrepresentable (FK) |
| 2. Provider-neutral boundary; provider implementation isolated in the platform adapter | PASS | Architecture B1/B2/B3/B5/B7 + the full dependency-rule scan (zero violations; sanctioned imports unchanged `fastify, pg`); the protocol suite proves the adapter over real HTTP; the domain boundary sees only the port; the provider status vocabulary exists only in the adapter (mapped onto the neutral observation vocabulary) |
| 3. Waiting executions resume after application/provider restart without duplicate authoritative effects | PASS (real PG) | `workflow-recovery.test.ts`: the crash matrix (intent→start, resolution→effect, effect→signal, outage exhaustion, moved-on, provider-reported failure) converges through fresh-coordinator recovery; duplicate delivery converges by the executions idempotency arbitration (one governed effect ever — proven by the authority-side rejection of a fresh-key re-application) |
| 4. Callbacks and approvals durable, tenant-scoped, attributable, never bypassing governed admission/transitions | PASS (real PG) | `workflow-callback.test.ts` + `workflow-approval.test.ts`: digest-only durable notifications with approver attribution; approve→`resume`, reject→`cancel` through the single write path; conflicting decisions refused (first durable); forged scope refused with bounded evidence; one accepted notification per wait physically |
| 5. Retry bounded and deterministic; explicit failure/expiration outcomes; no infinite loop | PASS | Bounded policy validation (unit + discrimination); start→`deferred`, effect→`abandoned` with explicit reasons; replacement lineage budget (root + 3, then skip — proven); the deadline scan idempotent |
| 6. Timeout/expiration durable, observable, never silently changing authoritative state outside the governed path | PASS (real PG) | `workflow-deadline.test.ts`: due→`expire` through the governed path (`WAITING_*`→`EXPIRED`); the PG deadline is the authority (a provider-observed finished sleep is evidence only); a due deadline elapses with the provider unreachable; late callbacks refused stale |
| 7. Provider outage never a second authority, never reports success PostgreSQL does not establish | PASS (real PG) | Provider outage → typed transient failures, bounded, `deferred` (the declared orchestration-paused mode), execution untouched; provider-reported instance failure → the WAIT abandons (bounded replacement) while the authority is untouched; instance completion is evidence only |
| 8. Orchestration state bounded/compactable; large artifacts and secret values never enter workflow state | PASS (real PG) | `workflow-compaction.test.ts`: refused-notification folding (bounded rows, durable counter); terminal-instance termination; 404-class compaction; the read-only snapshot proof; the notifications table has NO payload column (digest-only); oversized intakes rejected before any durable write; the pointer payloads are ids-only |
| 9. Provider limits and degradation explicit, inspectable, testable at the exact revision | PASS | `describeLimits()` carries the documented provider facts (payload 1MiB, state 1GB, sleep 365d, steps, rates, retention) + the enforced bound; `deploy:workflow -- inspect` executed at the exact revision against the real local environment; the probe (real-API 401 fail-closed) is the wire-reachability evidence |
| 10. Evidence: exact revision, configuration, correlation model, wait/callback/approval semantics, retry/expiration rules, recovery proof, inventory | PASS | This document + the PR body/comment (three-layer evidence chain, the WORK-042/043/044 convention) |

## Orchestration configuration (repository-defined)

- Variables (all declared in `deploy/manifests/variables.json`, **47 total** — 34 at D-03 + 13 new): `ZECK_WORKFLOW_NAME` (the deployed workflow name), `ZECK_WORKFLOW_PROBE_NAME` (**the dedicated operator-owned probe workflow — the probe never targets the orchestration workflow; equal to `ZECK_WORKFLOW_NAME` is rejected fail-closed**), `ZECK_WORKFLOW_API_BASE_URL` (optional), `ZECK_WORKFLOW_API_TOKEN` (credential-shaped materialized secret; reference binding `ZECK_SECRET_WORKFLOW_API_TOKEN_REF` → `zeck-secret://<environment>/workflow-api-token`, pre-declared in the secret inventory, now mapped in the env secret store), `ZECK_WORKFLOW_MAX_START_ATTEMPTS` (default 3 → `deferred`), `ZECK_WORKFLOW_MAX_SIGNAL_ATTEMPTS` (3), `ZECK_WORKFLOW_MAX_EFFECT_ATTEMPTS` (3), `ZECK_WORKFLOW_MAX_REPLACEMENTS` (3), `ZECK_WORKFLOW_RETRY_BACKOFF_MS` (500), `ZECK_WORKFLOW_MAX_PAYLOAD_BYTES` (4096, bounded [256,65536]), `ZECK_WORKFLOW_MAX_RETAINED_NOTIFICATIONS` (32, the fold bound), `ZECK_WORKFLOW_REQUEST_TIMEOUT_MS` (10000), `ZECK_WORKFLOW_WAIT_TIMEOUT_MS` (0 = no deadline; the deadline elapses through the governed expiration path).
- Providers manifest: `cloudflare-workflows` concern `durable-orchestration` is now `established` with port contract `src/platform/workflow/port.ts` (validated by `deploy:validate`); degradation mode `orchestration-paused` unchanged (waiting orchestrations pause; execution status, budget, policy and ledger state remain authoritative in PostgreSQL and resume on recovery).
- `bun run deploy:validate` — **valid** (4 environments / 6 providers / 10 resource kinds / **47 variables** / 4 secret-reference inventories, 0 problems).
- `deploy/smoke.ts` gained the `durable-orchestration` concern probe: unmaterialized → the exact reference-binding reason; materialized → the REAL probe round trip; both directions **executed locally for evidence** (without materialization: the exact `ZECK_SECRET_WORKFLOW_API_TOKEN_REF` unavailability; with an invalid token against the real public API: `401, provider code 10000: Authentication error`, honest fail-closed — never a silent success).
- `deploy/workflow.ts` — the D-04 operator surface: `inspect` (read-only snapshot + provider limits + exact revision), `scan` (arm waiting executions), `recover` (deadlines + restart/outage recovery + observations), `compact` (bounded provider-state compaction), `probe` (dedicated probe workflow round trip). `deploy:workflow` script added; `deploy/README.md` documents the D-04 configuration, the workflow-code contract (account-plane: hold the wait, await the events, never act on execution state), the probe-workflow provisioning and the orphan-instance guidance.

## The test battery (102 new tests)

- **Unit** (17): port vocabulary disjointness (case-insensitive, both directions), wait-key/effect-key derivation, canonical digest stability, the transition table, bounded policy/state-bounds validation, backoff determinism; config defaults/overrides/garbage.
- **Real-PG integration** (65): the arm/correlation suite (intent-before-start, one-to-one, idempotency, outage→deferred, permanent rejection, bounded lineages, the FK), the callback suite (resolution through the governed path, duplicate/stale/unbacked/scope/oversized refusals, digest-only storage, crash-healing, bounded delivery), the approval suite (approve/reject through the frozen commands, attribution, conflict arbitration, race-window evidence), the deadline suite (governed expiration, PG-deadline authority, provider-unreachable elapse, late-callback refusal, idempotent scan, moved-on supersession), the recovery suite (the full crash matrix over fresh coordinators sharing the same provider), the compaction/inspection suite (folding, termination, 404-class, read-only proof, attention conditions, limits descriptor).
- **Protocol** (14 over real HTTP): the documented REST shapes (create/observe/signal/terminate), Bearer verification, 401/503/429/404 classification, malformed-envelope fail-closed, shape/payload guards before any wire call, the config loader's exact variable names, and the **probe-isolation regression battery** (a contaminated probe workflow still yields exactly one own-instance termination; ZERO requests against the orchestration workflow; weakened configurations rejected fail-closed).
- **Architecture** (10): B1 platform isolation, B2 the neutral port, B3 vocabulary confinement, B4 no execution-state vocabulary, B5 the DatabasePort-only authority boundary with the two executions adapters bridging through the port, B6 secret-free sources, B7 the unchanged runtime import set, B8 the established provider port contract, B9 the secret/probe declarations, B10 the operator surface.
- **Discrimination** (10): weakened policies/bounds rejected; default-and-go rejected with variable names; permanent-vs-transient classification; secret hygiene (token never in errors); probe isolation guards; overlapping vocabularies DETECTED; oversized payloads rejected before any write; **authority-side**: a second governed effect rejected `INVALID_STATE_TRANSITION` by the execution authority itself; a resolution against a non-waiting execution rejected (the orchestration never widens authority); unbacked intake refused with zero effects (provider state is never authority).
- **Gated live** (5): the real-provider round-trip suite — 4 tests skip with the exact missing-variable reasons + the mirror suite naming them.

## Verification at the exact final head (this doc's commit)

- `python3 scripts/governance-check.py` — **PASSED** (44 Work Orders, 102 requirements; zero `spec/` changes on the branch).
- `bun run typecheck` — 0 errors.
- `bun run lint` — biome clean (1072 files).
- `bun run deploy:validate` — valid (47 variables, 0 problems).
- `bun run deploy:migrate` — 26/26 converged (0027 applied; executed against the real local environment).
- `bun run deploy:workflow` — inspect / scan / recover / compact executed against the real local environment (read-only snapshot + explicit provider limits + exact revision; honest zero-activity outcomes); `probe` executed in both fail-closed directions (missing probe workflow → the exact variable name; invalid token against the REAL public Cloudflare API → honest `401, provider code 10000` permanent fail-closed).
- `deploy:smoke` (preview, both directions) — the durable-orchestration concern reports the exact unavailability without materialization and the honest real-API 401 fail-closed with materialization.
- Full suite with real PostgreSQL 16.4, **TWICE CONSECUTIVELY at the exact final head** — both runs identical: **330 files / 4573 tests, 4562 passed, 11 skipped** (5 real-R2 + 2 real-queue + 4 real-workflow NOT RUN with exact reasons), **0 failed**.

## Honest boundaries

- Live Cloudflare Workflows round-trip: **NOT RUN** (no credential — the documented wire protocol proven over real HTTP; gated live suite committed with exact skip reasons; real-API reachability and 401 classification evidence retained from the probe execution).
- The provider workflow-code contract (hold the wait / await events / never act on execution state) is account-plane: it is documented in `deploy/README.md` and enforced by construction on the Zeck side (the machinery never relies on instance state for continuation), but no deployed workflow was exercised (no credential).
- A crash between provider instance-accept and the armed mark can orphan at most one provider instance per crash event; instances are non-authoritative, traceable by the deterministic instance hint (`zeck-w-<digest>-a<attempt>`), bounded by the provider's documented retention and disposable at operator discretion — documented in `deploy/README.md`.
- The dispatch automation's registration commits (WORK-045 authorization, program/dependency/frontier state) sit on `main` AFTER the binding base per the established dispatch convention; the branch base is exactly `6cfbd93` and the branch carries zero `spec/` changes.
- Inherited disclosed flake (from the WORK-044 record): the WORK-027 computer-use-lifecycle full-load flake is timing sensitivity; it did not appear in any recorded run of this order.
