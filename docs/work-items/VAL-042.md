# VAL-042 — Strong Manually Optimized Non-Zeck Baseline

**Work order:** `spec/validation-work-orders/VAL-042.md`
**Authorization:** Tech Lead dispatch (worker implementation; the Lead reviews, re-runs the live battery and merges)
**Assurance:** HIGH_ASSURANCE
**Dependencies:** VAL-040, VAL-030 complete (merged at the dispatch base)
**Dispatch base:** `6e0b410449cd779b97621ae21d329abc348a6dd4` (verified main's head at dispatch — VAL-026/030/031/032/033 and VAL-040 all merged at this base; the parallel longitudinal wave VAL-034 and the sibling economics wave VAL-041 are in flight elsewhere on disjoint surfaces — this slice owns `benchmarks/validation/apps/economic-controls-optimized/**` ONLY)
**Implementation head:** the branch head of `work/VAL-042-optimized-baseline` (exact SHA in the completion report)
**Mode:** worker dispatch (one work order, one branch, one delivery tarball)

## Rails (per the work order's design guidance; live verification is the Lead's re-run)

- **The offline corpus needs NO provider rail** — the optimized-baseline semantics under test are the inventory conformance, the cache safety, the routing, the compression bound, the replay fidelity and the frozen VAL-040 protocol: the recorded VAL-006-style accounting replays drive every offline row deterministically (settled routed usage tokens, recorded latencies, recorded verdicts, the deterministic cache simulation — zero credentials, zero network), and the REAL accounting rails (the REAL recorder, the REAL evaluation oracle, the REAL accounting aggregate with the Wilson interval — imported from the VAL-040 delivery, never copied, never modified) do the accounting.
- **A worker-local PostgreSQL 16.14 RAN the full crown in this sandbox.** The pod allowed a worker-local embedded PostgreSQL (the zonky embedded-postgres binaries 16.14 — initdb/pg_ctl/postgres only, no psql — downloaded OUTSIDE the repository to `/tmp/pgtool`, never touching the repo's package.json/bun.lock), `initdb`'d at `/tmp/pgdata`, served on `127.0.0.1:5433` with trust auth (`ZECK_PG_TEST_URL=postgres://val@127.0.0.1:5433/postgres`). The full integration battery — the whole platform battery plus this slice's crown — RAN over the REAL SQL authorities through the REAL wire. This is recorded honestly as a **worker-local bonus verification**: the Lead's re-run over the program's own PostgreSQL remains the live verification of record, and the suite still skips cleanly when `ZECK_PG_TEST_URL` is absent (the visible skip path is unchanged). The PG 16 line is asserted by the platform's own `pg-database-port` suite (the VAL-040 lesson: zonky 18.x FAILS it — provisioned with 16.14 directly).
- **NOT RUN in this sandbox: the live model rail** — no operator-authorized `OPENROUTER_API_KEY` exists in the pod, so the two live rows (`live-fixed-quality-optimized-real-dispatch`, `live-fixed-cost-optimized-real-budget`) are a recorded NOT RUN boundary with the requirement surfaced (never a fake success). The Lead drives them at review time.

## Architecture note (within the work order's allowed surfaces)

`src/**`, `spec/**`, `benchmarks/validation/platform/**`, `benchmarks/validation/apps/shared/**`, every other application's directory and `benchmarks/validation/apps/economic-baseline/**` (the VAL-040 delivery) are untouched — verified mechanically: `git status` shows ZERO modifications, only NEW files, all under the allowed surfaces (`benchmarks/validation/apps/economic-controls-optimized/**`, `tests/**`, this document). The slice is an **app-slice** (the economics wave's shape): the pricing oracle, the executor, the accounting rails and the protocol derivations are INJECTED seams that IMPORT the existing modules (the VAL-040 delivery's `pricing.ts`/`normalization.ts`/`protocol.ts`/`driver.ts` and the platform's `longitudinal-baseline.ts` digest derivations — never copied, never modified).

**The optimization inventory** (`optimizations.ts`): the STRONG OPTIMIZED NON-ZECK baseline's EXPLICIT, EXHAUSTIVE, content-addressed declaration of every optimization it applies — model routing by task class (the routing table: summarize → retry-relay, extract → openrouter, transform-batch → batch-relay; the fallback route serves unnamed classes), prompt compression (the dispatched input tokens within a BOTH-sided ratio bound of the raw payload — a real compression, never a token-understatement masquerade), semantically-safe response caching (keyed on (task class, content digest); a `fresh-only` task is NEVER served from the cache — mechanically), batched throughput (adjacent same-class rounds coalesce onto the batched rail) and provider-side optimization features (the batched metering priced through the manifest's own batched entries). The inventory registry is content-addressed ALONGSIDE the price manifests (the same sha256-over-canonical-JSON discipline) and append-only: `opt-rev-002` supersedes `opt-rev-001` (the prompt-compression ceiling tightened from 0.75 to 0.70 — the bound-correction discipline; `opt-rev-001` stays frozen and readable).

**The verification core (the two new oracles):**
- **the inventory-conformance oracle** — every optimization the baseline APPLIES must be declared in the pinned inventory revision: the executor reports its per-round applied set, and an applied name outside the inventory (the undeclared-optimization masquerade — something the baseline does that its inventory does not name) FAILs mechanically (`deriveInventoryConformance`);
- **the cache-safety oracle** — the semantically-safe response cache (`SemanticResponseCache`, the driver's own REAL component) serves a round only when the task's declared policy is `cacheable` AND the (class, content digest) identity is stored; an UNSAFE reuse (a `fresh-only` round served from the cache — correctness demanded a fresh dispatch) FAILs mechanically (the `cache-safety` criterion).

Plus the inventory-integrity oracle (the content-digest agreement — an in-place bound mutation with a stale digest FAILs), the routing-conformance oracle (a round riding a rail the routing table does not declare for its class FAILs), the compression-bound oracle (the dispatched/raw ratio within the declared BOTH-sided bound), the replay-fidelity oracle (the offline executor's reported usage equals the recorded facts — an understated token count FAILs) and the frozen-portfolio reference integrity (the VAL-030 content digests re-derive through the platform's own `manifestDigestOf`).

**The execution driver** (`driver.ts`): drives one optimized row through the platform path — the landed execution's chain records the ARM DECISIONS BEFORE DISPATCH (the routing table, the cache policy and — for fixed-cost arms — the per-round budget bound, the worst case over EVERY routed rail priced from the pinned manifest) in the durable planning decision, then each pre-registered round runs through the optimized stack: the cache check first (a cacheable hit serves the stored response — no dispatch, ZERO new measured cost, the honest attribution), the fixed-cost budget gate before each DISPATCH (cache hits are not dispatches — the cache stretches the pinned budget honestly), the routed dispatch with bounded retry on RETRYABLE categories only, and the per-fact usage normalization through the pinned manifest onto the canonical micro-USD basis. Every round — dispatched or cache-served — is sealed through the REAL recorder (the cache-hit rounds carry a `cache-event` trajectory entry and ZERO cost facts), evaluated through the REAL oracle and aggregated through the REAL aggregate; the comparison is derived + validated against the frozen VAL-040 protocol (`deriveNormalizedComparison` + `validateNormalizedComparison` + `deriveEconomicRowCriteria` — all imported from the VAL-040 delivery and reused unchanged over the structurally-compatible row). The verdict is MECHANICAL: any failure, any failed criterion → verdict fail → terminal FAILED; the observed terminal is read back from the ledger and a disagreement FAILs the row honestly.

**The corpus** (`corpus.ts`): 9 rows — 7 offline (deterministic) + 2 live (env-gated). Every row references the FROZEN app portfolio (the VAL-030 baseline revisions) by CONTENT DIGEST (the manifest entries read from the frozen corpus's exported surface — the artifacts never copied), declares the arm manifest entry (the frozen VAL-040 grammar), the pinned optimization-inventory revision and the expected normalized outcome (the REAL derivations over the declared fixtures + the deterministic cache simulation + the budget-gate simulation — exactly what the driver's own accounting must reproduce). The task bodies carry REFERENCES ONLY (never an inline price, never an optimization bound).

**The app** (`application.ts`): the customer application riding the public SDK boundary — one pinned corpus row per run (the submission → the completion poll → the result retrieval → the deterministic assertions), with the app-side economic contract (the VAL-040 derivation, imported) verifying the terminal↔criteria agreement over the public result read.

## The pinned corpus (`economic-controls-optimized.experiment.v1`)

| Row | Arm | Optimization surface | Expected |
|---|---|---|---|
| fixed-quality-optimized-routing-cache-amortized | fq-optimized-routing (0.75, 8, min 8) | 3 routed classes + 1 retry-amortized round + 2 cache hits; the first extract round carries a planner quote (reported separately) | COMPLETED; 8/8 resolved; measured 554 µ$ over 6 dispatches; cpr 69 µ$; est 17 µ$ |
| fixed-quality-optimized-prompt-compression | fq-optimized-compression (0.75, 6, min 6) | every round compressed 120/200 = 0.60 within the corrected opt-rev-002 bound [0.25, 0.70] | COMPLETED; 6/6; measured 132 µ$; cpr 22 µ$; est 17 µ$ |
| fixed-quality-optimized-batch-throughput | fq-optimized-batch (0.75, 6, min 6) | the batched rail's ceil-to-batch on 400/450/500/280/520/490 (520 → 1000 charged) | COMPLETED; 6/6; measured 1240 µ$; cpr 207 µ$ |
| fixed-cost-optimized-cache-budget-stretch | fc-optimized-stretch (61 µ$, 8, min 5) | 5 fresh dispatches + 3 cache hits complete the FULL slice (without the cache the arm would stop after 6 rounds — the honest stretch, never an exclusion) | COMPLETED; 7/8; measured 42 µ$; cpr 6 µ$ |
| fixed-cost-optimized-budget-exhausted-honest-stop | fc-optimized-stop (70 µ$, 8, min 3) | the pinned round bound (25 µ$, the worst case over the routing table) stops after 3 rounds — the declared PREFIX stop | COMPLETED; 2/3; measured 59 µ$; cpr 30 µ$ |
| zero-resolved-optimized-null-discipline | fc-optimized-zero (200 µ$, 6, min 6) | every fresh dispatch fails honestly; failures never populate the cache | COMPLETED; 0/6 resolved; measured 42 µ$; **costPerResolved NULL** |
| fresh-only-optimized-cache-inert | fq-optimized-fresh-only (0.75, 6, min 6) | the collision pairs: rounds 2, 4, 6 share the content digests of rounds 1, 3, 5 but demand freshness — the active cache NEVER serves them | COMPLETED; 6/6; 6 dispatches, 0 cache hits; cpr 9 µ$ |

The live rows (env-gated): `live-fixed-quality-optimized-real-dispatch` (3 REAL compressed dispatches through the routed pinned model + 1 REAL cache hit replaying the cached REAL response of its duplicate-content original + 1 fresh-only collision dispatch) and `live-fixed-cost-optimized-real-budget` (the 600 µ$ REAL budget gating 4 REAL fresh-only dispatches).

## Changed-file inventory (ALL under the allowed surfaces; ZERO edits to existing files)

- `benchmarks/validation/apps/economic-controls-optimized/optimizations.ts` — the content-addressed optimization inventory: the append-only registry (opt-rev-001 + the opt-rev-002 bound-correction), the structural validation, the inventory-integrity derivation (digest agreement + the adversarial registry/declaredDigest probes), the append-only discipline derivation, the routing-table resolver, the compression-bound derivation (BOTH-sided) and the inventory-conformance derivation (the exhaustiveness oracle).
- `benchmarks/validation/apps/economic-controls-optimized/driver.ts` — the execution driver: the semantically-safe response cache (the REAL component), the optimized executor seam, the corpus-row contract (structurally an economic row + the optimization declarations + the frozen-portfolio reference), the row driver (the arm decisions before dispatch → the cache check → the budget gate before each dispatch → the routed bounded-retry dispatches → the normalized/sealed/evaluated/aggregated accounting → the mechanical verdict → the read-back agreement), `deriveOptimizedSliceCriteria` (the seven optimized-slice oracle families), `deriveOptimizedRoundBudgetBoundMicroUsd` (the worst case over every route).
- `benchmarks/validation/apps/economic-controls-optimized/corpus.ts` — the corpus (9 rows): the arms (5 fixed-quality + 2 fixed-cost offline personas + 2 live arms), the round plans (class/content/policy + the recorded fresh-dispatch facts), the frozen-portfolio references (the VAL-030 manifests by digest), the golden-task derivation, the expected-normalized-outcome derivation (the deterministic cache + budget-gate simulation over the REAL derivations), the submission-key/task-body discipline (references only) and the live gates.
- `benchmarks/validation/apps/economic-controls-optimized/fixtures.ts` — the deterministic fixtures: the optimized recorded-replay executor (the honest binding + the seven leaky discrimination knobs: masqueradeOptimization, misrouteClass, fabricateCompression, conflateCurrency, estimateOnly, dropFailedRounds, truncateSamples), the mutated/corrected inventory builders, the fake public API world (the create/replay semantics over THIS slice's corpus); the tick clock, the fake ledger, the fake submission seam and the fake lifecycle are the VAL-040 fixtures IMPORTED (never copied).
- `benchmarks/validation/apps/economic-controls-optimized/application.ts` — the customer application over the public SDK boundary + the exported task slice.
- `benchmarks/validation/apps/economic-controls-optimized/{config.json,README.md}` — the repository-reproducible secret-free configuration (mirrored from the exported task slice — consistency unit-tested) and the README (the corpus table, the optimized stack, the boundaries, the run entry point).
- `tests/unit/validation/val-042-baseline.test.ts` — 24 unit tests: the inventory discipline (append-only, the correction-as-new-revision, the in-place mutation catch, malformed inventories, the routing resolver, the conformance/compression derivations on both sides, the cache keying, the worst-case bound); the corpus (the per-row grammar, the honest hand-computed economics table, the reference-only bodies, the frozen-portfolio digest agreement); the driver over EVERY offline row (terminals, criteria, the pinned outcomes, the cache-hit/dispatch counts, the retry amortization, the budget stretch + the honest stop, the NULL discipline, the collision pairs' fresh dispatches, the arm-decisions-before-dispatch journal proof, the per-decision-distinct keys, the canonical transitions).
- `tests/unit/validation/val-042-apps.test.ts` — 8 unit tests: the task-slice↔corpus↔config.json consistency; the app over the honest fake world (every row PASSES with valid evidence, one durable execution per row); the app discriminations (the fabricated pass-with-fail, the terminal override, the never-landing submission).
- `tests/discrimination/optimized-baseline-validation.discrimination.test.ts` — 28 discrimination tests (AC6): the ten families (the undeclared-optimization masquerade, the unsafe cache reuse, the mixed-currency conflation, the estimate-backed cost, the post-hoc arm exclusion, the sample-size violation, the mutated manifest, the mutated inventory, the routing deviation, the fabricated compression) each at the derivation AND row level, plus the honest controls; the AC4 probes (the below-minimum arm, the confidence-less comparison, the inventory-revision agreement).
- `tests/integration/validation/val-042-optimized-baseline.test.ts` — the crown proof (AC3/4/5/7): the REAL Fastify public API over the seeded REAL PostgreSQL world; the app riding the public wire while the driver drives the landed lanes through the REAL executions service (the canonical transitions, the durable OPTIMIZED-ARM-DECISION planning records, the gapless digest-only step-event journal with per-record-distinct idempotency keys); every round — dispatched or cache-served — sealed through the REAL recorder, evaluated through the REAL oracle and aggregated through the REAL aggregate (the Wilson 95% interval on every comparison); the comparisons reproducing the pinned expected economics exactly; the battery-level ledger cross-checks (zero phantoms, zero drift, zero orphan events, gapless sequences); the live rows env-gated on `OPENROUTER_API_KEY` with ONE shared dispatch binding (ONE registered connection `val-042-openrouter` — the VAL-025 lesson) applying the REAL optimized stack (the routed pinned model + the compressed prompt verified against the declared bound + the REAL response cache) and the honest NOT RUN boundary absent credentials.
- `docs/work-items/VAL-042.md` — this evidence document.

## REAL end-to-end run — RAN over the worker-local PostgreSQL 16.14 (this sandbox)

The crown suite ran end to end against the REAL platform path over the worker-local embedded PostgreSQL 16.14 (recorded honestly above; the Lead's production-PG re-run is the live verification of record). The measured per-row facts (the suite's own console output, from the recorded battery run):

| Row | Arm (kind) | Terminal | Rounds | Dispatches | Cache hits | Resolved | Measured µ$ | µ$/resolved | Wilson 95% | App |
|---|---|---|---|---|---|---|---|---|---|---|
| fixed-quality-optimized-routing-cache-amortized | fq-optimized-routing (fixed-quality, 0.75) | **COMPLETED** | 8 | 6 | 2 | 8 | 554 | 69 | [0.676, 1.000] | PASS |
| fixed-quality-optimized-prompt-compression | fq-optimized-compression (fixed-quality, 0.75, opt-rev-002) | **COMPLETED** | 6 | 6 | 0 | 6 | 132 | 22 | [0.610, 1.000] | PASS |
| fixed-quality-optimized-batch-throughput | fq-optimized-batch (fixed-quality, 0.75) | **COMPLETED** | 6 | 6 | 0 | 6 | 1240 | 207 | [0.610, 1.000] | PASS |
| fixed-cost-optimized-cache-budget-stretch | fc-optimized-stretch (fixed-cost, 61 µ$) | **COMPLETED** | 8 | 5 | 3 | 7 | 42 | 6 | [0.529, 0.978] | PASS |
| fixed-cost-optimized-budget-exhausted-honest-stop | fc-optimized-stop (fixed-cost, 70 µ$) | **COMPLETED** | 3 (prefix stop) | 3 | 0 | 2 | 59 | 30 | [0.208, 0.939] | PASS |
| zero-resolved-optimized-null-discipline | fc-optimized-zero (fixed-cost, 200 µ$) | **COMPLETED** | 6 | 6 | 0 | 0 | 42 | **null** | [0.000, 0.390] | PASS |
| fresh-only-optimized-cache-inert | fq-optimized-fresh-only (fixed-quality, 0.75) | **COMPLETED** | 6 | 6 | 0 | 6 | 54 | 9 | [0.610, 1.000] | PASS |

**Battery-level ledger integrity (the whole offline corpus, over the REAL SQL):** 7/7 rows COMPLETED; the ledger holds exactly **7 executions and 7 idempotency records** (the exact oracle sum — zero phantoms, zero drift); **zero orphan events**; every `execution_events` sequence gapless; every round's run record sealed through the REAL recorder (`validateRunRecord` clean on every round — the cache-served rounds included, carrying the `cache-event` trajectory and ZERO cost facts); every comparison aggregated through the REAL accounting rails with the Wilson 95% interval carried; the inventory-conformance, cache-safety, routing, compression and replay-fidelity oracles green on every row. The offline economics are honestly source-labeled replayed-record facts (deterministic — never presented as live measurements); latency measured; payload DIGESTS only in evidence.

The crown was green in the standalone run AND in the full-battery run (the per-row numbers byte-identical — the replays are deterministic).

## Acceptance-criteria mapping

| AC | Evidence |
|---|---|
| 1. Public SDK boundary, secret-free reproducible config, executable tests; the corpus declares per row the frozen portfolio revision, the arm manifest entry (optimized-baseline kind, the declared optimization inventory, pinned price revisions, arm kind, statistical minimums) and the expected normalized outcome | the app (the harness pattern), `config.json` (consistency unit-tested), the corpus module (9 rows with the full oracle per row — the grammar validation unit-tested), the unit suites |
| 2. Deterministic fixtures; frozen portfolio artifacts referenced by content digest (never copied); the optimization inventory content-addressed alongside the price manifests | `fixtures.ts` (the optimized replay executor + the deterministic cache — every offline row reproducible with zero network and zero credentials); the frozen-portfolio references (the VAL-030 manifest entries read through the exported surface — the digest agreement unit-tested); `optimizations.ts` (the sha256-over-canonical-content digest; opt-rev-002 supersedes opt-rev-001 with opt-rev-001 frozen and readable) |
| 3. REAL end-to-end (env-gated) driving the optimized baseline over representative portfolio slices with REAL provider dispatches through the declared optimizations: runs complete, economics measured by the REAL accounting rails, normalized comparisons recorded through the REAL recorder | `val-042-optimized-baseline.test.ts` — **RAN** over the worker-local PostgreSQL 16.14 (the measured table above); the two live rows are the env-gated NOT RUN boundary in this pod; the Lead's credentialed + production-PG re-run is the live verification of record |
| 4. Mechanical verification per row: inventory conformance (an undeclared optimization FAILs — probed adversarially), cache safety (an unsafe reuse FAILs), normalization integrity, protocol conformance, manifest integrity | `deriveOptimizedSliceCriteria` (the seven oracle families) + the VAL-040 twelve criteria families — unit- and discrimination-tested incl. the ten AC6 families + the AC4 probes (the mutated manifest, the mutated inventory, the below-minimum arm, the confidence-less comparison) |
| 5. Honest economics and latency (measured on the live rows, none-reported honestly offline); payload digests (never payloads) in evidence | the `optimization-economics` criterion (the cache hits' ZERO-cost attribution; replayed-record provenance offline, rail-measured on the live rows); FNV-1a/sha256 digests in every journal record + run-record trajectory — payload bytes never journaled |
| 6. Discrimination tests against controlled fakes (the undeclared-optimization masquerade, the unsafe cache reuse, the mixed-currency conflation, the estimate-backed cost, the post-hoc arm exclusion, the sample-size violation — plus the mutated manifest, the mutated inventory, the routing deviation and the fabricated compression) | 28 discrimination tests covering the ten families at the derivation/comparison/row levels + the honest controls |
| 7. Evidence document records REAL outcomes, timings, costs, NOT RUN boundaries and the executed battery honestly | this document |

## Executed battery (at the implementation state, sequential, this sandbox; the worker-local PostgreSQL 16.14 served every PG-gated suite)

| Command | Outcome | Detail |
|---|---|---|
| `bun install` | pass | 116 packages (no changes — the zonky PG tooling lives OUTSIDE the repository) |
| `bun run typecheck` | pass | 0 errors |
| `bun run lint` | pass (exit 0) | 47 warnings + 8 infos, 0 errors — byte-identical to the pristine base worktree at the dispatch base (linted side by side: 47 warnings + 8 infos on BOTH sides, 0 errors on both, zero new, zero gone) |
| `bun run test:unit` | pass | 290 files / 4580 tests (pristine base, run side by side: 288 files / 4548 tests; **+2 files, +32 tests** — exactly the two VAL-042 unit suites: 24 baseline + 8 apps) |
| `bun run test:architecture` | pass | 125 files passed + 1 skipped / 1729 tests passed + 4 skipped (pristine base: 124+1 files / 1701+4 tests; **+1 file, +28 tests** — exactly the VAL-042 discrimination suite) |
| `bun run test:integration` | pass (worker-local PG 16.14) | 147 files passed + 1 skipped / 1183 tests passed + 20 skipped (256 s); the VAL-042 crown green (2/2 tests: the offline corpus 7/7 COMPLETED + the honest live-rail NOT RUN skip). The skips are the env-gated live rails (`ha-failover`, `queue-live`, `r2-live`, `runner-live`, `workflow-live`, `provider-redundancy`'s live row) and the credential-absent NOT RUN rows. Pristine base under the same PG (run side by side): 146 files passed + 1 skipped / 1181 tests passed + 20 skipped, fully green — **+1 file, +2 tests**, exactly the VAL-042 crown |
| `python3 scripts/governance-check.py` | pass | `Governance OK: 60 Work Orders, 110 requirements, inFlight=[], frontier=[]` |

## Issue findings and protocol

- **The mutated-inventory discrimination initially failed to FAIL the right oracle** (the first implementation checked the inventory-integrity criterion against the GLOBAL registry instead of the inventory the driver ACTUALLY used — the mutated override passed integrity while only the compression-bound criterion caught it). Classification: implementation defect in this slice (caught by the slice's own discrimination battery before any delivery). Solution: the criterion now verifies the digest of the inventory that actually declared the run's optimization set (the registry's pinned revision, or the injected override — an in-place bound mutation with a stale digest fails its own digest integrity mechanically). Verification: the `mutated inventory` family now FAILs via `inventory-integrity` (and the compression bound still catches the tightened-bound deviation).
- **The estimate-only discrimination initially reported measured costs** (the knob was not wired into the usage construction — the executor still reported the recorded measured usage). Classification: implementation defect in this slice (caught by the slice's own discrimination battery). Solution: the knob now nulls the measured usage and rides the planner quote only — the cost-per-resolved is REFUSED and the comparison FAILs with `ESTIMATE-BACKED COST-PER-RESOLUTION`.
- **The first expected-outcome derivation of the budget-stop row did not simulate the fixed-cost gate** (all 8 rounds executed in the oracle while the driver honestly stopped after 3 — the `normalized-outcome-oracle` caught the disagreement). Classification: oracle-derivation defect in this slice (caught by the unit battery). Solution: the expected-outcome derivation simulates the SAME BEFORE-dispatch budget gate (cache hits consume no budget). Verification: the stop row's pinned outcome is 3 runs / 2 resolved / 59 µ$ — reproduced exactly by the driver.
- **No live model-rail verification was possible in this sandbox** (no operator-authorized `OPENROUTER_API_KEY`). The live rows' dispatch binding is the VAL-025/040-proven model-gateway posture (the REAL gateway, the REAL OpenRouter rail registry, the REAL fetch transport, the REAL dispatch journal, BYOK credential registration at run time) with the ONE-binding-for-all-live-rows wiring (ONE registered connection `val-042-openrouter` shared across the live rows) and the optimized-stack semantics ON the REAL dispatch path (the routed pinned model through the inventory's routing table, the COMPRESSED prompt with the raw-baseline token count for the compression-bound verification, and the driver's REAL response cache serving the duplicate-content round with the cached REAL response). Any live divergence is an honest finding to record here.
- **The live compression-bound margin** (a calibration note, not a defect): the live executor reports `rawInputTokens` as its own estimate of the VERBOSE prompt it would have sent uncompressed (chars/4), and the rail reports the COMPRESSED prompt's actual tokenization. The pinned corpus prompts are sized so the expected ratio lands mid-band (~0.45 within [0.25, 0.75]); if a live run's tokenizer ratio drifts outside the declared bound, the `compression-bound` criterion FAILs that round HONESTLY — that is the oracle working, and the finding should be recorded as a re-pin (a new inventory revision with a recalibrated bound), never an in-place edit.
- **The pre-existing `computer-use-lifecycle` flake documented by VAL-025** remains a platform-suite tolerance finding for the Lead's follow-up discretion (outside this slice's allowed surfaces). It did not surface in this sandbox's recorded runs.

## NOT RUN boundaries

- **The live model rail (both live rows):** `OPENROUTER_API_KEY` absent — the suite prints the exact requirement (an operator-authorized OpenRouter credential covering the pinned chat model `meta-llama/llama-3.3-70b-instruct` — the manifest's openrouter entry at rev-001, which the inventory's `extract` route pins for the live class) and asserts no fake success. The offline corpus covers every optimized-stack path without credentials.
- **The Lead's production PostgreSQL:** the crown ran over the WORKER-LOCAL embedded PostgreSQL 16.14 (the same REAL SQL authority code path over the REAL wire); the program's own PG re-run at review time is the live verification of record. The suite skips cleanly when `ZECK_PG_TEST_URL` is absent.
- **Live-measured economics:** zero model dispatches happened in this sandbox — the offline rows' usage is honestly source-labeled replayed-record facts (the `optimization-economics` criterion records the provenance); the live rows' usage will be RAIL-MEASURED on the Lead's re-run (the crown asserts `measured > 0` on live rows — never a fabricated basis).

## Handoffs

- The Lead's live re-run should drive the two live rows with the operator-authorized OpenRouter credential (ONE dispatch binding, ONE registered connection `val-042-openrouter` shared across both rows — the VAL-025 review lesson is baked into the crown's wiring), append the measured per-row facts to the table above (the console output is emitted for exactly that purpose), and re-run the crown over the program's own PostgreSQL — recording any divergence as a re-pin finding.
- The optimization inventory is now the optimized-baseline arm's shared artifact: VAL-043 (competing stacks) declares any stack-side optimization vocabulary against the same content-addressed discipline (a bound correction is a NEW revision; each arm declares the revision that declared its optimization set). The price manifest remains the economics wave's shared pricing registry (VAL-041/042/043 all price through it).
- The corpus policies (slice sizes, minimums, thresholds, budgets, compression ratios, cache policies) are single constants in `corpus.ts`; a larger live sample (wider Wilson intervals) or a recalibrated compression bound is a one-constant re-pin plus the config.json mirror (consistency-tested).
- The pre-existing `computer-use-lifecycle` flake documented by VAL-025 remains a platform-suite tolerance finding for the Lead's follow-up discretion (outside this slice's allowed surfaces).
