# WORK-015 Evidence — Public API, SDKs, CLI and developer dashboard

Work Order: `spec/work-orders/WORK-015.md`
Assurance: `HIGH_ASSURANCE` · Architecture: `v1.0` (frozen) · ADR-0001/0002/0013/0015 normative
Branch: `work/WORK-015-public-api` · Base: `814eab0f1a8e7bb5eb64f980c8152e6fbc52128f` (actual current main at pickup, verified by `git fetch origin` — the post-WORK-012-finalization tip; main's own governance fully green at pickup: `Governance OK: 31 Work Orders, 94 requirements, frontier=['WORK-014', 'WORK-015']`)
Implementation revision (this file binds): `eda15c9205cac70b0c1d2c68041e9e8ccb1985ac` (implementation commit)
Final branch head: bound in the PR body (the two-phase SHA binding convention — this evidence commit cannot contain its own SHA)

Parallelization status: WORK-014 (learning telemetry) is in-flight on its own open PR **#24** from base `814eab0` with DISJOINT declared surfaces (`src/modules/learning/` + `src/modules/planning/` vs this branch's `src/api/` + `sdk/` + `cli/` + `apps/dashboard/`); this branch moves ONLY WORK-015 to in-flight and leaves WORK-014 `eligible` in ITS branch-local state (the parallel-in-flight precedent of WORK-012/WORK-013). Both PRs target the same main tip; the architect sequences the merges.

No pre-existing main defects at pickup: pristine-main baseline measured **1242/1242 (124 files)** at the base `814eab0` (the exact post-merge state WORK-012's evidence records for its rebased final head — re-measured during WORK-014's session at the same base).

## Requirement mapping

| Requirement | Acceptance criterion | Implementation | Proof |
|---|---|---|---|
| API-001 (criterion 1) | Execution creation without provider selection | `POST /executions` — a closed-vocabulary create contract (the body accepts ONLY `applicationId/environmentId/task/inputArtifactRefs/constraints/metadata/userId`; unknown keys — including provider/tenant injections — are rejected 422 fail-closed). The route delegates to the executions authority's `createExecution` with the caller-provided idempotency key; the service's own FORBIDDEN_INPUT_KEYS rule is the second layer. The SDK enforces the same vocabulary client-side | Unit execution routes (creation, closed vocabulary, missing-key rejection); PG api-surface (creation through the real SQL authority); discrimination M2/M3 static + runtime; SDK client tests (provider selection rejected client-side) |
| API-002 (criterion 2) | SDKs expose a stable execution-oriented abstraction | `sdk/` — the execution-centric TypeScript SDK: `ExecutionRequest/Execution/ExecutionReceipt/ExecutionResult/ExecutionEvent/ExecutionStatus/VerificationResult/ArtifactReference/CostSummary/UsageSummary/RouteSummary/AgentSummary/AgentStatusView/WebhookEvent/PublicError` + the fetch-based `ZeckClient` + the webhook verification helper. The wire contract is the ONE canonical source `src/shared/wire.ts` (shared by the transport and the SDK — drift is impossible by construction); NO provider SDK concept exists anywhere in the surface (M17) | SDK client tests (8 tests: headers/payload shape, provider-selection rejection, error mapping, method inventory); architecture public-surface (provider-neutral scan over sdk/cli/apps) |
| API-004 (criterion 5) | Execution lifecycle events available through webhooks | `src/api/webhooks/delivery.ts` — signed/versioned webhook delivery: the `WebhookEvent` envelope (schema version, execution, durable event identity, type, sequence, delivery attempt, timestamps, payload), HMAC-SHA256 signing over the DOCUMENTED canonical basis (`webhookSignatureBasis`), bounded retry with per-attempt re-signing and attempt accounting, secret-shaped-key scrubbing of payloads (M8). Receiver guidance in the SDK: `verifyWebhookSignature` + `webhookDedupeKey` (dedupe on eventId; ack replays; the attempt field distinguishes redeliveries) | Webhook unit suite (11 tests: envelope identity set, signature round-trip, tamper failure, retry attempt accounting, secret-never-crosses, dedupe key); discrimination M8/M9/M10 static + runtime |
| API-005 (criterion 5) | Developers can inspect execution receipts, provenance, cost and quality | `GET /executions/:id` (the record with policy-visible metadata), `GET /executions/:id/results` (the IMPLEMENTATION.md §6 result package: route summary from the ledger's planning decisions, cost from settled ledger facts — null when unset, honest warnings), `GET /executions/:id/events` (the gapless public event projection), `GET /executions/:id/verification` (the verification evidence with evaluator provenance) | Unit routes (all four surfaces incl. the completed/failed paths); PG api-surface (the full lifecycle drives the real state machine to COMPLETED with a durable PASS, then reads through HTTP) |
| Criterion 3 | CLI primitives for submitting/inspecting executions | `cli/index.ts` — submit/inspect/result/events/cost/verify/cancel/agents/agent. Execution-centric (no provider command — M17/M18); credentials ONLY from env (`ZECK_TOKEN`/`ZECK_API_URL` — never flags, never printed — M6) | CLI suite (11 tests incl. the secret-safety and no-provider-command proofs) |
| Criterion 4 | Developer dashboard views | `apps/dashboard/index.ts` — the read/compose projection surface: execution receipt/route/cost/artifacts/verification/events, the governed cancel command (through the API's cancel route), and the agent inventory — every view rendered from a LIVE read through the SDK (no local state — M24), HTML-escaped interpolation (M7) | Architecture public-surface (dashboard statelessness + provider neutrality); discrimination R-M24 |
| Criterion 6 | Never expose secret plaintext or internal authority mutation endpoints | The serialization boundary (`src/api/serialization.ts`): allowlist construction (field-by-field; NO domain-record spread — a leaked field is unrepresentable) + the deep scrub guard (secret-shaped keys redacted at every depth); the route table carries ONLY the ten public routes (architecture-gated — M21); unknown errors fail closed to a disclosure-free body (M25) | Secret-safety suite (allowlist shapes, scrub guard, error boundary); architecture route-table gate; discrimination M4–M8/M21/M25 static + runtime |
| Criterion 7/8 | Read-only governed agent inventory views; projections cannot mutate | `GET /agents`, `GET /agents/:id`, `GET /agents/:id/versions`, `GET /agents/:id/status` — every read delegates to the agents authority's public registry (getAgent/listVersions/currentSelection/listSelections); there is NO agent mutation route (the registry's mutating methods are unreachable from the surface — the fake registry records every attempt and the route table is architecture-gated); the active version follows the CURRENT selection (M23); the projection EQUALS the authority's rows (M16/M22) | Agent route unit suite (10 tests); PG api-surface (real registry projection + no-mutation proof); architecture route-table; discrimination R-M15/M16/M22/M23 |
| Criterion 9 (§9 tenant/authorization) | Public API derives effective identity/scope server-side | Every route: authenticate (the injectable credential seam → `Principal`) → resolve the application scope through the auth module's scope resolver over DURABLE membership/ownership rows → scope-check every read (cross-tenant lookups 404 — no tenant leak). A client-supplied tenantId/ownerId is never read: the closed create vocabulary rejects the keys outright (M2/M3) and no route code path reads a tenant from the request | Unit routes (cross-tenant read/cancel 404, foreign-application 403, body-tenantId 422); PG api-surface (real membership rows, real isolation); discrimination M1/M2/M3 static + runtime |
| Criterion 10 (§10 idempotency) | The platform's established idempotency semantics | POST routes REQUIRE the `Idempotency-Key` header and DELEGATE the semantics to the executions authority (the established `(application, operation, key, fingerprint)` arbitration — no second idempotency store exists anywhere in the api/sdk/cli/apps trees): same key + same fingerprint replays the durable outcome (201, `replayed: true`); same key + different fingerprint → 409 `IDEMPOTENCY_KEY_REUSED`; concurrent identical requests converge on ONE durable row (the PG proof fires two concurrent POSTs) | Unit routes (replay + 409); PG api-surface (concurrent duplicate convergence — one row); discrimination M11/M12 static + runtime |

Related-not-owned: `API-003` (idempotency of mutating requests) is owned by WORK-006 — this branch CONSUMES the authority's semantics (criterion 10). Agent identity/version authority is WORK-011's — this branch only projects it. `LRN-002`/policy-side boundaries stay with their owners. The durable webhook delivery journal (persisted attempts, dead-letter queues) belongs to the webhooks module's durable surface — outside this Work Order's declared surfaces (disclosed as a limitation).

## Implementation

Surfaces (declared): `src/api/` — `server.ts` (the Fastify composition over injected authorities), `request-identity.ts` (authentication + server-side scope resolution), `serialization.ts` (the secret-safety wire boundary), `error-mapper.ts` (the canonical public error model), `routes/executions.ts`, `routes/agents.ts`, `webhooks/delivery.ts`, `index.ts` (barrel). `sdk/` — `index.ts` + package manifest (the execution-centric client contract). `cli/` — `index.ts` + manifest (execution-centric primitives). `apps/dashboard/` — `index.ts` + manifest (the read/compose projection surface).

Surfaces (directly required, per the Work Order's "Only directly-required tests/evidence may extend beyond these surfaces" and the WORK-012/013/014 precedents):
- `src/shared/wire.ts` — the ONE canonical public wire contract (a NEW file in the cross-cutting dependency-light shared layer; shared is exactly the sanctioned home for a contract both the api transport and the SDK need — it imports NOTHING).
- `package.json` + `bun.lock` — the sanctioned runtime dependency `fastify` (IMPLEMENTATION.md §1 "HTTP/API: Fastify"; the SDK-boundary table already reserves fastify for `src/api/`). No other dependency added.
- `tsconfig.json` + `biome.json` — the include sets extended with `sdk/**`, `cli/**`, `apps/**` (the typecheck/lint gates must cover the new declared surfaces — directly required).
- `tests/architecture/dependency-direction.test.ts` + `tests/architecture/provider-sdk-boundaries.test.ts` — the two pre-existing gates updated for the sanctioned fastify dependency (they asserted zero runtime deps, which predates the api transport; the fastify-confined-to-src/api rule was ALREADY in the boundary table).
- `tests/**` — 5 unit suites + world/helpers, 1 discrimination suite + shared scanner lib + architecture fakes, 1 architecture gate, 2 real-PG suites + world fixture.
- `spec/development-state/*.json` — the in-flight transition + checkpoint outcomes (below).
- `docs/work-items/WORK-015.md` — this file.

## Design decisions (architect-review pointers)

1. **The API is a projection/command facade over the authorities (§6).** Every route delegates: creation/cancellation → the executions service (the single write path — cancellation goes THROUGH `transition({command: "cancel"})`, M26); agent inventory → the registry's read methods; the API NEVER writes execution/agent tables (the architecture gate proves no SQL exists anywhere in `src/api/`, `sdk/`, `cli/`, `apps/` — M13/M14). The composition is dependency-injected: `createApiServer(deps)` receives the module services through their PUBLIC interfaces; the production wiring (SQL stores over the pg DatabasePort) is the composition root's concern — the api layer imports module public barrels + shared ONLY (the scanner-pinned api-boundary).
2. **Server-side scope derivation is the M1/M2/M3 protection.** The transport authenticates ONCE per request (the injectable credential seam → the auth module's `Principal` — "an already-authenticated actor reference"); the effective application/tenant scope is ALWAYS derived by the auth module's scope resolver from durable membership/ownership rows. There is no code path that reads a tenant from the request; the closed create vocabulary rejects client tenant/provider keys outright; scope-checked misses are 404s indistinguishable from missing (no tenant leak).
3. **The secret-safety boundary is constructive, not advisory (M4–M8).** Every response body is built FIELD BY FIELD in `serialization.ts` (allowlist construction — a mutated domain record with an added secret field cannot leak because the serializer never spreads); a deep scrub guard redacts secret-shaped keys at every depth (defense in depth for caller-provided task/metadata payloads); the webhook payloads are scrubbed before delivery; the signing secret is injected as an opaque string and appears in no response, header, payload or log. The architecture gate pins the allowlist discipline (a spread mutant is detected).
4. **The wire contract has ONE source (API-002 + drift-proofing).** The public shapes live in `src/shared/wire.ts` (imported by the api transport — api → shared is the sanctioned direction — and re-exported by the SDK). The SDK adds only client behavior (fetch client, signature verification, dedupe guidance). The alternative (twin type definitions kept in sync by convention) is exactly the drift surface this design eliminates.
5. **Idempotency is DELEGATED, never duplicated (§10).** The api layer adds NO idempotency store: POST routes require the `Idempotency-Key` header and pass it to the executions authority, whose `(application, operation, key, fingerprint)` arbitration already converges/fails-closed. The PG proofs (concurrent duplicate POSTs → one durable row; same key + different fingerprint → 409) run through the REAL HTTP surface.
6. **Webhooks are signed, versioned and honestly retryable (API-004).** The envelope carries the full identity set (schema version, execution, durable event identity, sequence, attempt, timestamps); the signature is HMAC-SHA256 over a DOCUMENTED canonical serialization (the SDK's verifier and the CLI's docs share the exact basis — a receiver never trusts an unsigned webhook, M9); retries rebuild + re-sign the envelope per attempt so receivers can always tell WHICH attempt they see; the receiver-side dedupe contract is `eventId` (ack replays — M10). Durable delivery state (a persisted attempt journal) is the webhooks module's surface — outside the declared surfaces — and is disclosed as a limitation, NOT silently improvised in the transport (the api layer is transport-only per the frozen architecture).
7. **The dashboard is a projection, never truth (M24).** It holds no state: every render is a live read through the SDK client; the ONE governed command (cancel) goes through the API's cancel route (the lifecycle authority validates it). HTML-escaping on every interpolation (M7).
8. **The public error model is the canonical taxonomy, closed (M25).** `PlatformError` codes map to a total, deterministic status table; the transport's own input failures use canonical codes; unknown errors fail closed to a disclosure-free 500 (originals logged server-side only — never serialized; the mapper scans clean of stack/cause serialization). The status mapping never leaks SQL, host paths or provider internals.
9. **Provider neutrality is scanner-enforced (M17/M18).** The provider-identifier scan runs over `src/api/`, `sdk/`, `cli/`, `apps/` (the architecture gate); the SDK rejects provider-selection client-side; the CLI has no provider command; route summaries carry provider/model as opaque neutral strings exactly like the policy vocabulary.

## Discrimination results (the 26 mandatory mutants)

| Mutant | Proof | Where |
|---|---|---|
| M1 API bypasses tenant authorization | Runtime: cross-tenant execution read AND cancel are 404 (no tenant leak — the error body never names the target tenant); PG: real isolation over real membership rows (the cancel-leaves-CREATED proof) | discrimination R-M1 + PG |
| M2 API trusts client tenantId | Static: a `body.tenantId` read appearing in the api layer is scanner-detected (`client-tenant-trust`); runtime: a body tenantId is rejected 422 (closed vocabulary) | discrimination + unit routes |
| M3 API trusts client applicationId | Runtime: a foreign applicationId does NOT adopt its scope — the caller's membership for it does not exist → 403 AUTHORIZATION_DENIED (the body's applicationId never grants scope; the scope comes from durable membership) | discrimination R-M3 |
| M4 API exposes secret plaintext | Runtime: secret-shaped caller metadata is redacted (`[redacted]`) in API responses; static: the scrub guard's removal is detected | discrimination + secret-safety suite |
| M5 SDK serializes secret material | Static: no SDK export names a secret-bearing concept; the wire `ExecutionRequest` fields are structurally pinned (no secret-named field, no provider field) | SDK client suite |
| M6 CLI prints secret material | Runtime: CLI error paths print ONLY the public error code/message (the token never appears); a missing ZECK_TOKEN fails closed; the usage text documents env-only credentials (no credential flags) | CLI suite |
| M7 dashboard displays secret material | Static: the provider/secret scan over `apps/`; runtime: every interpolation is HTML-escaped (`esc`) | architecture + dashboard source |
| M8 webhook contains secret plaintext | Static: the envelope builder referencing `secret` is detected (scoped to the returned object); runtime: the secret appears in neither the envelope nor the signature basis | discrimination + webhook suite |
| M9 webhook unsigned | Static: removing the signature header from the delivery path is detected (`unsigned-delivery-path`); runtime: a tampered payload FAILS signature verification | discrimination + webhook suite |
| M10 webhook replay accepted without idempotency | Static: removing the attempt field from the envelope is detected; runtime: the dedupe key is the durable eventId; attempts increment per retry (the receiver contract: ack replays, never re-apply) | discrimination + webhook suite |
| M11 duplicate execution creation | Runtime: duplicate POSTs with the same key converge on ONE durable execution (unit: replayed receipt; PG: two CONCURRENT POSTs → one row) | unit + PG |
| M12 same idempotency key different request accepted | Runtime: 409 IDEMPOTENCY_KEY_REUSED (unit + PG); static: a create-body spread replacing the closed parse is detected | discrimination + unit + PG |
| M13 API directly writes execution tables | Static: SQL appearing anywhere in `src/api/` is detected (`api-sql`); the architecture gate scans the whole public surface (no INSERT/UPDATE/DELETE/driver import) | discrimination + architecture |
| M14 API directly writes agent tables | Static: the same SQL scan + the route-table gate (no agent mutation route exists at all) | architecture + discrimination |
| M15 dashboard mutates agent authority | Runtime: the dashboard surface carries NO mutating agent call (its only POST is the governed cancel through the API) | architecture + discrimination |
| M16 public API creates second agent registry | Runtime: the projected inventory EQUALS the authority's rows (id/createdAt/updatedAt byte-equal — unit fake + real PG registry) | discrimination R-M16 + PG |
| M17 SDK becomes provider-specific | Static: a provider identifier appearing in the SDK is detected; runtime: provider selection is rejected client-side; the client method inventory carries no provider concept | discrimination + SDK suite |
| M18 API bypasses policy | Runtime: creation goes through the policy-authorized authority path (the PG world wires the REAL policy authority behind the authorize seam); the API has no direct policy surface | discrimination R-M18 + PG |
| M19 API bypasses budget | Runtime: the authority chain is intact — the API exposes no budget surface and creation delegates to the service whose budget seams stay at their owner | discrimination R-M18/M19 |
| M20 API bypasses verification | Runtime: the API exposes no pass/complete route (the route table is gated); completion requires the authority's `pass` with durable verification results (the PG lifecycle drives the real path) | discrimination + architecture + PG |
| M21 API exposes internal authority mutation endpoint | Static: the route table carries EXACTLY the ten public routes (architecture-gated via the onRoute introspection; every internal mutation pattern asserted absent) | architecture + unit agents |
| M22 agent inventory differs from agent authority | Runtime: projection equality (unit + PG); cross-tenant inventory lists NOTHING (real isolation) | discrimination + PG |
| M23 stale agent version accepted as authoritative | Runtime: the status view's active version follows the CURRENT selection, not the newest version (a published-but-unselected v9.9.9 is not active) | unit agents + PG + discrimination |
| M24 dashboard cache becomes truth | Static: the dashboard source holds no cache/store/registry (the statelessness pattern is scanned — every render reads through the SDK) | discrimination R-M24 |
| M25 SQL/internal errors leak through public API | Static: the unknown-error path serializing internals is detected; runtime: SQL-shaped errors map to the disclosure-free `PROVIDER_ERROR` body (no SQL, no paths, no relation names) | discrimination + secret-safety suite |
| M26 cancellation bypasses execution lifecycle | Static: the cancel route dropping the `command: "cancel"` transition is detected; runtime: cancel goes THROUGH the authority (a terminal execution rejects re-cancel with 409 INVALID_STATE_TRANSITION; the durable row's status changes only through the lifecycle) | discrimination + unit + PG |

## Verification (branch state finalized by this evidence commit)

Environment: Bun 1.3.14, real PostgreSQL 17.11 at `127.0.0.1:55432` (`ZECK_PG_TEST_URL`), the shipped migration set 0001–0008 applied per suite on disposable databases (this branch's base predates WORK-014's unmerged 0009).

| Command | Result |
|---|---|
| `bun install --frozen-lockfile` | clean (runtime deps `["fastify"]` — the sanctioned api transport) |
| `bun run typecheck` | 0 errors |
| `bun run lint` | 0 errors, 0 warnings (497 files — now covering sdk/cli/apps) |
| `python3 scripts/governance-check.py` | exit 0 — `Governance OK: 31 Work Orders, 94 requirements, frontier=['WORK-014']` (WORK-015 in-flight on this branch; WORK-014 in-flight on its own open PR #24 with disjoint surfaces; checker byte-identical to main) |
| `bun run test:unit` | 773/773 (60 files; incl. 82 WORK-015 tests: api routes 43, sdk 8, cli 11 + the pre-existing suites) |
| `bun run test:architecture` | 354 passed + 1 PG-gated skip (40 files; incl. public-surface 6 tests + the two fastify-sanctioned gate updates) |
| `bun run test:integration` | 7 passed + 32 PG-gated skips (33 files) |
| `ZECK_PG_TEST_URL=… bun run test:pg` (real PostgreSQL) | 215/215 (31 files; incl. the 14 WORK-015 PG tests: 9 execution-surface + 5 agent-surface, all through the REAL Fastify HTTP surface over the REAL SQL authorities) |
| `ZECK_PG_TEST_URL=… bun run test` (FULL suite, **twice consecutively at the implementation head `eda15c9`**) | **1350/1350 (133 files) — runs 2–3 exit 0, zero unhandled errors; run 1 fired the documented `57P01` teardown-race unhandled error once (the WORK-010/013/014-documented intermittent in the shared harness teardown — results byte-identical 1350/1350, zero failing tests, not a WORK-015 file)**; pristine-main baseline at the base `814eab0`: 1242/1242 — the delta is +108 tests / +9 files, zero main-inherited failures |

Full-suite counts at the exact final head (after this evidence commit): recorded in § "PR binding" below (the evidence-change rule).

## Checkpoint evidence

Recorded in `spec/development-state/checkpoint-state.json` under `WORK-015` (all four required contracts, verdict `passed`, evidence-file pointers):

- `IMPLEMENTATION-COMPLETENESS` — this file + the unit/api-surface/sdk/cli suites.
- `IDENTITY-IDEMPOTENCY` — the delegated idempotency semantics: same-key replay, same-key-different-fingerprint 409, concurrent duplicate convergence (one durable row) — unit + PG + discrimination.
- `CONCURRENCY-CRASH-SAFETY` — the PG concurrent-creation proof through the real HTTP surface; all durable writes remain authority-transactional (the api layer holds no write surface at all).
- `SELF-HOSTING-BOUNDARY` — the architecture gates (public-surface boundary, dependency-direction with the sanctioned fastify, provider-sdk boundaries) + the 26-mutant discrimination suite.

## Limitations

- The production composition root (constructing SQL stores over the pg `DatabasePort`, wiring the platform secret store for signing secrets) is NOT part of this branch: the executions module's public barrel does not export its SQL fabric, and `src/platform/db/` has no production pg adapter (the harness note: "the production adapter arrives with the Work Order that owns that surface"). The api layer ships as the dependency-injected transport; the tests wire the REAL SQL stores directly (the established test-composition pattern). A production `main.ts` entry needs a composition Work Order or an additive executions-barrel export — an amendment decision for the architect.
- Webhook delivery retry state is in-flight transport state: a durable delivery journal (persisted attempts, dead-letter queues, redelivery windows) belongs to the webhooks module's durable surface, which is outside this Work Order's declared surfaces. The transport semantics (signature, envelope, attempt accounting, bounded retry, receiver guidance) are complete and proven.
- The agent inventory enumeration seam (`listAgentIdsOfApplication`) is injected: the agents module's public registry exposes per-id/slug getters but no bulk listing (ACP-001's catalog surface). The PG world wires it over the real agents table; a registry-level listing method would be an additive agents-module change (an amendment decision).
- The API's route/cost projections read the canonical event ledger; settled cost facts surface only when the executions ledger carries them (the result package reports null cost rather than a fabricated number until the budgets authority settles through its own surface).
- The dashboard is server-rendered HTML over the SDK (no frontend framework — no new runtime dependencies beyond fastify); rich client-side interactivity is future experience-surface work.
- CI runs the architecture/unit/integration suites but not the real-PG suites (the standing WORK-002 flag: no PostgreSQL service in CI); PG proofs are local-verified against real PostgreSQL 17.11 and recorded above.
- Transport authentication is the injected `authenticate` seam (bearer-token authenticator provided); production credential transport (per-application API keys with rotation) is a future auth-surface Work Order — the seam is the frozen contract ("an already-authenticated actor reference").

## PR binding

BOUND — PR **#25** (https://github.com/pectoraux/Zeck/pull/25), head `b215351d9e748b64025513fbc60c8ca187916fe1`, base `814eab0f1a8e7bb5eb64f980c8152e6fbc52128f`. The complete gate was RE-EXECUTED at the exact final head after this evidence commit landed (the evidence-change rule): install clean; typecheck 0; lint 0 (497 files); governance OK frontier=['WORK-014']; unit 773/773; architecture 354+1skip; real-PG 215/215; full suite 1350/1350 twice consecutively (exit 0, zero unhandled errors) — all suites identical to the implementation-head gate (the docs-only change is behavior-neutral). CI on the exact final head `b215351`: workflow run **33392351978** — all three check-runs success (governance 99488722987, toolchain-detection 99488723232, implementation 99488750488). The PR is open, not merged, not approved — the architect is the sole merge authority. Parallel coordination: WORK-014 remains in-flight on PR #24 from the same base with disjoint surfaces.
