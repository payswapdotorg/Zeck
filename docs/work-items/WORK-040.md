# WORK-040 Evidence — Advanced inspection and multimodal work experience

Work Order: `WORK-040` (spec/work-orders/WORK-040.md) · Assurance: **HIGH_ASSURANCE** · Requirement IDs: N/A (dashboard presentation of existing domain/API contracts; the planning, capabilities, tools, deployments, sandbox, economics and executions authorities' ownership unchanged — frozen technical requirement ownership untouched)

Frozen base: `596c90ef91275a64cbef137e78a26c4ea14394da` (main — the WORK-040 dispatch head: WORK-039 complete and merged as PR #70 at `62d954d…`, the governance state at the base OK with 41 Work Orders / 102 requirements / inFlight=['WORK-040'] / frontier=[], exactly as dispatched via issue #71). Branch: `work/WORK-040-advanced-inspection-multimodal-work` · **Final head: this doc's commit** (the house two-phase binding — the exact SHA is recorded by the orchestrator in the PR body; the last code commit, the correction `f6ea5d9`, precedes it) · Zero merge commits; the merge-base is `596c90e…` exactly.

**Dispatch-record note (report-only, never worker-fixed — the W037/W038 lesson)**: the dispatch record itself was consistent (base `596c90e`, branch name, IN-FLIGHT — the Work Order and issue #71 agree exactly; the worker branch was verified 0 commits ahead / 0 behind the dispatch base at start). A LATENT substrate inconsistency was DISCOVERED during the substrate study and fixed WITHIN the dashboard surfaces only: the WORK-037-era `deriveWorkloadFacts`/`eventStageLabel` matched the UNPREFIXED step-event command strings ("checkpoint-recorded") while the real public wire prefixes every step event (`execution.checkpoint-recorded` — the platform's own `eventTypeFor`), so the long-running view would never have lit against the real API (the W037 fixtures mirrored the unprefixed spelling, which is why it was never caught). WORK-040 adds `normalizeStepEventType` — ONE vocabulary accepting BOTH spellings — so the existing W037 fixtures AND the real wire light the same view; no backend file was touched.

## Implementation history (the ratchet — every commit compiles and is suite-green)

1. The implementation commit `6313c4d` — the complete WORK-040 realization:
   - **`apps/dashboard/inspection.ts` (NEW, ~700 lines)** — the ONE advanced-inspection + modality presentation module (Implementation Requirement 1): `inspectionPanel` (AC1: the recorded planning decision — selected approach and rationale FIRST, the effective-policy/capability/sufficiency explanations SECOND, the candidates/substrate/subgraph/integrity internals inside collapsed disclosures, the events/lineage/audit cross-links), `COMPUTER_USE_MODE_LABELS`/`computerUseSection` (AC2: the access-mode envelope deterministic/browser/desktop, the recorded isolation verdict with its zero-inherited-host-state proof, the filesystem/network constraint disclosure with the enforced-platform-side boundary, the approval/risk story BEFORE consequential interaction, the denials rendered verbatim, NO computer-use action anywhere), `deploymentSessionExecutionRows`/`realtimeMessagingSection` (AC3: the Deployment/Session/Execution distinction — three rows, never merged — plus the live session provenance), `mediaSection` (AC4: the asynchronous job lifecycle, digest-only artifact lineage, the verification/retry-cancel state through the run's own status and evidence), `edgeSection` (AC6: the workload-class and substrate evidence, the LOCAL hard-real-time safety boundary sentence, the current-physical-command and local-safety-state honest absences, NO cloud-control affordance), `trainingDetailSection` (AC7: the workload facts, the resource selection and checkpoints as advanced detail, the never-claims-release boundary), `economicAxisRows`/`economicSection` (AC8: the four SEPARATE axes — bounded intent, authorization, settlement, resource/outcome verification — with the execution-bound provenance timeline and the bounded envelope's honest absence), `modalitySections` (AC9: the composition rendering each section ONLY when the run's stream carries that modality's events, every section linking back to the canonical run), `deploymentSessionExecutionSection` (the /deployments extension with the live session evidence from the recents scope).
   - **`apps/dashboard/projection.ts`** (+~800 lines) — the pure derivations: `normalizeStepEventType` (the step-event vocabulary normalization — the real prefixed wire types AND the fixture spellings, ONE vocabulary), `planningDecisionOf` (the LAST `planning.decision-recorded` payload read field-by-field, every field null-safe — taskProfile, policyInputs, capabilityResolution, deterministicSufficiency, candidates with typed admissibility, substrateSelection with resource characteristics and typed inadmissible reasons, recordDigest), `computerUseFactsOf` (the tool-axis payloads' own vocabulary: sessionId/mode/phase/environmentRef/inheritedHostStateCount/deterministicFirst/routeStageCount + the typed denials), `agentSessionFactsOf` (the shared agent-session vocabulary realtime/messaging/media all ride, reading each modality's own payload keys), `mediaFactsOf` (the media vocabulary: generationKind/verificationMode/digests/providerStateLabel/verifiedByAuthority), `trainingFactsOf` (the workload vocabulary: workloadId/workloadKey/workloadKind/attempt + the metrics-digest checkpoints), `economicFactsOf` (the five phases with the economicActionId — the ONLY field the public payload carries), `edgeFactsOf` (the workload-class evidence from the substrate selection). `deriveWorkloadFacts`/`eventStageLabel` now normalize (the prefix fix above).
   - **`apps/dashboard/pages.ts`** — the run page's 4th **Inspection** tab (`?tab=inspection` — additive; the Result view stays the landing view, the default flows unchanged), the contextual modality sections on the run page (computed once, after the long-running block, before the whyPanel), the `/deployments` overview extension (the three-level distinction + the live session evidence read through the governed client from the recents scope). **ZERO new routes, ZERO new nav items, ZERO new POSTs** — the W033 route map, the W035 IA pins and D11's exactly-the-governed-POST-routes pin are all unchanged.
   - **`apps/dashboard/tokens.ts`** — the WORK-040 CSS block: `.modality-section` (+ the mobile 390 rules: single-column timeline, tightened padding) — every rule consuming the existing spacing/surface/border/text tokens; the timeline/distinction/table primitives reused as-is.
   - **`apps/dashboard/shell.ts`** — the ONE honest nav-description update (Deployments: live session evidence now that the surface carries it; labels/paths/mode-visibility untouched).
   - The proof suites (the exact inventory below): the inspection-state unit suite (23), the inspection-presentation unit suite (20), the (an)–(au) journeys (+27), the D23–D26 discriminations (+13).
2. The correction commit `f6ea5d9` — the Architect review cycle on PR #72 (the blocker comment 5549532441): `deploymentsOverviewPage()` swallowed EVERY `listEvents()` failure via `.catch(() => [])`, converting auth/scope/transport/422 failures into a false “no session facts” state (a fail-closed violation). The narrow correction applies the house pattern every other dashboard read already uses: ONLY the 404 absence of an event stream renders as “no session facts” (the run exists — its row is truthfully absent); every other error PROPAGATES to the router's fail-closed surfaces (403 “Not authorized” / 502 “Upstream failure” — never a successful-looking overview). The regression proof: the (av) journeys ×3 through the real dashboard server + the real SDK client + the scope-enforcing fake wire — a 403 auth-class events-read failure renders the 403 surface with NO overview and NO empty-session state; a 500 transport-class failure renders the 502 surface; the normal 404 absence still renders the overview with the TRUTHFUL empty session state. Mutant-verified: reverting to the swallowed catch fails exactly the 403/500 regression tests while the 404-absence test is unchanged under both — the proof has teeth exactly on the defect.
3. This doc (the final head — the second edition: the correction record, the re-run records and the updated inventories; the review cycle is recorded honestly in the branch history, never amended away).

## Correction record (the Architect review cycle on PR #72 — CHANGES-REQUIRED, resolved)

The Architect accepted the implementation as substantially complete but blocked the merge on ONE architectural fail-closed defect (PR comment 5549532441): the `/deployments` overview's live session evidence read caught EVERY `listEvents()` error and rendered the empty projection — a scope/auth/422 failure would therefore publish a successful-looking page while the authoritative read failed (also undermining the claimed application-scope proof: a missing/incorrect scope header could be hidden rather than failing closed). The required correction, applied exactly as specified:

- **`404` ⇒ the honest absence of events** — the run contributes no session facts, the row's absence is truthful (the world's own scope-checked miss);
- **every other error ⇒ propagate / fail closed** — the router's `apiErrorResponse` renders the 403 “Not authorized” surface (auth/scope class) or the 502 “Upstream failure” surface (transport class), exactly like `getExecution`/`getResult`/`listVerification`/`listAgents` reads;
- **the regression proof** — the (av) journeys ×3 (the fake wire gained a `failEventList` facility that fails the scoped events read NON-404 for one id: 403 POLICY_DENIED and 500 PROVIDER_ERROR), pinned on both sides: the non-404 failures are surfaced (never an empty session projection) and the normal 404 absence stays the honest absence;
- **the mutant check** — the original swallowed catch was re-applied as a mutant and run against the (av) block: the 403/500 tests FAILED against the defect (the false 200), the 404-absence test passed under both (the absence semantics are unchanged) — then the fix was restored and the full suite re-run green;
- **the re-runs** — the driven-browser harness was rebooted on the corrected code and the full 143-check drive re-executed (143/143 again, zero console/page errors); the complete gate was re-run TWICE consecutively at the new exact final head (the record below); no other surface, route, POST or test was touched (the surface diff stays EXACTLY the same 10 files — `pages.ts` and the journeys suite were already in it).

## Baseline gate (readiness checkpoint — the exact frozen base, BEFORE implementation)

governance OK (41 WOs, inFlight=['WORK-040'], frontier=[]) · typecheck 0 · biome clean (970 files) · **full suite with real PG = 282 files / 4087 tests, exit 0** (161.09s) at exactly `596c90e` — the WORK-039-era baseline reproduced before any change.

## What changed (the surface diff, every touched file)

Exactly 10 files — 5 under `apps/dashboard/` (4 modified + the NEW `inspection.ts`) + 4 dashboard-local test files (2 NEW unit suites + 2 extended suites) + this evidence document; the count cross-checked against `git diff --name-only 596c90e…HEAD` AND the GitHub PR file list (the W038 correction lesson: the count stated with its explicit breakdown, both sources agreeing); rg-verified — zero files under `src/`, `sdk/`, `cli/`, `scripts/`, `spec/` (incl. development-state — untouched), `migrations/`, `.github/`, or root configs; the count of files outside `apps/dashboard/` + `tests/` + `docs/work-items/` is ZERO. The branch carries FOUR commits — the implementation `6313c4d` → the evidence v1 `cfc1853` → the correction `f6ea5d9` → this evidence v2 (the final head) — the review cycle recorded honestly in the history; zero merge commits:

- `apps/dashboard/inspection.ts` (NEW — the ONE advanced-inspection + modality presentation module)
- `apps/dashboard/projection.ts` (the WORK-040 derivations; the step-event normalization; every existing derivation's semantics preserved — the W037 fixtures prove it)
- `apps/dashboard/pages.ts` (the Inspection tab; the contextual modality sections; the /deployments live session evidence; NO new routes/POSTs; the correction — the /deployments events read is fail-closed: 404-only absence, every other error propagates to the router's error surfaces)
- `apps/dashboard/tokens.ts` (the WORK-040 CSS block)
- `apps/dashboard/shell.ts` (the ONE honest description string; nothing structural)
- `tests/unit/dashboard/inspection-state.test.ts` (NEW, 23)
- `tests/unit/dashboard/inspection-presentation.test.ts` (NEW, 20)
- `tests/integration/dashboard/journeys.test.ts` (118 → 148 — the (an)–(au) journeys + the (av) correction journey + the WORK-040 fixtures with the REAL wire shapes + the (k) frame list extension + the `failEventList` non-404 proof facility)
- `tests/discrimination/dashboard-journey.discrimination.test.ts` (40 → 53 — D23–D26 + the modality/inspection fake-world executions)
- `docs/work-items/WORK-040.md` (this doc)

No migration claimed (the migration inventory is untouched). No new dependency (the driven-browser harness lives OUTSIDE the repository at /home/z/w040-harness; the repository stays zero-dependency).

## Acceptance-criteria mapping (the Work Order's AC1–AC10)

- **AC1** (expert mode exposes plans, capabilities, effective policy, route/provider/model detail, compute substrate, events, lineage and audit without changing default user flows) — the Inspection tab: the recorded planning decision (the real `planning.decision-recorded` payload — the platform's own record) renders the selected approach and rationale first, then the task profile, the effective-policy admission capture (policy set id + version), the capability resolution (catalog revision, satisfied/unmet), the deterministic-first sufficiency decision, then the candidate strategies (typed estimates + admissibility verdicts + route rationales) and the compute substrate (admissible candidates with resource characteristics, typed inadmissible reasons, the selection, the workload class) inside collapsed advanced disclosures, ending with the decision-integrity anchor (the platform's own digest) and the cross-links to the raw events/raw payloads/evidence/lineage/audit views; the route/provider/model detail also rides the existing advanced disclosures; a run without a decision renders the honest absence — nothing fabricated; the DEFAULT flows are unchanged (the Result view stays the landing view — pinned by the (as) journey); pinned by inspection-presentation (the disclosure placement, the absence mutants), inspection-state (the last-decision-wins, the null-safe reads), the (as) journeys ×4, D23 and the harness (all the facts, the collapsed-by-default disclosures, the openable internals, the honest absence).
- **AC2** (computer-use views show explicit Browser/Desktop/Terminal access, filesystem/network constraints, approval requirements and risk before consequential interaction) — the computer-use section: the three access modes in the platform's own vocabulary (deterministic = zero browser/desktop dispatches; browser = isolated browser automation with the platform-side egress allowlist; desktop = isolated desktop and terminal interaction under the declared capability envelope), the filesystem/network constraint disclosure (the declared envelope enforced platform-side; the per-session details do not cross the public wire — stated, never approximated; the recorded isolation verdict IS the public proof: any inherited host state fails closed), the approval/risk story (the governed admission chain — policy, budget, capability — BEFORE consequential interaction; the run's waiting states; a denied session is the recorded proof with the platform's own code and reason verbatim), and NO computer-use action anywhere (read-only by construction); pinned by inspection-presentation (the mode list, the no-form pins), inspection-state (the vocabulary guard, the generic-tool mutants), the (an) journeys ×4, D24 and the harness (the modes, the session row, the isolation verdict, the denial verbatim, the no-form regex over every modality section).
- **AC3** (realtime and messaging surfaces distinguish Deployment, Session and Execution and show activity/provenance contextually) — the realtime/messaging section: the three-level distinction (Deployment = persistent availability, the authority's own projection NOT public yet — no facts invented; Session = the live conversation riding a governed execution, its evidence on the canonical ledger; Execution = the single canonical lifecycle — a session never has its own status vocabulary), the session provenance rows (the starts/turns/completions with the platform's own routing facts: callerRef/participantRef, railCapabilityId, routeClass, plannerOutcome), every row linking back to the canonical run; the /deployments surface gains the same distinction with the LIVE session evidence from the recents scope (each row linking its run), its events read FAIL-CLOSED after the correction (a non-404 failure of the authoritative read is surfaced, never a false “no session facts” projection); pinned by inspection-presentation, the (ao) journeys ×4 (incl. the deployments live evidence with the shared cookie jar), the (av) correction journey ×3 (the fail-closed events read — 403/500 surfaced, 404 the honest absence, mutant-verified) and the harness (the three levels, the provenance, the live rows, the authority absence).
- **AC4** (media-generation surfaces present asynchronous work, artifact lineage, verification and retry/cancel state) — the media section: the asynchronous job lifecycle (job-submitted → dispatched → observation → artifact → job-completed, from the real media payloads), the artifact lineage as DIGEST references only (media content never rides the ledger — stated; the artifact authority carries the live objects), the verification state (the run's own verification results + the verified-by-authority marker when recorded), and the retry/cancel state through the run's own status and the governed lifecycle commands (this dashboard issues neither); pinned by inspection-presentation (the digest-only pins — no img/data:), inspection-state (the media-vocabulary guard — a realtime session is NOT media), the (ap) journeys ×3 and the harness (the lifecycle, the digests, the verification story).
- **AC5** (long-running surfaces communicate phase, checkpoint, progress, spend and resume/recovery state without requiring lease/heartbeat knowledge) — the existing W037 long-running section, now FIXED to light against the real wire: `normalizeStepEventType` accepts the platform's prefixed step types (`execution.checkpoint-recorded`, `execution.resume-recorded`, …) alongside the recorded fixture spellings; the phase = the run's own status, the checkpoint recency = the recorded checkpoint ledger facts, the progress = the chronological timeline, the spend = the recorded settled cost, the recovery state = the resume/interruption/wake-up events; lease/worker/epoch mechanics never cross the public payload and are never shown; pinned by inspection-state (the prefixed normalization pins), the (au) journeys ×3 (the prefixed fixture lights the section; the timeline labels the prefixed events; the unprefixed fixture still lights the same view) and the harness (the prefixed run renders the workload section).
- **AC6** (edge/embodied surfaces show current physical command, local safety state, authorization and last verified state without implying cloud hard-real-time control) — the edge section: the authorization evidence (the governed admission chain — the planning decision's policy/capability captures, linked), the last verified state (the run's verification results, linked), and the current physical command and local safety state as their OWN honest absences (local runtime facts that do not cross the public wire — stated, never approximated), with the boundary sentence the platform's architecture demands: hard-real-time safety authority stays LOCAL — this cloud surface inspects recorded facts and never owns, implies or issues real-time control of the safety loop; NO command, actuation or override affordance exists; pinned by inspection-presentation (the boundary sentence, the honest-absence rows, the no-form pins), inspection-state (the edge/embodied workload-class guard), the (at) journeys ×3, D25 and the harness (the boundary sentence, the absences, the no-form edge section).
- **AC7** (training/accelerator detail exposes resource selection and checkpoints as advanced details while preserving compute/training/evaluation/release distinctions) — the training section: the workload's own recorded facts (workload id, kind, attempt, outcome class, steps completed, settled usage), the resource selection and checkpoints as ADVANCED details (the resource profile lives with the substrate-selection record on the Inspection tab; the training checkpoints render inside the collapsed disclosure with their metrics digests), and the compute/training/evaluation/release distinctions preserved (the W037 four-state list renders on the long-running section; the release row is always the explicit absence; this surface never claims release); pinned by inspection-presentation (the advanced-disclosure placement, the never-claims-release pins), inspection-state (the workload-vocabulary guard — a code-exec sandbox run and a long-running checkpoint run contribute nothing), the (aq) journeys ×3 (incl. the LONGRUN fixture proving a non-training checkpoint run renders NO training section) and the harness (the workload facts, the collapsed checkpoints, the explicit-absence release row).
- **AC8** (economic-action views show bounded purpose/recipient/amount/expiration, authorization result, settlement correlation and independent resource/outcome verification where available) — the economic section: the four SEPARATE axes rendered as distinct rows (bounded intent — the economics authority's own record; authorization — a separate governed decision whose result never crosses this wire; settlement — the external money movement as its own evidence, never a Zeck truth; resource/outcome verification — the verification authority's per-execution checks, LIVE through the run's evidence view), the execution-bound provenance timeline (the `economic-action-*` events with the recorded economicActionId — the ONLY field the public payload carries, exactly the real `journalToExecutionLedger` shape), and the bounded envelope's honest absence (purpose/recipient/amount/expiration do not cross the public execution wire — stated, never guessed; NO client-side payment authorization exists anywhere — the section renders no action); pinned by inspection-presentation (the four axes, the no-money-figure pins), inspection-state (the five-phase timeline, the row-shape pin — phase + id + timestamps ONLY), the (ar) journeys ×3, D26 and the harness (the axes, the timeline, the envelope absence, the no-money-figure regex).
- **AC9** (every advanced view links back to the canonical Work/Execution context) — every modality section and the Inspection tab link back to the run (`/runs/:id`), to the run's own advanced views (activity/evidence), and to the trust/control surfaces (lineage, audit, artifacts); the /deployments live rows link each run; the composition renders ONLY the sections whose facts exist on the run's stream; pinned by inspection-presentation (the link pins), the (an)/(ao)/(as)/(at) journeys and the harness (the link counts on every surface).
- **AC10** (expert and modality-specific views pass tenant, authority, responsive and accessibility discrimination tests) — the (l) scope journeys (unchanged, green through this change), D10/D11/D12 (unchanged — zero new routes/POSTs), the new D23 (expert-inspection fabrication), D24 (modality fabrication), D25 (cloud-control fabrication — the boundary-sentence mutant + the zero-POST runtime), D26 (economic-envelope fabrication); the harness record: one h1 + landmarks on every W040 surface (the frame checks ran on all twelve driven pages), the skip link the first tab stop, focus-visible proven, mobile 390 (the modality sections, the session table, the inspection tab, the collapsed disclosures, the ≥44px touch target) — 143/143 checks, ZERO console errors and ZERO page errors across the whole session.

## The required discriminations (D23–D26, in tests/discrimination/dashboard-journey.discrimination.test.ts)

- **D23 expert-inspection fabrication**: MODULE — the decision fact is the LAST recorded `planning.decision-recorded` payload ONLY (status-derived, lifecycle-derived and empty-input mutants all fail on the same input); STATIC — the guard is the typed `PLANNING_DECISION_EVENT_TYPE` constant (the mutant widening the event type is flagged); RUNTIME — a run with NO planning decision renders the honest absence with NO fabricated candidates/substrate/rationale, and the decision-carrying run renders its own facts.
- **D24 modality fabrication**: MODULE — computerUseFactsOf ignores tool events without the session vocabulary (the generic-tool mutant differs), and lifecycle events never produce computer-use/training/media facts; RUNTIME — a lifecycle-only run renders NO modality sections (zero `modality-section` elements), and the modality run renders EXACTLY its recorded sections (computer-use + economic, nothing more).
- **D25 cloud-control fabrication**: STATIC — the edge section carries the LOCAL-safety boundary sentence and the no-command sentence (the mutant replacing them with cloud-control language is flagged); the modality module renders no form/no POST/no GET form anywhere; RUNTIME — the modality and inspection journeys (run pages + inspection tab + deployments) issue ZERO POST wire calls.
- **D26 economic-envelope fabrication**: MODULE — economicFactsOf reads the five-phase timeline from the recorded events ONLY (lifecycle and empty inputs fail); the row shape is phase + economicActionId + timestamps (there is NO envelope field to fabricate); RUNTIME — the economic section renders the timeline and the honest envelope absence with NO money figure, recipient or expiration value (the regex pins).

## The driven-browser verification record (real Chromium over the real createDashboard server + the scope-enforcing fake world)

The harness (OUTSIDE the repository — /home/z/w040-harness, playwright/Chromium, built for WORK-040) boots the real `createDashboard` against a fake API world implementing the REAL application-scope wire rule (scoped reads without the header ⇒ 422; the create excepted — body-scoped, exactly the real routes' split) plus the WORK-040 modality fixtures — every fixture carrying the REAL public wire shapes (the prefixed step-event types and the real producers' payload keys): a computer-use run (admitted browser session + opened environment + denied desktop session), a realtime run (caller + routed turn), a media run (submitted + artifact + completed with generation kind), a training run (admitted fine-tune workload + metrics-digest checkpoint + completed attempt), an economic run (recorded → authorized → settled), a planning-decision run (the full record), an embodied run (the hardware-isolated realtime substrate) and a real-wire-prefixed long-running run. **143/143 checks passed, ZERO console errors and ZERO page errors across the whole session** (screenshots at /home/z/w040-browser-*.png, the machine-readable record at /home/z/w040-harness/report.json): the Inspection tab (the selected approach, the policy/capability/sufficiency facts, the substrate, the integrity anchor in the collapsed disclosure, the collapsed-by-default advanced disclosures, the openable candidates disclosure, the events/lineage/audit links); the honest absence on a decision-less run; the computer-use section (the three modes, the session row, the isolation verdict, the denial verbatim, the no-form regex over every modality section, the never-issues-action boundary); the realtime section (the three levels, the caller, the routed turn, the link-back counts) and the deployments surface (the distinction, the live session rows, the authority absence); the media section (the lifecycle stages, the input/output digests, the generation kind, the digest-only lineage); the training section (the workload facts, the $2.50 settled usage, the checkpoints in the collapsed disclosure with the digest in the DOM, the explicit-absence release row); the economic section (the four axes, the timeline, the envelope absence, the no-money-figure regex); the edge section (the embodied class, the substrate evidence, the LOCAL boundary sentence, the no-command sentence, both honest absences, the no-form pin); the prefixed long-running view (the workload section + the timeline labels); the lifecycle-only run (zero modality sections; the Inspection tab present); the keyboard discipline (the skip link first, focus-visible); the frame (one h1 + landmarks on all twelve driven pages); and mobile 390 (the modality sections, the session table, the inspection tab, the collapsed disclosures, the ≥44px touch target).

**Disclosed harness limitation (honest, carried over from W038/W039)**: synthesized Enter in this headless Chromium does not activate plain anchors; keyboard navigation is proven through the skip-link-first tab stop, the focus-visible styling, and link-reachability checks; every linked surface is driven and verified in the same session. The repository's own automated proof is unaffected (the journeys drive HTTP directly).

**Correction re-run (the Architect review cycle on PR #72)**: the harness server was rebooted on the corrected code (the correction touches ONLY the /deployments events-read error path — every success rendering is byte-identical) and the full 143-check drive re-executed at the correction head: **143/143 checks passed again, ZERO console errors and ZERO page errors** (the re-run's screenshots/report.json replaced the originals at the same paths — /home/z/w040-browser-*.png, /home/z/w040-harness/report.json).

## The complete gate (run at the final head, TWICE consecutively green)

`python3 scripts/governance-check.py` (OK: 41 WOs, inFlight=['WORK-040'], frontier=[]) · `bun run typecheck` (0 errors) · `bun run lint` (biome clean, 973 files, zero diagnostics/warnings) · `ZECK_PG_TEST_URL=postgres://postgres@127.0.0.1:55432/postgres bun run test` = **284 files / 4173 tests, exit 0** — run TWICE consecutively green at the exact final head (THIS doc's second-edition commit, after the correction; the run-pair record with timestamps is carried in the PR body and the verification comment). The 282/4087 baseline + 2 files / +86 tests vs the base: inspection-state 23, inspection-presentation 20, the (an)–(au) journeys 27, the (av) correction journeys 3, the D23–D26 discriminations 13.

## Test inventory (the exact delta vs the base)

- NEW unit: `inspection-state.test.ts` (23 — the normalization 3, the planning decision 4, the computer-use facts 3, the agent-session facts 3, the media facts 2, the training facts 3, the economic facts 3, the edge facts 2), `inspection-presentation.test.ts` (20 — the inspection panel 3, the computer-use section 2, the realtime section 2, the media section 2, the edge section 2, the training section 2, the economic section 3, the composition 2, the deployments extension 2)
- EXTENDED integration: journeys 118 → 148 ((an) the computer-use journey ×4, (ao) the realtime/messaging journey ×4 incl. the deployments live evidence, (ap) the media journey ×3, (aq) the training journey ×3, (ar) the economic journey ×3, (as) the inspection journey ×4, (at) the edge journey ×3, (au) the real-wire normalization journey ×3, (av) the correction journey ×3 — the fail-closed deployments events read: 403/500 surfaced, 404 the honest absence) + the WORK-040 fixtures (the computer-use/realtime/media/training/economic/decision/edge/prefixed-longrun runs, every payload the real wire shape) + the (k) frame list extension (the inspection tab + all seven modality run pages) + the `failEventList` non-404 proof facility
- EXTENDED discrimination: dashboard-journey 40 → 53 (D23 ×4, D24 ×3, D25 ×3, D26 ×3) + the modality/inspection fake-world executions
- Total dashboard-local: 18 unit files / 347 unit tests + 148 journeys + 53 journey discriminations + 21 surface discriminations (unchanged) = 569 dashboard-local tests

## Required checkpoint contracts

- **SELF-HOSTING-BOUNDARY**: the dashboard still imports ONLY `../../sdk` + node builtins (inspection.ts adds no import outside apps/dashboard + the module's own siblings); the client script performs ZERO network calls (unchanged, D5 of the foundation suite); no new dependency; every fact remains a live SDK read (the inspection and modality facts are all derived at render time from the scoped reads — the recents scope is the disclosed navigation-only cookie, unchanged since the foundation); the transport scope remains the SDK's bound application scope; ZERO new routes and ZERO new POSTs (D11's pin unchanged and green); the only mutations remain createExecution and cancelExecution through the governed SDK client (D11/D12 unchanged and green).
- **EXECUTION-PROVENANCE**: no new platform facts — the planning decision is the platform's own recorded record (rendered field-by-field, never re-derived or re-computed — the digest renders as recorded); every modality fact is the platform's own event payload (the prefixed wire types, the producers' own keys — a payload without the vocabulary contributes nothing); the modality sections project the SINGLE canonical execution ledger (no modality gets a second status language — the run's status IS the state); policy remains the admission boundary (the inspection view renders the platform's own policy capture, never resolves policy); the economic axes remain separate (the bounded envelope, the authorization result and the settlement correlation are the economics authority's own records — the public wire carries the provenance timeline only, stated); edge safety authority remains local (the boundary sentence renders; no cloud-control affordance exists); advanced views inspect existing facts and never manufacture them (D23/D24/D25/D26 prove it by mutants); no visual primitive manufactures inspection or modality facts.

## Required verification mapping (the Work Order's list)

| Required | Evidence |
|---|---|
| governance checker | OK: 41 WOs, inFlight=['WORK-040'], frontier=[] at the base and at the head |
| typecheck | 0 errors at the base and at the head |
| lint (biome) | clean (973 files) at the head |
| dashboard/unit/integration tests | 347 unit (18 files) + 148 journeys + 53 journey discriminations + 21 surface discriminations |
| expert-mode inspection tests | inspection-state's planning-decision pins + inspection-presentation's panel pins + the (as) journeys ×4 + D23 + the harness inspection checks |
| computer-use risk/authorization tests | inspection-presentation's envelope/no-action pins + inspection-state's vocabulary guards + the (an) journeys ×4 + D24 + the harness computer-use checks |
| realtime/messaging/session provenance tests | inspection-presentation + inspection-state's agent-session pins + the (ao) journeys ×4 + the (av) correction journey ×3 (the fail-closed events read) + the harness realtime/deployments checks |
| long-running/training/edge/economic state tests | inspection-state's training/economic/edge pins + the (aq)/(ar)/(at)/(au) journeys + the harness training/economic/edge/prefixed-longrun checks |
| tenant isolation and secret-exposure discrimination | D10 (unchanged, green) + D23/D24/D25/D26 + the hostile-payload escapes (the existing suites, green through this change) + D3/D5 of the foundation suite, unchanged |
| responsive browser verification | the harness record (desktop 1280 + mobile 390: the modality sections, the session table, the inspection tab, the collapsed disclosures, the ≥44px touch target) |
| keyboard/accessibility verification | the harness record (the skip link first, focus-visible, one h1 + landmarks on all twelve driven pages) |
| full suite twice consecutively at exact final head | the complete gate section above |

## Limitations (honest)

1. **The bounded economic envelope is not on the public wire** — the economics authority's own records (purpose, recipient, amount, expiration, the authorization result, the settlement correlation) do not cross the public execution wire: the `economic-action-*` events carry the economicActionId only (exactly the real `journalToExecutionLedger` shape). The economic section therefore renders the four-axis structure with the live provenance timeline and states the envelope's absence — never a guessed value. When the economics authority ships its public projection, its facts feed the same presentation.
2. **The computer-use declared envelope details are not per-session public facts** — the desktop envelope, terminal policy and browser profile live in the platform's session records; the public payload carries the mode, the phase, the environment reference and the isolation verdict. The section renders the recorded facts and states the enforced-platform-side boundary; the isolation verdict (zero inherited host state) is the public proof the environment context failed closed.
3. **The current physical command and the local safety state are local edge facts by design** — publishing them would imply cloud ownership of the hard-real-time safety loop; the edge section renders the boundary sentence and the honest absences instead (the recorded events and the run's verification results are the inspectable history).
4. **The deployment authority remains absent from the public wire** — the /deployments surface renders the three-level distinction with the live session evidence from the recents scope; the authority's own inventory/health/channels/version facts are stated as absent (an execution status never renders as availability).
5. **The realtime/messaging/media session detail is payload-scoped** — the public event payload carries each modality's own keys (caller/participant refs, rail capability, route class, generation kind, digests); the deployments module's richer session records (channel coordinates, pinned plans, journals) are not public. The sections render the recorded facts only.
6. **The driven-browser harness is a session artifact OUTSIDE the repository** (the repository stays zero-dependency); its 143-check record is the evidence (re-run at the correction head — same 143/143); the repository's own automated proof is the 569-test dashboard-local suite (347 unit + 148 journeys + 74 discriminations). The synthesized-Enter-anchor limitation is disclosed above (carried over from W038/W039; the skip-link and focus-visible paths prove keyboard discipline).
7. **The W037 prefix normalization is a dashboard-local compatibility fix** — the real public wire prefixes step events (`execution.<command>`); the W037-era fixtures recorded the unprefixed commands. WORK-040 accepts both spellings through ONE normalization so the existing W037 suites stay green AND the real wire lights the views. No backend file was touched (the report-only disclosure above).
8. **The substrate changes are additive** (the Inspection tab is a 4th tab; the modality sections render only when a run's stream carries their facts; the nav change is one description string); the W033–W039 renderings are unchanged — pinned by the full W036–W039 suites green through this change.
