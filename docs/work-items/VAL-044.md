# VAL-044 — Quality-Adjusted, Latency-Adjusted and Failure-Adjusted Cost

**Work order:** `spec/validation-work-orders/VAL-044.md` (pre-issued by session B at `43a65c0`)
**Authorization:** Tech Lead dispatch (session A claimed the freed slot at `55a3b69` under the first-PR rule with a coordination note; the Lead reviews, re-runs the battery and merges)
**Assurance:** HIGH_ASSURANCE
**Dependencies:** VAL-041, VAL-042, VAL-043 complete
**Dispatch base:** `55a3b69` (the frontier-claim commit "VAL-044 in flight (Lead session A)" on main)
**Implementation head:** the branch head of `work/VAL-044-adjusted-cost` (exact SHA in the merge)
**Mode:** local engine — the slice was built by the Lead's local worker engine in two phases (core / discrimination+crown), and the battery below is the LEAD's own independent re-run over the program's embedded PostgreSQL (the live verification of record)

## Rails (per the work order's design guidance)

- **The offline synthesis corpus needs NO provider rail** — the adjusted-cost semantics under test are ledger-level: the RECORDED arm corpora (VAL-041/042/43, digest-referenced, never copied, never re-measured), the VAL-040 substrate (imported), the REAL recorder and accounting rails, over REAL PostgreSQL (`127.0.0.1:55432`, the program's embedded PostgreSQL 16.4).
- **The Lead's battery RAN the full crown over the program's own PostgreSQL** — `ZECK_PG_TEST_URL` pointed at the program PG; the suite skips cleanly when absent (never a fake success).
- **NOT RUN in this sandbox: the live model rail** — no operator-authorized `OPENROUTER_API_KEY` exists in this environment (session B holds the live credential and completed the live reviews of VAL-032/040/042/043). The live row demands ONE REAL adjusted comparison over a REAL representative slice with every arm priced at its pinned manifest revision. The live machinery surfaces the full requirement honestly; per the Task 65-b engineering note, the gate-OPEN branch fails VISIBLY (digest-disagreed) rather than silently passing until the live-input composition lane exists — the live completion is B's live review with the operator's credential (the VAL-032/041 precedent). Never a fake success.

## Architecture note (within the work order's allowed surfaces)

The work order forbids `platform/**`, `shared/**` and every sibling app's directory — the slice is strictly additive inside `benchmarks/validation/apps/economic-adjusted-cost/**`, mirroring the control-arm layout. The app rides the `ValidationHarness` boundary and imports (never modifies) the VAL-040 substrate (`protocol`/`normalization`/`pricing`/`driver`) and the three arms' recorded corpora.

**The synthesis oracles (all in the app-local driver, PURE):** `deriveQualityAdjustmentHonesty` (the denominator is the VERIFIED quality attainment — a quality-inflated denominator FAILs named with both sides), `deriveLatencyAdjustmentInclusion` (cost weighted by latency-budget compliance — an omitting or short-listed latency set FAILs named; stop-prefix latencies ride in), `deriveFailureAdjustmentCompleteness` (retries and failed attempts fully amortized — a hidden failed-round cost or a zeroed retry-overhead FAILs; NULL never zero), `deriveEstimateMeasureSeparation` (estimates conflated into measured facts FAILs named against the recorded basis), `deriveConfidenceAndMinimum` (Wilson intervals on every comparison; minimum sample sizes enforced; below-minimum claims FAIL; post-hoc arm exclusions and undeclared inputs FAIL), and the input-integrity oracle (every input digest-verified against its RECORDED arm corpus — a re-measurement masquerading as synthesis FAILs field-by-field; a digest-disagreed or unresolvable reference FAILs; an unpinned price revision FAILs). Every adjusted comparison carries the pinned price revisions of its inputs and the pre-registered arm set.

The corpus declares, per row: the recorded arm inputs (digest references into the VAL-041/042/43 corpora + the platform runs), the adjusted family, the pre-registered arm set, the statistical minimums + Wilson config, and the expected comparability verdict. 7 honest rows (the three headline family syntheses — comparable; two honest NULL incomparabilities — zero resolved/attainment pools; the honest stop-prefix; the honest below-minimum REFUSAL where the refusal IS the verified outcome) + 6 adversarial probes pinned as recorded data (quality-inflated denominator, latency omission, failure hiding, estimate conflation, re-measurement masquerade, below-minimum comparability claim) + 1 env-gated live row (honest NOT RUN).

## Changed-file inventory

- `benchmarks/validation/apps/economic-adjusted-cost/driver.ts` (1896 lines) — the six PURE synthesis oracles + the per-row criteria + honest terminals + measured latencies; imports the VAL-040 vocabulary.
- `benchmarks/validation/apps/economic-adjusted-cost/corpus.ts` (496 lines) — the corpus (13 offline rows + 1 live row): the honest syntheses, the probes, the live rail; digest references and arm pins per row.
- `benchmarks/validation/apps/economic-adjusted-cost/fixtures.ts` (410 lines) — the deterministic controlled fakes: the recorded-input fakes with the quality-inflated / latency-omitting / failure-hiding / estimate-conflating / sample-starved / re-measurement variants.
- `benchmarks/validation/apps/economic-adjusted-cost/application.ts` (207 lines) — the synthesis application on the ValidationHarness boundary: submission → poll → retrieve → outcome contract RE-DERIVED → assert; the exported task slice.
- `benchmarks/validation/apps/economic-adjusted-cost/{config.json,README.md}` — the secret-free configuration (consistency unit-tested) and the README.
- `tests/unit/validation/val-044-adjusted-cost.test.ts` — 41 unit tests: the oracle matrices; input integrity; Wilson/minimum enforcement; digest discipline; the driver over every offline row; the adversarial worlds.
- `tests/unit/validation/val-044-apps.test.ts` — 31 unit tests: consistency; well-formedness (digest references resolve; pinned revisions valid; live gating); the app over the honest fake world; each fixture variant FAILs its specific criterion.
- `tests/discrimination/adjusted-cost-validation.discrimination.test.ts` — 35 discrimination tests (AC6): the quality-inflation family (claimed > recomputed FAILs with both sides; at-value passes); the latency-omission family (stripped AND short lists FAIL; the exact weighting control); the failure-hiding family (both decompositions probed); the estimate-conflation family; the below-minimum family (claim FAILs; post-hoc exclusion + undeclared input FAIL; the honest refusal COMPLETES); the input-integrity family (re-measurement field-by-field, digest-disagreed, unresolvable, unpinned pricing); Wilson application; digest discipline; honest controls (7 controls / 6 probes).
- `tests/integration/validation/val-044-adjusted-cost.test.ts` — the crown proof (AC3/4/5/7): the offline synthesis corpus over the REAL platform path over REAL PG; the live rail env-gated on `OPENROUTER_API_KEY` (absent credentials = the recorded NOT RUN boundary).
- `docs/work-items/VAL-044.md` — this evidence document.

**Shared files:** ZERO edits outside `apps/economic-adjusted-cost/**` + the new test files. `src/**`, `spec/**`, `platform/**`, `shared/**` and every sibling app untouched (the delivery diff is 9 files, all NEW).

## REAL end-to-end run — RAN over the program's embedded PostgreSQL 16.4 (the Lead's battery)

The crown suite ran end to end against the REAL platform path over the program's own PostgreSQL (`ZECK_PG_TEST_URL`, `127.0.0.1:55432`). The measured per-row facts (the suite's own console output, from the recorded battery run):

| Row | Family | Inputs verified | Pooled | Measured | Adjusted | Wilson 95% | Verdict | Terminal |
|---|---|---|---|---|---|---|---|---|
| quality-adjusted-three-arm-synthesis | quality | 3/3 | 24r/24x | 957 µ$ | 957 µ$ | [0.862, 1.000] | comparable | **COMPLETED** |
| latency-adjusted-three-arm-synthesis | latency | 3/3 | 24r/24x | 957 µ$ | 42 µ$ | [0.862, 1.000] | comparable | **COMPLETED** |
| failure-adjusted-three-arm-synthesis | failure | 3/3 | 24r/24x | 957 µ$ | 40 µ$ | [0.862, 1.000] | comparable | **COMPLETED** |
| quality-adjusted-null-attainment-incomparable | quality | 3/3 | 18r/0x | 354 µ$ | null | [0.000, 0.176] | honestly-incomparable | **COMPLETED** |
| failure-adjusted-null-resolved-incomparable | failure | 3/3 | 18r/0x | 354 µ$ | null | [0.000, 0.176] | honestly-incomparable | **COMPLETED** |
| latency-adjusted-honest-stop-prefix | latency | 3/3 | 9r/6x | 177 µ$ | 30 µ$ | [0.354, 0.879] | comparable | **COMPLETED** |
| quality-adjusted-below-minimum-honest-refusal | quality | 3/3 | 9r/6x | 177 µ$ | 266 µ$ | [0.354, 0.879] | refused-below-minimum | **COMPLETED** |
| probe-quality-inflated-denominator | quality | 3/3 | 24r/24x | 957 µ$ | 957 µ$ | [0.862, 1.000] | adversarial-failed | **FAILED** (honest) |
| probe-latency-omission | latency | 3/0 | 24r/24x | 957 µ$ | null | [0.862, 1.000] | adversarial-failed | **FAILED** (honest) |
| probe-failure-hiding | failure | 3/1 | 24r/24x | 957 µ$ | 40 µ$ | [0.862, 1.000] | adversarial-failed | **FAILED** (honest) |
| probe-estimate-conflation | quality | 3/0 | 24r/24x | 1008 µ$ | 1008 µ$ | [0.862, 1.000] | adversarial-failed | **FAILED** (honest) |
| probe-remeasurement-masquerade | quality | 3/2 | 25r/24x | 957 µ$ | 997 µ$ | [0.805, 0.993] | adversarial-failed | **FAILED** (honest) |
| probe-below-minimum-comparability-claim | quality | 3/3 | 9r/6x | 177 µ$ | 266 µ$ | [0.354, 0.879] | comparable (pinned claim) | **FAILED** (honest; the below-minimum oracle) |

The six probe rows are adversarial data pinned in the corpus: their runs FAIL mechanically with the named criterion, and the observed-terminal read-back agrees.

**Battery-level synthesis integrity (the whole offline corpus, over the REAL SQL):** 7 COMPLETED + 6 honest FAILED of 13 driven rows; every input DIGEST-VERIFIED against its recorded arm corpus (3 inputs per synthesis, all verified on the honest rows; the masquerade probe's third input FAILs field-by-field); the ledger holds exactly **13 executions + 13 idempotency records** (zero phantoms, zero drift, zero orphan events); every `execution_events` sequence gapless; pinned syntheses reproduced exactly (measured/estimated/attainment/adjusted/Wilson/latency-compliance); app passed on every row (the honest expected terminal IS the row's own); usage honestly none-reported offline.

**NOT RUN boundary (recorded honestly):** the live row demands ONE REAL adjusted comparison over a REAL representative slice with every arm priced at its pinned manifest revision (`OPENROUTER_API_KEY`, pinned model/rail/rev-001, temperature unset per the provider's documented default, max_tokens 64, measured facts composed through the REAL recorder); no operator-authorized credential exists in this environment, so the row is NOT RUN — never a fake success. Per the Task 65-b engineering note: the gate-OPEN branch fails VISIBLY (digest-disagreed) until the live-input composition lane exists — the live completion is session B's live review with the operator's credential (the VAL-032/041 precedent: offline delivery + B's live review completion).

## Acceptance-criteria mapping

| AC | Evidence |
|---|---|
| 1. Public SDK boundary, secret-free config, executable tests; per row: the recorded arm inputs (digest references), the adjusted family, the expected comparability verdict | the app (ValidationHarness boundary), `config.json` (consistency unit-tested), the corpus module (13+1 rows with the full oracle per row), the unit suites |
| 2. Deterministic fixtures; recorded arm results digest-referenced (never copied); every input's pinned price revision verified against the VAL-040 manifests | `fixtures.ts`; the input-integrity oracle (digest-verified recorded inputs; unpinned pricing FAILs) unit- and discrimination-tested |
| 3. REAL end-to-end (env-gated) over the REAL platform path: one REAL adjusted comparison, measured facts, verdict through the REAL recorder | `val-044-adjusted-cost.test.ts` — **RAN** over the program's embedded PostgreSQL (the measured table above); the live row is the env-gated NOT RUN boundary |
| 4. Mechanical verification per row: quality-attainment honesty, latency inclusion, failure amortization, estimate/measure separation, confidence and minimum-sample enforcement — probed adversarially | the six driver oracles — unit- and discrimination-tested incl. every named adversarial shape (35 discrimination tests) |
| 5. Honest economics and latency; payload digests in evidence | the per-row table (measured/adjusted µ$ facts, Wilson intervals, latencies); FNV-1a digests — payload bytes never journaled |
| 6. Discrimination tests against controlled fakes (quality inflation, latency omission, failure hiding, estimate conflation, below-minimum) | 35 discrimination tests covering exactly the five named families + the input-integrity family + honest controls |
| 7. Evidence document records REAL outcomes, timings, costs, NOT RUN boundaries and the executed battery honestly | this document |

## Executed battery (the Lead's independent re-run at the implementation state, sequential, over the program's embedded PostgreSQL)

Baselines measured by the Lead on the dispatch base `55a3b69` (main including the merged VAL-041): unit 4987 / arch 1907 / integration 1193+20.

| Command | Outcome | Detail |
|---|---|---|
| `bunx tsc --project <scoped benchmarks+tests>` | pass | exit 0 — a superset of the delivery's entire import graph (verified at phase 1 by the Lead and at phase 2 by the worker); the full-repo invocation was SIGKILLed by the sandbox's documented memory flake (the dev-server infrastructure holds ~1.4GB of the 4GB box — required services). CI runs full-repo tsc on GitHub runners and is the binding full check; the main baseline is type-clean (CI green at `ef69ee9`) and the delivery's delta is benchmarks+tests only |
| `bunx biome check . --diagnostic-level=error` | pass | ZERO errors (full-repo, CI parity) |
| `bun run test:unit` | pass | 302 files / 5059 tests (**+72** vs the 4987 baseline — exactly the VAL-044 unit suites: 41 driver + 31 apps) |
| `bun run test:architecture` | pass | 1942 tests (**+35** vs the 1907 baseline — exactly the VAL-044 discrimination suite) |
| `bun run test:integration` | pass | 153 files passed + 1 skipped / 1195 tests passed + 20 skipped (**+2** vs the 1193+20 baseline — exactly the VAL-044 crown; exit 0, no flakes this run) |
| `python3 scripts/governance-check.py` | pass | `Governance OK: 60 Work Orders, 110 requirements, inFlight=[], frontier=[]` |

## Defects found

None in the delivered surfaces. The six honest-FAILED probe rows are the pinned adversarial data working as designed. Two development-time type errors were caught and fixed before commit (one `LatencyFact` source literal by the Lead in phase 1; one possibly-undefined spread by the worker in phase 2) — the direct-exit-code discipline (the Task 60 lesson) catching what a piped check would have masked.

---

## Live-review addendum — 2026-09-15 (post-close): the live row's genuine delivery defect, found, repaired and live-verified

The post-close live-review lane (R-1 actioned — the operator-authorized
`OPENROUTER_API_KEY` provisioned 2026-09-15 ~02:39 UTC, after the program's
2026-09-14T22:26:00Z completion at `28adb03`) drove this work order's live row
for the first time anywhere (the lane's battery 02:56–03:09 UTC + two disclosed
re-runs). It FAILED — a genuine delivery defect, recorded here:

- **The finding:** `live-adjusted-synthesis-real-comparison` failed fast
  (~300 ms of driving; ZERO provider dispatches — the route hypothesis
  exonerated: the text rail was healthy all session) with the named
  criterion `input-integrity-digest-verified` FAIL — DIGEST-DISAGREED ×3
  (direct `3af163c7` ≠ `6a447122`; optimized `f5a3fd21` ≠ `631383f4`;
  competing `f462552c` ≠ `6a9bfd48`) plus the consequent
  `below-minimum-refusal-honesty` and `row-outcome-contract` FAILs; driver
  failure category `input-integrity-failed`, verdict `adversarial-failed`,
  deterministic ×3.
- **Root cause:** the row declared `needsDispatch: true` but NO dispatch
  seam existed in the crown binding — the VAL-041/042/043 precedent
  (needsDispatch rows bind a REAL dispatch executor) was not followed, so
  the integrity oracle re-derived digests over the live arm rows'
  empty/placeholder traces; as delivered the row could NEVER COMPLETED.
- **The repair (commit `bcc49a1` on `work/live-review-val-044-repair`, from
  main `28adb03`):** the new `apps/economic-adjusted-cost/live-rail.ts`
  binds the three arms' REAL dispatch seams (the 041/042/043 precedent
  split — test-side gateway, app-side `driveLiveArmsAndRecord` recording
  the measured traces into the arm corpora); only the val-044 integration
  test changed alongside (offline rows byte-for-byte unchanged; the honest
  NOT RUN skip path untouched).
- **The live verification of record (~03:35–03:45 UTC):** 11 REAL dispatches
  on the pinned rail `meta-llama/llama-3.3-70b-instruct`, measured **111 µ$**
  total, arm wallclock 11340 ms — direct 4r/4x, 40 µ$, digest `38ef7c3a`;
  optimized 4r/4x over 3 dispatches + 1 REAL cache hit, 31 µ$, digest
  `bcadcded`; competing 4r/4x, 40 µ$, digest `9a1360d2` — every digest
  oracle-verified over the RECORDED arm facts. The row then derived
  mechanically: `live-adjusted-synthesis-real-comparison -> COMPLETED
  (quality-adjusted)` — inputs=3 verified=3, pooled 12r/12x (attainment
  1.000000), measured=adjusted=111 µ$, Wilson 95% [0.757, 1.000], latency
  62 ms. (A first confirmation run measured 105 µ$ over the same 11
  dispatches; both live runs disclosed — $0.000216 total.)
- **Scoped battery at the repair head:** unit 72/72 (counts unchanged — the
  offline surface untouched); integration with key + PG 2/2 GREEN;
  honest-skip without key 2/2 GREEN (env var named — never a fake success);
  neighbors VAL-041/042/043 6/6 with all live rows COMPLETED (the shared
  seams intact); adjusted-cost discrimination 35/35; tsc EXIT 0; biome clean.
- **Honest classification:** a delivery defect caught by the live-review
  lane — the F-2 pattern (the credential-bearing live review as the merge
  authority; Recommendation R-7) working as designed, post-close. The
  offline corpus of record above is unchanged; the branch awaits the
  Lead's push/PR/review (never self-declared merged).

**Coexistence note (added 2026-09-15 post-rebase):** PR #119 — the session-B
review-fix "live-rail drivers for VAL-050/051/052", merged 2026-09-14T23:36:06Z onto
`origin/main` (`4341726`, parent `28adb03`) — had ALREADY repaired the VAL-050/051/052
crowns' welded-shut env-gates before this lane's window (those crowns never read
`OPENROUTER_API_KEY`, so their live rows were undriveable even with a credential): the
SAME defect class as the no-dispatch-seam live row repaired above — a needsDispatch
live row that could never dispatch — both F-2 pattern, disjoint apps, found
independently by the two parallel post-close live-review sessions. This lane's battery
honestly ran at stale `28adb03` (no fetch before the battery; the lane's fetch guard
caught the divergence at the Lead closeout before any push — disclosed in the
VALIDATION-REPORT post-close addendum's coexistence note). The branch is now rebased
onto `4341726` (conflict-free; the repair replayed as `fdfec67`, the docs as `95cefec`;
merge-tree pre-proof tree `ca23b92`); the live row re-verified COMPLETED at the rebased
head on-key (11 REAL dispatches, 111 µ$ measured, arm digests `eb933651` / `7cff6755` /
`9a1360d2` over the fresh measured traces; neighbors VAL-041/042/043 green on-key 6/6
and off-key 8/8 with this WO); and this lane additionally drove session B's deferred
VAL-050/051/052 live rows on-key at the rebased head — all three COMPLETED (their
evidence addenda carry the measured facts).
