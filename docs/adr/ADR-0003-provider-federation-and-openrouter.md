# ADR-0003 — Provider Federation and OpenRouter

**Status:** Accepted

OpenRouter and comparable aggregation/gateway systems are upstream infrastructure, not the product abstraction. The platform provides provider adapters and can use OpenRouter as one supply rail while retaining direct and customer-hosted provider paths.
