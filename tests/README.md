# Test Layout

Tests are separated by proof purpose. `architecture/` rejects illegal dependency/authority shapes; `discrimination/` proves weakened protections fail; `integration/` exercises real authority boundaries; `unit/` covers local behavior.
