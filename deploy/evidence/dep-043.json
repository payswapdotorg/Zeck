{
  "schemaVersion": 1,
  "workOrder": "DEP-043",
  "title": "Production readiness, rollback and provider-exit drill — verification evidence record",
  "recordedBy": "DEP-043 implementation worker (this session)",
  "environment": "sandboxed worker pod (no cloud credentials); git worktree /home/z/my-project/Zeck, branch work/DEP-043-production-drills at base e50fe7e54b094d676c68ff878e7fda3ec095f5c3 (origin/main HEAD, DEP-040 merged); THREE REAL local PostgreSQL servers (theseus-rs user-space portable builds, trust auth, credential-less URLs): 17.11 on 127.0.0.1:54333 — the drill driver's OWN rail (ZECK_PG_ADMIN_URL / ZECK_DRILL_PG_ADMIN_URL), 16.15 on 127.0.0.1:54331 — the standard-suite battery rail (ZECK_PG_TEST_URL; 16 because the base tests/integration/postgres/pg-database-port.test.ts pins the server major at 16), 17.11 on 127.0.0.1:54329 — the DEP-040 e2e-driver suite rail (ZECK_E2E_PG_ADMIN_URL) — battery-separated so the drill's final teardown (which DROPS zeck_local) never races the other suites",
  "date": "2026-09-20",
  "doctrine": "Every operation that could not run in the worker pod is listed NOT RUN with its owner. Nothing in this record claims a result that was not executed. ONE driver (deploy/production-drill.ts) composes the four DEP-043 drills over the delivered tools as REAL subprocesses (every plane a real deploy/api.ts process over real HTTP, boot-document waits, refusal-reason pinning — structurally the DEP-040 driver pattern, whose URL-hygiene preflight and boot-document detector it REUSES); the live-provider halves (Neon, Cloudflare R2/Queues/Workflows, Upstash, Vercel, a real runner daemon, an OTLP collector) have no credentials here — every live half is recorded NOT RUN with its owner, never fabricated or assumed-pass.",
  "notRun": [
    {
      "check": "Live-provider promotion/rollback rails: promote/rollback with --plane-url against a real hosted production plane (Vercel-hosted deploy/api.ts + Neon authority) over the public internet",
      "reason": "No cloud credentials and no live hosted deployment exist in this environment. The promotion identity core and the rollback re-attestation are verified end to end against real local plane processes over real HTTP (both directions + the wrong-revision/unreachable/tampered refusals) over the REAL local release ledger; the live-hosted promotion is the credentialed owner's boundary.",
      "owner": "Lead credentialed re-run (deploy/PUBLIC-DEPLOYMENT.md §2 exit paths + §9)"
    },
    {
      "check": "Live relational-state exit: restore the authoritative backup into a real replacement provider (Neon Launch / any managed PostgreSQL) over the public internet",
      "reason": "No replacement-provider credentials exist in this environment. The backup/restore round-trip ran FOR REAL against the local PostgreSQL server (the adapter is provider-neutral over the wire protocol — connection URLs only); the live replacement-restore is the credentialed owner's boundary.",
      "owner": "Lead credentialed re-run (deploy/PUBLIC-DEPLOYMENT.md §2 exit paths + §9)"
    },
    {
      "check": "Live artifact-bytes exit: the real R2 → alternate object-store byte migration (deploy:drill artifact-exit with adopted artifacts over two real S3-compatible endpoints)",
      "reason": "No R2/alternate-store credentials and no adopted-artifact data exist in this environment; the local environment defines no operative S3-compatible endpoint BY DESIGN (local artifacts are filesystem-scoped). The tool's fail-closed configuration refusal was driven for real (exit 2, the exact NOT RUN reason — never a fabricated PASS); the live migration is the credentialed owner's boundary.",
      "owner": "Lead credentialed re-run (deploy/PUBLIC-DEPLOYMENT.md §2 exit paths + §9)"
    },
    {
      "check": "Live async-transport exit: the bounded republish through the real transport adapter against a live broker (Cloudflare Queues or the replacement broker)",
      "reason": "No queue transport credentials exist in this environment. The replay-plan classification (the authority-side half of the exit — every dispatch envelope classified, fail closed on drift) ran FOR REAL against the local authority; the tool's own not-run registry carries the republish half verbatim.",
      "owner": "Lead credentialed re-run (deploy/PUBLIC-DEPLOYMENT.md §2 exit paths + §9)"
    },
    {
      "check": "Live durable-orchestration exit: terminating and re-arming real Cloudflare Workflows provider instances against a live replacement engine",
      "reason": "No Cloudflare Workflows credentials exist in this environment. The authority-side exit machinery (the waits table as the authority, compaction, the recovery scan re-arm) ran FOR REAL over the local PostgreSQL with SYNTHETIC provider configuration (empty waits substrate → zero provider calls, recorded as such in the driver report); the live provider half is the credentialed owner's boundary.",
      "owner": "Lead credentialed re-run (deploy/PUBLIC-DEPLOYMENT.md §2 exit paths + §9)"
    },
    {
      "check": "Live ephemeral-coordination exit: repointing at a real managed Redis-compatible replacement (Upstash or alternate)",
      "reason": "No Upstash/managed-Redis credentials exist in this environment. The drop-it exit posture is proven live on the local substrate (the plane operates with NO Redis — coordination degraded-but-alive, its declared first-class state); the replacement repoint is configuration-only by doctrine and is the credentialed owner's re-verification.",
      "owner": "Lead credentialed re-run (deploy/PUBLIC-DEPLOYMENT.md §2 exit paths + §9)"
    },
    {
      "check": "Live experience-delivery exit: a live alternate delivery host over the public internet (the real Vercel exit / alternate web host)",
      "reason": "No live host and no transport credential for one exist in this environment. The repoint proof ran FOR REAL locally: two planes on distinct endpoints attest byte-identical identity documents through the same PostgreSQL authority (hosting-independence is by construction — the document carries no host fields); the live-hosted repoint is the credentialed owner's boundary.",
      "owner": "Lead credentialed re-run (deploy/PUBLIC-DEPLOYMENT.md §2 exit paths + §9)"
    },
    {
      "check": "Live execution-compute exit: a real alternate runner daemon implementing the documented REST protocol",
      "reason": "No runner daemon exists in this environment. The exit posture is proven live on the local substrate (/health attests the compute concern's honest degraded state with its declared execution-compute-unavailable mode; the runner is substitutable and the control plane stays alive); the live alternate-runner execution is the credentialed owner's boundary.",
      "owner": "Lead credentialed re-run (deploy/PUBLIC-DEPLOYMENT.md §2 exit paths + §9)"
    },
    {
      "check": "Live observability-export exit: a live OTLP collector endpoint",
      "reason": "No OTLP collector exists in this environment. The logs-only degraded mode is the declared operating state and is proven live (the plane boots and serves with zero observability configuration); the live endpoint substitution is configuration-only and is the credentialed owner's re-verification.",
      "owner": "Lead credentialed re-run (deploy/PUBLIC-DEPLOYMENT.md §2 exit paths + §9)"
    },
    {
      "check": "Live provider meters and receipts (Neon / Cloudflare / Upstash dashboards) for the drill's live halves",
      "reason": "No cloud credentials in this environment. Zero live cost was consumed (no live-provider operation was executed); the credentialed re-run owns the live meter parity and the verbatim cost receipts.",
      "owner": "Lead credentialed re-run (deploy/PUBLIC-DEPLOYMENT.md §2 exit paths + §9)"
    },
    {
      "check": "CI execution of the full suite on the branch",
      "reason": "Workers do not push and do not open PRs; CI runs on the Lead's merge.",
      "owner": "Lead merge + CI (.github/workflows)"
    }
  ],
  "verifiedLocally": [
    {
      "check": "Typecheck",
      "command": "bun run typecheck",
      "evidence": "exit 0 (tsc --noEmit), 0 errors at the branch head (including the new deploy/production-drill.ts, the deploy/drill.ts DEFECT-C fix, and both new test files)"
    },
    {
      "check": "Lint (house baseline, no NEW violations)",
      "command": "bun run lint",
      "evidence": "68 warnings / 8 infos / 0 errors, exit 0 — the recorded baseline exactly; the formatter errors biome initially reported on the new files were fixed with biome --write before this run; zero diagnostics remain on deploy/production-drill.ts, deploy/drill.ts, tests/unit/deployment/production-drill.test.ts or tests/integration/deployment/production-drill.test.ts"
    },
    {
      "check": "Unit suite",
      "command": "bun run test:unit",
      "evidence": "Test Files 349 passed (349); Tests 6156 passed (6156); 0 failures. Base e50fe7e unit was 348 files / 6135 tests; the +1 file / +21 tests are exactly tests/unit/deployment/production-drill.test.ts (the driver's plan/boundary/provider-exit contract: the four-drill plan pinned step by step incl. both-directions ordering; the provider-exit coverage cross-checked against the REAL provider-tiers.json through the repository's own parser; the NOT-RUN registry completeness + ownership; the digest-comparison drift matrix; the tamper construction keeping original checksums; the truncation/wrong-format constructions; the reused e2e disciplines; the DEFECT-C source pin)"
    },
    {
      "check": "AC1 — the promote→verify→rollback drill, both directions, journaled (over the REAL local PostgreSQL release ledger)",
      "command": "the drill driver's promote-verify-rollback segment (deploy/production-drill.ts d1-* steps: release.ts record/gate/promote/rollback as real subprocesses; real planes over real HTTP)",
      "evidence": "record bound the checkout revision + the deployment identity (identity.gitRevision == HEAD); gate run validation passed; promote REFUSED on the wrong-revision plane (exit 1, 'deployment identity verification failed before promotion: identity revision bbbb… does not match…', journaled allowed:false), on the unreachable plane (exit 1, '…is unreachable (fail closed)') and on the TAMPERED plane (a git archive of HEAD plus ONE well-formed variables.json row — the tampered tree still passes deploy:validate; exit 1, '…runtime identity id does not recompute at this revision/manifest/tier state (drift or tampering)'); promote on the verified plane: exit 0, promoted=true, planeIdentity.verified=true, attestedRevision=HEAD e50fe7e, runtimeIdentityId bc7e26aa… (the journal carries the pre-promotion attestation); rollback drill with the base release (c8eb89d8, a manifest-identical ancestor resolved by the driver's ancestor walk, recorded from a real git worktree, gate-passed): pre-repoint (plane still serving HEAD) → the pointer flip happens + exit 1 with the exact honest instruction 'post-rollback re-attestation failed: identity revision e50fe7e… does not match the expected revision c8eb89d8… (repoint the plane at the target release and re-run)'; re-promote then post-repoint (a real plane booted from the base worktree attesting c8eb89d8) → exit 0, planeIdentity.verified=true, attestedRevision=c8eb89d8 — both directions proven"
    },
    {
      "check": "AC2 — the backup/restore round-trip with digest verification and fail-closed partial-failure behavior",
      "command": "the drill driver's backup-restore-roundtrip segment (deploy/backup.ts + deploy/restore.ts as real subprocesses against the real local PostgreSQL authority)",
      "evidence": "backup: 121 authoritative tables / 32 rows / 33-migration history in the artifact (per-table sha256 content checksums); restore into the fresh disposable target (--drop): exit 0, allTablesVerified=true, tablesRestored=121, rowsRestored=32, verification method 'per-table re-read + sha256 content checksum + row count (deterministic)'; the SECOND backup of the same store carries IDENTICAL per-table digests (digest stability — equivalent state); the three corrupt-artifact negatives each REFUSED with the exact reason: wrong-format (well-formed JSON, foreign format) → exit 2 'the artifact is not a zeck-logical-backup v1 manifest (refusing to restore)'; TRUNCATED (cut mid-JSON at 55%) → exit 1 'error: JSON Parse error: Unterminated string' with NO disposable recovery target created (verified by pg_database round trip — never a partial restore); DATA-TAMPERED (one row dropped from release_control.active_deployments, checksums left original) → exit 1 'error: restore verification failed for 1 table(s): release_control.active_deployments (row counts or content checksums do not match the backup)' + the retained-target disclosure; the driver then dropped the retained diagnosis target (recorded) and swept its scratch"
    },
    {
      "check": "AC3 — the provider-exit drill covers EVERY manifest provider class with honest local verification or NOT RUN boundaries with owners",
      "command": "the drill driver's provider-exit segment + the report's providerExitCoverage (derived from the REAL provider-tiers.json at runtime)",
      "evidence": "all 8 manifest classes covered: relational-state (drill 2's executed backup→restore round-trip — the documented exit path, provider-neutral adapter); artifact-bytes (deploy:drill artifact-exit driven → its own fail-closed configuration refusal, exit 2 'requires the alternate object-store configuration… NOT RUN without it — never claimed as PASS' — the local environment defines no operative S3 endpoint BY DESIGN); async-transport (deploy:drill queue-recovery executed against the real authority: transportPlan items=0 complete=true, the tool's own notRun registry carrying the republish half verbatim; recovered=true, rpoMs=0 — the DEFECT-C fix, objectives measured, exit 0); durable-orchestration (deploy:workflow inspect/scan/recover/compact executed over the real authority — the waits table IS the authority, compaction + the recovery scan re-arm — with SYNTHETIC provider configuration recorded as such, zero provider calls on the empty substrate); ephemeral-coordination (the plane runs with NO Redis — /health attests coordination degraded-but-alive with its declared coordination-degraded mode: the drop-it exit posture, live); experience-delivery (two real planes on distinct local endpoints — 127.0.0.1:35295 and 127.0.0.1:46261 on the recorded run — attest BYTE-IDENTICAL /identity documents, no hosting coordinates in the raw document text, the same runtimeIdentityId bc7e26aa… as the promoted plane, both serving through the same PostgreSQL authority: the hosting-independent repoint proof); execution-compute (/health attests the compute concern degraded with its declared execution-compute-unavailable mode, control plane ready, plane NOT down — the runner-substitutable posture, live); observability-export (the plane boots and serves with ZERO observability configuration — logs-only is the declared operating state; substitution is endpoint configuration only); the domain authority proven UNTOUCHED by the whole exit segment (backup-3 per-table digests identical to the pre-segment fingerprint); every live half in the notRun registry with the Lead owner"
    },
    {
      "check": "AC4 — teardown classification guards refuse ambiguous classifications under drill conditions (tested)",
      "command": "the drill driver's teardown-guards segment (deploy/teardown.ts as real subprocesses, incl. two manifest-mutated scratch trees)",
      "evidence": "staging teardown REFUSED exit 3 ('teardown refused — environment \"staging\" is class \"persistent\"… protected by classification'); production teardown REFUSED exit 3 (the same guard); the AMBIGUOUS classification (a scratch tree whose environments.json carries local class disposable + teardownAllowed=false — a class/teardown-policy contradiction) REFUSED at manifest load BEFORE any destruction (exit 1, 'invalid deployment manifest set (1 problem(s)): … disposable environment \"local\" must allow teardown', nothing dropped/removed); the RECLASSIFIED environment (a coherent class persistent + teardownAllowed=false for local) REFUSED at the guard itself (exit 3, '…is class \"persistent\"'); the local teardown with a dead PG endpoint: the provisioned records are removed first (pure local artifacts) and the PG drop FAILS CLOSED (exit 1 'error: connect ECONNREFUSED…', no 'dropped' claim — never pretends); the REAL local teardown: exit 0 — provisioned records removed, 'dropped (if present): zeck_local', the object-store root already-absent/removed; post-teardown round trip (pg client against the same authority): zeck_local verified GONE"
    },
    {
      "check": "AC5 — defects: reported with reproduction, root-cause classification, fix within boundaries, regression pin",
      "command": "the drill development run log",
      "evidence": "ONE chain-tooling defect found and fixed WITHIN deploy/'s own boundaries (the full report under notes): DEFECT-C (deploy/drill.ts objective gate) — reproduction: 'bun deploy/drill.ts queue-recovery --environment local' against a healthy migrated authority → all phases green, drill.recovered=true, transportPlan complete, the tool's own notRun registry honest — and exit 1, because the D-07 objective evaluation counted 'RPO was not measured (scenario lacks a recovery-point anchor)' as a breach for a scenario that carries NO recovery-point anchor by construction (queue-recovery, artifact-exit and worker-evacuation all set lastConsistentPointAt=null) — the contradictory gate no environment could ever pass (all phases green + recovered:true + exit 1), invisible to every recorded battery because no test ever drove these three commands as subprocesses. Root cause: the anchor-less scenarios were left in the objective-measuring set without the anchor their evaluation contract requires. Fix (within deploy/drill.ts only): the durability-anchored scenarios (queue-recovery, artifact-exit, worker-evacuation — their recovery point is the authority's durable state at drill start, RPO 0 by durability: the dispatch envelopes, artifact digests and worker claims ARE the authority's durable state) now carry the startedAt anchor — the SAME anchor class authority-loss uses (the backup-creation instant). Regression pins: the drill driver's d3-queue-recovery step (exit 0 + recovered=true + objectives measured rtoWithinTarget/rpoWithinTarget — the contradiction can never return silently) and tests/unit/deployment/production-drill.test.ts's DEFECT-C source pin. authority-loss/authority-failover semantics untouched (their anchors unchanged); no src/ file touched. NO defect found in the release/backup/provisioning authorities themselves: every refusal under the hostile negatives was the exact fail-closed reason (the tools never crashed, never half-mutated, never downgraded a failure to a warning)."
    },
    {
      "check": "AC6 — governance, architecture, integration, the full suite and deploy:validate stay green (existing pins in sync)",
      "command": "the phase-2 battery (run after checkpoint 1; this entry refreshed at the delivery state)",
      "evidence": "SEE the refreshed entries below (architecture / integration house + both-railed / full suite / governance / deploy:validate) — recorded after the phase-2 runs at the delivery head; the numbers in the final record are the single real runs of record"
    },
    {
      "check": "The drill driver's plane-boot discipline (the known race class) + the repeated-run pin",
      "command": "ZECK_DRILL_PG_ADMIN_URL=postgres://postgres@127.0.0.1:54333/postgres bunx vitest run tests/integration/deployment/production-drill.test.ts",
      "evidence": "5/5 tests pass: the full-drill-fact test (every AC fact asserted from the driver's report — totals 40/40, revision equality, both-direction promotion/rollback facts, the round-trip + digest facts, the three corrupt-artifact refusal reasons, the provider-exit facts incl. the byte-identical repoint identity and the untouchability fingerprint, the teardown facts, the notRun registry, the 8-class coverage with liveOwner on every entry) + the plane-boot race pin: THREE consecutive full drill runs, each exit 0 + valid=true (measured 9882ms / 10313ms / 10271ms — the driver waits for the plane's BOOT DOCUMENT on stdout, reused from the DEP-040 driver, never a bare /health 200; the 2-of-3 flaky class cannot hide) + the two ungated preflight negatives (no ZECK_PG_ADMIN_URL → exit 2 with the exact reason; a credential-carrying URL → exit 2 URL-hygiene refusal)"
    },
    {
      "check": "The drill driver's own recorded run (the evidence-of-record run over the real local rails)",
      "command": "ZECK_PG_ADMIN_URL=postgres://postgres@127.0.0.1:54333/postgres bun deploy/production-drill.ts",
      "evidence": "exit 0, valid=true, problems=[] — 40 steps: 27 positives passed + 13 negatives each REFUSED with its exact reason; durationMs=9947 (startedAt 2026-09-20T11:26:39.942Z, finishedAt 11:26:49.889Z). Drill timeline (per-step durations in the report): rails 288ms (validate 71 / bootstrap 61 / provision 75 / migrate 81) → promote-verify-rollback 1229ms (record 94, gate 106, refused-wrong-revision 102, refused-unreachable 103, refused-tampered 102, promote-verified 124, record-base 149, gate-base 108, rollback-prerepoint-refused 104, re-promote 128, rollback-postpoint-verified 109) → backup-restore-roundtrip 3468ms (backup 404, restore 1297, backup-2 408, digest-comparison 2, wrong-format-refused 48, truncated-refused 48, tampered-refused 1233, retained-target-cleanup 28) → provider-exit 966ms (queue-recovery 100, workflow-inspect 76, scan 73, recover 83, compact 93, artifact-exit-refused 82, delivery-repoint 4, exit-postures 6, fingerprint-backup 447, fingerprint-comparison 2) → teardown-guards 429ms (staging 67, production 63, ambiguous 68, reclassified 65, dead-pg 72, teardown-real 90, post-teardown-verify 4). The negatives' exact exit codes: 10 × exit 1 + 2 × exit 2 + 3 × exit 3 across the 13 hostile steps. facts.liveCostConsumed = zero; facts.authorityUntouched = true; facts.zeckLocalDropped = true"
    }
  ],
  "notes": [
    "DEFECT-C (the full report — AC5): REPRODUCTION: ZECK_PG_ADMIN_URL=<healthy local rail> bun deploy/drill.ts queue-recovery --environment local (any environment, any time) → stdout: all phases ok, drill.recovered=true, transportPlan complete, the honest notRun registry; exit code: 1 — because evaluateDrillAgainstTarget counts 'RPO was not measured (scenario lacks a recovery-point anchor)' as a breach and deploy/drill.ts's exit gate turns any breach into exit 1. The same holds for artifact-exit and worker-evacuation (both leave lastConsistentPointAt null) — these three commands could NEVER exit 0 in ANY environment, fully credentialed included (a gate that can never pass). Invisible to every recorded battery: no test drives these three commands as subprocesses (the architecture suite only string-checks the command names; the integration suites drive authority-loss and authority-failover, which DO set anchors). CLASSIFICATION: deploy-tooling defect (the objective-evaluation contract applied to anchor-less scenarios), NOT a chain defect and NOT src/ (the runner's rpoMs=null and the evaluator's breach list are both internally correct — the CLI composition was wrong). FIX (within deploy/drill.ts only — no src/ change, no new authority): the durability-anchored scenarios (queue-recovery, artifact-exit, worker-evacuation) now pass lastConsistentPointAt=startedAt — the same anchor class authority-loss uses — because their recovery point IS the authority's durable state at drill start (RPO 0 by durability: the dispatch envelopes, artifact digests and worker claims are the authority's durable state). Measured after the fix: queue-recovery exit 0, recovered=true, rtoMs within target, rpoMs=0, rpoWithinTarget=true. Trade-off considered and rejected: filtering the not-measured breach at the exit gate — that would keep reporting rpoMs=null (an unmeasured-looking claim) while the durable-state semantics are real and measurable; the anchor is the honest fix. REGRESSION PINS: the drill driver's d3-queue-recovery step (exit 0 + objectives measured) and the unit-test source pin (tests/unit/deployment/production-drill.test.ts, 'the DEFECT-C fix' describe block). MERGE NOTE for the Lead: artifact-exit and worker-evacuation inherit the same fix (not driven green here — artifact-exit's full run needs adopted artifacts + two real S3 endpoints, worker-evacuation needs a populated compute plane; both are credentialed-rail boundaries recorded in notRun).",
    "The drill's durable-orchestration steps (deploy:workflow inspect/scan/recover/compact) run the AUTHORITY-side machinery with SYNTHETIC provider configuration: the account id and API token are runtime-generated random values (never printed, never literals), the workflow name is the drill's own label, and on the empty waits substrate the engine makes ZERO provider calls (every provider interaction is per-row: arming, recovery, compaction and observation all iterate store rows; inspect's describeLimits is static metadata). This is recorded as facts.workflowSyntheticConfig in every driver report — never presented as a live-provider verification.",
    "The provider-exit drill's delivery-repoint proof boots TWO real planes on distinct local endpoints; the /identity documents are byte-identical AND the raw document text contains neither plane's URL nor any loopback coordinate — the hosting-independence invariant re-proven live (pinned statically by tests/unit/deployment/repoint.test.ts, re-asserted here against real served documents).",
    "Battery-config separation (the DEP-040 doctrine, extended to three rails): the drill driver rides ZECK_PG_ADMIN_URL (its own config; the integration suite gates on ZECK_DRILL_PG_ADMIN_URL), the standard PG suites ride ZECK_PG_TEST_URL, and the DEP-040 e2e-driver suite rides ZECK_E2E_PG_ADMIN_URL. The drill's final teardown DROPS zeck_local on its configured server — a DEDICATED third instance (54333) prevents the fixture-race class the base DEFECT-B recorded (bootstrap-smoke vs release-cli sharing one server).",
    "The known base defects DEFECT-A (audit-schema stale 0032-era pin) and DEFECT-B (bootstrap-smoke vs release-cli zeck_local fixture race) are PRE-EXISTING, recorded by DEP-040, NOT re-investigated and NOT fixed here; if the both-rails battery shows exactly their failure shapes, they are recorded as known and moved past (see the integration entry).",
    "MERGE NOTES for the Lead: new files deploy/production-drill.ts, tests/unit/deployment/production-drill.test.ts, tests/integration/deployment/production-drill.test.ts, deploy/evidence/dep-043.json (this record), PUBLIC-DEPLOYMENT.md §11 (the drill-section extension); MODIFIED deploy/drill.ts (the DEFECT-C fix ONLY — the durability anchors; nothing else in the file changed). No src/**, apps/dashboard/**, sdk/**, examples/** or spec/platform-delivery-state/** file touched; deploy/manifests/** byte-identical to base; no package.json script added (the driver runs as 'bun deploy/production-drill.ts'). The docs/ ledger row for DEP-043 is the Lead's to add (outside this worker's writable surface).",
    "Zero live cost consumed: no live-provider operation was executed from this environment (the notRun registry is the recorded boundary)."
  ]
}
