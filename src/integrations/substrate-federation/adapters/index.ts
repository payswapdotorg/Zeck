/**
 * `substrate-federation` integration adapters. No vendor SDK exists at
 * v1 — external operators arrive with WORK-027..030 behind the neutral
 * seam.
 */
export { InMemorySubstrateOperator } from "./in-memory-operator";
