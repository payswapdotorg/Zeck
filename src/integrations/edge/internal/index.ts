/**
 * Edge integration internal surface marker (WORK-029).
 *
 * `internal/` is never imported from outside this integration. The
 * integration is wired by composition roots through `public.ts` only
 * (the WORK-016 integration discipline).
 */
