/**
 * Application layer barrel of the compatibility integration: the proof
 * orchestration (assess / correlate / build) and the Demo Mirror
 * registry binding (status always DERIVED from evidence records).
 */
export * from "./compatibility-service";
export * from "./demo-registry";
