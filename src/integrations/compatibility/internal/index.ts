/**
 * Internal barrel of the compatibility integration — never imported
 * from outside `src/integrations/compatibility/` (the
 * internal-never-cross-module rule). Reserved for internals that may
 * accrete as the layer evolves; currently empty by design.
 */
export {};
