{
  "applicationId": "run-time bound (the served world provisions it)",
  "baseUrl": "run-time bound (the served API address)",
  "tokenEnvVar": "ZECK_VALIDATION_TOKEN",
  "applicationRevision": "repository revision at run time",
  "corpusRevision": "repository revision at run time",
  "integrationSurface": "sdk",
  "pollIntervalMs": 50,
  "completionTimeoutMs": 300000,
  "tasks": [
    { "kind": "voice-loop", "session": "dlg-001", "interrupt": "none" },
    { "kind": "voice-loop", "session": "dlg-002", "interrupt": "after-turn-1" },
    { "kind": "voice-loop", "session": "dlg-003", "interrupt": "stale-worker" },
    { "kind": "voice-loop", "session": "dlg-004", "resume": "after-terminal" },
    { "kind": "voice-loop", "session": "dlg-005", "interrupt": "corrupt-checkpoint" }
  ]
}
