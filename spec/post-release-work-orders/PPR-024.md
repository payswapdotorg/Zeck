# PPR-024 — Browser Use Zeck-Complete Application Proof

Status: PRE-AUTHORIZED / DEPENDENCY-GATED
Depends: PPR-023
Target: Browser Use

Pin an exact Browser Use revision. Prove the model/intelligence plane and browser actuation plane separately and together.

A model routed through Zeck alone is insufficient. Where browser actions are materially delegated to Zeck, prove the browser substrate is represented by the neutral tool/substrate execution contract and its lifecycle/evidence. Where no real browser rail exists, remain BLOCKED/NOT-RUN rather than converting fixtures to PASS.

Run the full ACR-006 battery and Demo Mirror flow.