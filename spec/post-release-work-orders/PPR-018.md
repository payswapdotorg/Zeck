---
# PPR-018 — Aider Zeck-Complete Application Proof

Status: AUTHORIZED / READY FOR TECH-LEAD DISPATCH
Program: zeck-post-release-public-productization
Authorization date: 2026-09-26
Preferred concurrency: 1 worker
Target: Aider coding assistant

## Objective

Produce the first real external application proof that Aider can remove its direct AI-provider execution infrastructure and operate with Zeck as its sole AI execution authority for the declared representative corpus.

## Integration strategy

Pin an exact Aider upstream revision.

Prefer Aider's existing model abstraction/LiteLLM seam rather than forking broad Aider internals.

Create only the smallest compatibility adapter required to delegate every material AI execution edge to Zeck.

The integration must cover every AI call Aider makes for the declared tasks, including auxiliary/summarization/weak-model paths that are actually active.

Aider may retain repository, filesystem and Git domain operations. Those are not direct AI-provider execution and must remain outside the Zeck-completeness criterion unless they themselves invoke an AI service.

## Proof

Run the complete PPR compatibility battery:

- edge inventory;
- provider credential removal from Aider runtime;
- direct-provider egress block;
- representative coding corpus;
- Zeck trace correlation;
- duplicate/retry/failure validation;
- cost per successfully resolved outcome versus direct and optimized non-Zeck baselines;
- customization test;
- deterministic/reuse opportunity measurement;
- telemetry inspection;
- no-bypass audit.

Do not claim COMPLETE if any material AI path remains direct, provider-specific, fixture-only or unverified.

## Website demonstration

Register Aider in the Demo Mirror only with the evidence status produced by the compatibility framework.

The demo must run the actual pinned Aider integration or its exact application runtime, not a synthetic coding response.

Expose the real task result and its Zeck execution trace.

## Evidence

Create deploy/evidence/ppr-018.json with exact upstream revision, integration revision, execution graph, AI edge coverage, Zeck executions, direct-provider egress result, quality/failure/economic measurements, and limitations.

## Boundaries

No architecture change.
No alternate execution authority.
No direct provider credential fallback hidden behind a feature flag.
No demo pass based on a partial Aider workflow.
