---
# PPR-012 — The Governed Dependency Advance (fastify + pg — the evo-002 deferred candidates)

**Status:** OPEN / WAVE F (authorized at the evo-005 eligibility review — spec/evolution/runs/evo-005-wave-f-eligibility.json)
**Program:** zeck-post-release-public-productization (successor continuation — the governed evolution program)
**Base:** current governed main at dispatch (the wave-F authorization head or later — never dispatch from a stale base; fetch the actual head)
**Origin:** the evo-002 OBSERVE scan detected the in-range advances (fastify ^5.12.1 with 5.12.5 latest; pg 8.16.3 with 8.23.0 latest); the evo-004 scan recorded them UNCHANGED — a bump is a governed IMPLEMENT step, never auto-promoted at autonomy level 2. This order is that governed step.

## Requirement 0 — the fresh zero-drift re-verification (BEFORE any change)

Re-query the registry LIVE (never from memory): fastify + pg latest versions, licenses, and the release ladder between the installed and the latest (the registry packument or `npm view <pkg> versions`). Record the facts in the evidence record. If the latest moved since the evo-005 authorization, advance to the CURRENT latest (in-range for fastify's ^; pg's exact pin advances) and record the fact — never a stale target.

## Requirements

1. **The advance**: fastify → the current in-range latest (the ^5.12.1 range already admits it — the lockfile's resolution advances; package.json's range stays unless the latest exceeds it, in which case it advances minimally and records why), pg → the current latest (the exact pin in package.json advances). One clean DEPENDENCY-ONLY diff: package.json + bun.lock; ZERO source changes expected. If the battery surfaces a breaking change, STOP: record the exact failure honestly and fall back to the highest green version (the evidence records the ladder honestly).
2. **The RESEARCH record**: the release notes' substance between the installed and advanced versions for BOTH packages (from the packages' own changelogs/releases — the zero-drift rule): what changed, and anything touching the repository's usage surface (fastify's server registration/hooks; pg's Client/pooling/types).
3. **The battery** (the worker's scope): typecheck 0; lint EXACT baseline; unit green; the targeted deployment suites (`tests/unit/deployment/`) green; governance OK; deploy:validate valid=true. The PG-rail integration + the FULL suite on the advanced dependency set is the LEAD's binding merge gate (the standing rule — a database driver's real evidence is the real rail).
4. **The evidence record** (NEW `deploy/evidence/ppr-012.json`): the fresh registry facts (versions, licenses, the ladder), the diff summary (the two dependencies, lockfile only), the battery numbers, the honest notRun registry (the PG-rail full battery — owner: the Lead's merge gate; the live-plane redeploy — owner: the Lead's wave-F redeploy).

## Verification

Requirement 3's battery, with exact numbers in the evidence record.

## Completion

One branch `work/PPR-012-dependency-advance`, one commit (or a small clean series), pushed + a PR opened (the worker never merges; the Lead reviews, batteries and merges). File ownership: package.json + bun.lock + the evidence record ONLY (if a test pins an exact version, survey and update the pin additively — record it); no other surface.
---
