---
# PPR-014 — The GAP-006 Seam Bindings: the Economics + Codebase-Analysis Authorities over the Preview Materialization

**Status:** AUTHORIZED / WAVE G (the evo-008 eligibility review, 2026-09-25)
**Program:** zeck-post-release-public-productization (successor continuation — the governed evolution program)
**Base:** current governed main at dispatch (the wave-G authorization head or later — never dispatch from a stale base; fetch the actual head)
**Origin:** GAP-006 ("economics and codebase-analysis seams unbound on the preview plane", spec/evolution/gap-ledger.json): the bootstrap composition's two remaining `unboundCapability` stubs — `economics` and `codebaseAnalyzer` (deploy/api.ts) — answer the honest 422 `CAPABILITY_UNAVAILABLE` with the composition split recorded in PPR-008's boot document. PPR-008's design guidance deliberately scoped them out ("do not over-bind — the 7 credentialed journey steps only exercise the executions surface"); the gap ledger's target state has always been "bound authorities when their work orders are issued (the materialization-gate pattern)". The post-release roadmap (PPR-001..013) is closed end-to-end; the evo-007 OBSERVE confirmed the plane healthy with zero credential-free drift remaining. **Lead decision (recorded in the evo-008 eligibility review):** this is that work order. Both families bind through the SAME materialization gate PPR-008 established — zero new environment variables, zero manifest changes (the PPR-013 precedent: derive from the existing materializations) — with the REAL module authorities over the relational `DatabasePort`, honestly labeled, no model behind either (the analyzer is deterministic by construction; the economics bounded authorization is the governed WORK-032 boundary; the payment rail is the in-repo simulated rail, honestly disclosed).

## Pre-implementation reconciliation (REQUIRED — read before writing code)

- `deploy/preview-authorities.ts` — PPR-008's authority materialization module (THE pattern: the environment-contract reader, the `PreviewAuthorities` shape, `buildPreviewAuthorities`, the runtime-idempotent seeding gate, `deterministicPreviewUuid`).
- `deploy/api.ts` — the composition root: lines binding `economics: unboundCapability("economics")` and `codebaseAnalyzer: unboundCapability("codebaseAnalyzer")`; the boot document's truthful composition-fact strings (both must be updated truthfully when the new authorities bind).
- `deploy/preview-substrate.ts` — the honest-labeling precedent for deterministic preview machinery.
- `tests/integration/postgres/economics-world.ts` — the canonical economics SQL wiring (NOTE its own comment: its policy/capability admission are test-scoped ALLOWING FAKES that pin the chain order; the preview binding must use the REAL adapters — see Requirement 3).
- `tests/integration/postgres/budgets-world.ts` — the canonical budgets SQL wiring (`SqlBudgetStore` + `SqlBudgetsIdempotency` + `createBudgetService`, migration 0003's real ledger).
- `tests/integration/postgres/learning-opportunities.test.ts` + `codebase-analysis.test.ts` + `api-world.ts` — the canonical analyzer wiring (`createOpportunityAnalyzer` over `SqlOpportunityStore` + the node digest; the api-world's full-route composition).
- `src/modules/economics/public.ts` — the module's ONLY supported import surface (the authority map comment: policy → policies, capabilities → capabilities, budget → budgets' `BudgetAuthority`, executions → the canonical ledger's `recordStepEvent`, verification → the delivery-evidence consumer).
- `src/modules/economics/adapters/policy-economic-admission.ts` + `capability-economic-admission.ts` — the REAL admission adapters (wrap the policies authority's `admitDispatch` and the capabilities registry).
- `src/modules/learning/public.ts` + `src/modules/learning/application/opportunity-analyzer.ts` — the analyzer contract: `OpportunityAnalyzerDeps` (store, digest, generateId, now), `AnalyzeSubgraphRequest` (the MANDATORY `executionId` — "Analysis is an Execution — the route composes the authorities, M2/M26"), the deterministic finding/prompt construction.
- `src/integrations/payment-rails/adapters/simulated-rail.ts` — the in-repo deterministic rail (`createSimulatedPaymentRail`); NO external payment provider ever.
- `src/api/server.ts` lines ~218/~235 — the frozen consumption seams (`economics: deps.economics`, `analyzer: deps.codebaseAnalyzer`); `src/api/routes/economic-actions.ts` + `codebase-analysis.ts` — the frozen route shapes.
- `spec/architecture.md` §6 + ADR-0018 (WORK-032's governed economic-action boundary) — the authority map's governing law.
- `deploy/evidence/ppr-008.json` — the evidence-record pattern; `deploy/PUBLIC-DEPLOYMENT.md` — the append-only public documentation surface.

## Objective

When the preview environment materializes its existing authority set (the SAME gate: `ZECK_DATABASE_URL` + `ZECK_TRANSPORT_TOKEN` + `ZECK_PREVIEW_APPLICATION_ID` — nothing new), the bootstrap composition binds the REAL economics and codebase-analysis authorities over the same relational `DatabasePort`: the economic-action service (SQL store + idempotency + the REAL policy/capability admission adapters + the REAL budgets authority + the executions ledger seam + the in-repo simulated payment rail, honestly disclosed) and the opportunity analyzer (the deterministic analysis over the SQL opportunity store). Their routes serve real wire shapes instead of 422s. Local/dev shapes are EXACTLY today's unbound behavior (pinned both ways). Zero new environment variables; `deploy:validate`'s variable count does not move.

## Requirements

1. **The economics authority** (extend `deploy/preview-authorities.ts`): construct over the materialized `DatabasePort` — `SqlEconomicStore` + `SqlEconomicsIdempotency` (migration 0014; idempotency arbitration on `platform.idempotency_records`) + `createEconomicActionService` with EVERY dependency REAL: policy admission through `createPolicyEconomicAdmission` wrapping the real policies authority; capability admission through `createCapabilityEconomicAdmission` wrapping the real capabilities registry; the budgets authority (`SqlBudgetStore` + `SqlBudgetsIdempotency` + `createBudgetService` — the budgets-world wiring); the executions ledger seam (the already-bound execution service's `recordStepEvent`); the in-repo `createSimulatedPaymentRail` as the payment rail (honestly labeled — the preview never touches an external payment provider). If a required real authority cannot be wired through the modules' PUBLIC surfaces, STOP and record the constraint honestly in the evidence (do NOT edit `src/**`, do NOT fake an admission seam — the integration world's allowing fakes are test-scoped by their own design and are NEVER a preview form).
2. **The codebase-analysis authority** (same module): `createOpportunityAnalyzer` over `SqlOpportunityStore` + the learning module's node digest + the shared uuidv7 generator + the clock. The route's executionId admission flows through the ALREADY-BOUND executions authority (the composition supplies the analyzer; the route composes the authorities — no second admission path).
3. **The composition binding** (`deploy/api.ts`): when the preview authority set is materialized, bind both real seams; otherwise EXACTLY today's `unboundCapability` shapes (both pinned by tests). The boot document's composition-fact strings update truthfully (economics + codebase analysis move from "honestly unbound" to the materialized description — derive, never hardcode).
4. **Runtime-idempotent seeding**: the preview application's funded developer wallet (the budgets ledger the economic actions reserve against — create-if-absent on every cold start, never duplicate, safe under concurrent isolates; the seeded amount recorded honestly in the evidence). Any additional required seed rows (e.g. the capabilities catalog entries the admission resolves) follow the same law.
5. **The proofs**: unit — both composition shapes (materialized vs unbound), the seeding idempotency (run twice → same rows), the honest labels (the simulated rail's disclosure surfacing in the boot document or the route's honest response shapes wherever the module's contract already carries provenance); integration on the real PG rail (`ZECK_PG_TEST_URL`) — the full chains: create economic action → the admission order (policy → capability → budget reserve → issuance → rail charge → budget settle) on the real ledgers; analyze subgraph (over a real executions-authority execution) → findings + evaluation prompts recorded → the read-seam signals project. The journey route-probes' auth-boundary expectations for these routes must remain true (401 without a credential — binding changes the AUTHORIZED shape, never the boundary).
6. **The evidence record** (NEW `deploy/evidence/ppr-014.json`): the design decisions (the zero-new-env derivation, the real-admission mandate, the simulated-rail disclosure, the wallet seeding design), the proof numbers, the honest notRun registry (any live-Neon/live-Vercel boundary — owner "Lead credentialed run"; the plane-side activation — owner "the Lead's §13.6 redeploy chain after merge").
7. **The documentation** — `deploy/PUBLIC-DEPLOYMENT.md` APPEND: the two newly-bound capability families, their honest shape (deterministic analysis; governed bounded authorization over the simulated rail — no external payment provider, no model behind either), the env contract (unchanged — the same materialization set).

## Verification

typecheck 0 errors; lint EXACT baseline; unit green (the new proofs); the integration suites on the real PG rail green (the economics + budgets + learning worlds' own suites UNTOUCHED and green — they are read-only reconciliation subjects); the full suite EXIT 0; governance OK; `deploy:validate` valid=true with ZERO manifest delta (97 variables — the zero-new-env law's machine check). The Lead's independent battery on the merged tree is the binding gate.

## Completion

One branch `work/PPR-014-gap006-seam-bindings`, one commit (or a small clean series), pushed + a PR opened (the worker never merges; the Lead reviews, batteries and merges). File ownership: `deploy/preview-authorities.ts` + `deploy/api.ts` (the binding extensions), NEW test files, `deploy/evidence/ppr-014.json`, the PUBLIC-DEPLOYMENT APPEND. It does NOT touch `src/**` (import the modules' public surfaces; record constraints, never edit), the frozen route shapes, `spec/**` (Lead-only), another order's surfaces, or the manifests (zero delta).
