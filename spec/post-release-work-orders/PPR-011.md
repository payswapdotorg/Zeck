---
# PPR-011 — The Redirect-Following Landing Audit (GAP-004's recorded residual)

**Status:** OPEN / WAVE F (authorized at the evo-005 eligibility review — spec/evolution/runs/evo-005-wave-f-eligibility.json)
**Program:** zeck-post-release-public-productization (successor continuation — the governed evolution program)
**Base:** current governed main at dispatch (the wave-F authorization head or later — never dispatch from a stale base; fetch the actual head)
**Origin:** GAP-004 (spec/evolution/gap-ledger.json): the bare-root landing answers a 307 redirect, not HTML — the platform's filesystem-phase fact (the framework's root function owns '/'); the harness's redirect:manual discipline records the honest not-run. The gap's own target state names THIS work order: "a redirect-following landing audit as its own harness-contract work order". The newcomer's real landing surface — what a browser actually renders after the bridge — has never been audited.

## Pre-implementation reconciliation (REQUIRED — read before writing code)

- `tests/journey/journeys.ts` — `journeyDiscover`: the reachability step (`auditSurface("/")`); the discover-landing step's two shapes (an HTML landing ⇒ the dimension audit; a non-HTML answer ⇒ the honest not-run owned by `OWNERS.leadPostDeployment`). The redirect-following extension slots EXACTLY here.
- The harness's auditSurface machinery + the dimensions model (survey `tests/journey/` — the surface audit, the console-error dimensions, `titleOf`) — the landed surface gets the SAME audit a direct HTML landing gets.
- The redirect:manual discipline (GAP-004's record; correction #8's landing facts) — every hop is fetched with `redirect: "manual"`; the chain is recorded hop by hop; NOTHING is auto-followed by fetch.

## Objective

The harness's discover journey follows the plane's own redirect bridge (bounded, same-origin) and audits the surface the newcomer actually lands on — the 307 platform bridge DISCLOSED in the evidence, never masked; the landing itself finally audited (title, console errors, the audited dimensions).

## Requirements

1. **The redirect-chain follower** (in the journey harness): when the root answers a redirect status (301/302/307/308) with a Location header, follow the chain manually — one auditSurface-class fetch per hop with `redirect: "manual"`, SAME-ORIGIN targets only, a hard budget of 3 hops (a named constant, e.g. `MAX_LANDING_REDIRECT_HOPS = 3`). Each hop's status + resolved location is recorded in the step's evidence chain.
2. **The landed-surface audit**: an HTML terminal gets the EXACT existing discover-landing treatment (the dimension audit + `titleOf`); the step's observed discloses the chain (e.g. "307 / -> /console (1 hop); HTML landing served (title: …)").
3. **The honest terminal shapes**: (a) a chain terminating in non-HTML ⇒ today's honest not-run, extended with the chain facts; (b) a redirect LOOP or a chain exceeding the hop budget ⇒ a FINDING (defectClass "landing-chain", severity major — a real browser fails to land too; the viableSolutions name the route-chain fix); (c) a CROSS-ORIGIN hop ⇒ an honest not-run (reason: the chain leaves the plane's origin; cross-origin following is out of audit scope; owner: the Lead).
4. **The fixture-matrix proof** (NEW test file, placement per the repository's convention — survey `tests/integration/journey/`): a local fixture server serving six scenarios — direct HTML; 307→HTML; 307→307→HTML (within budget); a redirect loop; a chain exceeding the budget; a cross-origin hop — the harness's follower asserted against each. No live plane needed; the LIVE re-run is the Lead's MEASURE step at the wave's redeploy.
5. **The evidence record** (NEW `deploy/evidence/ppr-011.json`): the PPR-009/010 evidence format — verifiedLocally with EXACT numbers (the fixture matrix, the harness-e2e run); the notRun registry owning the live-plane re-run (owner: the Lead's wave-F redeploy + journey re-run); notes recording the budget/same-origin design decisions.

## Verification

- typecheck 0 errors; lint at the EXACT baseline (3 errors + 68 warnings + 8 infos — zero new); the unit suite green; the journey harness e2e (`tests/integration/journey/harness-e2e.test.ts`) green on the PG rail (the targeted-file run is the worker's; the FULL integration battery is the Lead's binding merge gate per the standing rule); `bun run governance:check` OK; `bun run deploy:validate` valid=true (no manifest changes — the audit reads no new environment).
- The fixture matrix green; the existing discover journey's two shapes (direct HTML landing; non-HTML not-run) UNCHANGED (both pinned by the existing tests — zero regression).

## Completion

One branch `work/PPR-011-landing-audit`, one commit (or a small clean series), pushed + a PR opened (the worker never merges; the Lead reviews, batteries and merges). File ownership (the conflict partition): the worker owns the journey-harness files it extends + the NEW fixture test + the evidence record + the PUBLIC-DEPLOYMENT.md APPEND (§13.7's landing-audit addendum); it does NOT touch the deployment pipeline, another order's surfaces, or the governance records.
---
