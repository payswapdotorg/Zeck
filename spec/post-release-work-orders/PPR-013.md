---
# PPR-013 — The Composition Auto-Rebind Policy (PPR-010's explicitly-deferred residual)

**Status:** OPEN / WAVE F (authorized at the evo-005 eligibility review — spec/evolution/runs/evo-005-wave-f-eligibility.json)
**Program:** zeck-post-release-public-productization (successor continuation — the governed evolution program)
**Base:** current governed main at dispatch (the wave-F authorization head or later — never dispatch from a stale base; fetch the actual head)
**Origin:** PPR-010's substitution drill proved the SEAM-level substitutability (the same durable coordinates + the same stable rail-level idempotency keys replayed across rails after primary refusal, converging exactly-once per rail with identical neutral shapes) and recorded the COMPOSITION-level auto-rebind policy as its honest residual: "a preference order + automatic failover inside the composition touches the frozen admission path and can mask failures — a future governed work order". This is that order, taken WITH the frozen-seam guarantee.

## Pre-implementation reconciliation (REQUIRED — read before writing code)

- `src/modules/deployments/adapters/livekit-realtime-rail-binding.ts` (`bindEnvironmentRealtimeRail`) + `socketio-realtime-rail-binding.ts` (`bindEnvironmentSocketIoRail`) — the two materialization-gate bindings (the pattern to compose; each fail-closed on its own gate).
- `tests/conformance/realtime-rail/rail-substitution.drill.test.ts` — the drilled convergence this policy rests on.
- `src/modules/deployments/application/realtime-session-service.ts` — the FROZEN admission ordering (POLICY → CAPABILITY → BUDGET → SECRET MEDIATION → adapter). The policy lives BELOW this seam: the session service still calls ONE `RealtimeRail`.
- `src/modules/deployments/ports/realtime-rail.ts` — the port's shapes (FROZEN). The rebind rail is a port-level composition — ZERO shape changes; the serving rail's own `railCapabilityId` IS the honest disclosure (no shape change needed).

## Objective

A composition-level rail preference + bounded failover behind the SAME port: when multiple real rails materialize, the composition binds a preferred order; a RETRYABLE normalized failure on `openSession` may re-bind to the next materialized rail UNDER THE SAME COORDINATES AND THE SAME STABLE IDEMPOTENCY KEYS (the drill's proven convergence — exactly-once per rail); the acknowledgment honestly carries the rail that served.

## Requirements

1. **The rebind binding** (NEW `src/modules/deployments/adapters/realtime-rail-rebind-binding.ts` or the established naming): composes the existing materialization gates — the preference derives from the environment (the LiveKit set materializes ⇒ preferred; the socket.io set ⇒ alternate; NOTHING materializes ⇒ EXACTLY today's simulated shape — the terminal fallback, pinned both ways). NO new environment variables, NO manifest changes (derive from the existing rails' materializations).
2. **The bounded failover**: per-invocation, on a RETRYABLE normalized failure from the preferred rail's `openSession`, at most ONE re-bind (preferred → alternate) under the same coordinates + keys; the alternate's answer returns through the SAME neutral shapes; NON-retryable failures (authentication, not-found) NEVER re-bind (an auth failure is not a capacity event — masking it would violate the honest-failure discipline); `deliverTurn`/`transferCall`/`closeSession` follow the bound rail (no mid-session flapping — a session lives on the rail that opened it; the honest boundary recorded).
3. **The frozen-seam guarantee**: the session service, the port, the admission ordering — UNTOUCHED (the binding is the only new surface; the composition consumers bind the rebind binding instead of a single rail — additive where the rails already wire; survey `deploy/api.ts` + the adapters barrel first and mirror the existing wiring pattern; if no composition consumer binds a rail today, ship the binding + its proofs and record the wiring point honestly).
4. **The proofs** (unit; port-level stubs are legitimate for the POLICY — the policy codes against the port contract, and the rails' own realism is already proven by their conformance subjects): preference resolution (both materializations, one, none); retryable-open failover converges under the same keys with the alternate's `railCapabilityId` disclosed; non-retryable never re-binds; the simulated terminal fallback EXACTLY today's shape; no mid-session flapping; OPTIONAL (if cheap): the substitution drill's scenario extended through the POLICY seam with the honest label.
5. **The evidence record** (NEW `deploy/evidence/ppr-013.json`): the design decisions (the preference order, the one-rebind bound, the no-flapping rule), the proof numbers, the honest notRun registry (the live-plane failover — owner: the Lead after a real rail binds on the plane; the durable idempotency ledger — PPR-009's residual, unchanged).

## Verification

typecheck 0 errors; lint EXACT baseline; unit green (the new proofs); the existing conformance subjects UNTOUCHED and green (the rails' contracts are read-only reconciliation subjects); governance OK; deploy:validate valid=true (NO manifest delta); the Lead's battery on the merged tree is the binding gate.

## Completion

One branch `work/PPR-013-auto-rebind-policy`, one commit (or a small clean series), pushed + a PR opened (the worker never merges; the Lead reviews, batteries and merges). File ownership: the NEW binding + its unit proofs + the OPTIONAL drill extension + the evidence record + the additive barrel/composition wiring + the PUBLIC-DEPLOYMENT APPEND; it does NOT touch the port's shapes, the frozen admission ordering, the existing adapters' internals, or another order's surfaces.
---
