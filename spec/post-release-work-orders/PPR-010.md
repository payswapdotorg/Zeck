# PPR-010 — The Alternate Realtime Rail (socket.io) + the Substitution/Failover Drill (GAP-003's level-5 rung)

**Status:** AUTHORIZED / WAVE E
**Program:** zeck-post-release-public-productization (successor continuation — the governed evolution program)
**Base:** current governed main at dispatch (f4b1955 or later — never dispatch from a stale base; fetch the actual head)
**Origin:** the wave-E eligibility review (spec/evolution/runs/evo-003-wave-e-eligibility.json): the maturity ladder's level-5 rung is "multi-provider replaceable + failure drills" and GAP-003 records that no alternate-solution substitution drill exists — the realtime plane now has the fresh PPR-009 conformance contract (C1-C12) and exactly ONE real rail (LiveKit, local-server rung); a SECOND REAL, self-hostable, credential-free rail adapter passing the SAME contract plus a cross-rail substitution drill is the level-5 evidence. socket.io 4.8.3 (registry-verified MIT) is the chosen alternate — an independent third-party realtime server product with its own wire protocol (engine.io), not a re-skin of LiveKit and not Zeck-built semantics.

## Requirement 0 — the feasibility probe (BEFORE any design commitment)

The battery runtime is bun 1.3.14 executing vitest. Boot a MINIMAL socket.io server (the published `socket.io` package, unmodified, latest 4.8.x) on loopback inside a throwaway vitest file and complete one real WebSocket handshake + one acked emit round-trip against it (a plain `socket.io-client` or raw-engine.io client is acceptable for the probe). If (and only if) the server cannot boot or serve under the battery runtime, STOP, record the exact failure honestly, and apply the evo-003 RECORDED FALLBACK: the raw `ws` 8.21.3 rail (registry-verified MIT) with the adapter owning the channel semantics — amending every place this order says "socket.io" accordingly, including the evidence record's notes. The probe's result (either way) is recorded in `deploy/evidence/ppr-010.json` notes.

## Pre-implementation reconciliation (REQUIRED — read before writing code)

The PPR-009 machinery is the foundation; reconcile against it, do not re-invent it:

- `src/modules/deployments/ports/realtime-rail.ts` — the provider-neutral port: neutral coordinates only; NO admission/budget/capability/execution handles; RAW MEDIA never crosses (bounded previews + artifact references only); STABLE rail-level idempotency keys on every upstream side effect (`openSession`/`deliverTurn`/`transferCall`/`closeSession` — same key ⇒ exactly-once upstream effect + `replayed: true` on re-issue).
- `tests/conformance/realtime-rail/realtime-rail-conformance.ts` — THE contract (C1-C12, every assertion pinned in the header comment). This is what the new adapter codes against — never LiveKit's shapes, never socket.io's shapes.
- `tests/conformance/realtime-rail/livekit-rail.conformance.test.ts` — the subject-registration pattern to MIRROR: boots its own real server, refusal injection (SIGSTOP/SIGCONT), the crash model (fresh adapter instance against the same surviving server), the honest-skip discipline with an explicit reason, the independent server-side corroboration vantage.
- `src/modules/deployments/adapters/livekit-realtime-rail.ts` + `livekit-realtime-rail-binding.ts` — the adapter discipline to MIRROR: vendor types confined to the adapter file; the in-memory idempotency ledger; the neutral failure-normalization table (`LIVEKIT_FAILURE_NORMALIZATION` is the pattern — the socket.io adapter gets its own); the short-lived client-access seam (hard ceiling + default TTL constants); the environment-backed credential source; the materialization-gate binding (bound ONLY when the env materializes; otherwise EXACTLY today's simulated shape; both ways pinned by unit tests).
- `src/modules/deployments/application/realtime-session-service.ts` — the frozen admission ordering (POLICY → CAPABILITY → BUDGET → SECRET MEDIATION → adapter). This order is FROZEN — this order does NOT touch it (see "explicitly out of scope" below).

## Objective

A REAL socket.io adapter implementing `RealtimeRail`, passing the SAME C1-C12 conformance contract against a REAL in-process socket.io server, plus THE SUBSTITUTION/FAILOVER DRILL — the cross-rail proof that the same durable session coordinates, replayed against the alternate rail after primary refusal, converge with exactly-once upstream effects per rail and identical neutral acknowledgment shapes. Provider independence stops being a claim and becomes drilled evidence.

## Requirements

1. **The socket.io rail adapter** (NEW `src/modules/deployments/adapters/socketio-realtime-rail.ts`) — implements `RealtimeRail` over the published `socket.io` server package (unmodified, **no fork**): `openSession` maps the neutral session request to a server-side socket.io room + a SHORT-LIVED signed join grant (the handshake auth payload); `deliverTurn` maps to the room emit with the ack callback (the upstream's real answer, never fabricated); `transferCall` maps to the adapter's handoff pattern (the PPR-009 precedent — socket.io has no native transfer either; re-tag + move under the same key); `closeSession` to the room close + connected-socket disconnect + ledger record. ALL socket.io/engine.io types stay INSIDE the adapter file; the port sees only neutral shapes. Room/socket identities are OPAQUE, NON-PII, derived deterministically from the neutral coordinates (the same discipline as the LiveKit room-name derivation — never caller strings verbatim). The idempotency ledger follows the PPR-009 in-memory discipline (`SOCKETIO_RAIL_*` naming mirroring `LIVEKIT_RAIL_*`), honestly documented as in-memory with the durable binding as the recorded residual.

2. **The short-lived client-access seam** — mirror the LiveKit pattern exactly: a bounded issuance minting a SHORT-LIVED (≤ the session policy's ceiling; hard ceiling + default TTL constants mirroring `CLIENT_ACCESS_*`) single-purpose join descriptor (the socket.io auth token), derived only after the frozen admission order ran; vendor grant confined to the adapter; the neutral `clientAccess` projection the session service can project.

3. **The conformance subject** (NEW `tests/conformance/realtime-rail/socketio-rail.conformance.test.ts`) — the SAME `defineRealtimeRailConformance` suite over the socket.io adapter against its REAL in-process server (real sockets on loopback, a synthetic `sk-…`-style credential, the honest label `local-socketio-server` as the adapter's LOCAL proof label constant mirroring `LOCAL_LIVEKIT_SERVER_LABEL`). Refusal injection uses the server's own REAL failure modes (e.g. closing the listener ⇒ ECONNREFUSED, or stopping the process); the crash model is the fresh-adapter-instance-against-the-surviving-server; an independent server-side observation vantage (the socket.io server's own rooms/sockets state) cross-checks the exactly-once open. No skips except an explicit honest reason.

4. **THE SUBSTITUTION/FAILOVER DRILL** (NEW `tests/conformance/realtime-rail/rail-substitution.drill.test.ts`) — GAP-003's level-5 evidence: (a) assert the shared neutral vocabulary across BOTH real rails (the descriptors' grammar, the acknowledgment shape keys, the failure-reason neutrality — the port is the invariant); (b) open + deliver on the PRIMARY (the REAL local livekit-server while `ZECK_LIVEKIT_SERVER_BIN`/`/tmp/livekit-server` is present; else the SIMULATED reference rail with the run labeled honestly — the ALTERNATE is ALWAYS real); (c) INJECT FAILURE on the primary (SIGSTOP — the C9 machinery; the bounded request timeout fires); (d) REPLAY the same durable session coordinates against the ALTERNATE under the SAME stable rail-level idempotency keys — prove convergence, exactly-once upstream effects PER RAIL (each rail's own effect ledger), and identical neutral acknowledgment shapes; (e) the drill's record labels exactly which rails were real vs simulated. The COMPOSITION-level auto-rebind policy (a preference order + automatic failover inside the composition) is EXPLICITLY OUT OF SCOPE — it touches the frozen admission path and can mask failures; record it as the honest residual for a future governed work order.

5. **The composition binding + manifests** (NEW `src/modules/deployments/adapters/socketio-realtime-rail-binding.ts`) — the established materialization-gate pattern: the alternate binds in the composition ONLY when the environment materializes its set (`ZECK_SOCKETIO_URL` non-secret + `ZECK_SOCKETIO_AUTH_SECRET` credential-shaped, materialized from `ZECK_SECRET_SOCKETIO_AUTH_SECRET_REF`); otherwise EXACTLY today's shapes; both ways pinned by unit tests. The binding result carries presence + the non-secret URL only — never values. Manifest APPENDS: `deploy/manifests/variables.json` 94 → 97 (the three names above, descriptions following the PPR-009 grammar — "PPR-010: …") + `deploy/manifests/secret-references.json` +`socketio-auth-secret`; the additive pins wherever PPR-009 pinned (env-contract, provision, manifest counts — survey the PPR-009 diff `48e1f86..02fc6b4` and mirror); the architecture boundary pins extended for the socket.io dependency (the provider-sdk boundary pattern that covers livekit-server-sdk).

6. **The barrel + docs** — the adapters barrel (`adapters/index.ts` + the module's `public.ts`) gains the additive exports (value imports actually wired — the Task-79 lesson: no bogus `type` prefixes on value re-exports; verify the barrel compiles before committing); `deploy/PUBLIC-DEPLOYMENT.md` gains the alternate-rail section (append-only): the env contract, the honest availability state (local-real proof only; external/managed NOT RUN), the drill record, the operator sequence for a future credentialed/managed run.

7. **The evidence record** (NEW `deploy/evidence/ppr-010.json`) — the PPR-009 evidence format: verifiedLocally entries with EXACT numbers (conformance counts per subject, drill assertions, unit-proof counts); the notRun registry owning every external boundary (external/managed socket.io; the composition auto-rebind policy; the durable ledger binding — with owners); notes recording the design decisions (the socket.io choice per evo-003, the feasibility probe's result, the drill's primary-rail labeling, the idempotency-ledger choice) and the exact operator sequence for a future credentialed run.

## Verification

- Full battery per the worker runbook (typecheck; lint at baseline parity — the established baseline is 2 pre-existing errors + 68 warnings + 8 infos; unit; architecture; integration on the PG rail; the FULL suite in ONE inline run; `bun run governance:check`; `bun deploy/validate.ts` — environments=4, variables=97);
- the conformance suite GREEN over ALL THREE subjects: the simulated rail (the semantic reference), the LiveKit adapter (local server), the socket.io adapter (local server);
- the substitution drill GREEN with its honest labeling;
- unit tests: the adapter's neutral-shape guarantee (vendor types unexported), the opaque-ref derivation, the short-lived grant's expiry/single-purpose bounds, the failure-normalization table, the binding gate pinned BOTH ways;
- the evidence record with exact numbers and the honest notRun registry.

## Completion

One branch `work/PPR-010-alternate-realtime-rail`, one commit (or a small clean series). The worker never merges; the Lead reviews, batteries and merges. File ownership (the conflict partition): the worker owns the NEW adapter + binding + conformance subject + drill + evidence record + manifest APPENDS + the PUBLIC-DEPLOYMENT APPEND + the additive barrel/pins; it does NOT touch the port's shapes, the frozen admission ordering, PPR-009's adapter/conformance files (read-only reconciliation subjects), or another order's surfaces.
